--- a/crates/interaction-session/src/state.rs
+++ b/crates/interaction-session/src/state.rs
@@ -285,4 +285,6 @@
     pub(crate) last_interaction: Option<LastInteraction>,
     pub(crate) members: Vec<MemberView>,
     pub(crate) reduced_motion: bool,
+    #[serde(default, skip_serializing_if = "Option::is_none")]
+    pub(crate) drill_optional: Option<String>,
 }
@@ -299,5 +301,6 @@
             last_interaction: None,
             members: Vec::new(),
             reduced_motion: false,
+            drill_optional: None,
         }
     }
