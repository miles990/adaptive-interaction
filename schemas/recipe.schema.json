{
  "$defs": {
    "ActuationMode": {
      "enum": [
        "single",
        "parallel",
        "sequence",
        "fallback",
        "adaptive",
        "redundant"
      ],
      "type": "string"
    },
    "ActuationSpec": {
      "additionalProperties": true,
      "properties": {
        "candidates": {
          "description": "Candidate actuator ids, in preference order.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chance": {
          "default": 1.0,
          "description": "Probability 0..1 the actuation fires at all (surprise factor).",
          "format": "double",
          "type": "number"
        },
        "jitter": {
          "description": "Max random start jitter, e.g. `\"2s\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "maxChannels": {
          "default": 3,
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "minChannels": {
          "default": 0,
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "mode": {
          "$ref": "#/$defs/ActuationMode",
          "default": "adaptive"
        }
      },
      "required": [
        "candidates"
      ],
      "type": "object"
    },
    "AiAssistMode": {
      "description": "When AI may be involved in this recipe.",
      "oneOf": [
        {
          "const": "never",
          "description": "Never involve AI; fully deterministic (the default).",
          "type": "string"
        },
        {
          "const": "when-uncertain",
          "description": "Only when the deterministic rules cannot decide (ambiguous /\nlow-confidence / contradictory observations).",
          "type": "string"
        },
        {
          "const": "interpret",
          "description": "AI may help interpret observations before planning.",
          "type": "string"
        },
        {
          "const": "choose-channel",
          "description": "AI may help choose among candidate channels.",
          "type": "string"
        },
        {
          "const": "generate-text",
          "description": "AI only generates message text; channel choice stays deterministic.",
          "type": "string"
        },
        {
          "const": "draft-only",
          "description": "AI may draft recipe changes, but a human must confirm them.",
          "type": "string"
        }
      ]
    },
    "AiAssistSpec": {
      "properties": {
        "allowSensitiveEgress": {
          "default": false,
          "description": "Whether sensitive observations may leave this machine to reach the AI.\nDefaults to false and is additionally bounded by global policy.",
          "type": "boolean"
        },
        "dailyCallCap": {
          "description": "Hard cap on assist requests per day for this recipe.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "dataScope": {
          "default": [],
          "description": "Fact/data categories that may be shared with the AI. Empty = only\nnon-sensitive event metadata.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxWaitMs": {
          "default": 5000,
          "description": "How long to wait for an AI host before falling back (ms).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "minConfidence": {
          "default": 0.6,
          "description": "Below this inference confidence the situation counts as \"uncertain\".",
          "format": "double",
          "type": "number"
        },
        "mode": {
          "$ref": "#/$defs/AiAssistMode",
          "default": "never"
        },
        "onUnavailable": {
          "$ref": "#/$defs/AiUnavailableBehavior",
          "default": "fallback"
        },
        "requireHumanConfirmation": {
          "default": false,
          "description": "Whether AI proposals need explicit human confirmation before acting.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "AiUnavailableBehavior": {
      "description": "Deterministic behavior when AI is needed but unavailable / times out.",
      "oneOf": [
        {
          "const": "fallback",
          "description": "Proceed with the deterministic plan (conservative candidate order).",
          "type": "string"
        },
        {
          "const": "no-action",
          "description": "Do nothing this round.",
          "type": "string"
        }
      ]
    },
    "ConditionSpec": {
      "anyOf": [
        {
          "description": "`\"event == task.completed\"` style expression.",
          "type": "string"
        },
        {
          "additionalProperties": true,
          "description": "`{ event: task.completed, state: present }` — all pairs must match.",
          "type": "object"
        }
      ]
    },
    "ConsentRequirements": {
      "additionalProperties": true,
      "properties": {
        "required": {
          "default": [],
          "description": "Consent scopes that must be active for the recipe to run,\ne.g. `[\"channel:haptic\"]`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ContextSpec": {
      "additionalProperties": true,
      "properties": {
        "maxAge": {
          "description": "Ignore observations older than this, e.g. `\"5m\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "minConfidence": {
          "description": "Minimum inference confidence used during fusion.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "receptors": {
          "default": [],
          "description": "Additional receptors consulted (not required to fire) when planning.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "DecisionSpec": {
      "additionalProperties": true,
      "properties": {
        "allowNoAction": {
          "default": true,
          "description": "The recipe accepts \"do nothing\" as a legitimate outcome.",
          "type": "boolean"
        },
        "objective": {
          "description": "Human-readable objective, e.g. `celebrate-without-interrupting`.",
          "type": "string"
        }
      },
      "required": [
        "objective"
      ],
      "type": "object"
    },
    "FusionMode": {
      "enum": [
        "single",
        "all",
        "any",
        "quorum",
        "weighted",
        "sequence"
      ],
      "type": "string"
    },
    "LimitsSpec": {
      "additionalProperties": true,
      "properties": {
        "cooldown": {
          "description": "Minimum interval between firings, e.g. `\"15m\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "expiresAfter": {
          "description": "The produced plan expires after this, e.g. `\"30s\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "maxExecutionsPerSession": {
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "maxPerHour": {
          "description": "Max firings per rolling hour.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "MessageMode": {
      "oneOf": [
        {
          "const": "fixed",
          "description": "Always use the fixed template(s).",
          "type": "string"
        },
        {
          "const": "random",
          "description": "Random pick among candidates.",
          "type": "string"
        },
        {
          "const": "adaptive",
          "description": "Adaptive pick (anti-repetition, tone, persona).",
          "type": "string"
        },
        {
          "const": "ai-generated",
          "description": "The AI supplies text at plan time.",
          "type": "string"
        },
        {
          "const": "none",
          "description": "Deliberately no text.",
          "type": "string"
        }
      ]
    },
    "MessageStrategy": {
      "properties": {
        "allowSilence": {
          "default": false,
          "description": "Whether silence is an acceptable outcome.",
          "type": "boolean"
        },
        "intents": {
          "default": [],
          "description": "Candidate intents (looked up in the catalog).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "language": {
          "description": "BCP-47 language tag preference, e.g. `zh-Hant`, `en`.",
          "type": [
            "string",
            "null"
          ]
        },
        "mode": {
          "$ref": "#/$defs/MessageMode",
          "default": "adaptive"
        },
        "repeatCooldownMs": {
          "description": "Suppress a candidate if it was used within this window (ms).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "templates": {
          "default": [],
          "description": "User-supplied templates; `{task}`-style placeholders are allowed.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tone": {
          "description": "Tone hint, e.g. `neutral`, `warm`, `serious`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RecipeId": {
      "description": "Identifies a recipe.",
      "type": "string"
    },
    "TriggerSpec": {
      "additionalProperties": true,
      "properties": {
        "mode": {
          "$ref": "#/$defs/FusionMode",
          "default": "single"
        },
        "quorum": {
          "description": "Required matches for `quorum` mode.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "steps": {
          "items": {
            "$ref": "#/$defs/TriggerStep"
          },
          "type": "array"
        },
        "threshold": {
          "description": "Threshold for `weighted` mode (sum of matched weights).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "within": {
          "description": "Time window for `sequence` / `all`, e.g. `\"10m\"`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "steps"
      ],
      "type": "object"
    },
    "TriggerStep": {
      "additionalProperties": true,
      "description": "One trigger step: a receptor plus a condition on its observations.",
      "properties": {
        "condition": {
          "anyOf": [
            {
              "$ref": "#/$defs/ConditionSpec"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Condition over observation facts (see [`crate::condition`])."
        },
        "receptor": {
          "type": "string"
        },
        "weight": {
          "default": 1.0,
          "description": "Weight for `weighted` fusion (default 1.0).",
          "format": "double",
          "type": "number"
        }
      },
      "required": [
        "receptor"
      ],
      "type": "object"
    },
    "VerificationSpec": {
      "additionalProperties": true,
      "properties": {
        "receptors": {
          "default": [],
          "description": "Receptors to consult for verification evidence.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "strategy": {
          "$ref": "#/$defs/VerificationStrategy",
          "default": "best-effort"
        },
        "timeout": {
          "description": "e.g. `\"10s\"`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "VerificationStrategy": {
      "oneOf": [
        {
          "const": "best-effort",
          "description": "Acknowledgement from the driver is enough to complete.",
          "type": "string"
        },
        {
          "const": "observed",
          "description": "Require an observation confirming the effect.",
          "type": "string"
        },
        {
          "const": "none",
          "description": "Do not verify (receipt stops at acknowledged/uncertain).",
          "type": "string"
        }
      ]
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "properties": {
    "actuation": {
      "$ref": "#/$defs/ActuationSpec"
    },
    "ai": {
      "anyOf": [
        {
          "$ref": "#/$defs/AiAssistSpec"
        },
        {
          "type": "null"
        }
      ],
      "description": "AI involvement policy for this recipe (default: never involve AI)."
    },
    "consent": {
      "$ref": "#/$defs/ConsentRequirements",
      "default": {
        "required": []
      }
    },
    "context": {
      "$ref": "#/$defs/ContextSpec",
      "default": {
        "receptors": []
      }
    },
    "decision": {
      "$ref": "#/$defs/DecisionSpec"
    },
    "description": {
      "default": "",
      "type": "string"
    },
    "enabled": {
      "default": true,
      "type": "boolean"
    },
    "id": {
      "$ref": "#/$defs/RecipeId"
    },
    "intent": {
      "default": "presence",
      "description": "Semantic intent produced when the recipe fires.",
      "type": "string"
    },
    "limits": {
      "$ref": "#/$defs/LimitsSpec",
      "default": {}
    },
    "message": {
      "$ref": "#/$defs/MessageStrategy",
      "default": {
        "allowSilence": false,
        "intents": [],
        "mode": "adaptive",
        "templates": []
      }
    },
    "metadata": {
      "additionalProperties": true,
      "description": "Extra free-form metadata.",
      "type": "object"
    },
    "name": {
      "type": "string"
    },
    "schemaVersion": {
      "default": "1.0",
      "type": "string"
    },
    "trigger": {
      "$ref": "#/$defs/TriggerSpec"
    },
    "verification": {
      "$ref": "#/$defs/VerificationSpec",
      "default": {
        "receptors": [],
        "strategy": "best-effort"
      }
    }
  },
  "required": [
    "id",
    "name",
    "trigger",
    "decision",
    "actuation"
  ],
  "title": "Recipe",
  "type": "object"
}
