[package]
name = "interaction-aip"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Adaptive Interaction Protocol (AIP) 1.0: versioned message envelope, outcomes, errors, capability negotiation, limits and the canonical JSON Schema source"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
schemars = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
sha2 = "0.10"
