[package]
name = "interaction-agent-gateway"
version = "0.3.0"
edition = "2021"
license = "MIT"
description = "Local AI agent connectors (Codex app-server JSON-RPC, Claude Code stream-json) normalized into honest gateway events"

[dependencies]
async-trait = { workspace = true }
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = { workspace = true }
