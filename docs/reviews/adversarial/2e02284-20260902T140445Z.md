# 對抗審查 2e02284-20260902T140445Z（run 2：run 1 的 75 項 unverified 以 seeds 重驗）

- HEAD：`2e02284f78a1c94e874b1d81a4ebb7483e71529c`（worktree 有未提交修改）
- 規格：`docs/specs/adaptive-interaction-v05-core-experience-prompt.md`

| seeds | confirmed | fixed-meanwhile | refuted | unverified（verifier 額度用盡） |
|---:|---:|---:|---:|---:|
| 75 | 25 | 1 | 2 | 47 |

> persist agent 同樣因額度失敗，由主迴圈落盤。仍 unverified 的 47 項會再以 seeds 重驗（run 3）。

## Confirmed（25）

### F-2e02284-20260902T140445Z-rig-renderer-011 · high · 需要確認（ask／waiting-consent 真相狀態）在遊玩場被降為「只點亮頭飾」，身體繼續追球
- 檔案：`apps/interaction-desktop/src/companion/rig/stage.ts:61 (unchanged); the pinning test is apps/interaction-desktop/src/test/rig.test.ts:435-436 (finding wrote "rig.test.ts:434" — the file lives under src/test/, not companion/rig/)`
- 維度：rig-renderer；verdict：confirmed
- 主張：`ask` 是 truthState 表情（runtime 真的在等 consent／input，machine PRIORITY requesting-consent=80、CPP §4.3 floor 80「可搶占任何非安全演出」），但 stage.ts 把它列進 OVERLAY_STATUS：遊玩中只覆蓋 STATUS_CHANNELS（core/headpiece/skirt/ear），ask.hold 的舉手、問號浮標、張嘴全部丟掉，且 playfieldActive() 回 true 讓她繼續追球／散步。使用者看到的是「在玩，頭飾亮一點」而不是「在等你確認」。同檔第 80–81 行註解把「需要確認」列在 takeover 名單，與程式碼自相矛盾；rig.test.ts:434 反而把 ask=overlay 釘死。
- 證據：stage.ts:53-63 `OVERLAY_STATUS = new Set([..., "ask", "listening"])`；stage.ts:66-74 STATUS_CHANNELS 只有 coreGlow/corePulse/headpieceGlow/skirtGlow/skirtTone/earL/earR；stage.ts:131-137 `playfieldActive` 對 overlay 回 true → stage.ts:664 stepWorld ambient=true 繼續遊玩。probe 實跑：`stageExpressionPlan("ask","chase")` → `{expression:"play-chase", useMachineSlice:false, statusChannels:{"headpieceGlow":0.8}}`；`EXPRESSIONS.ask.hold` = {armPose:"raise", armPhase:0.8, mouth:"open", overlay:"question", headpieceGlow:0.8,…} 只剩 headpieceGlow 存活。machine.ts:49-63 PRIORITY requesting-consent 80 > performing 25 的搶佔在 stage 被抵銷。stage.ts:80-81 註解：「takeover ＝ 整體搶佔（…成功、需要確認…）」。
- verifier（reproducibility）confirmed=True severity=high：Reproduced on HEAD 2e02284 (worktree clean; stage.ts unchanged since that commit, `git log -S'"ask",'` shows it was introduced there).

Code facts (current file contents):
- stage.ts:53-63 `OVERLAY_STATUS` contains "ask"; stage.ts:66-74 STATUS_CHANNELS = coreGlow/corePulse/headpieceGlow/skirtGlow/skirtTone/earL/earR only.
- stage.ts:76-81 doc comment for the same function says takeover covers "…成功、需要確認…" — the code contradicts its own comment.
- stage.ts:131-137 `playfieldActive` returns true for any overlay state; stage.ts:283-296 `machineStageFlags` uses that for `ambient`; stage.ts:659-664 
- verifier（spec-honesty）confirmed=True severity=medium：Verified at HEAD 2e02284 (clean tree, no drift). Ran a scratchpad vitest probe (no project files modified): stageExpressionPlan("ask","chase") and ("ask","stroll") both return {expression:"play-chase", useMachineSlice:false, statusChannels:{headpieceGlow:0.8}}; the ask.hold channels armPose/armPhase/earPerk/mouth/browL/browR/overlay("question")/tailAngle are all dropped because STATUS_CHANNELS (stage.ts:66-74) only carries core/headpiece/skirt/ear. machineStageFlags("idle",{kind:"requesting-consent"},"ask",false) → {ambient:true, frozen:false}, so stepWorld (stage.ts:664 ambient=this.flags.amb
- 回歸測試：In src/test/rig.test.ts assert `statusOverlay("ask")==="takeover"`, `playfieldActive("ask",false,false)===false`, `stageExpressionPlan("ask","chase")` returns `{expression:"ask", useMachineSlice:true}` and `machineStageFlags("idle",{kind:"requesting-consent"},"ask",false).ambient===false`; generically, for every expression with `truthState:true`, `statusOverlay(id)` must not be "overlay". / In rig.test.ts assert statusOverlay("ask")==="takeover", stageExpressionPlan("ask","chase") deep-equals {expression:"ask", useMachineSlice:true, statusChannels:null}, and playfieldActive("ask",false,false)===false (and machineStageFlags("idle",{kind:"requesting-consent",untilMs:∞},"ask",false).ambient===false); move "ask" from the overlay list at rig.test.ts:435 to the takeover list.

### F-2e02284-20260902T140445Z-director-pipeline-021 · high · presence-set 只寫 prefs 不套用視窗，卻 ack `completed`（隱藏／顯示角色從未發生；tray 標籤跟著不一致）
- 檔案：`apps/interaction-desktop/src/companion/CompanionApp.tsx:1062-1068 (unchanged); Rust lines drifted: desktop_prefs_patch = src-tauri/src/lib.rs:1618-1718, companion_apply_prefs = lib.rs:2271-2303, tray label = lib.rs:2573-2585, toggle_companion_window = lib.rs:1797-1829`
- 維度：director-pipeline；verdict：confirmed
- 主張：Runtime 的 `companion.presence.set`（AI 可呼叫的 presentation actuator）在角色視窗端只呼叫 `desktop.prefsPatch({ companionVisible })`，而 Rust 的 `desktop_prefs_patch` 只驗證、存檔、同步 autostart，不 hide/show 視窗也不 emit；真正套用視窗的是另一個命令 `companion_apply_prefs`，這條路徑從未呼叫它。結果：presence-set visible:false 後視窗照樣顯示、visible:true 時隱藏的視窗也不會叫出來，但 ACK 回報 `completed`（誠實階梯：completed ≠ 效果發生）。副作用：prefs.companion_visible 與實際視窗狀態脫鉤，tray 選單文字依 prefs 顯示「顯示桌面角色」但角色明明在畫面上；之後任何無關的角色偏好變更觸發 companion_apply_prefs 時視窗才「突然」隱藏。
- 證據：CompanionApp.tsx:1062-1068：`if (plan.presence !== undefined && isTauri) { try { await desktop.prefsPatch({ companionVisible: plan.presence }); } catch ... }` 之後 `api.presentationAck(actionId, plan.outcome ...)`，plan.outcome 由 presentationCommands.ts:123 固定為 `"completed"`。desktop.ts:131-132 `prefsPatch → invoke("desktop_prefs_patch")`，:136 `companionApplyPrefs → invoke("companion_apply_prefs")` 是分開的命令。src-tauri/src/lib.rs:1615-1707 `desktop_prefs_patch`：驗證 → `supervisor::save_prefs` → autostart 同步 → 回傳，沒有任何 `get_webview_window("companion")`／hide/show/emit。lib.rs:2185-2216 `companion_apply_prefs` 才做 `ensure_companion_window`／`w.hide()`；grep 顯示 companion_apply_prefs 只被 pages（CompanionPage.tsx:145、FirstSuccess.tsx:167、Onboarding.tsx:195）呼叫，CompanionApp.tsx 沒有。lib.rs:2487-2499 tray 文字 `if companion_visible { "隱藏桌面角色" } else { "顯示桌面角色" }` 讀的是 prefs。HEAD 版（git show HEAD）同樣如此。docs/CHANGELOG 未記為已知限制。
- verifier（reproducibility）confirmed=True severity=high：Verified against HEAD 2e02284 (working tree clean for all files involved; only .claude/workflows/*.js modified).

1. Path taken by `companion.presence.set`: CompanionApp.tsx:1062-1068 — `if (plan.presence !== undefined && isTauri) { await desktop.prefsPatch({ companionVisible: plan.presence }) }`, then line 1092 `api.presentationAck(actionId, plan.outcome, ...)`. `plan.outcome` is hard-coded `"completed"` at presentationCommands.ts:123 (existing test presentation.test.ts:75-82 asserts exactly that; ran it: 1 passed). No call to `desktop.companionApplyPrefs()` anywhere in CompanionApp.tsx (grep
- verifier（spec-honesty）confirmed=True severity=high：親自對照目前工作樹（HEAD 2e02284＋未提交修改）逐條驗證，finding 成立；只有 lib.rs 行號漂移（desktop_prefs_patch 1618 起、companion_apply_prefs 2271 起、tray 文字 2573-2585），CompanionApp.tsx 行號未漂移。

證據鏈：
1. CompanionApp.tsx:1062-1068 presence-set 路徑只做 `await desktop.prefsPatch({ companionVisible: plan.presence })`，:1093 以 `plan.outcome` ack；presentationCommands.ts:119-123 對 presence-set 固定回 `{ outcome: "completed", presence }`。
2. desktop.ts:131-132 `prefsPatch → desktop_prefs_patch`，:136 `companionApplyPrefs → companion_apply_prefs`，兩者是獨立命令。
3. lib.rs:1618-1707 `desktop_prefs_patch`：合併 patch → 驗證 → `supervisor::save_prefs` → autos
- 回歸測試：vitest CompanionApp integration test: mock `desktop.prefsPatch`/`desktop.companionApplyPrefs` spies and `api.presentationAck`; emit a runtime event `{eventType:"presentation.command", payload:{actionId:"a1", command:"presence-set", params:{visible:false}}}` and assert `companionApplyPrefs` was invoked before `presentationAck("a1","completed")`, and that when `companionApplyPrefs` rejects the ack outcome is "failed" (never "completed"). / vitest：以 mock desktop 掛載 CompanionApp（isTauri=true），送入 presentation.command presence-set {visible:false}，斷言 `desktop.companionApplyPrefs`（或專用 hide 命令）在 `api.presentationAck(actionId, "completed")` 之前被呼叫；若 apply 拋錯則 ack 必須是 "failed"。

### F-2e02284-20260902T140445Z-character-protocol-027 · high · 桌面視窗在 Runtime 端斷線後永不重新 hello：presence 逾時或外部 daemon 重啟後，角色再也收不到 character.intent
- 檔案：`apps/interaction-desktop/src/companion/CompanionApp.tsx:1228（未漂移；另見 :610、:958、:1208）；crates/interaction-runtime/src/character.rs:1402-1406；crates/interaction-runtime/src/presentation.rs:966；crates/interaction-character/src/gateway.rs:1133-1137、:470、:1294-1305`
- 維度：character-protocol；verdict：confirmed
- 主張：Runtime 的 character_sweep_at 在桌面 presence 逾時（PRESENCE_STALE_SECS=20）時對 desktop-companion 做 on_disconnect（generation+1、connected=false、negotiated=None）；之後的 /v1/presentation/hello 心跳只呼叫 gateway.heartbeat()，不會重新登記。而桌面端只有在 feed 由 legacy→protocol 改變、fallbackToText、或 reduced-motion 變更時才會再送 /v1/character/hello（helloSentRef 只在 fallbackToText 重設）。因此 WebView 計時器被節流／睡眠喚醒時 sweep 先於心跳、或外部 daemon 重啟（transport 只重設 SSE cursor）之後，桌面角色永遠不在 connected_instance_ids() 裡：非安全 intent 靜默略過、安全 intent 只剩 character.system-text，/v1/character/instances 顯示 connected=false、provider 顯示「桌面角色（尚未連線）」，使用者看到的是一個不再演出的角色，沒有任何提示。
- 證據：crates/interaction-runtime/src/character.rs:1404 `if gw.instance(&desktop).is_some_and(|v| v.connected) && !self.presentation.connected(now) { outputs.extend(gw.on_disconnect(&desktop, DisconnectReason::TransportClosed, now)); }`；crates/interaction-runtime/src/presentation.rs:966 presentation_hello_with_behavior 只做 `self.character_desktop_heartbeat(now)`（gateway.heartbeat 只更新 last_seen，不改 connected）。CompanionApp.tsx:1228 `if (feed === "protocol" && (feedChanged || !helloSentRef.current)) void sendHello(...)`，helloSentRef 只在 :610 設 true、:958（fallbackToText）設 false；poll 的 catch 分支只 apply offline，不重設 feedRef/helloSentRef；CompanionApp.tsx:1208 `if (e.eventType.startsWith("character.")) return;` 忽略 character.instance{connected:false}。src/transport.ts nextStreamCursor 偵測 daemon 重啟只重設 lastId/buffer。Rust 測試 character_loop.rs::desktop_presence_expiry_disconnects_the_desktop_instance 只驗證斷線後走 system.text，沒有任何測試驗證桌面會重連。
- verifier（reproducibility）confirmed=True severity=high：親自逐檔核對（工作樹目前內容，行號與 finding 一致，未漂移）：

Runtime 端斷線後只有 hello 能恢復 connected：
- gateway.rs:1255-1290 on_disconnect → generation+1、connected=false、negotiated=None。
- gateway.rs:470 是全檔唯一的 `inst.connected = true`，位於 on_negotiate，只能經 character_hello（character.rs:792-841）到達。
- gateway.rs:1133-1137 heartbeat 只寫 `inst.last_seen`。presentation.rs:951-975 presentation_hello_with_behavior 對 gateway 只呼叫 character_desktop_heartbeat（character.rs:1378-1383 → gateway.heartbeat）。所以 /v1/presentation/hello 心跳永遠不會把桌面 instance 接回來。
- 斷線觸發有兩條：character.rs:1402-1406 presence 逾時（PRESENCE_STALE_SECS=20，presentation.rs:39）
- verifier（spec-honesty）confirmed=True severity=high：I attempted to refute this and could not; every link in the chain checks out against the current working tree (the character-protocol code is uncommitted v0.5 work; HEAD has no helloSentRef/sendHello at all).

Runtime side, verified by reading:
- character.rs:1403-1407: character_sweep_at calls gw.on_disconnect(desktop, TransportClosed) when the gateway thinks the desktop is connected but presentation presence is stale (PRESENCE_STALE_SECS=20, presentation.rs:39/241-246). on_disconnect (gateway.rs:1255-1290) sets connected=false, negotiated=None, generation+=1, lifecycle=Reconnecting.
- presen
- 回歸測試：Rust：hello → character_sweep_at(+50 s) 斷線後呼叫 presentation_hello(true) 再讀 character_instances，斷言 connected 仍為 false（釘死「心跳不等於重新登記」）；前端 vitest：poll 收到 status.characterProtocol.activeCharacter==null 或 character.instance{instanceId:desktop, connected:false} 事件時，斷言 api.characterHello 被再次呼叫且 helloSentRef 重設。 / Runtime: after hello + character_sweep_at(now+25s) leaves desktop connected=false, call presentation_hello(true, ..) and assert connected_instance_ids() is still empty (documents that presence alone cannot reconnect) — then, desktop vitest: mount CompanionApp with a mocked api where the SSE stream delivers character.instance{instanceId:"desktop-companion",connected:false} (or status.startedAt changes), and assert api.characterHello is called a second time and a subsequent character.intent is dispatched to the gateway.

### F-2e02284-20260902T140445Z-character-protocol-028 · high · 拖放「記錄這些項目」在 protocol 模式一律失敗：TS 送 `{files:[…]}`，Rust 正規化器只接受 README §6 的扁平鍵
- 檔案：`apps/interaction-desktop/src/character/gateway.ts:1282（同時 crates/interaction-character/src/input.rs:218 與 :391-399、apps/interaction-desktop/src/companion/gatewayWiring.ts:643）`
- 維度：character-protocol；verdict：confirmed
- 主張：TS Gateway 把 character.file-dropped 正規化成 payload `{ files: FileDropGrant[] }`；Rust InputNormalizer 對 FileDropped 要求 payload 只含 name/mediaType/bytes/readableScope/grantId/expiresAt，出現任何其他鍵即 Dropped(InvalidPayload)。CompanionApp 的 dropPush 在 protocol 模式把事件經 gateway.ingestInput → POST /v1/character/events 送給 Runtime，Runtime 回 decision=dropped、reason=invalid-payload，dropPush 擲錯，recordDroppedItems 顯示「記錄失敗…（這些項目沒有被記錄）」。也就是 v0.5 daemon 下的桌面拖放記錄功能整條路徑是死的（誠實報失敗，但功能不可用）；TS 還把未知的 bytes／mediaType 補成 0／application/octet-stream（假資料）。兩邊各自的單元測試只驗自己的形狀，沒有跨實作的 fixture 測試。
- 證據：gateway.ts:1282 `payload = { files };`（fileGrants 回 FileDropGrant[]）；gatewayWiring.ts:643 `payload: { files }`；CompanionApp.tsx:1540-1546 `inputEventFor("companion-dropped", facts)` → `if (result.decision === "dropped") throw new Error(...)`。crates/interaction-character/src/input.rs:218 `const FILE_DROP_KEYS: [&str; 6] = ["name","mediaType","bytes","readableScope","grantId","expiresAt"]`、:394 `.find(|k| !FILE_DROP_KEYS.contains(&k.as_str()))` → `InputDropReason::InvalidPayload`。實測（scratch 程式呼叫 InputNormalizer::push 餵 TS 形狀）：`PROBE1 ts-shape file-drop -> Dropped(InvalidPayload { field: "files" })`。Rust 測試 character_loop.rs:948-975 用扁平鍵；TS 測試 character-gateway.test.ts:752 用 files 陣列。
- verifier（reproducibility）confirmed=True severity=high：親自追完整條路徑並以 scratch 程式重現，主張成立，行號無漂移（工作樹與 finding 一致）。

1. TS 送出的形狀：`apps/interaction-desktop/src/companion/gatewayWiring.ts:641-645` `inputEventFor("companion-dropped")` 回 `{ kind: "character.file-dropped", payload: { files: [{name}] } }`（只有檔名）；`apps/interaction-desktop/src/character/gateway.ts:1279-1284` `case "character.file-dropped"` → `payload = { files }`，`fileGrants`（:1359-1389）對每個檔案補 `mediaType` 缺省 `"application/octet-stream"`（:1371-1374）、`bytes` 缺省 `0`（:1375）。`deliver`（:1410-1411）把整個 event 交給 `opts.onInput`；`CompanionApp.tsx:721-726` onInput → `api.characterEvent` → `transport.ts:238-23
- verifier（spec-honesty）confirmed=True severity=high：親自驗證，無法反駁，結論為確認缺陷（以目前工作樹為準；Cargo.toml/Cargo.lock 等未提交變更早於我的驗證，非我造成，probe 已刪除、repo 未動）。

契約兩端證據：
1. TS 送出形狀：gateway.ts:1280-1282 對 `character.file-dropped` 設 `payload = { files }`，`files` 來自 fileGrants（gateway.ts:1359-1391），每筆為 `{name, mediaType, bytes, readableScope:"file", grantId, expiresAt}`；未知時補 `mediaType="application/octet-stream"`、`bytes=0`（gateway.ts:1371-1375）。gatewayWiring.ts:640-643 `inputEventFor("companion-dropped")` 只給 `{ files:[{name}] }`，所以桌面拖放實際送出的每個檔案都是 bytes 0／octet-stream。
2. 轉送不做任何變形：CompanionApp.tsx:722-726 `onInput` 直接 `api.characterEvent(PRIMARY_INSTANCE_ID, event)` → a
- 回歸測試：跨實作 fixture 測試：在 TS 以 CharacterGateway 對 `inputEventFor("companion-dropped", {attachments:["/a/report.pdf"]})` 做 ingestInput，把 onInput 收到的 CharacterInputEvent JSON 寫成 golden；Rust 測試（interaction-runtime character_loop）反序列化該 golden 呼叫 `rt.character_event(DESKTOP_INSTANCE_ID, …)` 並斷言 `decision == "queued"` 且 `companion.drag-drop` observation 的 `names == ["report.pdf"]`（目前會得到 `dropped`/`invalid-payload`）。 / 共用 JSON fixture：由 TS `CharacterGateway.ingestInput`（或 `inputEventFor("companion-dropped")`＋fileGrants）產生的 `character.file-dropped` 事件，餵給 Rust `Runtime::character_event`／`InputNormalizer::push`，斷言 decision=="queued" 且產生 `companion.drag-drop` 觀察、`names` 等於拖入檔名；反向亦以 Rust 端 schema 驗證 TS 產出的 payload。

### F-2e02284-20260902T140445Z-companion-gameplay-001 · medium · requesting-consent（ask，truthState）被歸為 overlay：等待使用者授權時角色照樣追球，只借了 headpieceGlow
- 檔案：`apps/interaction-desktop/src/companion/rig/stage.ts:61（相關：stage.ts:80-81 註解、:136 playfieldActive、:290 machineStageFlags；src/test/rig.test.ts:435-436 釘住錯誤行為）`
- 維度：companion-gameplay；verdict：confirmed
- 主張：`ask`（machine kind requesting-consent；CPP truthState waiting-consent／waiting-input，floor 80；expressions.ts 標 truthState:true）被放進 OVERLAY_STATUS，所以 statusOverlay("ask")="overlay"、playfieldActive=true：runtime 真的在等使用者 consent 時遊玩場不停，身體姿勢被 play-chase／sneak-closer／play-carry 取代，ask 的舉手（armPose raise）、張嘴與 `overlay:"question"` 問號都不在 STATUS_CHANNELS 內而被丟掉，只剩 headpieceGlow 0.8。這與同檔 80–81 行註解（takeover 含「需要確認」）、CPP §4.2/§4.3、規格 §3.2（waiting-for-consent 必須誠實呈現）矛盾；rig.test.ts:434-436 反而把 ask 釘成 overlay。
- 證據：stage.ts:53-63 OVERLAY_STATUS 含 "ask"；stage.ts:66-74 STATUS_CHANNELS 只有 coreGlow/corePulse/headpieceGlow/skirtGlow/skirtTone/earL/earR；expressions.ts:962-990 ask truthState:true、hold={armPose:"raise",mouth:"open",overlay:"question",headpieceGlow:0.8,...}。實測（vitest 探針）：statusOverlay(ask)=overlay；statusChannelParams(ask)={"headpieceGlow":0.8}；stageExpressionPlan("ask","chase")={expression:"play-chase",useMachineSlice:false,statusChannels:{headpieceGlow:0.8}}；playfieldActive("ask",false,false)=true；machineStageFlags("idle",{kind:"requesting-consent"},"ask",false).ambient=true。stage.ts:80-81 註解列「需要確認」為 takeover；規格 §3.2「waiting-for-consent」為真相階梯之一、§6.1「高風險安全事件可搶占任何表演」；regressions-phase7.test.ts:792 明確斷言 ask 是 truthState。
- verifier（reproducibility）confirmed=True severity=medium：親自重現，主張成立，行號無漂移（stage.ts:61 仍為 `"ask",`，位於 OVERLAY_STATUS 53–63）。

證據鏈（目前檔案內容）：
1. `apps/interaction-desktop/src/companion/machine.ts:473-475`：runtime 事件 `agent.session.state` 的 `waiting-input`／`waiting-consent` → transient `requesting-consent`（PRIORITY 80，高於 succeeded/unknown 的 60）；`machine.ts:305-306` pose → `{animation:"ask", ambient:false}`。
2. `expressions.ts:964-990`：`ask` 標 `truthState:true`，辨識特徵全在 hold：`armPose:"raise"`、`mouth:"open"`、`overlay:"question"`，僅 `headpieceGlow:0.8` 屬 STATUS_CHANNELS（stage.ts:66-74 只有 coreGlow/corePulse/headpieceGlow/skirtGlow/skirtTone/earL/earR）。而 `working
- verifier（spec-honesty）confirmed=True severity=medium：親自驗證（工作樹目前版本，stage.ts 的 diff 只新增 pause/resume，OVERLAY_STATUS 與 STATUS_CHANNELS 未變）：

1. 程式路徑成立：machine.ts:474-475 把 agent.session.state waiting-input／waiting-consent 投成 requesting-consent（priority 80），pose() 回 animation "ask"（machine.ts:305-306）；CompanionApp.syncPose（502-511）與 character/adapters/shu.ts:107-114 都把 p.animation="ask" 餵給 machineStageFlags → playfieldActive → statusOverlay。stage.ts:53-63 OVERLAY_STATUS 含 "ask"，所以 statusOverlay("ask")="overlay"。

2. 用 vitest 探針（scratchpad，未動 repo）實測：statusOverlay("ask")="overlay"；statusChannelParams("ask")={headpieceGlow:0.8}；stageExpressionPlan("as
- 回歸測試：在 rig.test.ts 斷言 `statusOverlay("ask")==="takeover"`、`stageExpressionPlan("ask","chase")` 回 `{expression:"ask",useMachineSlice:true,statusChannels:null}`、`machineStageFlags("idle",{kind:"requesting-consent"},"ask",false).ambient===false`，並在 timeline 合成後檢查 `armPose==="raise"` 且 `overlay==="question"`；同時把 rig.test.ts:435 的 overlay 清單移除 "ask"。 / stageExpressionPlan("ask","chase") 必須回 {expression:"ask", useMachineSlice:true, statusChannels:null}，playfieldActive("ask",false,false) 必須為 false，且 machine 在 play-chase performing 後收到 requesting-consent 時，StageRenderer.renderFrame 後 lastFrameParams() 的 armPose==="raise" 且 overlay==="question"。

### F-2e02284-20260902T140445Z-companion-gameplay-002 · medium · 使魔層不受 quiet／玩耍開關／非 ambient 真相狀態約束：blocked/failed/unknown 期間主角仍轉身回看並在頭上畫愛心
- 檔案：`apps/interaction-desktop/src/companion/playfield.ts:617 (stepFamiliars early return), :359-362 (facing flip before canPlay at :363), :653-661 (attendTo/greetBackUntil); apps/interaction-desktop/src/companion/rig/stage.ts:733 (gaze gate that DOES exclude takeover), :759 (drawFamiliar unconditional), :774-776 (drawGreetHeart gated only by greetBackUntil). Note: finding cited `src/companion/stage.ts` — actual path is `src/companion/rig/stage.ts`; stage.ts line numbers drifted +18 (741→759, 756→774) because the file was modified at 22:09:15 during review (pause/resume/frame-budget change, unrelated to this defect).`
- 維度：companion-gameplay；verdict：confirmed
- 主張：stepFamiliars 只檢查 reducedMotion 與使魔數，不看 ambient／playEnabled／quiet／deskMove；stepChar 在 canPlay 判斷之前就依 attendTo 翻轉 facing（359-362）；stage.ts:756 只依 greetBackUntil 畫愛心、不看 machine 狀態。結果：勿擾／quiet hours、關掉「玩耍」、或 machine 正在顯示 blocked/failed/unknown/success-claimed（takeover 但非 frozen）時，使魔照樣散步、追逐、打招呼冒愛心，主角會轉身面向使魔並在「剛遇到失敗／被擋下／結果未知」的畫面上冒出粉紅愛心——違反 §14「不可把 Unknown、Blocked 演成賣萌」與 §5.2「玩耍…可分別關閉」、§6.1 Quiet Hours。
- 證據：playfield.ts:604-619（early return 只有 `inputs.reducedMotion || w.familiars.length === 0`）、359-362（facing 翻轉在 canPlay 之前）、653-661（attendTo/greetBackUntil 設定）；stage.ts:741（drawFamiliar 無條件）、756-758（`if (greetBackUntil > now) drawGreetHeart`）。實測探針：inputs {ambient:false, quiet:true, playEnabled:false, deskMoveEnabled:false} 一步後 familiar.state=greet/greetWith=char、char.attendTo="a"、下一步 char.facing 由 1 變 -1、事件 greeted-by；同輸入 30 步使魔 x 60→75.08（state walk）。StageRenderer 設 setAnimation("failed"|"blocked"|"unknown")＋machineStageFlags("idle",{kind},anim,false) 後，第一個 greet 幀即出現 beginPath→moveTo:160.00（=char.x）→bezierCurveTo 的愛心繪製；rollCallNow 回報使魔「在跟大家打招呼」。
- verifier（reproducibility）confirmed=True severity=medium：Reproduced independently with a scratchpad-only vitest config (repo untouched) at /private/tmp/claude-501/-Users-user-Workspace-claude-lab-adaptive-interaction/a0f21e4d-b75c-47b2-abed-618b5f97d701/scratchpad/repro-002.test.ts — 9/9 assertions pass against current file contents.

Pure layer (playfield.ts): with inputs {ambient:false, frozen:false, quiet:true, playEnabled:false, deskMoveEnabled:false, reducedMotion:false} and one familiar at x=60, one stepWorld with rng [0.65, 0.1] yields familiar.state="greet", greetWith="char", char.attendTo="a", char.greetBackUntil=now+1800, and a `greeted-by
- 回歸測試：With one familiar present: (a) stepWorld under any of {quiet:true} / {ambient:false} / {playEnabled:false} must leave familiar.state unchanged, emit no `greeted-by`, and keep char.attendTo null and char.facing unchanged; (b) StageRenderer with machineAnim in {failed, blocked, unknown, success-claimed, quiet} (takeover, non-frozen) must never invoke drawGreetHeart nor flip char.facing across frames (spy on drawGreetHeart = 0 calls, no `scale(-1,1)` in the canvas log).

### F-2e02284-20260902T140445Z-companion-gameplay-003 · medium · frozen（emergency/offline/paused）沒有凍結使魔與玩具的裝飾動畫：使魔原地上下抖動、尾巴擺、愛心浮動、羽毛擺、小物件旋轉；Roll Call 在 estop 仍報使魔「在散步」
- 檔案：`apps/interaction-desktop/src/companion/rig/stage.ts:975-979（bob）、:1005（尾巴）、:1037（greet 愛心）、:924（trinket spin）、:952（wand sway）、:780（drawGreetHeart）；apps/interaction-desktop/src/companion/playfield.ts:253（stepWorld frozen 早退）、:748-762（rollCall 使魔標籤）；src/test/regressions-phase7.test.ts:744-766；行號與 finding 一致，無漂移（以工作樹目前內容為準）`
- 維度：companion-gameplay；verdict：confirmed
- 主張：stepWorld 在 frozen 時不步進世界，但 renderFrame 仍每幀繪製，而 drawFamiliar／drawToy／drawGreetHeart 的時間相依動畫只用 `reduced` 閘門、沒有 `this.flags.frozen`：使魔若在 walk/chase 被凍結，會在緊急停止畫面上持續原地上下抖動（bob 由 now 計算）、尾巴持續擺動、greet 狀態的愛心整段 estop 都在浮動；逗貓棒羽毛、小物件旋轉、光點亮度也照動。這違反 §15.2「emergency 搶占所有遊戲動畫」與 playfield.ts:8「frozen：一切凍結」；regressions-phase7.test.ts:744-766 甚至斷言 emergency 下前後兩幀必須不同（把缺陷釘成預期）。另外 rollCall 對使魔不看 machineLabel，estop 期間仍回報「在散步」給 runtime。
- 證據：stage.ts:975-979 `bob = !reduced && (walk||chase) ? Math.abs(Math.sin(now/120))*2 : 0`；1005 尾巴 `swayAt(now,400,2,reduced)`；1035-1042 greet 愛心；924 trinket `swayAt(now+id*900,...)`；952 wand `swayAt(now,260,...)`；以上皆無 frozen 判斷。playfield.ts:748-762 rollCall 使魔標籤只看 f.state。實測探針：idle 下讓使魔進入 walk，再 setAnimation("emergency")＋machineStageFlags("emergency",…) 後，相隔 60ms 的兩幀繪圖指令有 2 處不同（`translate:42.08,199.685` 使魔 y 抖動、`quadraticCurveTo` 尾巴），rollCallNow 兩次都回 F「在散步」。
- verifier（reproducibility）confirmed=True severity=medium：親自重現，主張成立。

程式證據（目前檔案）：
- stage.ts:633-761 renderFrame 每幀都呼叫 drawFamiliar/drawToy/drawGreetHeart 並傳入即時 `now`；stepWorld 在 frozen 時早退（playfield.ts:253），所以使魔 state 停在 walk/chase/greet、玩具留在場上，但繪製端的時間相依項只用 `reduced` 閘門：bob（stage.ts:975-979 `!reduced && (walk||chase) ? |sin(now/120)|*2`）、尾巴 swayAt(now,400,2,reduced)（:1005）、greet 愛心（:1037）、trinket spin（:924）、wand 羽毛（:952）、主角回愛心（:780）。同一函式裡主角的 walking（:698）與 gaze（:715）都有 `!this.flags.frozen`，timeline.ts:383-386 對 FROZEN_EXPRESSIONS 直接回 base 不做任何演出——顯示「凍結＝完全不動」是既定意圖，但使魔／玩具漏掉。
- playfield.ts:748-762 rollCall 對使魔只看 f.state，不看 machineLabel；主角在 estop 會回 ma
- 回歸測試：在 regressions-phase7 風格下：idle 讓使魔進入 walk 並 spawn wand/trinket，切到 emergency（及 offline/paused），不開 Reduced Motion，斷言相隔 60ms 與 10s 的兩幀 recordingCanvas 輸出完全相同（toEqual），且 stage.rollCallNow("已緊急停止") 的每一列 activity 都不是「在散步／在追朋友／在打招呼」；同時把 regressions-phase7.test.ts:744-766 的「非 reduced 兩幀不同」改成用 idle 而非 emergency 當基準。

### F-2e02284-20260902T140445Z-companion-gameplay-004 · medium · 光點／逗貓棒跟著游標，使互動框永遠罩住游標：視窗內大片透明區失去點擊穿透，且每次點擊都會抓住光點
- 檔案：`apps/interaction-desktop/src/companion/rig/stage.ts:534-549 (interactiveBounds, unchanged in working tree); apps/interaction-desktop/src/companion/playfield.ts:200-212 (grabToyAt) and :283-289 (light/wand follow); apps/interaction-desktop/src/companion/CompanionApp.tsx:1570 (`if (hit === "none") return;`); apps/interaction-desktop/src-tauri/src/lib.rs:2085-2098 (want_ignore = !on_character); CHANGELOG.md:325-328 (「空白區維持點擊穿透」)`
- 維度：companion-gameplay；verdict：confirmed
- 主張：interactiveBounds() 把所有玩具（±14 邏輯 px）併進 hit-rect，而 light/wand 每幀 lerp 到 pointer；Rust 只依這個框決定 set_ignore_cursor_events。結果只要丟出光點或逗貓棒（TTL 150 s），游標在加寬後的角色視窗（角色寬×2.6、高×1.35）內移動時框永遠包住游標，底下桌面在整條角色↔游標的聯集矩形內都點不到（CompanionApp 對 hit "none" 只 return、不放行）；而且 grabToyAt 沒排除 light/wand，每次點擊都會把光點「抓在手裡」，光點停止跟隨直到放開。CHANGELOG 承諾「空白區維持點擊穿透」在此情境下不成立。
- 證據：stage.ts:533-549 interactiveBounds 聯集所有 toys；playfield.ts:283-289 light/wand `toy.x += (pointer.x - toy.x)*min(1,dt*14)`；playfield.ts:200-212 grabToyAt 對所有 kind 命中（wand r=16、其餘 12）；src-tauri/src/lib.rs:2085-2098 依 companion_hit_rect 切換 ignore_cursor_events；CompanionApp.tsx:1570 `if (hit === "none") return`；playfield.ts:136 TOY_TTL_MS=150_000。實測探針：spawnToy("light")、pointerMove(30,40)（角色框 x 86..186）、30 幀後 interactiveBounds={x:16,y:26,w:170,h:186} 包含游標；pointerDown(30,40) 回 "toy"、playerGrabbedToys()=1。
- verifier（reproducibility）confirmed=True severity=medium：Reproduced against the current working tree (files are modified vs 2e02284 but the cited lines are unchanged). I ran a scratchpad vitest probe (StageRenderer with stubCanvas 416x216, idle machine flags so the world is not frozen, rng 0.5, autoStart=false): charHitRect = {x:134,y:88,w:52,h:124}; after spawnToy("light") bounds = {x:110,y:88,w:76,h:124}; after pointerMove(30,40) + 30 frames the toy sits at (30.05,40.06) and interactiveBounds() = {x:16.05,y:26.06,w:169.95,h:185.94} — identical to the finding's probe numbers, and the cursor is inside it. Moving the pointer to (400,30) yields bounds
- 回歸測試：StageRenderer with idle machineStageFlags: spawnToy("light"), pointerMove(30,40) far from the character, step 30 frames → assert interactiveBounds() does not contain (30,40) (i.e. cursor toys are excluded from the reported hit-rect, or the rect is char ∪ non-cursor toys only) and pointerDown(30,40) returns "none" with playerGrabbedToys()===0.

### F-2e02284-20260902T140445Z-companion-gameplay-005 · medium · 高頻反應變體不足：點擊只有單一 poked 且不經 Director／無冷卻，hover 氣泡每個性只有 2 句，拖起只有單一 lifted（§5.2 要求 3～6 變體＋防重複＋冷卻）
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/companion/CompanionApp.tsx:1671`
- 維度：companion-gameplay；verdict：confirmed
- 主張：最高頻的互動——單擊——直接 apply clicked → pose "clicked" → 別名 poked，永遠同一個表情，重複點擊在 machine 只是 refresh 續期；沒有變體池、沒有冷卻、不經 Director（只有 ≥3 連戳才走 director.react("poked-rapid")，也是單一表情）。Hover 短氣泡每個性只有 2 句；拖起只有 lifted 一個表情（規格要求被抱起／懸空／掙扎／好奇）。規格 §5.2「每個高頻反應至少 3～6 個變體，具防重複與冷卻」在點擊、hover、拖起三個最高頻反應上都不成立（ambient 池 10 變體、落地 3 種是有做到的）。
- 證據：CompanionApp.tsx:1671 `apply({ type: "transient", kind: "clicked" })`、1657-1670 只有連戳走 Director；machine.ts:324-325 clicked→"clicked"、130-134 同 kind 重複→refresh；expressions.ts:1346 alias clicked→poked，448 poked／470 poked-rapid 是僅有的兩個被戳表情；509 lifted 唯一拖起表情（machine.ts:326-327 dragged→dragged→alias lifted）；attention.ts:102-109 HOVER_LINES 每個 trait 2 句（探針輸出 {curious:2,playful:2,lazy:2,proud:2,witty:2,smart:2}）。
- verifier（reproducibility）confirmed=True severity=medium：親自驗證，所有引用行號與目前檔案一致（無漂移），主張成立：

1. 單擊路徑無變體、無冷卻、不經 Director：`apps/interaction-desktop/src/companion/CompanionApp.tsx:1671` `apply({ type: "transient", kind: "clicked" })`；只有 1.4 s 內 ≥3 次才走 `directorRef.current.react("poked-rapid", …)`（1659），而 `poked-rapid` 也是單一表情（8 s 冷卻，rig.test.ts:510-512 釘的就是這個）。`machine.ts:324-325` clicked→"clicked"，`rig/expressions.ts:1346` alias clicked→"poked"；`machine.ts:130-134` 同 kind 重複回 "refresh"（reduce() 只延長 untilMs），`rig/timeline.ts:239` `if (id === this.exprId) return;`——重播同一表情不會重啟，也沒有任何替代表情。

2. 結構上 reactions 就不可能有變體：`director.ts:53` `reactions: Readonly<Record<st
- 回歸測試：對 shu adapter 的 director tables 與 hover 表斷言：`react("poked")`/`react("lifted")` 在無冷卻下連續呼叫 N 次應回傳 ≥3 個不同 expression 且相鄰不重複、冷卻中回 null；`Object.values(HOVER_LINES).every(l => l.length >= 3)` 且 hoverBubblePolicy 連續兩次不回同一句。

### F-2e02284-20260902T140445Z-rig-renderer-012 · medium · idle→lie-flat 頭部仍單幀瞬移 21 px：blendPose 的 260 ms 窗口在 lie-flat 600 ms enter 中途就結束，把 poseBlend 硬覆寫成 1
- 檔案：`apps/interaction-desktop/src/companion/rig/timeline.ts:409-412 (no drift; line 410 is `const kp = clamp((now - this.switchAt) / POSE_TRANSITION_MS, 0, 1);` at HEAD 2e02284)`
- 維度：rig-renderer；verdict：confirmed
- 主張：poseBlend 機制只對「hold 立即是 lie」的表情有效（sleep）。lie-flat 的 enter 在 450 ms 才把 pose 從 crouch 切成 lie，此時 `(now-switchAt)/POSE_TRANSITION_MS` 早已 ≥1，blendPose(from=stand,to=lie,kk=1) 把 evalPhase 算好的 poseBlend≈0.5 覆寫成 1，頭中心從 mixLayout 的 70.7 直接跳到 lieLayout 的 92.0。CHANGELOG:155 宣稱「poseBlend 通道消除 lie↔stand 頭部瞬移」不成立；此路徑由 Director ambient `lie-flat`（shuTables SHU_AMBIENT_VARIANTS）與微動作 `lie` 正常觸發。
- 證據：timeline.ts:409-412 `const kp = clamp((now - this.switchAt) / POSE_TRANSITION_MS, 0, 1); params = blendPose(this.prevSnapshot, target, params, kp);`；params.ts:362-366 `pose: kk >= 0.5 ? to.pose : from.pose, poseBlend: kk < 0.5 ? 1 - kk : kk`（kk=1 → poseBlend=1，無視 target.poseBlend）；expressions.ts:123-131 lie-flat enter 600ms：kf(0 stand)→kf(0.5 crouch)→kf(1 lie)。probe（60fps 逐幀量 layoutFor().hy+bodyBob+nod）：`idle -> lie-flat: worst 21.33px at +617ms, 70.7->92.0 pose=lie blend=1.00`；對照 `idle -> sleep` 只有 2.97px、`sleep -> idle` 2.96px。
- verifier（reproducibility）confirmed=True severity=medium：Reproduced independently at HEAD 2e02284 with a frame-by-frame probe (vitest, node env, 60 fps, same method as the existing test at src/test/regressions-phase7.test.ts:894 but in the idle→lie-flat direction, measuring layoutFor(tl.paramsAt(t), RIG_PALETTES["maid-classic"]).hy):

idle -> lie-flat: worst 21.33px at +617ms, 70.7->92.0, prev frame (crouch, poseBlend 0.53) -> cur frame (lie, poseBlend 1.00)
sit -> lie-flat: worst 20.89px at +600ms, 71.1->92.0
idle -> sleep: 2.95px; sleep -> idle: 2.95px; lie-flat -> idle: 2.95px (these work because the pose flips at switchAt, so kp runs 0→1 in step
- verifier（spec-honesty）confirmed=True severity=medium：Independently reproduced at HEAD 2e02284; the finding's numbers are exact and the mechanism is as described.

Code path (verified by reading, no drift):
- expressions.ts:123-131 `lie-flat` enter = 600 ms, kf(0 stand) → kf(0.5 crouch) → kf(1 lie). evalPhase/lerpParams (params.ts:326-343) flips the pose string at the segment midpoint, so `target.pose` becomes "lie" only at t≥0.75 of enter = 450 ms after switchAt; before that lerpParams sets target.poseBlend to ramp 1→0.5 (crouch side), then 0.5→1 (lie side) — on its own this would be continuous (hy 52→72 then 69→92).
- timeline.ts:410-411 then a
- 回歸測試：In src/test/regressions-phase7.test.ts #15 add the reverse-direction case: `tl.setAnimation("idle",0); tl.paramsAt(3000); tl.setAnimation("lie-flat",3000);` then step t from 3000 by 16.7 ms to 4200 measuring `layoutFor(tl.paramsAt(t), PAL).hy`, assert max consecutive-frame |Δhy| < 12 and final hy ≈ 92 (and the same for "sit"→"lie-flat"); currently fails with 21.33 px. / Mirror of the existing #15 test in the opposite direction: new ExpressionTimeline(()=>0.5,0); setAnimation("idle",0); paramsAt(3000); setAnimation("lie-flat",3000); step 16.7 ms to 4200 recording |Δ layoutFor(paramsAt(t),PAL).hy|; expect max < 12 and final hy ≈ 92 (currently fails with 21.33 px at +617 ms).

### F-2e02284-20260902T140445Z-rig-renderer-013 · medium · exit 段從 hold 起算而非從當前參數：表情在 enter／loop 中途被打斷時單幀跳變（手臂 45 px、squash、眼睛、bodyBob）
- 檔案：`apps/interaction-desktop/src/companion/rig/timeline.ts:253（base 取 hold）與 :365-372（exit 分支直接回傳 evalPhase，無 crossfade）；行號無漂移，timeline.ts 與 HEAD 2e02284 相同`
- 維度：rig-renderer；verdict：confirmed
- 主張：setAnimation 把離開段的 base 設成 `clampParams({...DEFAULT_PARAMS, ...outgoing.hold})`，paramsAt 的 exit 分支直接回傳 evalPhase(e.base, …) 而沒有從 lastParams crossfade。只要上一表情還在 enter（stretch 1300ms、pounce-miss 1100ms、yawn 1500ms…）或 loop 的非零相位，切換那一幀就從「當前姿勢」瞬移到「hold 姿勢」。使用者在她伸懶腰時點一下（clicked→poked）即可重現。
- 證據：timeline.ts:247-257 `base: clampParams({ ...DEFAULT_PARAMS, ...outgoing.hold })`；timeline.ts:365-372 `const params = evalPhase(e.base, e.phase, (now - e.startAt) / span); this.lastParams = params; return params;`（無 lerp 自 prevSnapshot）。probe（連續兩幀，切換前/後）：`stretch(mid-enter 845ms) -> poked: exiting=true squash -0.224->0.000 armPose stretch->front armPhase 0.707->0.000 eyeOpen 0.378->1.000 headNod -0.636->0.009`（draw.ts:378 stretch 手 y≈40 → draw.ts:458 front 手 y≈86，一幀 45px）；`pounce-miss(330ms) -> idle: bodyBob -3.855->0.000 bodyLean 4.820->0.000`；`idle(mid-loop) -> notice: bodyBob -1.584->-0.125 tailSway 0.294->0.021`。rig.test.ts:349 只在 hold 穩態後切換，測不到。
- verifier（reproducibility）confirmed=True severity=medium：親自重現，主張成立。

程式碼證據（目前檔案內容）：
- timeline.ts:251-256 `this.exiting = { phase: seg.exit, base: clampParams({ ...DEFAULT_PARAMS, ...outgoing.hold }), startAt: nowMs, until: ... }`——離開段的基準是上一表情的 hold，不是 `this.lastParams`（切換那一刻實際輸出的參數）。
- timeline.ts:365-372 exit 分支 `const params = evalPhase(e.base, e.phase, ...); this.lastParams = params; return params;` 直接回傳，沒有從 prevSnapshot/lastParams 做任何 lerp；line 260 雖然把 prevSnapshot 設成 lastParams，但 exit 期間完全不使用它，且 line 375 在 exit 結束時又用「exit 最後一幀」覆寫 prevSnapshot，所以後續的 180ms crossfade 也是從離開姿勢起算，被打斷的姿勢從未被插值到。
- stretch 的 hold 為 `{}`（expressions.ts:228-238），enter 13
- 回歸測試：假時鐘：setAnimation("stretch",0) 跑到 845ms（armPose=stretch、squash<-0.2），setAnimation("poked",845) 後 paramsAt(861) 的 squash/armPhase/eyeOpen/headNod 與 845ms 的值差距須小於一幀合理步長（例如 |Δsquash|<0.05、armPose 不得在第一幀就變 front），且 exit 期間各幀應從 845ms 的參數連續過渡到 hold。

### F-2e02284-20260902T140445Z-rig-renderer-014 · medium · 規格 §6.2 範例「趴著＋核心顯示 Agent 工作中」不可達：組合式通道只對 chase/stroll/pounce/carry/sniff 遊玩姿勢生效，lie/sit/doze 一律被 working 整體搶佔
- 檔案：`apps/interaction-desktop/src/companion/rig/stage.ts:51 (comment), :90-106 (playExpressionFor), :242-247 (stageExpressionPlan takeover branch) — line numbers unchanged; working-tree Phase 8 diff to stage.ts only adds pause/resume/start/loop stats, does not touch this logic`
- 維度：rig-renderer；verdict：confirmed
- 主張：stage.ts:49-52 註解宣稱「身體姿勢仍然是遊玩中的姿勢（趴著＋核心顯示 Agent 工作中）」，但 playExpressionFor 只回 play-chase／sneak-closer／play-carry／curious，CharPlayMode 沒有任何躺／坐／打盹模式；躺下是 machine 的 performing transient（lie-flat），acting(40)>performing(25) 會直接取代它，pose 變成 "act"，此時 mode="free" → `!play` → takeover → 站起來插口袋。規格的代表性組合（趴著操作）在本實作中做不到，Phase 2 交付文字（CHANGELOG:149）只對「遊玩姿勢」成立。
- 證據：stage.ts:90-106 `playExpressionFor` 只有 chase/stroll→play-chase、pounce→sneak-closer、return/refuse/carry→play-carry、sniff→curious，default null；stage.ts:245-246 `if (overlay === "takeover" || !play) return { expression: machineAnim, useMachineSlice: true, statusChannels: null }`；playfield.ts:36-45 CharPlayMode = free|stroll|chase|pounce|carry|return|refuse|sniff；machine.ts:49-63 PRIORITY acting 40 / performing 25，machine.ts:260-277 replace 單一 transient 槽。probe：`stageExpressionPlan("act","free")` → `{expression:"act", useMachineSlice:true, statusChannels:null}`（takeover）。
- verifier（reproducibility）confirmed=True severity=medium：Reproduced end-to-end with a scratchpad vitest probe (run against the current working tree, 3/3 assertions pass = defect present):
1) machine: base idle → performing{animation:"lie-flat"} → pose()="lie-flat"; then acting arrives → transientCompetition returns "replace" (PRIORITY acting 40 > performing 25, machine.ts:49-63, 126-133) → pose()={animation:"act"}. Reverse direction also fails: with acting active, a Director lie-flat performing is "keep"-ed out, so 趴著 cannot be layered onto working from either side.
2) stage: CharPlayMode (playfield.ts:36-45) has no lie/sit/doze; playExpressionFor("
- 回歸測試：machine idle → performing lie-flat → acting transient; feed pose().animation ("act") + char mode "free" into stageExpressionPlan and the timeline; assert rig params keep pose==="lie" while coreGlow===1 (currently pose==="stand", armPose==="pocket").

### F-2e02284-20260902T140445Z-rig-renderer-016 · medium · armPose 等字串通道在 t≥0.5 硬切：stretch 手位單幀跳 45 px，只有 pose=lie 有 poseBlend 補償
- 檔案：`apps/interaction-desktop/src/companion/rig/params.ts:333（無漂移；rig/params.ts、draw.ts、expressions.ts、timeline.ts 在工作樹與 HEAD 2e02284 完全相同，只有 rig/stage.ts 有未提交修改）`
- 維度：rig-renderer；verdict：confirmed
- 主張：lerpParams 對所有字串通道（armPose/mouth/overlay/particles/pose）在中點硬切；armPose 決定手的位置（draw.ts drawArms 各 case 座標差 30–45px），所以任何 armPose 變化（stretch 的 front→stretch→front、ask 的 front→raise、caught-slacking 的 sit→stand＋front→raise 同時）都是一幀瞬移，armPhase 的數值插值救不了。acceptance-evidence.md:486 已知限制 #10 只承認 stand↔sit 10px，未提手臂。
- 證據：params.ts:330-334 `if (typeof av === "number" && typeof bv === "number") … else out[key] = tt >= 0.5 ? bv : av;`，params.ts:339-343 只對 lie 補 poseBlend。expressions.ts:228-251 stretch enter kf(0.2 armPose front)→kf(0.65 stretch)→kf(0.85 stretch)→kf(1 front)。probe（evalPhase）：`t=0.42 (546ms) armPose=front armPhase=0.49 handY≈86.0` → `t=0.43 (559ms) armPose=stretch armPhase=0.51 handY≈40.5`；`t=0.92 (1196ms) stretch handY≈40.6` → `t=0.93 (1209ms) front handY≈86.0`（draw.ts:378 `shY - 26 - ph*4` vs draw.ts:458 `L.waistY + 2`）。
- verifier（reproducibility）confirmed=True severity=medium：親自重現，主張成立。

程式碼證據（目前工作樹）：
- params.ts:333 `out[key] = tt >= 0.5 ? bv : av;` — 所有非數值通道（含 armPose）在中點硬切；params.ts:339-343 的 poseBlend 補償只在 `a.pose !== b.pose && (lie 參與)` 時生效，armPose 無任何補償。全 repo grep `armBlend|prevArm|armMix` 零命中。
- draw.ts:331-460 drawArms 依 armPose switch 決定手位：stretch 手 y = shoulderY+2.5-26-ph*4（:378），front 手 y = waistY+2（:458）；armPhase 只在同一 case 內微調 ≤4px，跨 case 不插值。
- renderer.ts:81、stage.ts:770 直接 `drawRig(ctx, params, pal)` 使用 timeline 輸出，無任何逐幀平滑。
- expressions.ts:228-238 stretch enter：kf(0.2 crouch/front)→kf(0.65 stand/stretch)→kf(0.85 stretch)→kf(1 front)。stretch 在 chara
- 回歸測試：用 ExpressionTimeline setAnimation("stretch") 以 60fps 取樣 paramsAt，透過 layoutFor＋drawArms 手位公式（或 Proxy ctx 抓 drawRig 的 hand ellipse y）計算連續兩幀手部 y 位移，斷言整段 enter 最大單幀位移 < 12px（比照 regressions-phase7.test.ts:894 的頭部測試）。

### F-2e02284-20260902T140445Z-rig-renderer-017 · medium · §4.2 頭飾＝網路／Agent 連線狀態、奔跑後歪掉扶正、袖口面板、口袋取物：頭飾光只由表情 hold 寫入，無任何真實連線狀態輸入；奔跑不歪頭飾
- 檔案：`apps/interaction-desktop/src/companion/rig/params.ts:105-108 (unchanged); walking secondary-motion block is stage.ts:712-727 in the working tree (HEAD 2e02284: stage.ts:650-665), not 697-710`
- 維度：rig-renderer；verdict：confirmed
- 主張：headpieceGlow 唯一的寫入來源是表情 hold（working 0.8、wait-codex/wait-claude 0.9、ask 0.8、emergency/offline 0，其餘 DEFAULT 0.25），沒有程式讀取 provider／agent 連線狀態（activeAgents、provider.state-changed、mobile provider paired）去點亮或熄滅它——docs/FEATURES.md:154 與 CHANGELOG:187 的「頭飾光=Agent 連線」實際上是「有 agent 工作類表情時亮」。headpieceTilt 只在 startled-awake/lifted/wobbly-landing/groom 關鍵幀出現；stage 的步行 secondary motion 與 playfield 都不因奔跑（chase 速度>80）歪頭飾，params.ts:105 註解「奔跑後歪掉→扶正」無對應行為。袖口展開工作面板、圍裙口袋取出玩具／檔案／知識卡片皆無實作（armPose "pocket" 只是姿勢）。
- 證據：grep -rn headpieceGlow/headpieceTilt src（排除 rig/ 與 test/）→ 零命中；expressions.ts:884,923,950,983,1146,1163 為僅有的 headpieceGlow 寫入（皆表情 hold）；headpieceTilt 只在 expressions.ts:197-209（startled-awake）、526-540（lifted）、556-557（wobbly-landing）、249-250（groom）；stage.ts:697-710 walking 只改 legPhase/bodyBob/hairSway/tailSway；grep -rn 'pocket|口袋|袖口|sleeve' src（排除 rig/）→ 零命中。
- verifier（reproducibility）confirmed=True severity=medium：Tried to refute; could not. Verified at HEAD 2e02284 (stage.ts has an uncommitted perf-budget diff that does not touch headpiece/walking).

1) headpieceGlow has no connection-state input. `grep -rn headpieceGlow apps/interaction-desktop/src` → only params.ts (type/DEFAULT 0.25/range), draw.ts:830-833 (render), stage.ts:69 (STATUS_CHANNELS list), preview-entry.ts, and expressions.ts holds at 884 (working 0.8), 923/950 (wait-codex/wait-claude 0.9), 937-938 (wait exit), 983 (ask 0.8), 1146 (emergency 0), 1163 (offline 0). No test file references it. The only code path that reacts to provider/devi
- 回歸測試：Stage/machine test: dispatch provider.state-changed {state:"paired"} then {state:"disconnected"} and assert lastFrameParams().headpieceGlow rises above the idle 0.25 baseline and then drops (currently stays 0.25 in both); plus a stage test with char.vx > 80 (chase) asserting headpieceTilt != 0 while running and returns to 0 after stopping.

### F-2e02284-20260902T140445Z-director-pipeline-022 · medium · presentation `cancel`（單一 actionId、AI 可呼叫）與 estop `clear-all` 無差別清掉 blocked／failed／unknown 與安全氣泡
- 檔案：`apps/interaction-desktop/src/companion/CompanionApp.tsx:1026-1036 (branch starts at 1026, not 1027; file unchanged at HEAD 2e02284)`
- 維度：director-pipeline；verdict：confirmed
- 主張：Rust 端 `cancel` 是「針對某個仍在等 ACK 的 presentation action」帶 actionId 送出，但角色視窗的 handler 忽略 actionId，一律 stopSpeech、清掉所有氣泡（含被擋下／失敗／未知的固定安全文字與 sticky 緊急氣泡）並 `clear-transient`；而 machine 的 `clear-transient` 對 SAFETY_TRANSIENTS 也無條件清除。AI 用同一把 token 走 `POST /v1/actions/{id}/cancel` 或 `interaction.cancel` 取消自己一則尚未 ACK 的 `companion.speak`（ACK 要等語音講完，最長 9 秒），就能把同時在台上的 blocked／failed／unknown 表演與其安全氣泡提早抹掉。另外緊急停止時每個 presentation actuator 的 emergency_stop 會先送 `clear-all`，`emergency.stop` 事件之後才發，所以「安全訊息本身留著」（regressions-phase7 #8 釘死的意圖）在真實流程中被前一步的 clear-all 先清光。
- 證據：CompanionApp.tsx:1027-1036：`if (command === "cancel" || command === "clear-all") { ... stopSpeech(); showBubbleRaw(null, 0); if (cancelPlan.transient === null) apply({ type: "clear-transient" }); return; }`（actionId 只在 1026 取出，這個分支完全不用）。machine.ts:249-250 `case "clear-transient": return { ...state, transient: null };`（不看 SAFETY_TRANSIENTS，機器 base 為 idle 時 blocked/failed/unknown 直接歸零）。presentation.rs:794-808 `cancel(action_id)`：`if self.bridge.take_pending(action_id).is_some() { publish PresentationCommand {actionId, command:"cancel"} }`；runtime.rs:820-829 `cancel_action` → `actuator.cancel(action_id)`；interaction-api/src/lib.rs:263 `.route("/v1/actions/{id}/cancel", post(routes::action_cancel))`、routes.rs:724 `"interaction.cancel"`。speakText 的 ACK 在 CompanionApp.tsx `if (plan.speech) await speakText(plan.speech)` 之後才送，最長 9s。presentation.rs:811-828 emergency_stop 無條件 publish `clear-all`；runtime.rs:889-896 actuator emergency_stop 迴圈在 `self.events.emit(EventType::EmergencyStop, ...)`（runt
- verifier（reproducibility）confirmed=True severity=medium：Verified against the current tree (HEAD 2e02284, source files clean) and reproduced with a scratch vitest run.

Confirmed — the cancel half (AI-reachable):
- /Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/companion/CompanionApp.tsx:1026-1036: `if (command === "cancel" || command === "clear-all") { ... stopSpeech(); showBubbleRaw(null, 0); if (cancelPlan.transient === null) apply({ type: "clear-transient" }); return; }`. `actionId` (line 1025) is never used in this branch. `showBubbleRaw` (351-359) unconditionally `setBubble(null)` on the single bubble slot —
- 回歸測試：In a CompanionApp/machine-level test: show a `plan.blocked` (transient "blocked" + safety bubble text), then deliver `presentation.command {command:"cancel", actionId:"<some other pending action>"}`; assert the blocked transient and the fixed "blocked" bubble are still present (and separately that `clear-transient` in machine.ts refuses to drop SAFETY_TRANSIENTS unless explicitly forced), while a "performing" transient IS cleared by the same cancel.

### F-2e02284-20260902T140445Z-companion-gameplay-006 · low · 「游標靠近時看過來」(companionApproach) 關掉後舞台仍會把眼睛／耳朵／頭轉向游標——這個行為實際由 companionCursorPlay 控制，一項行為兩個主人
- 檔案：`apps/interaction-desktop/src/companion/rig/stage.ts:724（工作樹；HEAD 2e02284 對應第 683 行——本次審查以工作樹為準，gatewayWiring.ts 為未追蹤新檔）`
- 維度：companion-gameplay；verdict：confirmed
- 主張：CompanionApp 只用 approachAllowed 擋掉 listening transient 與 pointer-approached 事件；舞台的注視偏移 pointerGazeDir 只看 toggles.cursorPlay，adapterReconfigureFor 也根本沒把 companionApproach 傳給 adapter/stage。使用者把「游標靠近時看過來」關掉後，角色在遊玩場內仍持續看向游標（且不受本機 quiet-1h 影響）；而關掉「游標互動（光點、逗貓棒跟著游標）」反而會停掉看過來。違反 §12.6 每項設定只有一個主人，UI 標籤與實際行為不一致。
- 證據：stage.ts:715-730 `else if (this.toggles.cursorPlay) dirWorld = pointerGazeDir(this.pointer, char.x)`；gatewayWiring.ts:436-449 只傳 play/cursorPlay/deskMove/familiars/tuning；shu.ts:248-252 toggles 只認 play/cursorPlay/deskMove；CompanionApp.tsx:1462-1478 approachAllowed 只擋 listening/pushInteraction；CompanionPage.tsx:642-647 兩個標籤分別為「游標互動（光點、逗貓棒跟著游標）」與「游標靠近時看過來」。
- verifier（reproducibility）confirmed=True severity=low：親自驗證，主張成立，未被修復。

1. 舞台注視只由 cursorPlay 閘門：stage.ts:712 註解寫「注視：有使魔向她打招呼就回看，否則**游標靠近時看過來**」——與 CompanionPage.tsx:646 的 companionApproach 開關標籤一字不差——但 :724 實際條件是 `else if (this.toggles.cursorPlay) dirWorld = pointerGazeDir(this.pointer, char.x)`。:715 的外層條件只看 reduced／frozen／takeover，連 flags.quiet（DND）都不看。

2. approach 根本到不了舞台：StageToggles（stage.ts:262-266）只有 play/cursorPlay/deskMove；adapterReconfigureFor（gatewayWiring.ts:436-450）只帶 name/scene/play/cursorPlay/deskMove/familiars/tuning；shu.ts:248-252 只轉發那三個 key。`grep -rn approach` 在 stage.ts／playfield.ts／character/ 只命中 tuning.approachDistance（個性距離，非開關
- 回歸測試：StageRenderer（autoStart:false、idle、ambient）在 reconfigure/setToggles 帶 approach:false（cursorPlay:true）後 pointerMove 到 char.x+60 再 renderFrame，lastFrameParams().pupilX／headTurn 必須等於無游標基線；且 adapterReconfigureFor({companionApproach:false}) 的 payload 必須含 approach:false。

### F-2e02284-20260902T140445Z-companion-gameplay-007 · low · 緊急停止／離線／暫停期間仍可從選單生成玩具並拖曳玩具，玩具懸在半空、跟著游標移動
- 檔案：`apps/interaction-desktop/src/companion/rig/stage.ts:501-503 (spawnToy), :552-563 (pointerDown), :571-590 (pointerMove), :592-603 (pointerUp); playfield.ts:169-189 spawnToy / :200-238 grabToyAt·dragToy·releaseToy / :253 stepWorld frozen early-return; CompanionApp.tsx:1671-1672 (apply clicked → setMenuOpen), :1760-1773 quickToy, :1878-1886 toy row; machine.ts:251-256 (finding cited 254-256, drift +3 lines); character/adapters/shu.ts:397-403 gameplay.spawnToy`
- 維度：companion-gameplay；verdict：confirmed
- 主張：frozen 只擋 stepWorld；StageRenderer.spawnToy／pointerDown／pointerMove／pointerUp 與 CompanionApp.quickToy 都沒檢查基態。estop 時點角色雖然 clicked transient 被 reduce 壓掉，但 setMenuOpen 仍切換選單，玩具列可用：新玩具生成在 ground-60 懸空不落地；既有玩具可被抓起跟著游標移動並被畫出來，放開時速度保留，解除 estop 瞬間飛出去並立刻被追（releaseToy 把 interest 設 1、cooldown 清 0）。與「停住的系統不做任何表演」及 §15.2 emergency 搶占的精神不一致。
- 證據：stage.ts:501-503 spawnToy、552-606 pointer* 無 frozen 判斷；playfield.ts:214-238 dragToy/releaseToy 無狀態判斷；CompanionApp.tsx:1671-1673 `apply clicked` 後仍 `setMenuOpen`（machine.ts:254-256 emergency 下 transient 被丟棄但選單照開）、1760-1773 quickToy 無基態檢查。探針：frozen:true 下 grabToyAt→dragToy(300,100) 後 stepWorld 回傳 toy x=300,y=100,grabbed:player。
- verifier（reproducibility）confirmed=True severity=low：Reproduced against the current working tree with a scratchpad vitest probe (config root=app dir, jsdom, same setup.ts) — no repo files modified.

Code evidence (all read directly):
- stage.ts: `frozen` is only consumed as a stepWorld input (l.665) and in the walking/gaze overlays (l.698, 715). `spawnToy` (501-503), `pointerDown` (552-563), `pointerMove` (571-590), `pointerUp` (592-603) have no frozen/base check.
- playfield.ts: header l.8 promises "frozen（緊急/離線/暫停）：一切凍結"; `stepWorld` returns early at l.253, so a toy spawned at `ground-60` (l.178) never falls. `grabToyAt`/`dragToy`/`releaseToy`
- 回歸測試：StageRenderer with machineStageFlags("emergency"): spawnToy must leave toyCount()===0 (or place it resting on ground), pointerDown on an existing toy must not return "toy"/grab it, and a toy released while frozen must have zero velocity after flags return to idle (toyPoints unchanged over the next 10 frames); plus a CompanionApp test that a click under baseState==="emergency" does not open the toy row.

### F-2e02284-20260902T140445Z-companion-gameplay-008 · low · 拖入檔案預覽只顯示檔名與數量，缺 §5.2 要求的大小、類型與可讀 Agent
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/companion/CompanionApp.tsx:1845-1875（工作樹版本；行號無漂移，該檔雖為 M 狀態但 diff 只動到 1737/1740 的 `trustedText` 條件，拖放對話框本體未改）`
- 維度：companion-gameplay；verdict：confirmed
- 主張：規格 §5.2「能接住拖入檔案，先顯示檔名、大小、類型、資料去向與可讀 Agent，再確認」；實作的 dropPreview 對話框只列 basename 與數量，加一句固定的「只記錄檔案位置…」說明，沒有大小、類型，也沒有列出哪些 Agent session 可讀（CompanionInput 已有 sessions/dataScope 資料可用）。此項不在 acceptance-evidence 的已知限制清單內。
- 證據：CompanionApp.tsx:1845-1875：`{p.split("/").pop()}`、`收到 {dropPreview.length} 個項目`、固定文案；onDragDropEvent 只取 paths（1523-1527）；docs/acceptance-evidence.md:466-497 已知限制未列此項。
- verifier（reproducibility）confirmed=True severity=low：親自核對後主張成立，且無法反駁：

1. 規格原文確認：docs/specs/adaptive-interaction-v05-core-experience-prompt.md:203（§5.2）「能接住拖入檔案，先顯示檔名、大小、類型、資料去向與可讀 Agent，再確認」。

2. 實作只有檔名＋數量＋固定資料去向文案：CompanionApp.tsx:1845-1875 的 `dropPreview` 對話框渲染 `收到 {dropPreview.length} 個項目`、`{p.split("/").pop()}`（前 5 筆）、以及固定字串「只記錄檔案位置，不讀取內容、不上傳、不離開本機」。整個 CompanionApp.tsx 對 `bytes`／`mediaType`／`KB`／`MB`／formatBytes 零命中；`api.agentSessionsList()` 只在 CompanionInput（文字輸入，1974 行）被呼叫，拖放對話框沒有任何 session／dataScope 顯示。

3. 大小／類型在 host 端根本沒有來源：onDragDropEvent（1523-1527）只取 `paths`；Tauri 2.11.1 的 `DragDropEvent` drop payload 只有 `paths`＋`position`（node_modu
- 回歸測試：vitest：以 paths=["/tmp/a.txt"] 觸發 dropPreview 並 mock host stat 回 {bytes:1234, mediaType:"text/plain"} 與 agentSessionsList 回一個 session，斷言對話框文字含「1.2 KB」「text/plain」與該 session 的 label／dataScope；無 stat 時斷言顯示「大小／類型：未知」而非省略。

### F-2e02284-20260902T140445Z-companion-gameplay-009 · low · 游標「離開」沒有任何反應：hover-left 輸入事件已定義卻從未送出（§5.1 靠近／進入／離開／點擊／連點）
- 檔案：`apps/interaction-desktop/src/companion/CompanionApp.tsx:1481-1485 (no drift); gatewayWiring.ts:630-631 (no drift)`
- 維度：companion-gameplay；verdict：confirmed
- 主張：onPointerLeaveCanvas 只清 pointer 與 hover 計時，既不 pushInteraction("pointer-left") 也不給 Director 任何反應；gatewayWiring.inputEventFor 雖已定義 "pointer-left"→character.hover-left，但 host 從未呼叫，角色對游標離開毫無反應（光點熄滅只是玩具行為）。
- 證據：CompanionApp.tsx:1481-1485 函式本體只有 pointerLeave()/toyDragRef=false/hoverSinceRef=0；RECEPTOR_FOR_KIND（125-139）沒有 pointer-left；gatewayWiring.ts:630-631 定義了 pointer-left→character.hover-left 但 `grep pointer-left` 在 CompanionApp 無任何呼叫。
- verifier（reproducibility）confirmed=True severity=low：Verified on current working tree (HEAD 2e02284 + uncommitted edits). (1) CompanionApp.tsx:1481-1485 `onPointerLeaveCanvas` only calls stage.pointerLeave(), resets toyDragRef and hoverSinceRef — no pushInteraction, no apply(), no director call. (2) gatewayWiring.ts:630-631 defines `pointer-left`→`character.hover-left`, but `grep -rn pointer-left apps/interaction-desktop/src` returns only that definition; RECEPTOR_FOR_KIND (125-139) also lacks it, and companion-gateway-wiring.test.ts:263-275 asserts pointer-approached but never pointer-left — the mapping is unreachable, untested dead code. (3) E
- 回歸測試：In a CompanionApp test with feed="protocol" and a stubbed gateway, dispatch pointerenter then pointerleave on the companion canvas and assert gateway.ingestInput was called with {kind:"character.hover-left"} (and/or that a director/stage leave reaction is triggered); additionally add `expect(inputEventFor("pointer-left")).toEqual({kind:"character.hover-left",payload:{}})` to companion-gateway-wiring.test.ts so the mapping is no longer untested.

### F-2e02284-20260902T140445Z-companion-gameplay-010 · low · 多角色「玩耍」只有使魔↔使魔：使魔不會一起玩玩具，主角也不主動找使魔（§5.1「互相注意、打招呼、愛心、追逐及玩耍」）
- 檔案：`apps/interaction-desktop/src/companion/playfield.ts:604 (無漂移；playfield.ts 在工作樹未修改，604 即 `function stepFamiliars(`)`
- 維度：companion-gameplay；verdict：confirmed
- 主張：stepFamiliars 完全不引用 toys（沒有追球／搶球／圍觀），stepChar 也沒有任何以使魔為目標的模式；互動只有：使魔→使魔 greet/chase（雙向有反應）、使魔→主角 greet（主角回看／回愛心）。主角從不主動向使魔打招呼或追逐，使魔對場上的玩具與主角的追逐無感——「玩耍」這一項只是資料結構（Familiar 有 state 但無 play 狀態），沒有行為。
- 證據：playfield.ts:604-707 stepFamiliars 內無 `toys` 字樣、FamiliarState 只有 idle/walk/sleep/greet/chase（65）；stepChar 349-588 的 chase/pounce/return 目標皆為 toy id，無使魔目標；CHANGELOG.md:337-339 也只宣稱「自主散步/睡覺/互相注意/打招呼/追逐」。
- verifier（reproducibility）confirmed=True severity=low：主張成立，靜態＋動態證據皆確鑿。

【靜態】/Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/companion/playfield.ts：
- :65 `export type FamiliarState = "idle" | "walk" | "sleep" | "greet" | "chase"` — 無 play 狀態。
- stepFamiliars（604-707）對 `toys` 零引用（grep 行號 ≥600 的 toy/toys 全無命中）；狀態轉移只依 rng、其他使魔位置、`w.char.x`。
- stepChar（349-588）唯一的 familiars 引用在 :360（被動回看 attendTo）；chase/pounce/return/refuse 目標一律是 `targetToy`（number 玩具 id），沒有以使魔為目標的模式。使魔→主角 greet 只在 stepFamiliars :641-661 由使魔發起；主角從不主動。
- 全 repo sweep（quoted glob）：playfield.ts 以外沒有任何檔案同時提到 familiar 與 toy/ball/chase/play；character/* 的 
- 回歸測試：vitest：createWorld＋2 使魔＋地面高 interest 毛球、playEnabled=true、跑 N 步，斷言至少一隻使魔曾進入以玩具為目標的狀態（新增 FamiliarState "play" 或 `familiar-played {id, toyId}` 事件），且主角曾進入以使魔 id 為目標的主動互動模式——現況兩者皆恆為 0，測試會紅。

### F-2e02284-20260902T140445Z-rig-renderer-015 · low · 36 表情四段式實為「hold＋派生」為主：手寫 enter 20/36、loop 29/36、exit 11/36，四段全手寫只有 4/36
- 檔案：`apps/interaction-desktop/src/companion/rig/timeline.ts:148（resolveSegments）；DERIVED_ENTER :96、DERIVED_AMBIENT_LOOP :104、DERIVED_STATUS_LOOP :112、DERIVED_EXIT :120；離散通道中點硬切在 apps/interaction-desktop/src/companion/rig/params.ts:333（lerpParams：`tt >= 0.5 ? bv : av`）。行號無漂移。`
- 維度：rig-renderer；verdict：confirmed
- 主張：規格 §7.5「每個表情定義進入、保持、小循環、離開」；實作中 25/36 表情共用同一個 140 ms DERIVED_EXIT（headNod／earPerk 過衝 4% 再回中性），16/36 共用同一個 120 ms DERIVED_ENTER（squash −0.06 壓一下），7/36 loop 也是派生。對 ask（舉手＋問號）、blocked（盾）、unknown 等，派生 exit 只是頭部微抖，浮標/手臂仍在 crossfade 中點硬切。docs/FEATURES.md:153 稱「真四段式」；派生一事在 CHANGELOG:148 有揭露，但 §7.5 的要求並未達成。
- 證據：node 腳本統計（resolveSegments(...).derived）：enter 手寫 20/36、loop 手寫 29/36、exit 手寫 11/36（poked, poked-rapid, lifted, wobbly-landing, pounce-miss, slip-play-cool, caught-slacking, startled-awake, wait-codex, success-verified, failed）、四段皆手寫 4/36（poked, poked-rapid, lifted, success-verified）；只有 hold+loop 兩段手寫者 14/36（question, deadpan, spaced-out, pretend-not-hear, sneak-closer, hold-ball, keep-ball, await-player, thinking, routing, working, wait-claude, blocked, not-found, unknown）。timeline.ts:96-128 DERIVED_ENTER／DERIVED_EXIT 為所有表情共用常數；timeline.ts:148-163 resolveSegments。
- verifier（reproducibility）confirmed=True severity=low：親自重現，主張的事實全部成立；但屬於「規格內容完成度不足＋已部分揭露」的缺口，不觸及誠實／安全不變量，故由 medium 降為 low。

1) 數字可重現。以 esbuild 打包 OFFICIAL_36 × resolveSegments(...).derived 跑 node（腳本在 scratchpad/count.ts）：36 個正式表情中手寫 enter 20/36、loop 29/36、exit 11/36；四段全手寫只有 poked、poked-rapid、lifted、success-verified（4/36）；只手寫 hold+loop（enter、exit 皆派生）者 15/36（finding 寫 14 但列了 15 個 id，含 wait-claude；實際 15）；無手寫 loop 7/36（startled-awake、wobbly-landing、pounce-miss、slip-play-cool、caught-slacking、success-claimed、failed）。timeline.ts:96–128 的 DERIVED_ENTER／DERIVED_EXIT／兩個 DERIVED_*_LOOP 是所有表情共用的常數，resolveSegments（:148–163）以 `expr.exit ?? DERIVED_EXIT` 補齊。


- 回歸測試：對 OFFICIAL_36 中 hold 設了離散通道（armPose≠"front" 或 overlay≠"none"）的表情，斷言 resolveSegments(expr).derived.exit === false 且 exit 最後一幀把 armPose 收回 "front"、overlay 收回 "none"；並用 ExpressionTimeline 假時鐘模擬 ask→idle，逐幀斷言 armPose/overlay 在 exit 結束前已回中性、crossfade 期間不出現單幀 raise→front／question→none 跳變。

### F-2e02284-20260902T140445Z-director-pipeline-023 · low · Attention／Utility Scoring 在生產路徑是死碼：scoreEvent 只在同優先級平手時以常數情境呼叫且結果恆為 replace；Director.score() 無人呼叫；第二套微動作排程器只存在於測試
- 檔案：`apps/interaction-desktop/src/companion/machine.ts:135-145 (transientCompetition 121-146；行號未漂移，HEAD 2e02284 與工作樹相同)；director.ts:148-150；behavior.ts:182, 232；character/adapters/shuTables.ts:115；docs/FEATURES.md:155；CHANGELOG.md:150, 207-209`
- 維度：director-pipeline；verdict：confirmed
- 主張：spec §6 的「Attention Manager → Utility Scoring」在實際程式裡沒有作用：(1) `Director.score()` 沒有任何生產呼叫者；(2) `scoreEvent` 唯一的生產呼叫在 `transientCompetition`，僅在兩個 transient PRIORITY 相等且非同一事件時執行，而 PRIORITY 表中所有同分配對（succeeded/unknown、clicked/dragged、waiting-for-receipt/acting）在 CLASS_OF 都屬同一 class，計算永遠是 nextScore = activeScore + 8 → 恆為 replace；EventScoreContext 的 recentSameClass／alreadyResponded／doNotDisturb／relevance 在整個 codebase 從未被真實狀態填入（只有 machine.ts 的常數）。實際決定舞台的只有靜態優先階梯。(3) behavior.ts 的 `scheduleMicroAction` 與 shuTables 的 `SHU_MICRO_ACTIONS` 是與 Director.tick 平行的第二套 ambient 排程器，只被測試引用，且沒有 isPlayable 白名單；FEATURES.md 卻宣稱 Director 有「注意力、utility 競爭」。
- 證據：grep 全 src（排除 test）：`\.score(`／`scheduleMicroAction`／`SHU_MICRO_ACTIONS` 只出現在 director.ts:148-150、behavior.ts:232、shuTables.ts:115 與 src/test/*。machine.ts:135-145：`const base = { recentSameClass: 0, alreadyResponded: false, interruptible: true, doNotDisturb: false, relevance: 1, novelty: 0 }; const activeScore = scoreEvent(CLASS_OF[active.kind], base); const nextScore = scoreEvent(CLASS_OF[next.kind], { ...base, novelty: 1 }); return nextScore > activeScore ? "replace" : "keep";`。machine.ts:49-63 PRIORITY 與 :68-82 CLASS_OF：succeeded=unknown=60 皆 task-state；clicked=dragged=55 皆 direct-interaction；waiting-for-receipt=acting=40 皆 task-state。我在 scratchpad 用 vitest 枚舉 13×13 全部 kind 配對，同優先級配對回 keep 的數量為 0。docs/FEATURES.md:155「**Interaction Director**：注意力、utility 競爭、…」。
- verifier（reproducibility）confirmed=True severity=low：親自驗證，主張全部成立，但影響僅為死碼＋文件超額宣稱，無執行期錯誤行為，故降為 low。

(1) Director.score()：grep 全 src（排除 test）`\.score(` 只有 director.ts:148 定義；src/test 也無呼叫。tick()/react() 內部不用 scoreEvent。→ 完全死碼。

(2) scoreEvent 生產路徑：唯一呼叫在 machine.ts:143-144，且只在 PRIORITY 相等、非 repeat 時執行，情境是常數（recentSameClass:0／alreadyResponded:false／doNotDisturb:false／relevance:1，只有 novelty 0 vs 1）。CLASS_OF（68-82）讓所有同分配對同 class（succeeded/unknown=task-state、clicked/dragged=direct-interaction、waiting-for-receipt/acting=task-state）。我用 esbuild 打包 scratchpad 腳本枚舉 13×13 全部配對：counts keep=75/refresh=13/replace=81；等優先相異配對共 6 組全部雙向 replace；scoring 分支回 keep 的次數
- 回歸測試：在 transientCompetition 注入真實情境（例如 doNotDisturb=true 或 recentSameClass>=1 的 task-state 新事件對上進行中的等優先 task-state 事件）並斷言回 "keep"；目前因情境為常數、同分配對同 class，此測試必然失敗，可釘死「評分只是 no-op」；另可加一條 lint/test 斷言 Director.score 與 scheduleMicroAction 要嘛有生產呼叫者、要嘛已刪除。

### F-2e02284-20260902T140445Z-director-pipeline-024 · low · 隱藏角色的兩條路徑不一致：companion_apply_prefs（控制中心「顯示角色」開關）只 hide 視窗，不像 toggle_companion_window 那樣主動告訴 Runtime 表面已隱藏
- 檔案：`apps/interaction-desktop/src-tauri/src/lib.rs:2303 (companion_apply_prefs hide branch; function starts at 2271; toggle_companion_window counterpart at 1798-1829)`
- 維度：director-pipeline；verdict：confirmed
- 主張：tray 的 toggle_companion_window 在 hide 之後明確呼叫 `presentation_hello_with_behavior(visible=false)`，並在註解說明理由：「不等 WebView 自己的心跳（隱藏後不保證會送）」，這樣 Presentation receptors 才會依 spec §6.1 停止。但控制中心「顯示角色」開關（CompanionPage patch → companionApplyPrefs）走的 companion_apply_prefs 在 `!visible` 時只 `w.hide()`，沒有同樣的通知；receptor／actuator 健康是否轉為 `companion hidden` 完全仰賴被隱藏 WebView 的 visibilitychange／10 秒心跳——依 repo 自己的判斷那是不保證的。兩條路徑對同一件事（隱藏角色）有不同的誠實度。
- 證據：lib.rs:1795-1826 toggle_companion_window：`let _ = w.hide(); // 角色被隱藏時 Presentation receptors 必須停止（spec §6.1）：直接告訴 Runtime 這個表面不在了，不等 WebView 自己的心跳（隱藏後不保證會送）。 ... runtime.presentation_hello_with_behavior(visible, Some(pack), None)`。lib.rs:2185-2216 companion_apply_prefs：`} else if let Some(w) = app.get_webview_window("companion") { let _ = w.hide(); }` 後直接 `Ok(())`，無 runtime 呼叫、無 emit。CompanionPage.tsx:141-145 `patch`：`await desktop.prefsPatch(p); await desktop.companionApplyPrefs();`，:298-299 顯示角色開關即走此路。presentation.rs:365-381 actuator/receptor 健康只看 bridge 的 `s.visible`（由 hello 更新）。
- verifier（reproducibility）confirmed=True severity=low：Verified at HEAD 2e02284 by reading the files. toggle_companion_window (lib.rs:1798-1829) hides the window and then explicitly spawns `runtime.presentation_hello_with_behavior(visible=false, Some(pack), None)` plus emits `companion-visibility`, with the comment that the hidden WebView's own heartbeat is not guaranteed. companion_apply_prefs (lib.rs:2271-2305, line drifted from the finding's 2214) on the `!visible` branch is exactly `else if let Some(w) = app.get_webview_window("companion") { let _ = w.hide(); }` then `Ok(())` — no runtime hello, no emit. The control-center「顯示桌面角色」toggle (Compa
- 回歸測試：Extract a shared `set_companion_visible(app, false)` helper used by both toggle_companion_window and companion_apply_prefs, and assert that after calling it (with no WebView hello at all) `bridge.actuator_health(PresentationKind::BubbleShow, now)` is Offline("companion hidden") immediately.

### F-2e02284-20260902T140445Z-director-pipeline-025 · low · Director 的 currentAction 在 performing→performing 替換時既不 notePreempted 也不 noteFinished，之後的真實搶佔會依早已下台的動作排「恢復」
- 檔案：`apps/interaction-desktop/src/companion/CompanionApp.tsx:486-493 (apply) and :1271-1276 (pump); machine.ts:158-166 (wasPreempted); director.ts:159-174 (notePreempted)`
- 維度：director-pipeline；verdict：confirmed
- 主張：CompanionApp 只在 `wasPreempted(before, after)` 為真時通知 Director 被搶佔，而 wasPreempted 明確排除 after.kind === "performing"；pump 的 noteFinished 也只在 t.kind 不再是 performing 時觸發。因此 Director 排的 ambient（如 legswing 6s）被另一個 performing（legacy 的 device-hello／operate-tool、CPP 的 notice/greet/play intent、落地演出）以同優先級 replace 後，Director.currentAction 仍指向舊動作；當那個新 performing 再被 clicked 等真實事件搶佔時，notePreempted 用舊動作的 startedAt/durationMs 計算 remaining 並建立恢復計畫，之後 Director 會 resume 一個其實早被別的表演換掉的動作（與 Gateway 的 resume-previous 也可能互相競爭）。
- 證據：CompanionApp.tsx:486-493：`const after = machineRef.current.transient; if (wasPreempted(before, after, now)) { behaviorState.current = noteInterruption(...); directorRef.current.notePreempted(now); }`；machine.ts:163-166 wasPreempted：`if (!before || before.kind !== "performing") return false; if (before.untilMs <= nowMs) return false; return after !== null && after.kind !== "performing";`；CompanionApp.tsx:1270-1276 `if (performingRef.current && t?.kind !== "performing") { performingRef.current = false; directorRef.current.noteFinished(); } else if (t?.kind === "performing") { performingRef.current = true; }`；machine.ts:121-146 同優先級（performing 25 vs performing 25）不同 animation → replace。scratchpad vitest 重現：Director.tick 給 legswing(6000ms) → reduce performing legswing → +500ms reduce performing device-hello（pose 變 device-hello，wasPreempted=false）→ +1000ms reduce clicked（wasPreempted=true → notePreempted）→ +2000ms Director.tick 回 `{ expression: 'legswing', durationMs: 5000, source: 'resume'
- verifier（reproducibility）confirmed=True severity=low：Verified against the current working-tree files (line numbers in the claim still match; the file is uncommitted-modified but this region is unchanged). Code facts: (1) CompanionApp.tsx:490-492 only calls directorRef.notePreempted when wasPreempted() is true; machine.ts:165 returns false whenever `after.kind === "performing"`, and regressions-phase7.test.ts:596-601 deliberately pins performing→still-running-performing as "不記". (2) pump at CompanionApp.tsx:1271-1276 calls noteFinished only when the active transient is no longer `performing`, so a same-priority replace (PRIORITY performing=25 vs 
- 回歸測試：Director.tick → performing A (≥4s); reduce a different same-priority `performing` B while A still runs (assert wasPreempted=false, transient.animation===B); then reduce `clicked`; after clicked expires, Director.tick with rng=0.99 must return null (no resume of A) — today it returns {expression:A, source:"resume"}.

### F-2e02284-20260902T140445Z-director-pipeline-026 · low · Director.react() 回 null 的路徑是靜默的：L1 的 `thinking` 意圖沒有反應表對應；連戳冷卻中第 3 次以後的點擊既無反應、無 clicked transient、也不開選單
- 檔案：`apps/interaction-desktop/src/companion/CompanionApp.tsx:1657 (claim 2, no drift) ＋ apps/interaction-desktop/src/companion/CompanionApp.tsx:1926 / apps/interaction-desktop/src/character/adapters/shuTables.ts:55-67 / apps/interaction-desktop/src/companion/conversation.ts:89 (claim 1；finding 引用的 shuTables.ts 實際位於 src/character/adapters/，非 companion/)`
- 維度：director-pipeline；verdict：confirmed
- 主張：(1) LocalTemplateProvider 對任務類輸入回 `behaviorIntent: "thinking"`，但 SHU_REACTIONS 沒有 thinking 鍵，react() 在 `this.tables.reactions[intent]` 取不到就回 null，所以 L1「判斷像任務」這條意圖永遠到不了舞台（不是 truthState 問題——thinking 在 expressions 中非 truthState，純粹是表缺項）。(2) onPointerUp 在 1.4 秒內第 3 次點擊起一律走 react("poked-rapid")，冷卻（8s × cooldownScale）或文字角色（EMPTY tables）時 react() 回 null，程式仍 `return`——沒有 clicked transient、沒有選單切換，使用者持續快點時角色完全不回應；director.ts 註解宣稱「那也是一個誠實的反應，不是靜默失敗」在冷卻分支並不成立。
- 證據：conversation.ts:82-91：`if (taskLike) { return { reply: ..., behaviorIntent: "thinking", suggestDelegate: true }; }`；shuTables.ts:55-67 SHU_REACTIONS 只有 notice/curious/peek/lean-in/player-back/await-player/praised/caught-slacking/question/block-cursor/poked-rapid；director.ts:185-187 `let expression = this.tables.reactions[intent]; if (!expression || !this.playable(expression)) return null; if ((this.cooldownUntil.get(expression) ?? 0) > nowMs) return null;`；director.ts:202 `this.markUsed(expression, nowMs, 8_000)`。CompanionApp.tsx:1655-1668：`if (clickTimes.current.length >= 3) { const reaction = directorRef.current.react("poked-rapid", ...); if (reaction) { ... } return; } apply({ type: "transient", kind: "clicked" }); setMenuOpen((v) => !v);`。
- verifier（reproducibility）confirmed=True severity=low：以目前工作樹（HEAD 2e02284＋未提交的 CPP 重構）親自驗證，並在 scratchpad 以獨立 vitest 設定跑最小重現（不動 repo）。

【Claim (1) 確認成立，且是 Phase 7 引入的回歸】
- conversation.ts:82-91 對任務類輸入回 behaviorIntent "thinking"；CompanionApp.tsx:1998 → 1923-1935 唯一消費者是 onIntent → directorRef.current.react(intent)（grep 全檔 behaviorIntent 只有 1986/1998 兩處，沒有其他路徑）。
- shuTables.ts:55-67 SHU_REACTIONS 沒有 thinking；director.ts:185-186 `this.tables.reactions[intent]` 取不到即回 null。
- 執行結果：L1 intents → in SHU_REACTIONS = {notice:true, praised:true, thinking:false, question:true}；isShuPlayable("thinking") = true（expressions.ts:833 為非 truthState，純表缺項，不是真相防線擋掉）；dir
- 回歸測試：用 LocalTemplateProvider 對「嗨／謝謝／幫我修這個測試／…？」等輸入收集所有非 null behaviorIntent，斷言每個都在 SHU_REACTIONS 內且 new InteractionDirector(DEFAULT_TUNING, SHU_DIRECTOR_TABLES).react(intent, t) 回非 null（目前 "thinking" 會失敗）。

## Fixed meanwhile（1）

### F-2e02284-20260902T140445Z-director-pipeline-020 · high · 舞台 rAF 迴圈在 60Hz 顯示器上永久降到 30fps（幀預算量的是 rAF 間隔，不是繪製成本）
- 檔案：`HEAD 2e02284: apps/interaction-desktop/src/companion/rig/stage.ts:578-590（finding 引的 618-631 是漂移後的行號）；目前工作樹（未提交）: stage.ts:634-648`
- 維度：director-pipeline；verdict：fixed-meanwhile
- 主張：StageRenderer.loop 把「兩次 rAF 回呼的間隔」餵給 frameBudgetPolicy。60Hz 顯示器的 rAF 間隔恆為 ~16.7ms > FRAME_DEGRADE_MS(12)，所以開跑一秒後 skipEveryOther 就變 true；而回復門檻 FRAME_RECOVER_MS(8) 在 60Hz 永遠達不到（120Hz 的 8.3ms 也 ≥ 8），一旦降級即永久 30fps。這不是「低效能裝置允許 30fps 降級」（spec §14），而是所有 60Hz 裝置從第一秒起都被砍半；perf 腳本走 autoStart:false + 直接 renderFrame，量不到這條路徑，文件裡的效能數字不能證明實機 60fps。
- 證據：stage.ts:618-631：`this.raf = requestAnimationFrame(this.loop); const now = this.now(); if (this.lastLoopAt > 0) { this.budget = frameBudgetPolicy(this.budget, now - this.lastLoopAt); } this.lastLoopAt = now; ... if (!shouldDrawFrame(this.budget, this.frameParity)) return;`——delta 是 rAF cadence。gameFeel.ts:69-73 `FRAME_DEGRADE_MS = 12`／`FRAME_RECOVER_MS = 8`／`FRAME_WINDOW = 60`；gameFeel.ts:102-104 降級後要 `avgMs < 8` 才回 60fps。我用 vitest 在 scratchpad 實跑：連續餵 16.7ms 60 幀後 `{avgMs:16.7, skipEveryOther:true}`，再餵 600 幀仍 `skipEveryOther:true`。perf-entry.ts:111 `new StageRenderer(c, "maid-classic", 1, { autoStart: false, ... })`、:130/:158/:184 直接 `stage.renderFrame(now)`，不經 loop；docs/acceptance-evidence.md:430-435 的數字皆來自這條直呼路徑。
- verifier（reproducibility）confirmed=False severity=high：主張在 HEAD（2e02284，本輪 run 錨定的 commit）成立，但審查當下工作樹已被並行修復，故依規則 refute（already_fixed=true）。

【HEAD 確實有此缺陷】`git show HEAD:apps/interaction-desktop/src/companion/rig/stage.ts` 第 578-590 行：`const now = this.now(); if (this.lastLoopAt > 0) { this.budget = frameBudgetPolicy(this.budget, now - this.lastLoopAt); } this.lastLoopAt = now; ... if (!shouldDrawFrame(...)) return; this.renderFrame(now);`——餵的是 rAF 間隔。frameBudgetPolicy 的邏輯 HEAD 與工作樹完全相同（diff 只改註解），我在 scratchpad 用 vitest 實跑：連續餵 16.7ms 60 幀 → skipEveryOther=true；再餵 600 幀仍 true（FRAME_RECOVER_MS=8 在 60Hz 下不可能達到）。行號：finding 寫 618-631，HEAD 實際為 578-590（漂
- verifier（spec-honesty）confirmed=False severity=high：The claim is factually correct against HEAD 2e02284 but the file has already been fixed in the working tree during this review run, so it cannot be confirmed as a current defect.

Evidence the claim held at HEAD: `git show HEAD:apps/interaction-desktop/src/companion/rig/stage.ts` lines 618-631 feed `now - this.lastLoopAt` (rAF cadence) into `frameBudgetPolicy`. HEAD gameFeel.ts has FRAME_DEGRADE_MS=12, FRAME_RECOVER_MS=8, and recovery requires `avgMs < 8`. I ran a scratchpad vitest against the HEAD copy of gameFeel.ts: 60 frames at 16.7 ms -> skipEveryOther=true; 600 more at 16.7 ms -> still t
- 回歸測試：用假 rAF（手動 tick）與可控 now() 建立 autoStart 的 StageRenderer，以 16.7ms 間隔驅動 600 tick、renderFrame 成本 ≤1ms，斷言 loopStats().drawn === loopStats().ticks 且 frameBudget().skipEveryOther === false；再把成本改為 15ms 斷言 120 tick 只畫 60、改回 2ms 後 skipEveryOther 回 false。 / Construct StageRenderer with injected now() and a fake requestAnimationFrame that invokes the callback with timestamps advancing 16.7 ms per tick while renderFrame costs ~0 ms; after 200 ticks assert loopStats().drawn === loopStats().ticks and frameBudget().skipEveryOther === false.

## Refuted（2）

### F-2e02284-20260902T140445Z-rig-renderer-018 · not-a-bug · Reduced Motion 不是「真靜態」：自動眨眼未受 reducedMotion 閘門，eyeOpen 每 2–5 秒仍在 1.0↔0.16 之間變動
- 檔案：`apps/interaction-desktop/src/companion/rig/timeline.ts:430`
- 維度：rig-renderer；verdict：refuted
- 主張：timeline.ts 檔頭與 CHANGELOG:153「Reduced Motion 真靜態」、manifest.ts:948 visual.expression reducedMotionBehavior="static" 都宣稱靜態，但 paramsAt 的 autoBlink 區塊沒有 `!this.reducedMotion` 條件，idle/sit/lie-flat/quiet 等 autoBlink 表情在 Reduced Motion 下仍持續眨眼；RigRenderer 也仍以 rAF 每幀重繪（renderer.ts:85-89）。
- 證據：timeline.ts:430-444 `if (this.expr.autoBlink) { if (now >= this.nextBlinkAt) {…} … params = clampParams({ ...params, eyeOpen: params.eyeOpen * (1 - dip * 0.95) }); }`（無 reducedMotion 判斷；對照 414 行 micro overlay 有 `!this.reducedMotion`）。probe：`setReducedMotion(true)`＋idle 取樣 20 s，唯一變動通道 `eyeOpen: 1.000,0.789,0.578,0.367,0.156`。
- verifier（reproducibility）confirmed=False severity=not-a-bug：行為本身可重現，但它是專案明文、有測試釘住的設計決策，不是缺陷。

我實際重現（scratchpad vitest，import 目前工作樹的 timeline.ts；該檔與 HEAD 相同、行號無漂移）：setReducedMotion(true) 後對 idle/sit/lie-flat/quiet 取樣 20 s，唯一變動通道只有 eyeOpen（0.05–1.0），bodyBob/tailSway/corePulse/pose/particles 全部凍結；emergency/working（無 autoBlink）則零通道變動。所以「Reduced Motion 下只剩眨眼」是事實。

但這正是專案的 Reduced Motion 政策，而且是刻意的、有多處證據：
1. apps/interaction-desktop/src/character/adapters/shuTables.ts:42/116/117 —— 只有 blink／double-blink 標 `reducedMotionOk: true`，其餘 ambient 變體全是 false。
2. apps/interaction-desktop/src/companion/director.ts:252 `if (ctx.reducedMotion && !v.reducedMotionOk) retur
- 回歸測試：（若專案改變政策才需要）ExpressionTimeline setReducedMotion(true)＋idle 取樣 20 s，斷言 eyeOpen 恆為 hold 值；但這與現行「Reduced Motion 只允許眨眼類」測試（rig.test.ts:250）互斥，現況不需新增。

### F-2e02284-20260902T140445Z-rig-renderer-019 · not-a-bug · resolveRigAnimation("success") 無 frameSlice 時解析為 success-verified（綠勾）：資訊缺席被當成「已驗證」，與 EXPRESSION_ALIASES 的「預設 claimed」相矛盾
- 檔案：`apps/interaction-desktop/src/companion/rig/timeline.ts:172 (unchanged); apps/interaction-desktop/src/companion/rig/expressions.ts:1349-1351 (finding cited 1309-1312, drifted); apps/interaction-desktop/src/companion/machine.ts:220-221 (unchanged)`
- 維度：rig-renderer；verdict：refuted
- 主張：誠實階梯要求資訊不足時降級（unknown 不演成功），但 rig 的解析規則是「沒有 slice ＝ verified」：任何未來忘記帶 slice 的呼叫端（或吃掉 frameSlice 的中介層）都會直接得到綠勾與慶祝粒子；drawExpressionPreview(ctx,"success")、MixerRenderer.setAnimation("success")→machineEventForAnimation 都走這條 verified 路徑。目前已查證所有生產呼叫端（machine.pose、shuTables.plan、stage useMachineSlice）都有守門，故為潛在風險而非現行洩漏；但 expressions.ts:1309-1312 的別名註解宣稱預設是 claimed，兩處規則相反。
- 證據：timeline.ts:170-173 `if (name === "success") wanted = frameSlice ? "success-claimed" : "success-verified";`；expressions.ts:1309-1312 `// 此處預設為誠實的 claimed。 success: "success-claimed"`；machine.ts:220-221 `case "success": return t("succeeded", { verified: !frameSlice })`。probe：`resolveRigAnimation('success')` → `success-verified`（hold.overlay="check"），`resolveRigAnimation('success',[0,1])` → `success-claimed`，`resolveExpression('success')` → `success-claimed`。
- verifier（reproducibility）confirmed=False severity=not-a-bug：事實部分屬實：timeline.ts:172 `wanted = frameSlice ? "success-claimed" : "success-verified"`；expressions.ts:1349-1351 別名 `success: "success-claimed"`；machine.ts:220-221 `verified: !frameSlice`。但這不是缺陷，而是 machine→renderer 的既定線協定，且已被文件與測試釘死：

1. 這是刻意、已文件化的編碼。machine.ts:312-315 `pose()` 把 verified 編成 `{animation:"success"}`（無 slice）、claimed 編成 `{animation:"success", frameSlice:[0,1]}`；machine.ts:185-186 註解「`success` 帶 frameSlice＝claimed、不帶＝verified；呼叫端（adapter）已依 truthState 決定是否給 slice，這裡不做升級」；timeline.ts:16-17 header 明寫「誠實映射（success+slice→claimed／無 slice→verified）在 resolveRigAnimation；truth-state 不可點播由 

## Unverified（agent failure；見 run 3）（47）

### F-2e02284-20260902T140445Z-character-protocol-029 · high · 緊急停止／離線的角色呈現撐不過下一個非安全 intent：Runtime 在 estop 中照常投影 notice／greet，Rust Gateway 對 queue 政策不排程，文字角色與外部 adapter 的「緊急停止中」被「注意到了。」覆蓋
- 檔案：`crates/interaction-runtime/src/character.rs:1557`
- 維度：character-protocol；verdict：unverified
- 主張：character_project／character_project_observation／character_project_provider 沒有任何 estop 閘門：緊急停止期間 receptor.observation（每受器 2 s 一次）、provider 上下線仍投影 notice／greet（priority 40、interruptPolicy=Queue）。Rust Gateway 的 dispatch 對 Queue 政策是空分支（沒有 channel owner 保留、從不產生 scheduled），因此 emergency 還在 Started 時，同一 bubble channel 的 notice 立刻被 Send 給外部 adapter。桌面端 TextCharacterAdapter（文件所稱「可信 fallback」）perform 無條件 `this.current = intentLine(...)`，而 emergency 命令沒有 durationHint 立即 completed、釋放 channel，下一個 notice 就把「緊急停止中」換成「注意到了。」；只有 shu-rig 靠共用 machine 的 base=emergency 擋住。README §0 不變量 5「Emergency…可搶占任何非安全演出」與 machine.ts「停住的系統不繼續演任何東西」在文字角色與所有外部 adapter 上都不成立。
- 證據：character.rs:1449 character_project 只看 connected_instance_ids()，:1557 character_project_observation 只排除 companion.* 受器與 2 s 節流，兩者都不讀 self.estop（grep estop 只命中 :1581-1589 的投影本身）。gateway.rs:817 `InterruptPolicy::Preempt => {…}`、:836 `_ => {}` 之後直接 push pending 並 `GatewayOutput::Send`。text.ts:195-196 `const line = intentLine(...); this.current = {...}` 無 base 狀態。實測 A（scratch Rust）：emergency 派送並回 Started 後派 notice(Queue)：`PROBE2 notice -> Send intent`、`Receipt Accepted`，emergency 仍 Started。實測 B（scratch vitest，TS Gateway＋TextCharacterAdapter）：emergency 後 2 s 派 notice(priority 40, queue) → currentLine 由 `{text:"緊急停止中",intent:"emergency"}` 變成 `{text:"注意到了。",intent:"notice"}`，systemTextCount=0。

### F-2e02284-20260902T140445Z-character-protocol-030 · medium · Reduced Motion 沒有傳到 Runtime：Rust Gateway 永遠以 reducedMotion=false 協商，回執與 instances 把 `reduced` 報成 `exact`
- 檔案：`crates/interaction-character/src/gateway.rs:396`
- 維度：character-protocol；verdict：unverified
- 主張：/v1/character/hello 的輸入（CharacterHelloInput）沒有 reducedMotion 欄位，Rust Gateway 的 hello_for 只讀 GatewayConfig.reduced_motion（預設 false，runtime／api 沒有任何地方設定它）。桌面 TS Gateway 用真實的 prefers-reduced-motion 協商並實際以 reduced 演出，但 Runtime 端 negotiated.resolutions／hello 回應的 negotiated.reducedMotion=false，且 on_receipt 一律用 Rust 自己的 negotiated_resolution 覆蓋 adapter 回報的 resolution（除了 failed），所以 character.receipt 事件、audit、`/v1/character/instances` 在 Reduced Motion 下都宣稱 exact。違反「不支援就誠實降級、不得假裝 exact」與「每項設定只有一個主人」。
- 證據：gateway.rs:69 `reduced_motion: false`（GatewayConfig::default），:396 `reduced_motion: self.config.reduced_motion`（hello_for）；`grep -n reduced_motion crates/interaction-runtime/src crates/interaction-api/src` 無任何命中；character.rs:844 `gw.on_negotiate(&iid, input.negotiate, now)` 直接用 hello_for 的 false。gateway.rs:982-988 `let negotiated_resolution = inst.pending[idx].resolution.resolution; … .with_resolution(if receipt.status == ReceiptStatus::Failed { Resolution::Failed } else { negotiated_resolution })`。實測：`PROBE4 hello.reduced_motion = false`。對照 TS negotiate.ts tryCapability 在 reducedMotion 時回 reduced，sprite/shu manifest 的 visual.expression reducedMotionBehavior=static。

### F-2e02284-20260902T140445Z-character-protocol-031 · medium · TS Gateway 把 merge 進既有演出的 intent 記成 `completed{merged}`（從未演出），重複／alreadyTerminal 回執硬編 resolution:"exact"；與 Rust 的 cancelled{merged} 不一致
- 檔案：`apps/interaction-desktop/src/character/gateway.ts:856`
- 維度：character-protocol；verdict：unverified
- 主張：arbitrate 的 merge 分支對同 intent 已有 owner 的新 envelope，直接把 status 設成 started 再發 completed{detail:"merged"}，adapter 從未收到 perform；completed 應只代表「呈現演完了」。Rust Gateway 對同情境回 cancelled{reason:"merged"}。這則 completed 轉送到 Runtime 時因 Accepted→Completed 非法而被丟棄，命令在 Rust 端留到 expiresAt 才被 sweep 記成 expired，兩邊回執分歧。另外 dispatch 的重複 messageId 回執與 cancel 的 alreadyTerminal 回執都硬編 resolution:"exact"，不反映實際協商結果。
- 證據：gateway.ts:851-858 `case "merge": { const same = owners.find(...); if (same) { p.startedAt = this.now(); p.status = "started"; this.emit(inst, p.envelope.messageId, { status: "completed", resolution: p.effective, detail: "merged" }); this.forget(inst, p, "completed"); return; } }`；:635 `this.emit(inst, messageId, { status: "accepted", resolution: "exact", duplicate: true, ... })`；:1025 alreadyTerminal 回執 `resolution: "exact"`。實測（scratch vitest）：兩則同 correlation 的 merge notice，第二則回執序列為 `accepted/exact/`、`completed/exact/merged`；重複派送回 `{status:"accepted",resolution:"exact",duplicate:true}`。Rust gateway.rs 對 merge 為 `ReceiptStatus::Cancelled ... .with_reason("merged")`；receipt.rs can_transition 無 (Accepted, Completed)。Runtime 端 character_project_observation 正是用 InterruptPolicy::Merge。

### F-2e02284-20260902T140445Z-character-protocol-032 · medium · adapter token 的 HTTP 回執／事件與 WS 壞訊息繞過 50 則/s 限流：外部 adapter 可無上限灌 audit 與 companion.click 觀察
- 檔案：`crates/interaction-runtime/src/character.rs:958`
- 維度：character-protocol；verdict：unverified
- 主張：README §8 規定每個 adapter ≤ 50 則/s，但 RateLimiter 只在 Gateway::on_message 內；Runtime 的 character_receipt／character_event（POST /v1/character/receipts|events，adapter token 可對自己 instance 呼叫）直接呼叫 gw.on_receipt／gw.on_event，完全不經限流。外部 adapter 的 character.clicked 沒有節流、origin=External 不走 surface gate，每一則都 ingest 成 companion.click 觀察寫入 DB 並觸發 recipe 評估。WS 路徑上 parse_wire 失敗時每一則都寫一筆 `character.wire-rejected` audit（限流在 parse 之後才做），audit 表沒有修剪。一個惡意或壞掉的第三方 adapter 就能讓 DB 無界成長，違反「無界 queue」與 §9「Adapter crash 不得拖垮 Runtime」。
- 證據：character.rs:923 `let outputs = gw.on_receipt(&iid, receipt.clone(), now);`、:958 `let decision = gw.on_event(&iid, event, now);`（兩處都不呼叫 on_message／rate）；gateway.rs:1174-1187 只有 on_message 做 `if !inst.rate.allow(now.timestamp_millis())`。character.rs:1285-1295 `match parse_wire(bytes) { Err(err) => { let _ = self.store.audit("character.wire-rejected", ...); hub.send_external(..error..); return WsStep::KeepOpen; } }`。character_input_to_observation：`let enforce_surface_gate = origin != InstanceOrigin::External; self.ingest_with_gate(...)`；input.rs 對 Clicked 沒有 interval 節流。storage/lib.rs:860 audit 只 INSERT，grep 無任何 DELETE／prune。interaction-api/src/lib.rs:394 允許 adapter token POST 這兩條路由。

### F-2e02284-20260902T140445Z-character-protocol-033 · low · 文件宣稱純聲音／燈光角色能表達工作／等待／未知，實作與其引用的測試相反：work/think → unsupported、wait/unknown → system.text
- 檔案：`docs/character-protocol/adapter-authoring.md:79`
- 維度：character-protocol；verdict：unverified
- 主張：adapter-authoring.md 說「純聲音／燈光角色：只宣告 audio.speech／audio.effect／light.cue；工作／等待／阻擋／未知／聲稱完成／已驗證／失敗全部都能透過這些通道表達（測試 negotiation.rs::pure_audio_character 證明）」。但 Rust intent_capabilities 對 Unknown/Cancelled 與 Think/Wait/Work 的候選能力不含任何 audio.*／haptic.cue，只有 light.cue；純聲音角色協商後 unknown／wait 走 system.text（文字通知），work／think 為 unsupported——被引用的測試正是斷言 Wait 與 Unknown 解析成 SYSTEM_TEXT。純聲音使用者聽得到 blocked／failed／claimed／verified 的音效，卻聽不到「結果未知」，unknown 在聲音通道上是靜默的，與 TS negotiate.ts 的 EXPRESSIVE（所有 intent 皆含 audio.speech／audio.effect）也不一致。
- 證據：crates/interaction-character/src/capability.rs:189 `Think | Wait => &["visual.expression","visual.pose","visual.textBubble","light.cue"]`、:195 `Work => &["visual.pose","visual.expression","visual.textBubble","light.cue"]`、:222 `Unknown | Cancelled => &["visual.expression","visual.textBubble","visual.pose","light.cue"]`；tests/negotiation.rs:213-221 `assert_eq!(via(&n, CharacterIntent::Wait), SYSTEM_TEXT); … assert_eq!(via(&n, CharacterIntent::Unknown), SYSTEM_TEXT);`。實測（scratch Rust，只宣告 audio.speech＋audio.effect）：`work -> Unsupported`、`think -> Unsupported`、`wait -> Substituted via system.text`、`unknown -> Substituted via system.text`、`blocked/claim-completed/verified-success/failed -> Exact via audio.effect`。protocol.ts EXPRESSIVE 陣列含 audio.speech、audio.effect 供全部 intent。

### F-2e02284-20260902T140445Z-ia-settings-034 · medium · 精靈「進一步自訂」寫入的 channelLimits["*"] 與 requireApprovalAt 在一般模式沒有任何主人，且「主動打擾次數」標籤與實際效果不符
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/pages/Onboarding.tsx:159`
- 維度：ia-settings；verdict：unverified
- 主張：精靈把「每小時最多主動打擾次數」寫成 policy.channelLimits["*"].maxPerHour，這個萬用鍵套用到所有通道（含 L0 desktop-pet 角色呈現與回應直接要求的 conversation），且是每個動器每小時上限，不是「主動打擾」；同時 askHighRisk 取消勾選會把 requireApprovalAt 降到 critical。這兩個欄位事後在一般模式任何頁面都看不到也改不回來（只有進階「政策／同意」原始 JSON），違反 §12.6「每項設定只有一個主人」與 §15.5。
- 證據：Onboarding.tsx:150-161 `policyPatch.channelLimits = { "*": { maxPerHour: draft.maxPerHour } }`、`policyPatch.requireApprovalAt = draft.askHighRisk ? "high" : "critical"`；標籤 Onboarding.tsx:391「每小時最多主動打擾次數」。crates/interaction-policy/src/lib.rs:469-482 `merged_channel_limits` 把 "*" 合併到每個 channel；lib.rs:257-265 `usage.actuator_fired_last_hour >= cap` → blocked（依動器計數，不分主動／被動）。grep `channelLimits|requireApprovalAt` 在 src/pages 只命中 Onboarding.tsx；AutomationsPage.tsx:438 是 recipe.limits、CompanionPage.tsx:848 是主動對話 config.maxPerHour，都不是這兩個 policy 欄位；Policy.tsx（進階）才能以原始 JSON 修改。

### F-2e02284-20260902T140445Z-ia-settings-035 · medium · 重新執行首次設定會靜默停用使用者已啟用、但非 beginnerSafe 的能力（例如已配對 iPhone 的動作／電量／觸碰受器），精靈沒有任何步驟可見或可改
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/pages/Onboarding.tsx:137`
- 維度：ia-settings；verdict：unverified
- 主張：三步精靈已無「選擇感知來源／回應方式」步驟，senses/responses 只由 beginnerSafe 預選；commit 時把所有「可用、不需同意、又不在預選」的受器／動器一律 disable。後端 commit 後把 draft 清成 null，所以從設定頁重跑精靈必定重新預選並停用其他能力，且畫面只列「AI 可以知道／可以做」不列「將被停用」。設定頁文案「重新選擇感知來源、回應方式」描述的是舊 7 步精靈，已過時。
- 證據：Onboarding.tsx:109-119 只有 draft 為空時用 beginnerSafe 預選；137-144 `disableR = receptors.filter(!senses.includes && availability !== "disabled" && !requiresConsent)`（動器同）；606-619 beginnerSafe：`leavesDevice !== false` 或無 data/effect 宣告一律 false。human.rs:545-546 commit 後 `set_meta(ONBOARDING_DRAFT_META_KEY, "null")`。具體受害者：mobile.rs:82-107 `iphone.motion`/`iphone.battery`/`iphone.touch` requires_consent:false 且 high_risk:false → 不掛 human meta；human_view.rs:497 `leaves_device: declared.map(..).unwrap_or_default()`，core human.rs:19-23 TriState 預設 Unknown → 卡片 leavesDevice "unknown" → 非 beginnerSafe → 重跑精靈即被 disable。SettingsPage.tsx:76-78「重新選擇感知來源、回應方式與互動偏好」；Onboarding.tsx:248-417 三步內沒有能力選擇 UI。

### F-2e02284-20260902T140445Z-ia-settings-036 · low · 窄視窗「更多」選單永遠不會高亮目前所在的子項（記憶／活動／設定／管理／進階功能）
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/App.tsx:760`
- 維度：ia-settings；verdict：unverified
- 主張：NarrowNav 收到的 tab 是 navAnchorFor 折疊後的一級 id（memory/activity/settings/manage/advanced-features 全部折成 "more"），但 more-sheet 用 `tab === t.id` 比對細項 id，因此 5 個細項永遠是非 active（只有 adv-* 會高亮）。
- 證據：App.tsx:580 `<NarrowNav tab={navTab} …>`；App.tsx:385 `navTab = navAnchorFor(tab)`；App.tsx:86-100 LEGACY_ANCHORS 把 memory/activity/settings/manage/advanced-features → "more"；App.tsx:757-760 `className={tab === t.id ? "more-item active" : "more-item"}` 其中 t.id 來自 NARROW_MORE_ITEMS（703-709）。

### F-2e02284-20260902T140445Z-ia-settings-037 · low · 解除緊急停止對話框把使用者已停用的動器列為「解除後會恢復可用」
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/pages/SafetyPage.tsx:100`
- 維度：ia-settings；verdict：unverified
- 主張：willResume 只排除需同意的動器，不看 availability；human 來自 capabilitiesHuman(includeUnavailable=true)，因此 availability="disabled" 的動器也被列在「解除後，以下能力會恢復『可用』」。後端 clear_emergency_stop 不會啟用任何東西，這句話對它們是假的。
- 證據：SafetyPage.tsx:100-102 `willResume = actuators.filter(a => a.consent.required !== true && a.requiresConsent !== true)`；108 文案「解除後，以下能力會恢復「可用」」；appstate.tsx:70 `api.capabilitiesHuman(undefined, true)`；runtime.rs:934-951 `clear_emergency_stop` 只 unlatch 驅動、發事件、寫 audit，不改 enabled。

### F-2e02284-20260902T140445Z-ia-settings-038 · low · 風險分級把「播放音效」「語音朗讀」標成「L2 個人資料 — 會用到你的檔案、偏好或記憶」
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/apps/interaction-desktop/src/riskTier.ts:151`
- 維度：ia-settings；verdict：unverified
- 主張：riskTierOf 對 requiresConsent===true 的能力一律回 TIERS[2]，而 L2 的 policy 文案是「會用到你的檔案、偏好或記憶」。內建 companion.sound.play／companion.speak 是 desktop-pet 通道、無資料語意、effect 皆 No、只是需同意，於是授權對話框與同意清單向使用者說音效會用到個人資料；規格 §2 把短音效列為 L1。
- 證據：riskTier.ts:150-151 `if (input.requiresConsent === true) return TIERS[2];`，TIERS[2] label/policy 在 81-85；presentation.rs:672-676 通道 "desktop-pet"、706-716 SoundPlay/Speak `.requires_consent(true)`，`local_effect_semantics` 只宣告 effect（adapter-sdk lib.rs:271-287），無 data；SafetyPage.tsx:311-324 GrantDialog 與 217-235 同意清單直接顯示 risk.label＋risk.policy。

### F-2e02284-20260902T140445Z-ia-settings-039 · low · pendingCount 只在最近 200 筆收據／200 筆事件／500 筆候選的視窗內計算，徽章卻被註解與 UI 當成「全部待辦」
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/crates/interaction-runtime/src/activity.rs:147`
- 維度：ia-settings；verdict：unverified
- 主張：activity_inbox 從 `list_actions(None, 200)`（依 created_at DESC LIMIT 200）、`events.recent(200)`、`knowledge_list(candidate, 500)` 組裝；一筆較舊的 uncertain（L3 實體效果結果未知）收據一旦被 200 筆較新的收據擠出視窗，就從 pendingCount、通知徽章與「待我決定」完全消失，而不是被標示為未顯示。
- 證據：activity.rs:147 `self.list_actions(None, 200)?`、178 `self.events.recent(200)`、95 `knowledge_list(Some("candidate"), 500)`；activity.rs:252-254 註解「徽章代表全部待辦」；runtime.rs:796-802 `list_actions` → store.receipts；storage lib.rs:576 `ORDER BY created_at DESC LIMIT ?2`。HomePage.tsx:383、App.tsx:464 直接把 pendingCount 當總數顯示。

### F-2e02284-20260902T140445Z-ia-settings-040 · low · 安靜時段預設會封鎖 desktop-pet，L0 呈現動作被擋的收據仍算「待你決定」（無事可決）
- 檔案：`/Users/user/Workspace/claude-lab/adaptive-interaction/crates/interaction-runtime/src/activity.rs:160`
- 維度：ia-settings；verdict：unverified
- 主張：activity.rs 只對 uncertain 排除 builtin.presentation，blocked 不分通道一律 needs_decision=true。而政策預設安靜時段的靜音通道含 desktop-pet，安靜時段內任何走 plan 的角色呈現步驟都會產生 Blocked 收據並進入「待我決定」／通知徽章，但使用者對這些項目沒有任何可做的決定；與 §2 L0「不產生干擾性 Receipt UI」相悖。
- 證據：activity.rs:157-161 `"uncertain" => !presentation_actuators.contains(..)`, `"blocked" => true`；crates/interaction-policy/src/lib.rs:90-91 `DEFAULT_QUIET_SILENCED = ["audio","haptic","notification","light","desktop-pet"]`、219-235 安靜時段 → `AuthorizationResult::blocked("quiet-hours")`；executor.rs:53-77 `refused_receipt` 產生 current_status Blocked 收據；CompanionPage.tsx:1042 QuietHoursEditor 寫入 `silencedChannels: []`（即用預設清單）。

### F-2e02284-20260902T140445Z-link-transports-041 · high · MQTT：rumqttc 在重連後會重送所有未 PubAck 的 QoS1 cmd 與 request channel 內未處理的訊息——遲到的實體效果；已知限制寫法低估範圍且其實可修
- 檔案：`crates/interaction-adapter-declarative/src/mqtt.rs:109`
- 維度：link-transports；verdict：unverified
- 主張：MqttRawLink 只在 send() 前檢查 connected（mqtt.rs:169-174），沒有覆寫 send_before，DeviceLink::command 帶的 deadline（protocol.rs:559-563）進不了 rumqttc；而 rumqttc 0.24 的 EventLoop::poll 在任何連線錯誤時呼叫 clean()，把 state.clean()（所有已送出但未收到 PubAck 的 QoS1 Publish）與 requests_rx.drain()（尚未被 eventloop 取走的 publish）全部搬進 pending，重連後 next_request 優先重送 pending。mqtt.rs 的 Err 分支（109-121）只做退避，沒有清 eventloop.pending。結果：keep-alive 15 s（mqtt.rs:56）偵測窗內送出的每一則 cmd，host 已用 link-reset／timeout 標成「結果未知」（甚至 Failed，見另一項），裝置卻在重連＋1–15 s 退避後才收到並套用（原始 cmd 未到達時韌體 dedupe 幫不上忙）。這正是 protocol.rs:161-163 與 serial.rs:215-225 drain_stale_queue 明文要避免的「遲到的實體效果」。docs/acceptance-evidence.md:479 只寫「廣播前檢查與 publish 之間斷線的訊息」且稱「deadline 伸不進」——實際範圍是整個 keep-alive 偵測窗＋所有 inflight，而且 EventLoop.pending 是 pub 欄位（eventloop.rs:82），adapter 自己的 task 持有 eventloop，可直接清空。
- 證據：mqtt.rs:109-121 `Err(e) => { if connected.swap(false,..) {..} state.store(STATE_DISCONNECTED); select!{ stop | sleep(backoff) } }`（無 pending.clear()）；mqtt.rs:160-181 send 僅檢查 closed/connected 後 client.publish(QoS::AtLeastOnce)；mqtt.rs 無 send_before 實作（走 protocol.rs:164-175 預設＝立即 send）。~/.cargo/registry/src/*/rumqttc-0.24.0/src/eventloop.rs:129-136 `pub fn clean(&mut self){ self.network=None; ... self.pending.extend(self.state.clean()); let requests_in_channel = self.requests_rx.drain(); self.pending.extend(requests_in_channel); }`、:165-169 poll 的 Err 分支呼叫 self.clean()、:198-231 next_request 「prioritises pending requests from previous session」；state.rs:109-128 clean() 把 outgoing_pub（未 ack 的 QoS1/2 publish）轉成 Request::Publish；lib.rs:499 clean_session 預設 true（broker 不重播，是 client 端在重播）。tests/mqtt_loop.rs:349-411 的重連測試在閒置時被踢掉（無 inflight publish），沒有覆蓋此路徑。docs/acceptance-evidence.md:479、CHANGELOG.md:172。

### F-2e02284-20260902T140445Z-link-transports-042 · high · ack 逾時／BLE 途中失敗的收據永遠不會自動變 Uncertain：Dispatched 時間戳在逾時後才蓋，executor 算出 age≈0，watchdog 只做 TTL→Expired
- 檔案：`crates/interaction-adapter-declarative/src/link_caps.rs:253`
- 維度：link-transports；verdict：unverified
- 主張：link_caps.rs:5-7 宣稱「runtime watchdog 會把它誠實標為 uncertain」，實際上沒有任何路徑會做。LinkActuator::execute 在 command() 回 Timeout／Uncertain 之後才呼叫 DriverReceipt::start(&action, Utc::now()).dispatched()（link_caps.rs:253-258、277-282），DriverReceipt::dispatched 用 Utc::now()（sdk lib.rs:179-183），所以收據的 Dispatched 時間戳＝逾時結束時刻而非送出時刻；executor best-effort 驗證用「now − dispatched_at > 5000ms」判斷（executor.rs:935-956），此時 age≈0 → 什麼都不做，收據停在 Dispatched；runtime watchdog（runtime.rs:2004-2015）只把過 expires_at 的收據轉 Expired，沒有像 sweep_presentation（presentation.rs:1122-1145）那樣把逾時未 ack 的裝置動作轉 Uncertain。結果：一個裝置根本沒回 ack 的實體命令，在角色與 UI 上顯示「Working／dispatched」直到 plan TTL（policy default_ttl_ms=30000）才變 Expired；ActionUncertain 事件從未發出，CHANGELOG:144 所述「外部裝置 uncertain 進『待我決定』」對此路徑不成立。
- 證據：link_caps.rs:251-258 `Err(LinkError::Timeout(detail)) => { return Ok(DriverReceipt::start(&action, Utc::now()).dispatched().note("ackTimeout", json!(true))...) }`（在 `.await` 逾時之後才建）；crates/interaction-adapter-sdk/src/lib.rs:179-183 `pub fn dispatched(mut self){ self.receipt.transition(ActionStatus::Dispatched, Utc::now()) }`；executor.rs:935-956 `else if receipt.current_status == Dispatched { dispatched_at = timestamps.find(Dispatched) ...; if age > DEFAULT_VERIFY_TIMEOUT_MS { ...Uncertain } }`（DEFAULT_VERIFY_TIMEOUT_MS=5000，executor.rs:16）；runtime.rs:2004-2015 watchdog 僅 `if receipt.expires_at.map(|e| now > e)` → Expired；grep 全 runtime 無 ackTimeout/sendOutcomeUnknown 消費者（只有 mobile.rs:757,772 產生同樣的 note）；character.rs:408-415 ActionDispatched→(Work, Working)、ActionExpired→(Unknown, Expired)；link_caps.rs:5-7 註解宣稱 watchdog 會標 uncertain。

### F-2e02284-20260902T140445Z-link-transports-043 · medium · LinkError::Reset（等待中重連，結果未知）被寫成 Failed 終態：對未知結果宣稱確定失敗，誘發重試
- 檔案：`crates/interaction-adapter-declarative/src/link_caps.rs:291`
- 維度：link-transports；verdict：unverified
- 主張：protocol.rs:576-578 明確把 Reset 描述為「outcome UNKNOWN (not retried)」，link_caps.rs:284-293 卻把它做成 `.dispatched()…failed("link-reset")`，收據 current_status=Failed（終態），只在 driver_response 放 outcomeUnknown 註記；runtime 對 Failed 發 ActionFailed，角色投影成 TruthState::Failed（character.rs:413），UI／AI 看到的是「失敗」而非「未知」。與同一函式對 Timeout（→Dispatched＋ackTimeout）與 Uncertain（→Dispatched＋sendOutcomeUnknown）的處理自相矛盾。Failed 會讓人或 AI 合理地重下同一命令；而在 MQTT 場景（見 rumqttc pending 一項）原命令會在重連後被重送並套用，重試＝雙重實體效果。違反規格 §2「外部動作結果未知不得顯示成功，也不得自動重試」與誠實階梯「未知要標 uncertain」。
- 證據：link_caps.rs:284-293 `Err(LinkError::Reset(detail)) => { return Ok(DriverReceipt::start(&action, Utc::now()).dispatched().note("transport",..).note("outcomeUnknown", json!(true)).failed("link-reset", &detail).finish()); }`；protocol.rs:576-578 `LinkError::Reset(format!("link reset (reconnected) while waiting for action {action_id} — outcome UNKNOWN (not retried)"))`；character.rs:413 `EventType::ActionFailed => (I::Failed, T::Failed)`；tests/protocol_honesty.rs:845-851 目前把 Failed 當成期望值固定下來。

### F-2e02284-20260902T140445Z-link-transports-044 · medium · MQTT 健康度＝broker 連線＋一次握手，從不追蹤裝置存活：ESP32 斷電後仍 healthy／「已啟用…此刻真的能用它」
- 檔案：`crates/interaction-adapter-declarative/src/protocol.rs:339`
- 維度：link-transports；verdict：unverified
- 主張：DeviceLink::readiness 只看 raw.link_state()（MQTT＝與 broker 的 ConnAck）與 ready_generation 是否等於當前世代；一旦握手過一次，只要 host 仍連著 broker，裝置本體離線幾小時都回 Ready → link_health 回 healthy → 動器／受器 availability=available → CapabilitiesHub 把 provider 標成「已啟用」並寫「此刻真的能用它」。韌體其實每 STATE_PERIOD_MS=5000 ms 對已配對通道推播 state，但 host 端 inbound broadcast 只在 wait_reply 期間有 receiver，未被等待的推播直接被丟掉，沒有「最後一次聽到裝置」的時間戳，也沒有 LWT。規格 §9.3 要求 UI 誠實呈現「裝置是否在線」，§14 要求 unknown 用誠實文字；這裡把「broker 在線」演成「裝置在線」。
- 證據：protocol.rs:334-349 `pub fn readiness(&self){ match self.raw.link_state(){ ... LinkState::Connected => if ready_generation == ready_marker(gen) { Ready } else { NotHandshaken } } }`；mqtt.rs:191-193 `fn connected(&self){ self.connected.load() && !closed }`（只由 ConnAck／poll Err 翻轉，mqtt.rs:92-96、112）；mqtt.rs:99-107 收到 publish 只 `inbound.send(msg)`，無持久 receiver、無 last-seen；link_caps.rs:97-113 `LinkReadiness::Ready => ComponentHealth::healthy()`；firmware esp32-companion.ino:53 `STATE_PERIOD_MS 5000UL`、:527-540 pushStateToPairedLinks；apps/interaction-desktop/src/pages/CapabilitiesHub.tsx:455-459 enabledCapabilities 以 `availability === "available"` 計，:251-257 `enabled && tested?.ok === true` → 「已啟用…此刻真的能用它」。

### F-2e02284-20260902T140445Z-link-transports-045 · medium · BLE：connected()/health 只在 ensure_open 時更新，裝置離開範圍後 status() 持續回 healthy，直到下一次派工
- 檔案：`crates/interaction-adapter-declarative/src/ble.rs:275`
- 維度：link-transports；verdict：unverified
- 主張：BleRawLink::connected() 只讀 state==STATE_CONNECTED，而 state 只在 connect() 成功（190-191）、ensure_open 發現 is_connected()==false（209-219）與 shutdown（290-294）時改變；沒有任何事件驅動的斷線偵測：notification task（179-189）在 stream 結束（GATT 斷線）時靜靜退出、不改 state，也沒有訂閱 CentralEvent::DeviceDisconnected。因此對只有動器的 BLE adapter（沒有 poll 受器來觸發 ensure_open），peripheral 離開範圍後 readiness()=Ready、link_health=healthy、availability=available 無限期維持；registry refresh_health 每 10 tick 呼叫 status() 也只是讀這個過期旗標。protocol.rs:182-184 的契約明文要求「健康度不得硬編 healthy：實作必須真的知道（ble＝GATT session 存在）」——session 物件存在≠GATT 連線存在。
- 證據：ble.rs:272-276 `fn connected(&self){ self.state.load()==STATE_CONNECTED && !closed }`（註解自承「斷線由 ensure_open 的 is_connected 檢查與 state 一起更新」）；ble.rs:179-189 `self.notify_task.replace(tokio::spawn(async move { while let Some(data)=notifications.next().await {..} }))`（loop 結束後無任何狀態更新）；ble.rs 全檔無 DeviceDisconnected／is_connected 之外的斷線來源；link_caps.rs:305-313 status() → link_health(readiness)；runtime.rs:2028-2030 每 10 tick refresh_health。

### F-2e02284-20260902T140445Z-link-transports-046 · medium · estop：stop_all 對未連線／連線中的 link 先做 ensure_open（serial/mqtt 等 2 s、BLE 起 6 s 掃描），在 runtime 逐一 2 s 逾時的序列迴圈裡拖慢其他裝置的停止；BLE 被取消時不會 stop_scan
- 檔案：`crates/interaction-adapter-declarative/src/protocol.rs:679`
- 維度：link-transports；verdict：unverified
- 主張：DeviceLink::stop_all 第一步是 self.raw.ensure_open()：serial/mqtt 在未連線時輪詢 connected 最長 2 s（serial.rs:369-379、mqtt.rs:147-157），BLE 則實際啟動掃描（ble.rs:100-146，SCAN_TIMEOUT 6 s）＋connect＋discover。runtime.emergency_stop 對每個動器序列 await，各 2 s 逾時（runtime.rs:889-897）；README 的 ESP32 serial 參考 spec 有 4 個動器共用同一條 link，裝置拔線時 estop 會在這台裝置上耗掉 ≈4×2 s＝8 s 才輪到其後的動器（例如 iphone.*、其他裝置）的 stop-all。stop_all 自身預算（ensure_open ≤2 s＋ack 等待 1 s）也超過 runtime 的 2 s，連線中的裝置永遠被截斷、永遠算未停止。BLE 情況下 runtime 逾時丟棄 future 時，connect() 迴圈內的 adapter.stop_scan() 不會執行（只在 return 路徑呼叫），radio 掃描被留著。
- 證據：protocol.rs:676-685 `pub async fn stop_all(..){ self.raw.ensure_open().await?; ... send_before(StopAll, now+timeout) ... }`；serial.rs:370-375 `for _ in 0..20 { if connected {return Ok} sleep(100ms) }`；mqtt.rs:148-153 同；ble.rs:107-146 掃描迴圈，stop_scan 只在 :115、:138、:146（return/break 路徑）；runtime.rs:889-897 `for actuator in self.registry.all_actuator_instances().await { if timeout(2s, actuator.emergency_stop()).await ... }`（序列）；link_caps.rs:354-364 emergency_stop → stop_all(1_000ms)；firmware/esp32-companion/README.md:289-335 參考 spec：1 受器＋4 動器同一 serial port。

### F-2e02284-20260902T140445Z-link-transports-047 · low · command() 把等待期間任何無 id 的 err 當成本次結果：平行步驟共用同一裝置時可能把已套用的命令誤標為 device-refused
- 檔案：`crates/interaction-adapter-declarative/src/protocol.rs:571`
- 維度：link-transports；verdict：unverified
- 主張：wait_reply 的 pick 對 `DeviceMsg::Err { id: None, .. }` 無條件回 Some，且多個 command() 可同時在同一 DeviceLink 上等待（executor 對 parallel 模式的每個 step tokio::spawn，同一 adapter 的 led/vibe/buzzer 動器共用一條 link）。韌體對無法解析或過長的輸入回無 id 的 err（bad-json／unknown-type／BLE busy）；當 cmd A 已被裝置套用（ack 稍後才到）而另一則訊息 B 引發匿名 err，A 的等待者會先吃到 B 的 err，收據寫成 failed(device-refused)，A 的真 ack 之後被丟棄；人或 AI 依「失敗」重送 A＝重複實體效果。參考韌體只在畸形／過長輸入時發匿名 err，所以觸發需要 params 帶長 message 或 BLE 佇列滿，機率低，但錯配邏輯本身確定存在。
- 證據：protocol.rs:564-573 `wait_reply(..., |m| match m { Ack{id:Some(id),..} if id==action_id => Some, Err{id:Some(id),..} if id==action_id => Some, DeviceMsg::Err { id: None, .. } => Some(m.clone()), _ => None })`；executor.rs:281-292 parallel 模式每 step `tokio::spawn(run_step)`；lib.rs:980-1063 同一 adapter 的所有 serial capability 共用一條 DeviceLink；firmware esp32-companion.ino:659 `sendErr(link, nullptr, "bad-json")`、:664 unknown-type、:798 serial 行溢位、:818 mqtt ≥640 bytes、:908-909 BLE too-long/busy 皆無 id。

### F-2e02284-20260902T140445Z-link-transports-048 · low · Serial reader：read 逾時（200 ms）時把已讀到的半行丟掉，之後的殘段解析失敗→ack/state 遺失變成「結果未知」
- 檔案：`crates/interaction-adapter-declarative/src/serial.rs:246`
- 維度：link-transports；verdict：unverified
- 主張：reader thread 每輪先 line.clear() 再 read_line；serialport 開埠設 timeout 200 ms（serial.rs:70），read_line 在 fill_buf 回 Err(TimedOut) 時依 std BufRead::read_until 契約會保留已讀進 line 的位元組，但這裡對 TimedOut/WouldBlock/Interrupted 只 `{}` 然後迴圈回頭 line.clear()，半行被丟棄；接下來讀到的是同一行的後半，parse_device_msg 失敗而被靜默丟棄。只要裝置在一行中間停頓 >200 ms（例如韌體在送 ack 途中被 DHT/超音波阻塞讀取打斷），該 ack／state 就消失，host 端記成 ack 逾時「結果未知」。pty fallback 無逾時不受影響；真硬體路徑才會踩到。
- 證據：serial.rs:240-263 `loop { ...; line.clear(); match buf.read_line(&mut line) { Ok(0)=>break, Ok(_)=>{parse...}, Err(e) if matches!(e.kind(), TimedOut|Interrupted|WouldBlock) => {}, Err(_)=>break } }`；serial.rs:68-72 `serialport::new(port, baud).timeout(Duration::from_millis(200)).open()`；std::io::BufRead::read_until 文件：「If an I/O error is encountered then all bytes read so far will be present in buf」。

### F-2e02284-20260902T140445Z-link-transports-049 · low · pairingCode／MQTT password 可用明文寫在 adapter YAML，build 時不警告：規格 §2「Secret 不寫入 YAML」只靠註解建議
- 檔案：`crates/interaction-adapter-declarative/src/lib.rs:1049`
- 維度：link-transports；verdict：unverified
- 主張：SerialSpec/MqttSpec/BleSpec 的 pairing_code 與 MqttSpec.password 都是 Option<String>，resolve_secret 對非 secret:// 的值原樣回傳（lib.rs:483-486），build() 直接把明文交給 DeviceLink／MqttOptions，沒有任何 warn、audit 或 provider detail 提示。規格 §2 要求「Secret 使用 Keychain／Credential Reference，不寫入 YAML 或 log」；lib.rs:183 只用註解「建議 secret://」。目前 log/receipt/describe 都沒有洩漏（已查 tracing 與 describe），但明文憑證落在 config/adapters/*.yaml 這條路徑是靜默允許的。
- 證據：lib.rs:183-185 `/// 配對碼（建議 secret://）` `pub pairing_code: Option<String>`；lib.rs:204-208 `/// broker 認證（值可用 secret://）` `pub password: Option<String>`；lib.rs:483-486 `let Some(name) = reference.strip_prefix("secret://") else { return Ok(reference.to_string()); };`；lib.rs:1046-1050、1087-1096 直接 resolve_secret 後使用，無警告分支。

### F-2e02284-20260902T140445Z-agent-honesty-050 · medium · 緊急停止先把「cancel」當成新的 user turn 送進每個 gateway agent（觸發模型呼叫、發 fetched），每個 session 最多等 5 秒後才殺程序
- 檔案：`crates/interaction-runtime/src/agents.rs:1043`
- 維度：agent-honesty；verdict：unverified
- 主張：estop_agent_sessions 對每個 open session 先 mailbox_send(ToSession, kind="cancel")，mailbox_send 對 gateway session 一律 gateway_deliver，而 gateway_deliver 不分 kind：把 body JSON `{"reason":"emergency stop — stop all work now"}` 當文字送 send_user_message —— codex 是 `turn/start`（開一個新的模型 turn）、claude 是新的 user message。等於 estop 本身對外部模型服務產生一次新的計費呼叫，並發出 taxonomy `fetched`；且每個 session 要等 handle lock＋SEND_TIMEOUT（5 s）才進入 close_agent_session → gateway_spawn_kill，多個卡死 session 時 estop 的實際終止被序列化延遲。
- 證據：agents.rs:1043-1057 `for id in ids { mailbox_send(&id, ToSession, "cancel", {reason…}); close_agent_session(&id, None, "cancelled") }`；agents.rs:658-660 `if direction == ToSession { gateway_deliver(id,&message) }`（不看 kind）；gateway.rs:537-543 body 無 task/text 時 `serde_json::to_string(&message.body)` 當作 text；565-570 `handle.lock().await`（無 timeout）＋ `timeout(SEND_TIMEOUT, handle.send_user_message(&text))`；578 成功即 emit "fetched"；codex.rs:557-574 send_user_message = `turn/start`；claude.rs:313-325 寫入 user message。

### F-2e02284-20260902T140445Z-agent-honesty-051 · low · codex exec fallback 的「暫停／中斷目前工作」實際上是終止整個 session（Cancelled、mailbox 關閉），且不走 close 收尾
- 檔案：`crates/interaction-agent-gateway/src/codex_exec.rs:254`
- 維度：agent-honesty；verdict：unverified
- 主張：exec fallback 的 interrupt 設 cancel_requested 並 SIGINT，子程序結束後送 TaskCancelled；runtime 事件泵把它報成 "cancelled" → AgentSessionState::Cancelled（terminal、mailbox closed）。UI 按鈕文案是「暫停／中斷目前工作」，spec §8.3 也把 interrupt 與取消／close 列為不同操作，但在這條路徑上一次中斷就讓 session 無法再交代（Conflict `mailbox closed`），而且沒有走 close_agent_session：consent_scope 不清、provider surface 仍 Available、record_task_experience 不記，直到看門狗以 record-closed 殺掉（已結束的）程序群。
- 證據：codex_exec.rs:254-260 `interrupt(): if busy { cancel_requested=true; group.interrupt() }`、215-218 `if cancel_requested.swap(false) || closed { if !saw_terminal { TaskCancelled } }`；gateway.rs:456-461 `TaskCancelled => report "cancelled"`；agents.rs:862 `"cancelled" => AgentSessionState::Cancelled`；agent.rs:48-57 Cancelled 非 open；AiPage.tsx:468 按鈕「暫停／中斷目前工作」；gateway.rs:831-843 sweep 以 record-closed 殺 managed。

### F-2e02284-20260902T140445Z-docs-claims-052 · medium · Gap matrix §10 與 DESKTOP-GUIDE 仍稱 instances/adapters API 缺 inputCapabilities／author／version，程式碼早已回報且前端已消費
- 檔案：`docs/v05-capability-gap-matrix.md:214`
- 維度：docs-claims；verdict：unverified
- 主張：docs/v05-capability-gap-matrix.md:214 寫「外部 adapter 列的「可以接收」與 executable／network→部分（/v1/character/instances 未帶 inputCapabilities／author／version…）」並在 :231 列為 Phase 8 開放問題 (4)；docs/DESKTOP-GUIDE.md:234 也說「在 instance 清單還沒帶 inputCapabilities／securityRequirements 之前顯示『未回報』」。事實：Runtime 已回報這三個欄位，前端也已讀取；同一輪的 CHANGELOG.md:98 寫的是相反且正確的「多回報 author／version／inputCapabilities」。兩份「誠實狀態」文件互相矛盾，gap matrix 把已完成項寫成「部分／開放問題」。
- 證據：crates/interaction-runtime/src/character.rs:571-599 `instance_entry()` 回傳 json 含 "author": manifest.author, "version": manifest.version, "inputCapabilities": supported_input_capabilities(manifest), "executable", "network"；:613-633 `adapters_view()` 同樣含 author/version/inputCapabilities/executable/network，註解明言「不必寫「未回報」」。前端 apps/interaction-desktop/src/pages/connect/CharacterAdaptersSection.tsx:166 `inputCapabilities: inst.inputCapabilities ?? record?.inputCapabilities ?? null`、:188 `a.inputCapabilities ?? null`；api.ts:401/416 型別已有 inputCapabilities。單元測試 character.rs:2078 `input_capabilities_and_security_flags_come_from_the_manifest`。

### F-2e02284-20260902T140445Z-docs-claims-053 · medium · Gap matrix §10 自相矛盾：firstSuccessSeen 一處寫「已有（Rust 驗證＋測試）」、另一處寫「後端無欄位，localStorage 備援」並列為開放問題 (6)
- 檔案：`docs/v05-capability-gap-matrix.md:220`
- 維度：docs-claims；verdict：unverified
- 主張：docs/v05-capability-gap-matrix.md:219-220 寫「只看一次的旗標持久化→部分（後端無欄位，localStorage 備援）」，:232 列開放問題「(6) host 持久化 companion_preferences／first_success_seen」。同一節前面（§10「匯入」段）卻寫「runtime first_success_seen 持久化→已有（Rust 驗證＋測試）」，CHANGELOG.md 與 DESKTOP-GUIDE.md:139 也說旗標存在後端 UI 偏好。程式碼證實後端欄位存在且有測試，「後端無欄位」為不實敘述。
- 證據：crates/interaction-runtime/src/human.rs:97 `pub first_success_seen: bool`（UiPreferences，:118 預設 false）；crates/interaction-runtime/tests/human_layer.rs:817 `first_success_seen_persists_through_ui_preferences`（含重啟後仍為 true、非布林值被拒）；crates/interaction-api/tests/api_e2e.rs:948-956 PATCH /v1/ui/preferences {firstSuccessSeen:true} 回讀 true；前端 apps/interaction-desktop/src/pages/FirstSuccess.tsx:55-56 先 `api.uiPrefsPatch({ firstSuccessSeen: true })`，host 沒保存才退 localStorage。Tauri host 端 companion_preferences 亦有 src-tauri/src/lib.rs:1954-1958 上限常數＋:2980-3005 測試。

### F-2e02284-20260902T140445Z-docs-claims-054 · medium · acceptance-evidence Phase 8 表的 Playwright／效能列指向不存在的「證據跑一節」；CHANGELOG 卻稱該章節每個數字都是本機實跑
- 檔案：`docs/acceptance-evidence.md:519`
- 維度：docs-claims；verdict：unverified
- 主張：docs/acceptance-evidence.md:519「| Playwright | pnpm test:e2e | 見證據跑一節 |」與 :520「| 效能 | pnpm perf | 見證據跑一節 |」——檔案在 :521 結束，整份文件沒有任何「證據跑」章節，Phase 8 沒有 Playwright 與 perf 數字。CHANGELOG.md:14-16 卻寫「測試數字見 docs/acceptance-evidence.md v0.5 Phase 8 章節（每個數字都是本機實跑…）」。讀 CHANGELOG＋evidence 的人會以為 Phase 8 有 Playwright／perf 實跑結果；實情（gap matrix :226「Playwright…＝未跑」）只寫在第三份文件裡。Phase 7 的 24 個 Playwright 對 Phase 8 改過的 app／narrow／evidence spec 未重跑，屬「部分」被寫成有證據。
- 證據：`wc -l docs/acceptance-evidence.md` = 521；`tail -5` 最後兩行即 :519-:520 的「見證據跑一節」；`grep -n 證據跑 docs/acceptance-evidence.md` 只命中這兩行。docs/v05-capability-gap-matrix.md:226「Playwright（app／narrow／evidence spec 已改）＝未跑」；DESKTOP-GUIDE.md:9-10「Phase 8 的新截圖會在下一次證據跑…後落到同一個資料夾」。

### F-2e02284-20260902T140445Z-docs-claims-055 · medium · CPP README §8.1（唯一契約）要求實作不存在的 `CharacterTransport`（`interaction-character::transport`）
- 檔案：`docs/character-protocol/README.md:266`
- 維度：docs-claims；verdict：unverified
- 主張：docs/character-protocol/README.md:266 寫「新增 transport：實作 `CharacterTransport`（Rust `interaction-character::transport`）——只要能收送 §8 的 JSON 訊息、提供 generation 與 close，就能掛進 Gateway」。crate 內沒有 transport 模組、沒有任何名為 CharacterTransport 的 trait／型別；WS session 型別在 runtime／api crate。這份文件被 CLAUDE.md、README、CHANGELOG 宣告為「唯一契約」，卻指示 adapter 作者實作不存在的介面。gap matrix :182/:231 已承認不一致，但契約本身未修。
- 證據：`ls crates/interaction-character/src` → capability.rs gateway.rs input.rs intent.rs lib.rs lifecycle.rs manifest.rs receipt.rs schema.rs wire.rs（無 transport.rs）；`grep -rn CharacterTransport crates/ apps/interaction-desktop/src-tauri/src` → 無任何命中；唯一出現處為 docs/character-protocol/README.md:266。CLAUDE.md:27、CHANGELOG.md:12 均稱該 README 為唯一契約。

### F-2e02284-20260902T140445Z-docs-claims-056 · low · ESP32 README 協定表寫「nonce 目前僅收不驗」，與韌體 nonce 重放環及 README 自身測試步驟／已知限制矛盾
- 檔案：`firmware/esp32-companion/README.md:153`
- 維度：docs-claims；verdict：unverified
- 主張：firmware/esp32-companion/README.md:153 協定訊息表：「id 必填；nonce 目前僅收不驗」。韌體實際維護 16 筆 nonce 環並對重複 nonce 回 dup:true 不套用效果；README 自己的測試步驟 #18 與已知限制 #6 也描述 nonce 重放被擋。這是 Phase 5 交付時的過期句子，Phase 7 加 nonce 環後未刪，讀者會以為重放防護不存在。
- 證據：firmware/esp32-companion/esp32-companion.ino:201-208「重放/重複防護：cmd id 與 nonce 各一組 16 筆環形緩衝，並行比對」、`static char g_seenNonces[CMD_RING_SIZE][CMD_ID_MAX]`；:753-757 `if (ringContains(g_seenIds, id) || ringContains(g_seenNonces, nonce))` → dup ack；:780 `ringRemember(g_seenNonces, …)`。CHANGELOG.md Phase 7「韌體：…nonce 環」；README 已知限制 6「韌體對 id 與 nonce 各維護一組 16 筆環形緩衝」。

### F-2e02284-20260902T140445Z-docs-claims-057 · low · reference-adapters.md 示範指令 `interact-ai events --limit 20` 使用不存在的旗標
- 檔案：`docs/character-protocol/reference-adapters.md:73`
- 維度：docs-claims；verdict：unverified
- 主張：docs/character-protocol/reference-adapters.md:73 的外部 fixture 走查步驟寫 `interact-ai events --limit 20 | grep character.receipt`；CLI 的 `events` 子命令只有 `--seconds`，沒有 `--limit`，照文件執行會被 clap 以 unexpected argument 拒絕。
- 證據：crates/interaction-cli/src/main.rs:250-254 `Events { #[arg(long, default_value_t = 30)] seconds: u64 }`；crates/interaction-cli/src/commands.rs:1224 `Command::Events { seconds } => client.tail_events(*seconds, cli.json)`。`--limit` 只存在於 Outbox／Audit（main.rs:247, :257）。CLI E2E 本身用的是 `events --seconds 1`（scripts/v03-cli-e2e.sh:449）。

### F-2e02284-20260902T140445Z-docs-claims-058 · low · DESKTOP-GUIDE 與 gap matrix 稱角色視窗載入失敗時顯示「角色離線，改用文字。」，實際視窗固定文案是「角色載入失敗，改用文字顯示」
- 檔案：`docs/DESKTOP-GUIDE.md:85`
- 維度：docs-claims；verdict：unverified
- 主張：docs/DESKTOP-GUIDE.md:85-86「角色 adapter 載入失敗、崩潰、或被停用時，視窗會退回純文字角色，並顯示「角色離線，改用文字。」」；docs/v05-capability-gap-matrix.md:188「角色視窗載入失敗／崩潰→文字＋「角色離線，改用文字。」→已有（瀏覽器單元測試）」。程式碼裡角色視窗（CompanionApp）顯示的是另一句固定文案；「角色離線，改用文字。」是「現在」頁 HomePage 的 Runtime 未連線文案。文件把兩句「可信固定安全文字」張冠李戴。
- 證據：apps/interaction-desktop/src/companion/gatewayWiring.ts:34 `export const CHARACTER_LOAD_FAILED_LINE = "角色載入失敗，改用文字顯示"`，CompanionApp.tsx:767 與 :957 `showTrustedText({ text: CHARACTER_LOAD_FAILED_LINE … })`；apps/interaction-desktop/src/pages/HomePage.tsx:26 `export const CHARACTER_OFFLINE_LINE = "角色離線，改用文字。"`，:335 只在 `!input.connected` 時使用；CompanionPage.tsx:44 `CHARACTER_UNAVAILABLE_TEXT = "角色目前無法顯示，改用文字"`。reference-adapters.md §2 反而寫對（「角色載入失敗，改用文字顯示」）。

### F-2e02284-20260902T140445Z-docs-claims-059 · low · iOS README 引用的「33 項」「11 項」測試數字在 repo 內無對應來源，與 XCTest 實際 8＋11＝19 混用
- 檔案：`apps/interaction-ios/README.md:37`
- 維度：docs-claims；verdict：unverified
- 主張：apps/interaction-ios/README.md:37「Protocol.swift … (Codable,經 33 項測試)」、:194「MotionClassifier … 11 項行為測試全過」、:196「Wire protocol:33 項編解碼測試全過」。repo 內唯一的測試是兩個 XCTest 檔，分別 8 與 11 個測試方法（acceptance-evidence 也寫 19 = 8＋11）；33 與 11 來自 README 自述「一次性在 macOS 編譯執行」的等價案例，不在 repo、不可重跑。同一份 README 交錯出現 33/11/19 三組數字，把不可重現的一次性檢查與 XCTest 混寫，讀者無法判斷哪個是可驗證的。
- 證據：`grep -c "func test"` → MotionClassifierTests.swift 8、ProtocolTests.swift 11；`grep -c XCTAssert` → 11、48（皆非 33）。docs/acceptance-evidence.md Phase 7 表「iOS XCTest … 19 passed（MotionClassifier 8＋Protocol 11）」；README:36-38 目錄樹將這些數字標在檔案旁。ESP32 README 對同類一次性檢查已明寫「沒有進 repo，不是可重跑的回歸測試」，iOS README 未如此標示。

### F-2e02284-20260902T140445Z-docs-claims-060 · low · acceptance-evidence v0.5 已知限制 #18 仍稱 HEAD 為 a898996、Phase 6＋7 未提交，實際已提交為 2e02284
- 檔案：`docs/acceptance-evidence.md:497`
- 維度：docs-claims；verdict：unverified
- 主張：docs/acceptance-evidence.md:497-498「HEAD 仍為 `a898996`，Phase 6＋Phase 7 全部為工作樹未提交變更」列在現行「已知限制（v0.5，誠實聲明；修掉時同步更新 CHANGELOG）」清單內；Phase 6＋7 已於 commit 2e02284 提交，該句已不成立。目前未提交的是 Phase 8（143 個檔案），文件未反映。
- 證據：`git log --oneline -3` → 2e02284 feat(v0.5): Phase 6 iPhone Mobile Provider + Phase 7 adversarial review, fixes, full regression／a898996 Phase 5／1b2be19 Phase 4；`git status --short | wc -l` = 143（Phase 8 工作樹變更）。本次審查 runId 亦以 2e02284 為基準。

### F-2e02284-20260902T140445Z-docs-claims-061 · low · README 文件表對 v0.5 Gap Matrix 的描述停在 Phase 7（§9），漏掉 §10 Phase 8 收尾狀態
- 檔案：`README.md:185`
- 維度：docs-claims；verdict：unverified
- 主張：README.md:185「v0.5 Gap Matrix | v0.5 誠實基線（§0–§8）與 Phase 7 收尾狀態（§9）」；同一 README 的 v0.5 段已宣稱 Character Presentation Protocol 與一般模式產品化（Phase 8），但入口文件把 gap matrix 描述成只有到 Phase 7，讀者不會知道 §10 才是現況（含 Phase 8 開放問題）。
- 證據：docs/v05-capability-gap-matrix.md:161 起有「## 10. Phase 8 收尾狀態（Character Presentation Protocol＋一般模式產品化，2026-09-02）」；README.md:97-113 已描述 Phase 8 交付內容。

### F-2e02284-20260902T140445Z-safety-invariants-062 · high · 「立即停止／停止所有感測」只停本機麥克風，iPhone 麥克風串流照舊——UI 卻回報「已停止所有感測」
- 檔案：`crates/interaction-runtime/src/sensors.rs:147`
- 維度：safety-invariants；verdict：unverified
- 主張：`Runtime::stop_all_sensors`（`POST /v1/sensors/stop`、Tauri `sensors_stop`）只呼叫 `mic.stop_listen()` 並寫 audit `sensor.stopped-all`，完全不碰 iPhone：不送 `stop-all{sensors:true}`、不 disable `iphone.mic-level`。但 `status.activeSensors` 走 `active_sensors_all()`＝本機＋手機，桌面感測橫幅把 `iphone.mic-level` 一起列出並掛同一顆「立即停止」，GlobalSearch 命令更直接寫「已停止所有感測」。使用者按下後手機麥克風仍在擷取、仍在往桌面推 mic-level，違反 spec §2「麥克風使用中的持續可見指示與立即停止」與誠實階梯（宣稱停止≠停止）。除 estop 與撤銷裝置外沒有任何桌面路徑能停手機麥克風（`set_receptor_enabled(false)` 只改 registry，不通知手機）。
- 證據：sensors.rs:147-154 `pub async fn stop_all_sensors(...) { if let Some(mic) = self.mic_receptor.as_ref() { mic.stop_listen(); } self.store.audit("sensor.stopped-all", ...)?; Ok(()) }`；對照 runtime.rs:866-872 estop 路徑另外呼叫 `self.mobile_estop_stop_sensors(actor).await`，而 stop_all_sensors 沒有。sensors.rs:39-43 `active_sensors_all` 會 `extend(self.mobile_active_sensors().await)`；mobile.rs:1319-1341 產生 kind=`iphone.mic-level` 的 SensorUse。App.tsx:505-522 對每個 `activeSensors` 顯示「感測使用中：…」並以 `api.sensorsStop()` 掛「立即停止」按鈕；GlobalSearch.tsx:256-258 `label: "停止所有感測", doneMessage: "已停止所有感測。", action: () => api.sensorsStop()`；routes.rs:1516-1519 `sensors_stop` → `stop_all_sensors("api")`；lib.rs(src-tauri):906-910 同。lib.rs(src-tauri):413-424 `set_receptor_enabled` 直接呼叫 registry，無 mobile 通知。

### F-2e02284-20260902T140445Z-safety-invariants-063 · medium · iPhone 麥克風開始／停止不發 sensor.started／sensor.stopped 事件——tray／overlay 靠 4 秒輪詢才亮，SSE 訂閱者永遠看不到
- 檔案：`crates/interaction-runtime/src/mobile.rs:1731`
- 維度：safety-invariants；verdict：unverified
- 主張：手機自報 `status.sensors.micLevel` 只更新 `conn.mic_since`，從不 emit `EventType::SensorStarted/SensorStopped`；本機麥克風走 `sensor_state_changed` 會發事件，手機的不會。桌面 host 只在 `host_safety_relevant`（EmergencyStop／SensorStarted／SensorStopped／ProviderStateChanged）事件時立刻刷新 tray＋overlay，其餘靠 4 秒 sleep 輪詢；因此手機麥克風亮起後可信指示最多延遲 4 秒，且 `/v1/events`／CLI 事件流完全沒有這個感測的開始／停止紀錄。違反 CLAUDE.md「啟用中的感測器必須同時反映在 status、事件、tray 與 UI」（事件這一環缺失）。
- 證據：mobile.rs:1722-1741 `Some("status") if authed.is_some() => { let mic_on = v["sensors"]["micLevel"] == json!(true); ... conn.mic_since = match (mic_on, conn.mic_since) { (true, Some(since)) => Some(since), (true, None) => Some(Utc::now()), (false, _) => None }; }` 區塊內沒有任何 `events.emit`。grep `SensorStarted|SensorStopped|sensor_state_changed` 於 mobile.rs 零命中（僅 runtime.rs:260 本機 mic）。sensors.rs:46-66 本機路徑 `self.events.emit(EventType::SensorStarted/SensorStopped, json!({"sensor": kind}))`。src-tauri/lib.rs:2415-2423 `host_safety_relevant` 只列 EmergencyStop／SensorStarted／SensorStopped／ProviderStateChanged；lib.rs:2730 `tokio::time::sleep(Duration::from_secs(4))` 輪詢。

### F-2e02284-20260902T140445Z-safety-invariants-064 · medium · HTTP 宣告式動器在 request timeout 後自動重送，且把逾時當「failed」而非 uncertain
- 檔案：`crates/interaction-adapter-declarative/src/lib.rs:822`
- 維度：safety-invariants；verdict：unverified
- 主張：`DeclarativeHttpActuator::execute` 對 `send_request` 的任何 `Err`（含 reqwest timeout——請求可能已送達裝置並執行）都會依 `retry.attempts` 重送同一實體命令（YAML 一寫 `retry: {}` 預設就是 2 次、250ms），與 v0.5 `link_caps.rs` 明文「送出成功後 ack 逾時絕不重送」的規則相反，違反 spec §2「外部動作結果未知…不得自動重試可能造成重複副作用的動作」。全部嘗試失敗後收據寫 `failed("device-unreachable")`，把「已送出、結果未知」演成 failed，違反誠實階梯（未知應標 uncertain）。此為 v0.3 起既有行為（commit 9ee4076），v0.5 只修了 link 路徑，HTTP 路徑回歸未補。僅靠 `Idempotency-Key` header 期待裝置去重，一般 HTTP 裝置（燈、繼電器）不會實作。
- 證據：lib.rs:817-833 `let retry = self.spec.retry.clone().unwrap_or(RetrySpec{attempts:1,backoff_ms:0}); for attempt in 0..retry.attempts.max(1) { if attempt > 0 { tokio::time::sleep(...).await; } match send_request(...).await { Ok(2xx) => return ..., Ok(status) => last_err=..., Err(e) => { last_err = e; } } }`；lib.rs:232-243 `RetrySpec { #[serde(default="default_attempts")] attempts }`，`fn default_attempts() -> u32 { 2 }`；lib.rs:657-684 `send_request` 用 `.timeout(...)`，`builder.send().await.map_err(|e| e.to_string())?` 逾時即 Err；lib.rs:864-867 `Ok(DriverReceipt::start(&action, Utc::now()).failed("device-unreachable", &last_err).finish())`。對照 link_caps.rs:197-199 註解「Retry 只適用『送出失敗』（Unavailable）——送出成功後 ack 逾時絕不重送」與 :255-262 `Err(LinkError::Timeout) => dispatched + ackTimeout（不重送）`。

### F-2e02284-20260902T140445Z-safety-invariants-065 · medium · 宣告式硬體 adapter（HTTP／Serial／MQTT／BLE）無法宣告裝置安全上限，Policy 的 min() 缺「裝置安全上限」這一項
- 檔案：`crates/interaction-adapter-declarative/src/link_caps.rs:155`
- 維度：safety-invariants；verdict：unverified
- 主張：`LinkActuator::manifest` 與 `DeclarativeHttpActuator::manifest` 都不呼叫 `ActuatorManifestBuilder::limits(...)`，`CapabilitySpec` 也沒有任何 limits／maxMagnitude／maxDurationMs／maxPerHour 欄位，裝置 hello 的 `caps` 只是字串陣列不含上限。結果所有真硬體動器的 `actuator.limits.{max_magnitude,max_duration_ms,max_per_hour}` 皆為 None，Policy Governor 的 `magnitude.device` clamp 與 per-actuator 頻率上限永遠不生效，「有效值 = min(AI 請求, 使用者偏好, session 限制, 裝置安全上限, 剩餘預算)」在 v0.5 新增的所有硬體路徑上缺了裝置那一項；只有 ESP32 參考韌體自己 clamp（ack.applied 回報），任何非參考裝置或未 clamp 的韌體都只受 policy 通道上限保護。spec §9.1 要求每個 adapter 有「硬體硬限制與 Runtime 限制」。
- 證據：link_caps.rs:155-183 builder 鏈只有 `.description/.risk/.external/.requires_consent(true)/.human`，無 `.limits`；lib.rs:770-800 HTTP 動器同；grep `limits|max_magnitude|maxDuration` 於 link_caps.rs／lib.rs（declarative）／protocol.rs 零命中；protocol.rs:29-40 `Hello { device_id, fw, proto, caps: Vec<String>, pairing }`；相對地 adapters/builtin/src/actuators.rs:342、536 有 `.limits(ActuatorLimits{...})`；interaction-policy/src/lib.rs:286-303 只在 `actuator.limits.max_magnitude` 為 Some 時 clamp（`("magnitude.device", actuator.limits.max_magnitude)`）；lib.rs(policy):251 `min_opt_u32(channel_limits.max_per_hour, actuator.limits.max_per_hour)`。

### F-2e02284-20260902T140445Z-safety-invariants-066 · low · Agent token 可經 /v1/providers 讀到 iPhone 的 fingerprint＝device token 的 sha256，與「配對指紋屬人類層」的宣告邊界不一致
- 檔案：`crates/interaction-runtime/src/mobile.rs:1384`
- 維度：safety-invariants；verdict：unverified
- 主張：`mobile_register_provider` 把 `ProviderIdentity.fingerprint` 直接設為 `device.token_hash`（即每機 token 的 SHA-256）。`agent_request_allowed` 對 GET 只封鎖 `/v1/mobile`（註解：「配對指紋/裝置清單屬人類層：agent token 不可讀」），但 `/v1/providers` 與 `/v1/providers/{id}` 對 LegacyAgent 開放，所以同一份指紋（＝token hash）仍可被 agent token 讀走。token 為 32 bytes 隨機值，hash 不可逆、也不能拿來通過 `auth`（需明文），故非憑證外洩；但用「憑證雜湊」當公開身分指紋，與宣告的人類層邊界矛盾，且若日後有人把 token_hash 當認證比對值（例如 log／備份比對）會形成混用風險。
- 證據：mobile.rs:1375-1386 `fingerprint: Some(device.token_hash.clone())`；interaction-api/src/lib.rs:433-436 `// 配對指紋/裝置清單屬人類層：agent token 不可讀。 && !path.starts_with("/v1/mobile")`，同函式 GET 分支未排除 `/v1/providers`；lib.rs 路由表 `/v1/providers`、`/v1/providers/{id}` 在 authed router 內、對 agent GET 放行；mobile.rs:1654-1660 auth 比對 `d.token_hash == sha256_hex(token)` 需明文 token。

### F-2e02284-20260902T140445Z-mobile-server-067 · high · AI 可透過 plan 讓 iPhone 顯示「緊急停止中」（emergency 真相狀態），而真正的 estop 反而從不投影到手機
- 檔案：`crates/interaction-runtime/src/mobile.rs:141`
- 維度：mobile-server；verdict：unverified
- 主張：`CHARACTER_STATES` 把 `emergency` 列為 plan 可帶入的白名單狀態，`map_wire_params` 對 `extra.state="emergency"`（或 message="emergency"）照單放行；iOS `CharacterView` 對 `.emergency` 顯示固定文案「緊急停止中」。同時，runtime 的真 estop 路徑（`mobile_estop_stop_sensors` → `stop-all`）與 iOS 端 stop-all 處理都不會把角色設成 `.emergency`。結果：手機上的「緊急停止中」唯一來源是 AI／plan，可在沒有任何 estop 時偽造安全狀態；真 estop 時手機反而不顯示。違反「角色呈現層無權限主權／truthState 只由 Runtime 決定（character-protocol README 第 28、31 行）」與「AI 不得冒充安全狀態」。
- 證據：mobile.rs:131-141 `CHARACTER_STATES = ["idle","working","waiting","verified-success","failed","unknown","emergency"]`；mobile.rs:350-372 只對 `VERIFIED_STATE` 回 Err，其餘含 `emergency` 直接 `p.insert("state", ...)`；tests/mobile_loop.rs:451-494 只鎖 verified-success，未鎖 emergency。iOS：CharacterView.swift:56 `if character.state == .emergency { Text("緊急停止中") }`；`grep -rn "= .emergency" apps/interaction-ios` 無任何賦值（stop-all 不設 emergency）；ActuatorCenter.swift `handleCharacterPresent` 對任何白名單 state 直接 `characterState.state = state`。runtime：`grep character.present crates/interaction-runtime/src` 除 mobile.rs 外無送出點；runtime.rs:872 estop 只呼叫 `mobile_estop_stop_sensors`（stop-all），不送 `character.present emergency`。docs/character-protocol/README.md:28、31、163-176：emergency／truthState 只由 Runtime 設定、priority 100 不可被 adapter/pack 影響。

### F-2e02284-20260902T140445Z-mobile-server-068 · medium · 感測不靜默只靠手機自報：mic-level 觀察被 ingest 時不更新 mic_since，activeSensors 可能為空
- 檔案：`crates/interaction-runtime/src/mobile.rs:1675`
- 維度：mobile-server；verdict：unverified
- 主張：`status.activeSensors` 中的 `iphone.mic-level` 完全由手機 `status.sensors.micLevel` 旗標決定；桌面端即使正在接收並 ingest `iphone.mic-level` 觀察（受器啟用＋consent 成立＋事件已發布），只要手機沒送過 `micLevel:true` 的 status（或 status 尚未到、或 App 有 bug／被改），tray／首頁／角色視窗都不會顯示麥克風在用。桌面端明明「知道」麥克風資料正在流入，卻不反映，違反「啟用中的感測器必須同時反映在 status、事件、tray 與 UI」。
- 證據：mobile.rs:1675-1710 `Some("observation") if authed.is_some()` 分支只做 estop／consent／facts 白名單過濾後 `self.ingest(...)`，不觸碰 `conn.mic_since`；mobile.rs:1720-1737 只有 `Some("status")` 分支設 `conn.mic_since = match (mic_on, ...)`；mobile.rs:1319-1345 `mobile_active_sensors` 僅依 `mic_streaming_devices()`（mobile.rs:684-691 讀 `mic_since`）；sensors.rs:42 `all.extend(self.mobile_active_sensors().await)`。tests/mobile_loop.rs:1160-1208 只用 `send_phone_status` 驗證，沒有「只送觀察不送 status」的案例。

### F-2e02284-20260902T140445Z-mobile-server-069 · medium · 未認證區網 peer 的 DoS 面：無連線上限、無認證期限、TLS/WS 握手無逾時、訊息上限 64 MiB（字元 WS 已限 128 KiB）
- 檔案：`crates/interaction-runtime/src/mobile.rs:1016`
- 維度：mobile-server；verdict：unverified
- 主張：mobile 伺服器綁 0.0.0.0 並在配對過就 autostart。任何區網 peer 可：(a) 開無上限條數的 TLS 連線（每條一個 task＋32 槽佇列）；(b) 停在 TLS 或 WS 握手不完成，`acceptor.accept`／`accept_async` 沒有逾時，task 永遠掛著；(c) 未認證狀態沒有期限——唯一的 45s idle 逾時只要送 Ping/任意 frame 就能無限續命；(d) `accept_async` 用 tungstenite 預設 `max_message_size = 64 MiB / max_frame_size = 16 MiB`，未認證 peer 可逼伺服器為每則訊息配置 64 MiB 並 `serde_json::from_str` 解析。同一 repo 的 character WS 已把上限收到 128 KiB，mobile 路徑漏掉。
- 證據：mobile.rs:1010-1024 accept loop：`tokio::spawn(async move { match acceptor.accept(stream).await { Ok(tls) => match tokio_tungstenite::accept_async(tls).await {...` 無 timeout、無 Semaphore／計數；mobile.rs:1440-1520 handler 未認證時只有 `ping_interval`/`idle_timeout` 邏輯（1488、1497-1510），任何 frame 都刷新 `last_seen`（1493）且 `_ => continue` 把 Ping/Pong/Binary 當活著證據（1515）；`grep max_message_size crates/interaction-runtime/src/mobile.rs` 無結果；~/.cargo/registry/…/tungstenite-0.24.0/src/protocol/mod.rs:76-86 `max_message_size: Some(64 << 20), max_frame_size: Some(16 << 20)`；對照 crates/interaction-api/src/character_ws.rs:24、56-57 `WS_MAX_FRAME_BYTES = 65_536*2`、`.max_message_size(...).max_frame_size(...)`。CHANGELOG 已知限制只記「配對期可被區網 peer 燒掉」，未記此面。

### F-2e02284-20260902T140445Z-mobile-server-070 · medium · accept loop 遇到單次 accept() 錯誤即永久退出，status 仍回報 started:true
- 檔案：`crates/interaction-runtime/src/mobile.rs:1010`
- 維度：mobile-server；verdict：unverified
- 主張：`listener.accept()` 回 Err（例如連線洪水下的 EMFILE/ENFILE、ECONNABORTED 等暫時性錯誤）時 `break`，accept loop 結束、listener 被 drop；但 `self.mobile.started` 仍為 true，`mobile_ensure_started` 會直接回 `mobile_status()`，Bonjour 也繼續廣播。之後 iPhone 永遠連不上，UI 卻顯示伺服器已啟動、`bonjour.advertised:true`，且沒有 audit／事件；只能重啟 daemon。與前一項合起來，區網 peer 可把 mobile 伺服器打成「看起來活著的死服務」。
- 證據：mobile.rs:1008-1027 `tokio::spawn(async move { loop { let Ok((stream, peer)) = listener.accept().await else { break; }; ... } })`，loop 外無任何狀態回寫；mobile.rs:1028 `self.mobile.started.store(true, ...)` 之後沒有任何路徑再把它設回 false；mobile.rs:928-932 `if self.mobile.started.load(...) { return self.mobile_status().await; }`。

### F-2e02284-20260902T140445Z-mobile-server-071 · medium · 多台 iPhone 時 act 送給 BTreeMap 第一台，收據與 audit 不記錄是哪台手機執行；provider 描述子宣告每台都有六個 actuator 但 registry 只有一組共享
- 檔案：`crates/interaction-runtime/src/mobile.rs:533`
- 維度：mobile-server；verdict：unverified
- 主張：兩台以上 iPhone 同時連線時，`iphone.haptic`／`iphone.torch` 等實體效果由 `pick_conn` 取 `conns.iter().next()`（device_id 隨機 hex 的字典序第一台）執行；`dispatch` 內部知道 device_id 但不回傳，`execute` 的 receipt 只記 `transport:"mobile-wss"` 與手機自報 `deviceApplied`，因此 audit／receipt 無法回答「哪台手機震了／哪台手電筒亮了」，使用者也無法指定目標。另一方面每台 `provider.mobile.{id}` 的 descriptor 都列出全部 6 個 actuator，暗示每台各有一組，但 `mobile_register_capabilities` 只註冊一組共用 `MobileActuator`（重複註冊錯誤被 `let _` 吞掉）。
- 證據：mobile.rs:533-540 `pick_conn`：`conns.iter().next().map(|(id, c)| (id.clone(), c.outbound.clone()))`，註解「目前：第一台」；mobile.rs:560-580 `dispatch` 回傳 `oneshot::Receiver<Value>`，device_id 只留在 `PendingAct`；mobile.rs:745-752 receipt 只 `.note("transport", ...)`／`.note("deviceApplied", ...)`；mobile.rs:1385-1395 `actuators: MOBILE_ACTUATORS.iter()...` 於每台 provider；mobile.rs:1083-1091 `let _ = self.registry.register_actuator(actuator).await;`。CHANGELOG／acceptance-evidence 未列多裝置語意為已知限制。

### F-2e02284-20260902T140445Z-mobile-server-072 · low · TLS 私鑰以預設 umask 落地後才 chmod 0600，chmod 失敗被忽略，既有私鑰檔權限也不再檢查
- 檔案：`crates/interaction-runtime/src/mobile.rs:869`
- 維度：mobile-server；verdict：unverified
- 主張：`mobile-key.der` 先以 `std::fs::write`（umask 022 → 0644）寫出，再 `set_permissions(0o600)` 且 `let _` 忽略失敗；首次啟動有一段其他本機使用者可讀的窗口；之後每次啟動直接 `std::fs::read` 既有檔案，不會校正／警告權限。私鑰外洩即可對已配對 iPhone 做中間人（App 只釘憑證指紋，指紋對應的私鑰就是全部信任）。
- 證據：mobile.rs:866-874 `std::fs::write(&key_path, &key_der)?; #[cfg(unix)] { let _ = std::fs::set_permissions(&key_path, Permissions::from_mode(0o600)); }`；mobile.rs:851-856 既有檔案路徑 `if let (Ok(cert), Ok(key)) = (read(cert_path), read(key_path))` 直接回傳；ConnectionManager.swift:66-84 只比對 leaf DER SHA-256。

### F-2e02284-20260902T140445Z-mobile-server-073 · low · pending_acts 在等待端 future 被丟棄（HTTP client 斷線／CLI Ctrl-C）且手機不回覆時永久殘留；手機斷線也不會結束對它的 pending
- 檔案：`crates/interaction-runtime/src/mobile.rs:1799`
- 維度：mobile-server；verdict：unverified
- 主張：`dispatch` 先登記 pending，只有 `await_reply` 走到逾時分支才移除。`/v1/mobile/ble/scan`／`mobile_present_verified` 的 handler future 若在等待期間被 drop（axum/hyper 在 client 斷線時會丟棄 handler future；CLI 中斷同理），逾時清理永不執行；此時若目標手機已斷線，該 id 永遠留在 `pending_acts`（只有 estop 的 `fail_all_pending` 會清）。此外 handler 斷線收尾（1743 起）不會對該 device 的 pending 送 err，呼叫端要等滿 4s／10s 才拿到 UNKNOWN。
- 證據：mobile.rs:560-580 `dispatch` 插入 pending；mobile.rs:582-596 `await_reply` 只在 `Err/Ok(Err)` 分支 `remove(id)`；mobile.rs:1799-1815 `mobile_ble_scan` 為 `dispatch` 後 `await_reply`，無 drop guard；mobile.rs:1743-1792 斷線收尾只處理 conns／provider／高風險受器，不觸碰 `pending_acts`。tests/mobile_loop.rs:1074 `ble_scan_timeout_is_honest_and_clears_pending` 只覆蓋正常逾時。

### F-2e02284-20260902T140445Z-mobile-server-074 · low · 伺服器端參數映射未套用 App 的 tts 200 字上限與型別檢查，兩端規則不一致只能靠手機拒絕才浮現
- 檔案：`crates/interaction-runtime/src/mobile.rs:323`
- 維度：mobile-server；verdict：unverified
- 主張：`map_wire_params` 聲稱產生「iOS App 真正驗證的 wire 參數」，但 `tts.speak` 不檢查長度（App 對 >200 字回 `text-too-long`），`notify.show` 的 `extra.title`/`extra.body`、`screen.flash` 的 `extra.color` 直接透傳而不驗證型別／格式（App 需 string、6 位 hex）。結果是 policy／simulate 階段看起來合法、到手機才 device-refused；測試的 `app_validate` 鏡像雖含 200 字規則，但只用短文字驗證。
- 證據：mobile.rs:323-332 `iphone.tts` 僅檢查 `text` 缺席；ActuatorCenter.swift `handleTtsSpeak`：`guard text.count <= 200 else { return .err(id, reason: "text-too-long") }`；`handleNotifyShow` 需 `params.string("title")`／`string("body")`；`handleScreenFlash` 需 `parseHexColor` 成功；tests/mobile_loop.rs:216-224 鏡像含 200 字規則，tests/mobile_loop.rs:401-404 只用「測試朗讀」。

### F-2e02284-20260902T140445Z-mobile-server-075 · low · mobile_present_verified 在手機回 err（含與 stop-all 競態的 stopped）時仍回 Ok，與文件「沒 ack 誠實回 Err」不符
- 檔案：`crates/interaction-runtime/src/mobile.rs:1347`
- 維度：mobile-server；verdict：unverified
- 主張：`mobile_present_verified` 只把 `act()` 的傳輸層 Err 轉成 DomainError；若手機回 `{type:"err"}`（例如與 estop 競態時 `fail_all_pending` 注入的 `stopped`，或 App 端 `bad-state`），函式仍回 `Ok(reply)`；唯一呼叫端 agents.rs 只在 Err 時記 debug log，因此「綠勾沒顯示」在 err 回覆的情況下無任何 log／事件（audit 只記 `reply:"err"`）。
- 證據：mobile.rs:1360-1372 `let reply = self.mobile.act(...).await.map_err(DomainError::Unavailable)?; ... Ok(reply)`，無 `reply["type"] == "ack"` 判斷；docstring 1343-1346「沒有手機連線／手機沒 ack 都誠實回 Err」；agents.rs:830-842 `if let Err(e) = runtime.mobile_present_verified(...)` 才 log；mobile.rs:626-632 `fail_all_pending` 對在途 id 注入 `{type:err,reason:stopped,stopAll:true}`。
