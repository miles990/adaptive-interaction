# 對抗審查報告 c3d1786-20260903T124638Z

- **Run ID**：`c3d1786-20260903T124638Z`
- **HEAD**：`c3d17862967c37d35b31d37aa7e6375d63efad07`（worktree dirty；dirty 內容為尚未提交的文件／證據更新）
- **產生時間（UTC）**：20260903T124638Z
- **規格**：`docs/specs/adaptive-interaction-v05-core-experience-prompt.md`（sha256 `80d78971228f3265eb20010d398ed83f6ba91fcb5de55455f66788c93059f573`）
- **維度**：companion-gameplay, rig-renderer, director-pipeline, character-protocol, ia-settings, memory-ui, link-transports, agent-honesty, protocol-conformance, perf-claims, docs-claims, safety-invariants, mobile-server
- **模型**：find＝opus、verify＝sonnet（兩個獨立懷疑者鏡頭）；workflow 的落盤步驟因 API 529 失敗，本檔由整合者從 workflow 結果原封寫出

## 總表

| 項目 | 數量 |
|---|---|
| reviewed | 78 |
| confirmed | 74 |
| fixedMeanwhile | 0 |
| refuted | 4 |
| unverified | 0 |
| seeds | 0 |

confirmed 依 severity：blocker 2、high 17、medium 35、low 20

## 已確認缺陷（confirmed）

### blocker（2 項）

#### F-c3d1786-20260903T124638Z-character-protocol-038

- **標題**：Adapter 可用 unsupported／cancelled／uncertain 回執吞掉安全 intent（emergency 不落 system.text）
- **維度**：character-protocol
- **Severity**：blocker（原報 critical）
- **file:line**：crates/interaction-character/src/gateway.rs:1134

**主張**：Gateway 只在 adapter 回 `failed` 時把安全 intent 改走 system.text。Adapter 先回 `accepted`，再回 `unsupported`／`cancelled`／`uncertain`（三者都是 can_transition 允許的合法終態），該安全 intent 就被記成終結、沒有任何 system.text 回退，emergency／blocked／request-consent／offline 的訊息直接消失。呈現層因此對安全訊息有實質否決權，違反「呈現層沒有權限主權」與「安全訊息永不遺失」。TS 鏡射同一個缺口。

**證據**：gateway.rs:1134 `if receipt.status == ReceiptStatus::Failed && envelope.intent.is_safety()` —— 只判斷 Failed；receipt.rs:76-93 `can_transition` 允許 `(Accepted, Unsupported)`、`(_, Cancelled)`、`(_, Uncertain)`，且 receipt.rs:54-63 這三者都是 `is_terminal`。實跑探針（scratch crate，依賴 crates/interaction-character）：dispatch `emergency`→`RECEIPT s1 status=Accepted`；adapter 回 `{status:Unsupported, resolution:Unsupported}`→`RECEIPT s1 status=Unsupported resolution=Some(Exact)`，`>> system texts: 0`。同樣 `blocked`+`Cancelled`、`request-consent`+`Uncertain` 皆 0 個 SystemText。TS 端 apps/interaction-desktop/src/character/gateway.ts:963 `if (status === "failed" && p.safety)`；vitest 探針對 in-process adapter 回 `unsupported` 得 `SYSTEMTEXTS []`，回 `cancelled` 得 `SYSTEMTEXTS2 []`。對照組：adapter 送 `goodbye` 時 gateway.rs:606 `resend_safety_as_system_text` 有正確補送（探針輸出 `SYSTEXT e2/system-text intent=emergency`），證明缺的就是這條分支。

**建議修法**：把 on_receipt 的安全回退條件由 `status == Failed` 改成「terminal 且非 Completed（Failed/Unsupported/Cancelled{非 preempted-by-higher-safety}/Uncertain/Expired 視政策）」，沿用既有 `<原 id>/system-text` 衍生 messageId 路徑；TS gateway.ts `finalize` 同步改，並在 conformance/gateway 測試補「adapter 對每個安全 intent 回每一種終態 → 必有 system.text」。

**回歸測試**：在 tests/gateway.rs 新增測試：dispatch 一個安全 intent（如 emergency／blocked／request-consent）在會解析到真實能力（非 system-text、非 unsupported）的 manifest 上，取得 Accepted+Send 後，用 adapter_receipt 依序送回 Unsupported、Cancelled、Uncertain 三種終態，各自斷言 `system_texts(&out) == 1` 且該 SystemText 帶正確 intent／truthState（比照既有 `failed_safety_intent_falls_back_to_system_text` 的斷言風格）；TS 端在 character-gateway.test.ts 對應補上相同三案例。 / 仿現有 `failed_safety_intent_falls_back_to_system_text`（tests/gateway.rs:983）新增：dispatch 一個 safety intent（如 Emergency）使其 Accepted 並送達 adapter，再以 `adapter_receipt(..., ReceiptStatus::Unsupported, ...)`（另兩案例分別用 Cancelled、Uncertain）餵回執，斷言 `system_texts(&out) == 1` 且能在輸出中找到對應 `GatewayOutput::SystemText{ intent: Emergency, .. }`；同時在 apps/interaction-desktop 的 gateway.test.ts 補一組對稱案例（adapter 回 unsupported/cancelled/uncertain 時 SYSTEMTEXTS 應非空）。

#### F-c3d1786-20260903T124638Z-agent-honesty-022

- **標題**：桌面「接續上次（唯讀）」放寬 scope：丟掉 workdir／費用上限／時間上限，workdir 落回含 human api-token 的 runtime home
- **維度**：agent-honesty
- **Severity**：blocker（原報 critical）
- **file:line**：apps/interaction-desktop/src/pages/AiPage.tsx:561

**主張**：AiPage 的 resume 按鈕只從舊 record 的 dataScope 撈 workspace: 前綴當 workdir，且完全不傳 dataScope／maxCost／ttlMinutes。因為它自己建立的新 session 不帶 dataScope，「接續」出來的 session 永遠 dataScope=[]；再按一次接續時 workdir 變成 undefined，runtime 於是把 workdir 落回 `paths.home`（`~/.adaptive-interaction`，內含 0600 的 human `state/api-token`）。同時 maxCost 歸 0（Claude 不再帶 --max-budget-usd、gateway_deliver 的預算閘門失效）、ttl 由使用者選的 30 分鐘變成預設 120 分鐘。CLI 註解與 commands.rs 都宣稱「Resuming never widens scope」，桌面這條路徑實際上放寬了資料範圍、費用與租約三項。

**證據**：AiPage.tsx:561-567 `const workdir = record.dataScope.find((s) => s.startsWith("workspace:"))?.slice(...)`；`api.agentSessionCreate({ agentId, label, workdir, resumeProviderSessionId })` — 沒有 dataScope / maxCost / ttlMinutes。agents.rs:328 `data_scope: input.data_scope`（`#[serde(default)]` → []）；agents.rs:310 `let ttl = input.ttl_minutes.unwrap_or(120)`；agents.rs:334 `max_cost: input.max_cost.unwrap_or(0.0)`。gateway.rs:239-241 `let workdir = workdir.map(PathBuf::from).unwrap_or_else(|| self.paths.home.clone());`；gateway.rs:257 `spec.max_cost_usd = (kind == ClaudeCode && record.budget.max_cost > 0.0).then_some(...)`；gateway.rs:621 預算閘門 `e.record.budget.max_cost > 0.0 && ...`。config.rs:14/98 `state_dir().join("api-token")  # human/control-center capability token (0600)` 就在 `paths.home` 底下。process.rs:15-25 `remove_runtime_auth_env` 特地把 INTERACT_AI_TOKEN 等從子程序 env 拿掉，證明「agent 不得取得 human token」是本專案明示的威脅模型；讓 read-only agent 以 runtime home 為 workdir（claude `--tools Read,Glob,Grep`）等於把同一把 token 從檔案系統送回去。拿到 human token 後 `/v1/agent-sessions/{id}/verify`（lib.rs:217、routes.rs:1393，human-only）與 `/v1/emergency-stop/clear` 都對 agent 開放。CLI resume 亦同（commands.rs:801-849 只傳 --workdir，未給時同樣落回 paths.home）。UI 只顯示「已接續上次的工作（只讀取；沿用先前的對話脈絡）」，完全沒說資料夾、費用上限與時間上限換了。

**建議修法**：resume 時把舊 record 的 dataScope／maxCost／ttlMinutes 一併帶入（或要求人類重新確認一次預覽）；並讓 `gateway_attach` 在沒有 workdir 時誠實拒絕，而不是預設 `paths.home`——runtime 自己的狀態目錄永遠不該成為 agent 的工作目錄。

**回歸測試**：在 AiPage 對『接續上次（唯讀）』按鈕的 onClick 斷言：mock 一筆 dataScope=[] 的 record，click 後 spy 到的 api.agentSessionCreate 呼叫參數必須包含正確的 dataScope（衍生自原 workdir 或明確為系統資料夾並提示使用者）、maxCost（沿用或明示上限）、ttlMinutes（沿用或明示 30 分鐘），且後端 gateway_attach 在 workdir 為 None 時應拒絕退回 paths.home（改為要求顯式系統資料夾或直接 Validation 錯誤），同時新增測試斷言 workdir==paths.home 時 SessionSpec 不得含 state/ 子樹（或該情境下拒絕啟動）。 / 在 vitest 對 AiPage 的「接續上次（唯讀）」按鈕加一條：以 dataScope=[] 的已關閉 record 觸發兩次接續（模擬第二次接續），斷言 api.agentSessionCreate 收到的 payload 一定帶有非空 workdir（或明確拒絕/警告），且 maxCost／ttlMinutes 沿用原 session 數值而非被省略；後端加一條 gateway_attach 測試，斷言 workdir 解析結果一律不得等於 paths.home（或該路徑下不得含 state/ 目錄），否則回傳 DomainError 拒絕建立。

### high（17 項）

#### F-c3d1786-20260903T124638Z-rig-renderer-056

- **標題**：blendPose 只在「目標姿勢仍是 lie」時生效，目標表情自己的 pose 關鍵幀離開 lie 的那一幀，頭部單幀跳 14px
- **維度**：rig-renderer
- **Severity**：high（原報 high）
- **file:line**：apps/interaction-desktop/src/companion/rig/timeline.ts:418

**主張**：paramsAt 的姿勢過場覆寫 `blendPose(this.prevSnapshot, target, params, kp)` 只在 from/to 其一為 "lie" 時作用（params.ts:392-393）。startled-awake 這類 enter 段自己跨 lie→crouch 的表情，在 crossfade 窗口內 target.pose 會從 "lie" 變成 "crouch"，那一幀 blendPose 突然不再覆寫，poseBlend 從線性過場值（0.754）掉回 evalPhase 值（0.514），layoutFor 的頭中心單幀位移 14.14px，而且方向與前後幀相反（前 3 幀每幀 +2.95px、跳 +14.14px、之後每幀 -3.5~-4.3px）。這正是 rig-renderer-012/016 想修掉的「單幀瞬移」，專案自己的回歸門檻是 <12px。

**證據**：timeline.ts:416-421 `const kp = clamp((now - this.switchAt) / POSE_TRANSITION_MS, 0, 1); if (kp < 1) params = blendPose(this.prevSnapshot, target, params, kp);`；params.ts:392-393 `if (from.pose === to.pose) return params; if (from.pose !== "lie" && to.pose !== "lie") return params;`；expressions.ts:194-197 startled-awake enter `kf(0,{pose:"lie"}) → kf(0.35,{pose:"crouch"}) → kf(1,{pose:"stand"})`。實測（ExpressionTimeline rng=0.5，16.7ms 取樣，layoutFor(...).hy，PAL=maid-classic）：
poked → startled-awake：+250ms pose=stand poseBlend=0.883 hy=51.40 d=+2.95 ／ +284ms pose=stand poseBlend=0.754 hy=57.31 d=+2.95 ／ **+301ms pose=crouch poseBlend=0.514 hy=71.44 d=+14.14** ／ +317ms hy=67.90 d=-3.54
idle → startled-awake 同樣路徑：+250ms d=+10.41px。
全表情對掃描（64×63 組合）最差 15 名全部是 `* → startled-awake`（10.33–14.14px）。既有回歸測試只覆蓋 idle/sit ↔ lie-flat（regressions-run2-companion.test.ts:647-683，門檻 <12px），沒有覆蓋這條路徑；vitest rig.test.ts + character-adapters.test.ts 目前 53 tests 全綠。

**建議修法**：blendPose 的守門條件改成「from/to 的 layout 高度差超過閾值就混合」（lie 92 / sit 56 / crouch 52 / stand 46），或在 timeline 記住過場起點姿勢、整個 POSE_TRANSITION_MS 窗口內都以線性 kp 覆寫 poseBlend（不要因 target.pose 中途改變就放手）；layoutFor 的 mixLayout 也應以「真正的另一個姿勢」而非硬寫 uprightLayout("stand") 當混合端。補一條「任意表情對之間連續兩幀頭部位移 < 12px」的全表情掃描回歸測試。

**回歸測試**：仿 regressions-run2-companion.test.ts:647-683 的寫法，新增 `for (const from of ["poked","idle"]) { setAnimation(from,0); paramsAt(3000); setAnimation("startled-awake",3000); 逐 16.7ms 量測 layoutFor(...).hy 兩幀差 } expect(maxJump).toBeLessThan(12)`，目前會在 +301ms（poked 起始）量到 14.14px 而失敗。 / 仿照 regressions-run2-companion.test.ts 既有的 lie-flat 迴歸測試，加一組 `for (const from of ["idle","poked","sit"]) { setAnimation(from,0); paramsAt(3000); setAnimation("startled-awake",3000); }`，16.7ms 取樣 layoutFor(...).hy，斷言連續兩幀差值 `toBeLessThan(12)`——目前會在 +250~+301ms 那幀失敗（實測 10.41~14.14px）。

#### F-c3d1786-20260903T124638Z-character-protocol-039

- **標題**：Adapter 自行送 negotiate 即可讓進行中的安全 intent 靜默消失
- **維度**：character-protocol
- **Severity**：high（原報 high）
- **file:line**：crates/interaction-character/src/gateway.rs:552

**主張**：`on_negotiate` 在重新協商時呼叫 `mark_all_uncertain` 並用 `let _ =` 丟棄它回傳的安全 envelope 清單，因此 pending 的 emergency／blocked 等只會變成 `uncertain`，不會像斷線那樣補送 system.text。`negotiate` 是 adapter→runtime 方向的合法訊息（on_message 會直接轉給 on_negotiate），外部 WebSocket adapter 可以在收到安全 intent 後立刻重送 negotiate，反覆讓安全訊息消失且不必演出。

**證據**：gateway.rs:552 `let _ = Self::mark_all_uncertain(inst, "re-negotiated", now, &mut out);`（同檔 601-606 的 `resend_safety_as_system_text` 只在 on_disconnect／goodbye 路徑被呼叫）。gateway.rs:1349 `WireMessage::Negotiate(offer) => match self.on_negotiate(...)` 證明 adapter 可隨時觸發。實跑探針：dispatch `emergency` 後送 `WireMessage::Negotiate(Negotiate::from_manifest(&m, 2))`，輸出為 `AUDIT instance text#1 re-negotiated; pending commands marked uncertain` + `RECEIPT e1 status=Uncertain reason=Some("re-negotiated")` + `SEND negotiated`，`>> systemtexts: 0`；同一情境改送 goodbye 則有 `SYSTEXT e2/system-text intent=emergency`。

**建議修法**：區分「host 主動重新協商（/v1/character/hello，generation 由可信 host 推進）」與「adapter 自行 negotiate」：後者對 in-flight 安全 intent 一律走 `resend_safety_as_system_text`；或更保守地對兩者都補送（安全訊息重複顯示優於遺失），並限制 adapter 端 negotiate 的頻率。

**回歸測試**：Dispatch a safety intent (e.g. Emergency) so it resolves to a real capability and sits in `pending`, then call `on_negotiate` again on the same connected instance; assert the resulting output contains a `GatewayOutput::SystemText` for that message (mirroring what `on_disconnect` already produces), not just a bare `Uncertain` receipt with zero system-text outputs. / 在 gateway.rs 測試中：connect 一個宣告 visual.expression 等真實呈現能力的 manifest，dispatch 一個 emergency 安全 intent 並斷言 resolution=Exact（非 system.text）；緊接著（不呼叫 on_disconnect）呼叫 `gw.on_message(&id, WireMessage::Negotiate(Negotiate::from_manifest(&m, next_gen)), now)`；斷言輸出中除了 Uncertain receipt 外，還必須包含一則 message_id 為 `<原id>/system-text` 的 Send{Intent} 或等價的 system.text 落地（即 on_negotiate 也要像 on_disconnect 一樣呼叫 resend_safety_as_system_text，或在協商完成後對目前仍作用中的安全狀態重新 dispatch）。

#### F-c3d1786-20260903T124638Z-character-protocol-041

- **標題**：權威 Rust 與 TS 鏡射的 INTENT_CAPABILITIES 表不一致，同一 manifest 有 6/20 個 intent 協商結果不同
- **維度**：character-protocol
- **Severity**：high（原報 high）
- **file:line**：apps/interaction-desktop/src/character/protocol.ts:329

**主張**：§3.4 是確定性演算法且 Rust 為權威、TS 為鏡射，但 TS 對除 idle 外的 19 個 intent 全部套用同一份 `EXPRESSIVE` 清單，而 Rust 是逐 intent 客製（emergency 含 visual.overlay、verified-success 含 visual.particles、offline 含 visual.presence、play 以 gameplay.toys 為主要能力、rest/sleep 含 visual.presence）。同一份 manifest 在 Runtime gateway 與視窗 gateway 會得到不同的 resolution 與不同的 fallback 鏈鍵，Runtime 端因此對外宣稱一個視窗其實不會使用的呈現能力；配合上一項 resolution 過濾缺陷，Runtime 甚至會把「視窗回 unsupported、什麼都沒演」記成 `resolution: exact`。

**證據**：protocol.ts:329-360（`const EXPRESSIVE = [visual.expression, visual.pose, visual.textBubble, audio.speech, audio.effect, light.cue, haptic.cue]`，除 idle 外每個 intent 都直接指向它）對照 capability.rs:171-282（Play => [gameplay.toys, visual.locomotion, visual.pose, visual.expression]；VerifiedSuccess 含 visual.particles；Emergency 含 visual.overlay；Offline/Rest/Sleep 含 visual.presence）。用同一份 manifest（capabilities 只宣告 visual.presence／visual.particles／visual.overlay／gameplay.toys／visual.locomotion，intents 全 20 個，無 fallbacks）實跑兩邊協商：Rust 得 verified-success=Exact via visual.particles、offline=Exact via visual.presence、emergency=Exact via visual.overlay、play=Exact via gameplay.toys、rest/sleep=Exact via visual.presence；TS 得 verified-success/offline/emergency=substituted via system.text、play/rest/sleep=unsupported。其餘 14 個 intent 兩邊一致。

**建議修法**：讓 TS 從單一來源產生 INTENT_CAPABILITIES（例如由 Rust 產生 golden JSON，隨 schemas/character-protocol.schema.json 一起重生並在 vitest 中比對），或至少加一個 conformance 測試：對 tests/common 的每份 fixture manifest 逐 intent 比對 Rust 與 TS 的 resolution 與 via。

**回歸測試**：用同一份 fixture manifest（只宣告 visual.presence/visual.particles/visual.overlay/gameplay.toys/visual.locomotion）同時跑 Rust `negotiate()` 與 TS `negotiate()`，斷言 20 個 intent 的 `resolution`／`via` 逐一相等（目前 verified-success/offline/emergency/play/rest/sleep 會斷言失敗）。 / 用同一份 manifest（capabilities 只宣告 visual.presence/visual.particles/visual.overlay/gameplay.toys/visual.locomotion，20 intents 全開，無 fallbacks）分別跑 Rust `resolve_intent`（或 conformance harness）與 TS `resolveIntent`，斷言兩邊對每個 intent 的 `{resolution, via}` 逐一相等；目前會在 verified-success/offline/emergency/play/rest/sleep 六項上失敗。

#### F-c3d1786-20260903T124638Z-ia-settings-012

- **標題**：「緊急停止中 — 前往解除」按鈕在已位於 safety 路由時完全無作用
- **維度**：ia-settings
- **Severity**：high（原報 high）
- **file:line**：apps/interaction-desktop/src/App.tsx:537（主引用無漂移）；次要佐證 apps/interaction-desktop/src/components/GlobalSearch.tsx:235-237（claim 原引用 196-199 有漂移）

**主張**：頂部列唯一的緊急停止解除入口 `onClick={() => setTab("safety")}` 在 App 的 `tab` 已經是 "safety" 時是 React state bail-out（Object.is 相等不重新 render），內容區 `key={tab}` 不變、ConnectPage 的 `initial` prop 也不變（同步 effect 依賴 `[initial]`），所以 ConnectPage 內部若已被使用者切到「裝置與能力」分頁，點這顆按鈕畫面完全不動，解除流程到不了。GlobalSearch 的「前往解除緊急停止」走同一條 `onNavigate("safety")`，同樣失效。

**證據**：App.tsx:537 `<button className="estop-indicator" onClick={() => setTab("safety")}>`；App.tsx:600 `<div className="content" id="main-content" key={tab}>`（key 是未折疊的 tab，值不變就不重掛載）；App.tsx:962-970 `case "safety":` 渲染 `<ConnectPage … initial="safety" />`；ConnectPage.tsx:133-139 `const [tab, setTab] = React.useState<ConnectTab>(initial); React.useEffect(() => { setTab(initial); }, [initial]);`（initial 沒變就不會同步回 safety）；ConnectPage.tsx:168 hub-tab 的 `onClick={() => setTab(id)}` 可把內部分頁切到 "devices"。重現路徑：從「現在」頁點頂部緊急停止指示 → 進 safety → 在頁內點「裝置與能力」→ 再點頂部「緊急停止中 — 前往解除」→ 無任何反應。components/GlobalSearch.tsx:196-199 同樣是 `action: () => onNavigate("safety")`。

**建議修法**：Shell 改用一個明確的導航函式（例如 `goTo(tab)`，在目標與目前相同時也 bump 一個 navNonce 加進 content 的 key，或把 ConnectPage 的 initial 改成 `{tab: "safety", nonce}`），讓「導到已在的路由」仍會把子分頁重設到指定的那一個。

**回歸測試**：render ConnectPage with initial="safety"，userEvent 點內部「裝置與能力」分頁，再用相同 initial="safety" 執行 rerender（模擬已在 safety route 再按一次頂列「前往解除」），assert 「同意與安全」分頁仍未被選中／內容未同步回來（即會失敗，暴露這顆按鈕在此情境下無效）。 / render App 於已觸發 estop 狀態、點頂列按鈕進 safety、在頁內點「裝置與能力」hub-tab、再點頂列「緊急停止中 — 前往解除」，斷言頁面內容切回顯示 SafetyPage／權限地圖區塊（而非停留在裝置與能力）。

#### F-c3d1786-20260903T124638Z-memory-ui-001

- **標題**：控制中心的「忘記這些」在角色視窗開啟時不會真的忘記：舊互動記憶會在下一次玩玩具時被寫回
- **維度**：memory-ui
- **Severity**：high（原報 high）
- **file:line**：apps/interaction-desktop/src/companion/gatewayWiring.ts:488

**主張**：角色互動記憶（spec §11 第一類）有兩個各自持有副本的寫入者，且控制中心的清空不會同步到角色視窗。使用者在控制中心按「忘記這些」後，角色視窗記憶體裡的 memoryRef 仍是舊值，下一次丟玩具就把已被清掉的玩具次數、常關掉的反應與相處天數整包寫回偏好——UI 明講「會清掉…；{name}會從頭開始記」，實際沒有，違反「沒有你不能刪除的記憶」。

**證據**：1) 控制中心：CompanionPage.tsx:763 `<button onClick={() => void patch({ companionInteractionMemory: emptyMemory() })}>忘記這些</button>`；patch 定義在 CompanionPage.tsx:167-171 → `desktop.prefsPatch(p)` 後 `desktop.companionApplyPrefs()`。
2) host：apps/interaction-desktop/src-tauri/src/lib.rs:2419 `let _ = app.emit("companion-reload", ());`（只有 companion_apply_prefs 發，desktop_prefs_patch 本身不發）。
3) 角色視窗收到 companion-reload → CompanionApp.tsx:456 `companionReloadPlan(prev, next)`；gatewayWiring.ts:478-489 把 `companionInteractionMemory` 放進 HOST_APPLIED_PREF_KEYS（註解：「host（Rust）自己套用、或角色視窗自己寫的偏好：變了不需要視窗做任何事」），所以 gatewayWiring.ts:529 `needsReload=false` → action="live"。
4) CompanionApp.tsx:413-437 `applyLivePrefs` 只更新 storyProgress/dnd/quietUntil/bubbles/sound/drag/approach/名字並 reconfigure，完全沒有碰 `memoryRef`（memoryRef 只在 CompanionApp.tsx:676 的 init 與 1853 的 quickToy 被寫）。
5) 復活點：CompanionApp.tsx:1853-1855 `const mem = notePlay(memoryRef.current, kind, Date.now()); memoryRef.current = mem; void desktop.prefsPatch({ companionInteractionMemory: mem })` —— 以陳舊的 memoryRef 為基底覆寫偏好。
6) 既有測試只覆蓋控制中心單邊（characterPage.test.tsx:439-461 只斷言 prefsPatch 被以 emptyMemory() 呼叫、摘要清空），沒有跨視窗案例；相關測試全綠（vitest 3 檔 61 passed）。
同一個缺陷也造成互相覆蓋：CompanionPage.tsx:612/659 的 patchReaction 在控制中心寫 disabledReactions，角色視窗下一次 notePlay 會用自己的舊副本把它清掉。

**建議修法**：把 `companionInteractionMemory` 從 HOST_APPLIED_PREF_KEYS 移到 LIVE_PREF_KEYS，並在 applyLivePrefs 內加 `memoryRef.current = sanitizeMemory(next.companionInteractionMemory)`；或改成角色視窗為唯一寫入者、控制中心的清空/關閉反應改走 host 命令並回讀。補一個跨視窗回歸測試：清空後模擬 companion-reload → notePlay，斷言寫回的 memory 只有新事件。

**回歸測試**：In a CompanionApp harness: mount with non-empty companionInteractionMemory, call the internal quickToy/notePlay path once to populate memoryRef, simulate a `companion-reload` event whose refetched prefs carry `companionInteractionMemory: emptyMemory()` (host-side "忘記這些"), then call quickToy again and assert the resulting `prefsPatch({ companionInteractionMemory })` payload is `emptyMemory()` + the new toy only — not the pre-clear toy counts/days resurrected from the stale ref. / 在 CompanionApp 的整合/單元測試中：以非空 companionInteractionMemory 開機 → 觸發 companion-reload 事件並模擬 desktop.prefsGet() 回傳 companionInteractionMemory 已被外部清成 emptyMemory()（僅此一鍵變動）→ 斷言 plan.action === "live" 之後緊接呼叫等同 quickToy 的 notePlay 路徑，其寫回 desktop.prefsPatch 的 companionInteractionMemory 必須以剛才清空後的值為底（新增一筆 toy、其餘欄位仍是空的），而不是把清空前的舊 toys/disabledReactions/daysSeen 又整包寫回去。

#### F-c3d1786-20260903T124638Z-memory-ui-002

- **標題**：Context Bundle 被 24 條／48KB 上限截斷時完全不回報，一般 UI 反而顯示「擋下來的：沒有」
- **維度**：memory-ui
- **Severity**：high（原報 high）
- **file:line**：crates/interaction-runtime/src/memory.rs:279（亦見同檔 215-217 的 domain pack 版本）

**主張**：memory_context_bundle 在達到 BUNDLE_MAX_ITEMS/BUNDLE_MAX_BYTES 時直接 `break`，但回傳的 `excluded` 只有 needsReview／notVisibleToAgent／sensitive／unreviewedCandidates／outsideGrantedDomains 五種原因，沒有任何「因上限被截斷」的欄位或旗標。結果是：(a) 一般模式的預覽會印出「擋下來的：沒有」，即使真的有記憶被扣住；(b) 送給 agent 的 contextBundle 也沒有截斷標記，agent 無從得知上下文不完整。這違反「未知要標 uncertain、不得靜默」的誠實階梯。

**證據**：crates/interaction-runtime/src/memory.rs:279-281：
```
if included.len() >= BUNDLE_MAX_ITEMS || bytes + size > BUNDLE_MAX_BYTES { break; }
```
（domain pack 進入時同樣的 break 在 215-217，且 pack 先吃額度）
memory.rs:292-306 的回傳只有：
```
"excluded": { "needsReview": …, "notVisibleToAgent": …, "sensitive": …, "unreviewedCandidates": …, "outsideGrantedDomains": … },
"note": "確定性選擇；stale 需重新確認、candidate 未經複審不提供、敏感標籤排除、知識類只給 Session 明確授權的 domain。"
```
沒有 cap/truncated 欄位。上限值在 memory.rs:18-19（24 條／48KB），來源清單本身也只取 `list_memory(None, 1000)`（memory.rs:199，storage clamp 見 crates/interaction-storage/src/lib.rs:990 `limit.clamp(1, 1000)`）。
前端 apps/interaction-desktop/src/pages/MemoryKnowledgePage.tsx:1085-1094：只列上述五個 key，`.join("、") || "沒有"` → 截斷時輸出「擋下來的：沒有」。進階模式倒原始 JSON 也一樣看不到截斷。
agent 路徑：crates/interaction-runtime/src/agents.rs:598-601 直接把同一份 bundle 塞進 mailbox body 的 `contextBundle`。
既有測試 crates/interaction-runtime/tests/memory_loop.rs:272 `context_bundle_is_deterministic_and_honest` 沒有任何超過上限的案例（10 tests 全綠）。

**建議修法**：在 bundle 回傳中加 `excluded.overCapacity`（條數）與 `truncated: true/false`；前端 reasons 表加「超過本次可提供的份量 N 條」，agent 端在 contextBundle 也保留該旗標。順帶把 `list_memory(None, 1000)` 的 1000 上限改成 keyset 分頁或回報未掃完。

**回歸測試**：在 memory_context_bundle 的確定性測試中額外建立超過 24 筆（或總 bytes 超過 48KB）皆為 active／可見／domain 命中的記憶，斷言 includes.len() 小於建立總數時，excluded（或新增的 cap/truncated 欄位）必須非零／為 true，而不是目前實測的全零。 / 在 memory_loop.rs 建構 30 筆以上（或內容加總 > 48KB）皆合法可見的 rust-domain memory items 後呼叫 memory_context_bundle，斷言 includes.len() < 建立總數 時，回傳中必須有一個非零的截斷計數／旗標（例如新增的 `excluded.truncatedByCap` 或 `truncated: true`），而非現況下五個 excluded 欄位全為 0 或空陣列。

#### F-c3d1786-20260903T124638Z-link-transports-047

- **標題**：宣告式 HTTP 動器的 emergency_stop 永遠回 Ok，runtime 把它算成「已確認停止」
- **維度**：link-transports
- **Severity**：high（原報 high）
- **file:line**：crates/interaction-adapter-declarative/src/lib.rs:991-1004

**主張**：DeclarativeHttpActuator::emergency_stop() 無條件回 Ok(())：(a) spec 沒有 stopRequest 時什麼都沒送；(b) 送了但逾時／連不上／回 HTTP 500 時結果被 let _ = 丟棄。runtime 的 estop 摘要以 Ok(Ok(())) 當「確認停止」計數，所以一台正在發聲／亮燈的 HTTP 外部裝置會被列進 stoppedActuators，而它根本沒被要求停止或停止失敗。違反誠實階梯（未確認不得算成已停止），且與同一 repo 的 link／mobile 路徑自相矛盾。

**證據**：lib.rs:991-1004 `async fn emergency_stop(&self) -> Result<(), ActuatorError> { if let Some(stop) = &self.spec.stop_request { let _ = send_request(&self.client, stop, stop.body.clone(), 2_000, None, self.home.as_deref()).await; } Ok(()) }`。stop_request 是 Option（lib.rs:120-121），validate_spec 對 externalSideEffect:true 的 http 動器不要求它（lib.rs:275-299 只驗 URL）。runtime.rs:932-937：`let results = join_all(...actuator.emergency_stop()); // Only a confirmed stop counts. Timed out / errored ones stay unconfirmed — never reported as stopped. let stopped_actuators = results.iter().filter(|r| matches!(r, Ok(Ok(())))).count();`，數值進 payload["stoppedActuators"]（runtime.rs:963）與 audit emergency.stop。反例：link_caps.rs:401-411 沒 ack 就回 ActuatorError::Unavailable；mobile.rs:1537-1541 註解「沒有手機連線 → Err，runtime 的 stoppedActuators 不計入（誠實）」。crates/interaction-adapter-declarative/tests/ 沒有任何 http estop 測試（grep emergency_stop 只命中 protocol_honesty.rs 的 link 測試）。

**建議修法**：沒有 stop_request → Err(Unavailable("這個動器沒有停止端點；裝置狀態未知"))；有 stop_request → 檢查 send_request 結果與 2xx，逾時回「已送出、未確認」的 Err。validate_spec 對 externalSideEffect:true 的 http 動器要求 stopRequest 或明確宣告無法遠端停止。加 regression test：mock server 回 500／不回應時 stoppedActuators 不含它。

**回歸測試**：起一個 mock HTTP 裝置：(a) spec 無 stopRequest 時，emergency_stop() 必須回 Err（或至少 runtime 的 stoppedActuators 不得將其計入）；(b) 有 stopRequest 但 mock 回 500 或逾時不回應時，emergency_stop() 必須回 Err(ActuatorError::Unavailable(...))，比照 protocol_honesty.rs 對 LinkActuator 的 stop_all_without_a_device_ack_is_reported_as_unconfirmed 寫法。 / 建一個 DeclarativeHttpActuator，spec 不含 stop_request（或 stop_request 指向會回 500／逾時的 mock server），呼叫 emergency_stop() 後斷言回傳 Err（而非目前的 Ok(())），並在 runtime 層加一條整合測試：stop_request 失敗／缺席時，該 actuator 不得計入 stoppedActuators。</parameter>
</invoke>


#### F-c3d1786-20260903T124638Z-link-transports-048

- **標題**：ensure_ready 的握手等待沒有連線世代守衛：握手期間重連後，cmd 會送到身分／配對從未在該世代驗證過的連線上
- **維度**：link-transports
- **Severity**：high（原報 high）
- **file:line**：crates/interaction-adapter-declarative/src/protocol.rs:426

**主張**：ensure_ready() 在等待前抓一次 generation，之後不再檢查；hello／pair 兩段等待用的是沒有世代感知的 wait_for（不是 command/cancel/read_state/stop_all 用的 wait_reply）。若連線在握手進行中重連（世代 G→G+1），ensure_ready 仍會以舊世代 G 標記握手完成並回 Ok，接著 command() 重新讀取 generation 得到 G+1 並把 cmd 寫上線。結果：實體命令送到一條「hello.deviceId 從未在此世代比對過、pair 從未在此世代完成」的連線上，直接違反 protocol.rs 開頭寫死的不變量「重連不得沿用舊握手」與「連線埠、IP、topic 都不是身分」。pairingCode 是選填的，沒設配對碼時裝置不會擋，命令會被真的套用。

**證據**：protocol.rs:425-499：`self.raw.ensure_open().await?; let gen = self.raw.generation(); let mut done = self.handshaken.lock().await; ...` → 436 `wait_for(&mut rx, HANDSHAKE_TIMEOUT, ...)`（hello）→ 475 `wait_for(&mut rx, HANDSHAKE_TIMEOUT, ...)`（pair）→ 495 `*done = (true, gen);` 全程沒有再讀 self.raw.generation()。對照 protocol.rs:527-559 wait_reply 每輪都做 `if self.raw.generation() != generation { return Err(WaitEnd::Reset); }`，且 177-178 的 trait 註解明寫「DeviceLink 用它偵測『重連過』並重新走 hello/pair 握手——重連不得沿用舊握手」。command() 於 572 呼叫 ensure_ready 後，於 596 `let generation = self.raw.generation();` 讀的是新世代，並未與握手世代比對，隨即於 606-608 send_before 寫出 cmd。握手窗最長 2×HANDSHAKE_TIMEOUT = 5 s（protocol.rs:342），serial 拔插與 mqtt 斷線重連都落在這個量級。

**建議修法**：把 ensure_ready 的兩段等待改用世代感知版本（或在 495 之前加 `if self.raw.generation() != gen { return Err(LinkError::Reset(...)); }`），並讓 command/cancel/read_state 在 ensure_ready 之後以「握手世代」而非「當下世代」當 wait_reply 的基準；regression test：mock RawLink 在 hello 與 pair 之間把 generation +1，斷言 ensure_ready 回 Reset 且 cmd 一個 byte 都沒寫上線。

**回歸測試**：A RawLink mock whose send() of `who`/`pair` bumps generation() and only then pushes the Hello/PairOk reply (modeling firmware resending hello unprompted after a fresh reconnect); assert that after ensure_ready() returns Ok, link.readiness() is NOT Ready for the RawLink's current generation, and that a subsequent command() still writes a `cmd` to the wire — then fix ensure_ready to re-check self.raw.generation() == gen (or route hello/pair through a generation-aware wait like wait_reply) before marking `done`, and assert the same test now returns LinkError::Reset with zero `cmd` writes. / 用一個假 RawLink：generation() 回傳一個共享 AtomicU64，在測試協程收到 Who 之後、於 wait_for 收到 hello 訊息之前，先把 generation 加一再把 hello/pair-ok 塞進同一條 broadcast channel（模擬重連期間插入的『新世代』訊息），斷言 ensure_ready() 要嘛回 Err(Reset)，要嘛完成後其內部握手世代必須等於 self.raw.generation() 的『當下』值（即 readiness() 不得回報 NotHandshaken）——目前程式碼下會斷言失敗：ensure_ready() 回 Ok，但 readiness() 回 NotHandshaken，暴露 done/ready_generation 與實際世代不一致。

#### F-c3d1786-20260903T124638Z-agent-honesty-023

- **標題**：codex exec fallback 把「exit 0 但沒有結局事件」報成 failed，而規格與 claude connector 都規定必須是 unknown
- **維度**：agent-honesty
- **Severity**：high（原報 high）
- **file:line**：crates/interaction-agent-gateway/src/codex_exec.rs:219-236

**主張**：codex exec fallback 的子程序收攤時，只要沒讀到 terminal 事件就無條件送 `TaskFailed`，完全沒有檢查 exit status。exit 0（例如 codex 輸出格式漂移、`turn.completed` 改名、JSONL schema 變動）會被記成 `AgentSessionState::Failed` 這個終局狀態，角色播失敗演出、UI 顯示「執行失敗」，而真實結局其實是未知。這正好違反 core 對 Unknown 的明文定義與 claude connector 的對照實作。

**證據**：codex_exec.rs:211 `let status = child.wait().await.ok();` 之後 219-236：`} else if !saw_terminal { ... let status_text = status.map(|s| s.to_string())... ; let _ = tx.send(GatewayEvent::TaskFailed { error }).await; }` — `status` 只被格式化進訊息字串，從未用 `status.success()` 或 `code()` 判斷。對照 claude.rs:265-282：「只有**觀察得到的錯誤**（非零 exit code）才是 failed。exit 0 卻沒有結果，或被訊號終止 ⇒ 結果未知，交給 SessionClosed」，實作是 `if let Some(code) = exit_code.filter(|code| *code != 0) { ... TaskFailed ... }`。interaction-core/src/agent.rs:39-44 對 `Unknown` 的文件：「A subprocess that exits without reporting a result lands here (only an observable error, e.g. a non-zero exit code, is honest enough to be `Failed`)」。codex_exec 也從不送 `SessionClosed`，所以 gateway.rs:521-541 那條「沒有結局 ⇒ unknown」的路徑對 exec fallback 永遠不會觸發。`cargo test -p interaction-agent-gateway`（20 passed）沒有任何測試覆蓋這個分支。

**建議修法**：比照 claude.rs：只有 `status.code()` 非零才送 TaskFailed；exit 0 或被訊號終止一律送 `TaskOutcomeUnknown`（或補送 `SessionClosed { resumable, detail }` 讓 runtime 記 unknown），detail 帶 exit note＋stderr tail。

**回歸測試**：Spawn a fake `codex` binary (via a test PATH shim or by refactoring the terminal-drain logic into a testable function taking a mock exit status + empty/unrecognized JSONL) that exits 0 while printing only unrecognized JSONL lines (e.g. a renamed `turn.completed` key); assert the resulting event is not `GatewayEvent::TaskFailed` — it should instead be treated as unknown (e.g. via a `SessionClosed`-equivalent event), mirroring `claude.rs`'s `error_results_map_to_failed_not_completed`-style exit-code gating test. / Spawn the codex_exec handle against a fake `codex` binary that prints a stdout line matching no known event shape (or nothing at all) and exits 0; assert the resulting GatewayEvent is SessionClosed (which the runtime maps to state "unknown"), not TaskFailed.

#### F-c3d1786-20260903T124638Z-protocol-conformance-027

- **標題**：模擬器對超出 float32 範圍的數值參數整個程序崩潰（韌體是 clamp 後回 ack）
- **維度**：protocol-conformance
- **Severity**：high（原報 high）
- **file:line**：scripts/esp32-serial-sim.py:183

**主張**：`f32()` 用 `struct.pack("<f", v)` 收斂單精度，對任何超出 IEEE754 float32 範圍的 JSON 數字（例如 `1e39`）會拋 `OverflowError`。該例外不是 `BadParam`，`apply_cmd` 的 `except BadParam`（line 412-415）接不到，`handle(msg)`（主迴圈 line 548）也沒有包 try/except，於是模擬器**整個程序退出**，對該 cmd 既不回 ack 也不回 err。韌體對同一輸入是 `roundToLong()` + `clampLong()` → 正常 ack（README〈數值參數的型別規則〉明寫「超出範圍一律 clamp（不是 err）」、已知限制 #1 宣稱模擬器「鏡射同一套規則」）。這是三端規則在模擬器上直接不成立，而且失敗形式是「裝置憑空消失」——host 只會看到 ackTimeout / read 逾時，把參數問題誤診成傳輸問題。

**證據**：實測（sim 版本為 repo 現狀，pairing 9927，已 pair-ok 後送單一 cmd）：
  {"durationMs": 1e+39}（buzzer.beep） -> reply: []  exit: 1
  {"r": 1e+39}（led.set）            -> reply: []  exit: 1
  {"angle": 1e+40}（servo.move）      -> reply: []  exit: 1
  {"strength":0.5,"durationMs":1e+39}（vibe.pulse） -> reply: []  exit: 1
四例 stderr traceback 尾端皆為：
  `return struct.unpack("<f", struct.pack("<f", v))[0]` / `OverflowError: float too large to pack with f format`
呼叫路徑：`read_int_param`(204-207) → `round_to_int`(199-201) → `f32`(179-183)；`read_led_channel`(215-223) 亦直接呼叫 `f32`。對照韌體 `readIntParam`(esp32-companion.ino:340-348) → `roundToLong`(337) → `clampLong`(285-289)：無論 float→long 轉出什麼值都會被 clamp 進 lo..hi，一定回 ack。

**建議修法**：1) `f32()` 捕捉 OverflowError 並回 `math.copysign(math.inf, v)`（鏡射 ArduinoJson `as<float>()` 的溢位→inf）；`round_to_int()` 對 inf/nan 直接回極大／極小整數讓後續 clamp 收斂。2) 把主迴圈的 `handle(msg)` 包進 try/except Exception，任何未預期例外回 `{"type":"err","id":…,"reason":"bad-params"}` 並 note 到 log——模擬器絕不能以「整個消失」當作協定回覆。3) 在 esp32_sim_conformance.rs 加一條 out-of-range clamp 的三端回歸。

**回歸測試**：對已 pair 的模擬器 pty 送 {"type":"cmd","id":"x","name":"buzzer.beep","params":{"durationMs":1e39}}，斷言：(a) 收到 {"type":"err","id":"x","reason":"bad-params"}（或等價的誠實錯誤），(b) 模擬器程序仍存活可繼續回應後續 cmd。 / Pair with the simulator over its pty, send `{"type":"cmd","id":"t1","name":"buzzer.beep","params":{"durationMs":1e39}}` (and similarly for led.set `r`, servo.move `angle`, vibe.pulse `durationMs`), and assert the simulator replies with an `ack` (applied clamped to the documented bound) or at worst an `err`, and that the process is still alive and responsive to a following `who` afterward.

#### F-c3d1786-20260903T124638Z-protocol-conformance-028

- **標題**：BLE device→host 方向完全沒有訊息長度紀律：state（193 bytes）超過 ATT notify 可攜長度且 host 靜默丟棄
- **維度**：protocol-conformance
- **Severity**：high（原報 high）
- **file:line**：firmware/esp32-companion/esp32-companion.ino:492；crates/interaction-adapter-declarative/src/ble.rs:303-307

**主張**：host→device 方向兩端都有明確上限（韌體 `BLE_MSG_MAX 512`→拒 ≥512、host `MAX_WRITE_BYTES = 480`，README 已知限制 #2/#9 也寫明），但 device→host 方向**任何一端都沒有上限**：`sendLine(LINK_BLE)` 直接 `setValue(line, strlen(line))` + `notify()`，既不檢查長度也不分段，setup 也從未呼叫 `NimBLEDevice::setMTU()`。ATT Handle-Value-Notification 的 payload 上限是 ATT_MTU−3，超過部分由協定棧截斷；預設 DEVICE_ID(`esp32-companion-01`) 下 `state` 訊息是 193 bytes、`hello` 是 182 bytes，在 macOS/iOS CoreBluetooth 作為 central 時協商到的 185 MTU（payload 182）下 state 必被截 11 bytes，hello 剛好卡在邊界（DEVICE_ID 再長一個字元就一起壞）。截斷後是破損 JSON，host `ble.rs:303-307` 的 `if let Ok(text) … if let Some(msg) = parse_device_msg(text)` 沒有任何 else／warn 分支，直接無聲丟棄 → `read_state` 只會逾時回「device did not answer read」，BLE 受器永遠讀不到值，而且沒有任何一行 log 指向真因。

**證據**：韌體 esp32-companion.ino:489-496 `case LINK_BLE:` → `g_bleNotifyChar->setValue((const uint8_t*)line, strlen(line)); g_bleNotifyChar->notify();`（無長度檢查）；setupBle()(688-716) 只有 `NimBLEDevice::init(DEVICE_ID)`，全檔 grep 無 `setMTU`。buildState()(524-543) 的欄位集合實測序列化為 193 bytes：`{"type":"state","deviceId":"esp32-companion-01","facts":{"button":false,"distanceMm":842,"lux":133,"tempC":24.5,"vibeActive":false,"buzzActive":false,"servoAngle":90,"led":{"r":0,"g":0,"b":0}}}`；sendHello()(505-522) 為 182 bytes。host 端 ble.rs:4-5 註解「一則訊息一個 write / notification」、301-310 notify task 無重組、無解析失敗記錄。README「單則訊息上限」段(164-166)與已知限制 #9(534-537) 只列 Serial/MQTT 639、BLE write 511，device→host 一個字都沒提，全檔 grep 無 `MTU`。

**建議修法**：韌體：setup 加 `NimBLEDevice::setMTU(517)`，並在 `sendLine(LINK_BLE)` 用 `ble_att_mtu(connHandle)-3` 檢查；超長時分段（在協定加 newline 界定）或至少改送一則 `{"type":"err","reason":"state-too-large"}`，不得靜默截斷。host：ble.rs 以 newline 為界做重組緩衝，並在 `parse_device_msg` 回 None 時 `tracing::warn!`（現在三種傳輸 serial.rs:350 / mqtt.rs:156 / ble.rs:305 都是無聲丟棄，違反「感測不靜默」）。README 補上 device→host 上限那一欄。

**回歸測試**：在 ble.rs 的 notify 解析 task 中，餵入一段被截斷（非完整 JSON）的 bytes 給 notifications stream，斷言目前無任何 tracing::warn/error 被觸發、且 inbound channel 收不到訊息（相較於 deviceId 不符情境會 warn，此路徑應同樣要有 warn 才算修復）；另可對 sendLine(LINK_BLE, ...) 加一個「payload 長度 > 假定 MTU-3」情境的單元/整合測試，斷言韌體要嘛分段要嘛至少記錄一個警示，而非目前的無條件 setValue+notify。 / 在 host 端 ble.rs 的 notify 處理路徑對 buildState() 產生的預設 193-byte state JSON 以 182-byte（MTU-3=182）切片模擬協定棧截斷後餵給 notify handler，斷言 parse_device_msg 回 None 且目前完全無 warn/log 產生（之後修復應改為斷言有 warn 且 read_state 回傳能區分「收到但截斷」與「純逾時」的錯誤）。

#### F-c3d1786-20260903T124638Z-protocol-conformance-029

- **標題**：in_flight 計數只涵蓋 command()，同一條 link 上的 read/cancel/stop-all 會讓 cmd 認領別人的無 id 錯誤
- **維度**：protocol-conformance
- **Severity**：high（原報 high）
- **file:line**：crates/interaction-adapter-declarative/src/protocol.rs:621

**主張**：`command()` 用 `self.in_flight.load(..) <= 1` 判斷「這則沒有 id 的裝置錯誤是不是我的結果」，但全檔只有 `command()`(605) 會 `InFlightGuard::enter`；`cancel()`(655)、`read_state()`(691)、`stop_all()`(743) 都不進入計數。而同一個 adapter 內所有 serial/mqtt/ble capability **共用同一個 DeviceLink**（lib.rs:1185-1214 明確要求並複用同一條 link），受器的輪詢 `read`（README 範例 pollIntervalMs 5000）與動器的 `cmd` 天然並行。韌體對沒有 id 的訊息（`read`、`stop-all`、壞行）一律回**沒有 id** 的 err（handleMessage:685-690 bad-json、755-758 not-paired），這些 err 會被同時在等的 `command()` 當成自己的回覆——正是 protocol.rs:615-620 註解自己警告的情境，但 guard 漏掉了三條路徑所以警告失效。

**證據**：protocol.rs:605 `let in_flight = InFlightGuard::enter(&self.in_flight);` 只出現在 command()；grep `InFlightGuard::enter` 全檔僅此一處。621-628 的 picker：`DeviceMsg::Err { id: None, reason } => { if self.in_flight.load(Ordering::SeqCst) <= 1 { Some(m.clone()) } … }`。read_state(691-738) / cancel(655-690) / stop_all(743-790) 本體內無任何 in_flight 操作。lib.rs:1186-1214 顯示同一 adapter 的 serial capability 共用 `serial_link`（`link.clone()`），mqtt/ble 同構(1262/1313)。link_caps.rs:260-281 把 `Ok(DeviceMsg::Err{reason})` 一律記成 `failed("device-refused", &reason)`。

**建議修法**：失效情境：受器 poll 送出的 `{"type":"read"}` 在序列線上掉一個 byte → 韌體回 `{"type":"err","reason":"bad-json"}`（無 id）；同一毫秒派出的 `cmd` 被韌體正常套用（LED 已亮／馬達已震）並在數 ms 後回 ack，但 host 的 command() 因 in_flight==1 先認領了那則 bad-json，收據記成 failed("device-refused","bad-json")——一個**已產生實體效果**的動作被記成失敗，誘導人／AI 重下同一命令＝重複實體效果。修法：read_state/cancel/stop_all 一律 `InFlightGuard::enter`，讓 `<= 1` 真的代表「這條 link 上只有我在等」；或更保守，無 id 的 err 一律只記進 unattributed、永不當成 cmd 的結果（逾時＝outcome UNKNOWN 才是誠實的預設）。

**回歸測試**：Concurrently run command() (ack withheld) and read_state() on the same DeviceLink/shared link, then inject a single id-less DeviceMsg::Err meant for the read; assert command() returns LinkError::Timeout("...cannot be attributed...") (unknown), not Ok(DeviceMsg::Err{..}) — i.e. extend the in_flight counter (or an equivalent per-link pending-request count) to cover cancel()/read_state()/stop_all() as well as command(). / 在 protocol_honesty.rs 新增一個 multi_thread 測試：ensure_ready 後，同時 spawn 一個 link.command(...)（ack_replies 關閉，讓它進入等待）與一個 link.read_state(...)（同樣讓它卡在等待，例如用一個永不回覆 State 的 mock），短暫 sleep 後 raw.emit_id_less_error("not-paired")，斷言 command() 的回傳必須是 LinkError::Timeout 且訊息含 "UNKNOWN"／"cannot be attributed"，而不是 Ok(DeviceMsg::Err{..})／不能被轉成 device-refused。

#### F-c3d1786-20260903T124638Z-docs-claims-067

- **標題**：發布文件把已在 HEAD 修好的三項 Rust 缺口仍列為「刻意保留的 partial／功能性殘留缺口」
- **維度**：docs-claims
- **Severity**：high（原報 high）
- **file:line**：docs/releases/v0.5.0-known-limitations.md:14（行號未漂移；唯該檔目前是 git 未追蹤的新檔，尚未進入任何 commit，其餘引用的 CHANGELOG.md:91/180、docs/v05-capability-gap-matrix.md:288-289/344/347、docs/releases/v0.5.0-release-readiness.md:21 行號亦與目前磁碟內容相符；release-readiness.md 的「:69 對應 #19」引用有誤，該檔僅 38 行）

**主張**：v0.5.0 已知限制清單（以及 CHANGELOG、gap matrix §11、release-readiness）宣稱 F-043 的「已測試證據歸屬未修」、credential_warnings() 未轉發進 provider 紀錄、mobile_ble_scan 沒有 deviceId 參數——這三項在 commit 6ec0528 就已全數實作並附回歸測試，文件敘述在 HEAD 為假。

**證據**：(1) F-043：known-limitations.md:14-18 寫「executor.rs::note_capability_tested 仍透過 provider_of_capability（list().find(...)，字典序第一個）記『已測試』證據」；同語句見 CHANGELOG.md:91-92、CHANGELOG.md:180、docs/v05-capability-gap-matrix.md:288-289 與 :344、docs/releases/v0.5.0-release-readiness.md:21（關卡 13「2 項刻意 partial」）與 :69 對應的 #19。實際程式：crates/interaction-runtime/src/executor.rs:733-743 取 `receipt.driver_response["deviceId"]` 後呼叫 `note_capability_tested_on(..., tested_device.as_deref())`，程式碼註解直接反駁文件（「driver 回報的 deviceId 才是事實，字典序第一個 provider 不是」）；crates/interaction-runtime/src/providers.rs:482-540 的 `provider_of_capability` 在指名 device 時只認 `provider.mobile.<deviceId>`，找不到就什麼都不記。回歸測試 crates/interaction-runtime/tests/mobile_loop.rs:3002 `tested_evidence_lands_on_the_phone_that_actually_ran_the_action`（斷言另一台 id_other 的 tested 為 None），本機 `cargo test --workspace` 685 passed/0 failed 全綠。 (2) credential_warnings：known-limitations.md:36-38 寫「providers.rs 的 register_declarative_spec 未把 built.warnings 存進 provider record，所以 providers show 還看不到」，同語句見 CHANGELOG.md:185、gap-matrix:347。實際 crates/interaction-runtime/src/providers.rs:367 與 :384 皆呼叫 `merge_provider_detail(..., &built.warnings)`，:97 `provider_detail_warnings()`、:117 `warning_summary()` 為此新增；回歸測試 crates/interaction-runtime/tests/providers_loop.rs:638 `plaintext_credential_warnings_reach_the_provider_record` 斷言警告寫進 detail、狀態轉換後不被洗掉。commit 6ec0528 自己的訊息第 38 行就寫「provider detail carries plaintext-credential warnings」。 (3) BLE：known-limitations.md:42-45 寫「mobile_ble_scan 沒有 deviceId 參數」，實際 crates/interaction-runtime/src/mobile.rs:3240-3253 簽章為 `mobile_ble_scan(&self, duration_ms: u64, device_id: Option<&str>)` 並以 `dispatch(target, ...)` 指名送出。

**建議修法**：把 known-limitations.md §1 第 2 項、§3 第 8、10 項刪除或改寫為「已於 6ec0528 修復（附測試 X）」；同步 CHANGELOG.md:91-92／:180／:185、gap-matrix:288-289／:344／:347；release-readiness 關卡 13 改為「1 項刻意 partial（僅 ia-settings-012）」、刪掉 #19。ia-settings-012 那一項經核對仍為真（apps/interaction-desktop/src/pages/Onboarding.tsx:345 確實仍寫 silencedChannels: []），保留。

**回歸測試**：已存在且綠燈：(1) crates/interaction-runtime/tests/mobile_loop.rs::tested_evidence_lands_on_the_phone_that_actually_ran_the_action 斷言雙手機情境下「已測試」證據只記到真正回 ack 的那一台；(2) crates/interaction-runtime/tests/providers_loop.rs::plaintext_credential_warnings_reach_the_provider_record 斷言明文憑證警告寫進 provider detail 且狀態轉換不洗掉；BLE deviceId 由 mobile_loop.rs 內多處 `mobile_ble_scan(_, Some(&id))` 呼叫佐證簽章已支援指名裝置。此 finding 本身應修的不是程式碼，而是同步訂正 known-limitations.md／CHANGELOG.md／gap-matrix.md／release-readiness.md 這四份文件，移除這三項已修復的殘留描述。 / 已存在：crates/interaction-runtime/tests/mobile_loop.rs 的 tested_evidence_lands_on_the_phone_that_actually_ran_the_action 與 tests/providers_loop.rs 的 plaintext_credential_warnings_reach_the_provider_record 已經是能釘死程式碼行為的回歸測試；本 finding 真正需要的是「文件同步」而非新測試——可加一個簡單的 grep-based CI 檢查，斷言 known-limitations.md／CHANGELOG.md／gap-matrix.md 不再同時出現「list().find(...)，字典序第一個」與「built.warnings」未轉發、「mobile_ble_scan 沒有 deviceId」等字樣，作為文件-程式碼漂移的守門。

#### F-c3d1786-20260903T124638Z-docs-claims-068

- **標題**：CHANGELOG／migration／known-limitations 宣稱桌面端「尚未送出 stop-all reason」與「iOS 不會在 stop-all 設 emergency 角色狀態」，兩者在 HEAD 皆已實作
- **維度**：docs-claims
- **Severity**：high（原報 high）
- **file:line**：CHANGELOG.md:192 (also :176); docs/releases/v0.5.0-migration.md:74; docs/releases/v0.5.0-known-limitations.md:49-60 (#12,#13); contradicted by crates/interaction-runtime/src/mobile.rs:67,70,472-475,1219,1274,2255,2505 and apps/interaction-ios/InteractionCompanion/Services/ActuatorCenter.swift:451,482-489

**主張**：v0.5.0 對外契約與已知限制段宣稱桌面 mobile.rs 還沒送 stop-all 的 reason 欄位、使用者主動停感測時手機「仍會顯示因桌面緊急停止而停用」，並宣稱 iOS ActuatorCenter.stopAll 不會把 characterState 設成 emergency。兩項在 commit c3d1786 都已完成並有測試。

**證據**：文件：CHANGELOG.md:176「桌面端尚未送出 `reason`（已知限制，見下）」、CHANGELOG.md:192-193「桌面端 `mobile.rs` 尚未送出 stop-all 的 `reason` 欄位…目前一律 fail-safe 顯示為『因桌面緊急停止而停用』」、docs/releases/v0.5.0-migration.md:74「桌面端尚未送出 `reason` 也尚未消費新的 `sensors` 回聲」、docs/releases/v0.5.0-known-limitations.md:49-52（#12）與 :53-57（#13）。實際程式：crates/interaction-runtime/src/mobile.rs:66-70 定義 STOP_REASON_USER／STOP_REASON_EMERGENCY，:472-477 `stop_all_wire_reason()` 把 audit reason「stop-all-sensors」映成 user、其餘映成 emergency，:1219 estop 路徑送 `{"type":"stop-all","sensors":…,"reason":STOP_REASON_EMERGENCY}`，:1270 `stop_sensors_for()` 送 `{"type":"stop-all","sensors":true,"reason":reason}`，:2255 以 `stop_all_wire_reason(reason)` 呼叫、:2505 每機端點傳 STOP_REASON_USER；單元測試 crates/interaction-runtime/tests/mobile_loop.rs:2920 `stop_all_wire_reason_only_calls_the_estop_path_emergency`。iOS 端 apps/interaction-ios/InteractionCompanion/Services/ActuatorCenter.swift:451 `func stopAll(sensors: Bool = false, reason: StopAllReason = .emergency)`，:471-481 依 reason 給不同文案並在註解寫明「stop-all{sensors:true,reason:emergency} 本身就是緊急停止的事實，所以這裡直接把角色狀態設成 emergency」。commit c3d1786 訊息亦明載「stop-all carries reason user|emergency … the App also switches the character to the emergency state」。（migration.md 同句的後半「尚未消費新的 sensors 回聲」經核對仍為真：mobile.rs:3105 只判 `v["stopAll"] == true`，未讀 sensors。）

**建議修法**：改寫 CHANGELOG.md:176／:192-193、migration.md:74、known-limitations #12／#13 為已完成，並把仍為真的殘留（桌面未消費 ack 的 `sensors` 回聲）獨立列為一條，不要與已修好的 reason 綁在同一句。

**回歸測試**：Add a pre-release doc check that fails if CHANGELOG.md / known-limitations.md still contain the string "尚未送出 stop-all 的 `reason`" while crates/interaction-runtime/src/mobile.rs already references STOP_REASON_EMERGENCY on the estop broadcast path — treat "known limitation" prose as a claim that must be re-verified against source before each release, not just at time of writing. / Add a doc-consistency check (or simply hand-edit) that fails CI/review when CHANGELOG.md / known-limitations.md assert 'reason 尚未送出' / 'stopAll 不會設 emergency' while mobile_loop.rs's stop_all_wire_reason_only_calls_the_estop_path_emergency test and ActuatorCenter.swift's reason==.emergency branch both exist -- i.e. treat the already-passing Rust/Swift tests as evidence the known-limitation entries are stale and must be deleted/updated before this branch is released.

#### F-c3d1786-20260903T124638Z-docs-claims-069

- **標題**：整套發布文件的測試數字全數過期，且被指名存放「最終數字」的檔案整欄是 TBD-FINAL
- **維度**：docs-claims
- **Severity**：high（原報 high）
- **file:line**：docs/releases/v0.5.0-test-matrix.md:40-55（「整合階段最終一次全套跑」表格；finding 給的 :38 為錨點小漂移，實際 TBD-FINAL 表格從 40 行標題起）

**主張**：CHANGELOG.md:22 與 gap-matrix:254 指示讀者「測試數字見 docs/releases/v0.5.0-test-matrix.md」，gap-matrix:336 更宣稱「Rust workspace、vitest、Tauri、CLI E2E 全部本輪實跑」、:340-341 宣稱「Playwright 全量本輪由整合階段最終跑一次，數字見 v0.5.0-test-matrix.md」；但該檔的整合階段區塊每一格都是 TBD-FINAL，而本機實跑的數字與所有文件記載的數字都對不上。

**證據**：文件：docs/releases/v0.5.0-test-matrix.md「整合階段最終一次全套跑」表 11 列全部 TBD-FINAL（Rust fmt/clippy/test、Tauri、typecheck、vitest、build、CLI E2E、Playwright、perf、ESP32），檔頭並自陳「不得把 TBD-FINAL 讀成通過或失敗」；docs/releases/v0.5.0-release-readiness.md 20 個關卡有 10 個是 pending final run；docs/releases/v0.5.0-final-report.md:152、:203、:227 亦是 TBD-FINAL。文件記載的數字（test-matrix 基準列、docs/acceptance-evidence.md:534-540）：Rust 595、Tauri 43、vitest 759/39 檔、Playwright 35/4 spec。本機在 HEAD c3d1786 實跑：`cargo test --workspace` = **685 passed / 0 failed / 0 ignored**（62 個 test target，其中 46 個測試 binary）；`cargo test --manifest-path apps/interaction-desktop/src-tauri/Cargo.toml` = **46 passed / 0 failed**；`pnpm test`（vitest）= **886 passed / 44 檔**。Playwright 未跑（依指示避開埠），但 apps/interaction-desktop/e2e/ 現有 12 個 spec、`test(` 共 65 個。另 docs/acceptance-evidence.md 最新章節停在「v0.5 Phase 8（2026-09-02）」（:510），全檔沒有任何 Phase 9 章節，違反 CLAUDE.md「交付前跑全套測試並回報實際數字」與「已知限制記錄在 CHANGELOG.md 與 docs/acceptance-evidence.md」。

**建議修法**：跑一次完整回歸並把 685／46／886／Playwright 實際數字填回 test-matrix 的 TBD-FINAL 欄，同步 acceptance-evidence 新增 Phase 9 章節（含環境與模擬器/fixture 標示）、gap-matrix §11 與 CHANGELOG；在數字填回前，gap-matrix:336 的「全部本輪實跑」與 :340-341 的「數字見 test-matrix」應改為明說「Stage 1 局部跑，整合全套跑尚未記錄」。

**回歸測試**：A pre-release lint/CI step that fails the release script if any row in v0.5.0-test-matrix.md's integration-stage table still reads TBD-FINAL, or that diffs the table's recorded pass-counts against a freshly executed `cargo test --workspace --no-fail-fast` / `pnpm test` / Tauri test run and fails on mismatch. / CI 加一個文件一致性檢查：發布前 `release.sh` 執行時，grep `docs/releases/v0.5.0-test-matrix.md` 整合階段表格若仍含 `TBD-FINAL`，或 `docs/acceptance-evidence.md` 最新章節標題日期早於 CHANGELOG 最新 Phase 段落日期，則直接 fail，阻止在數字未回填前打 tag。

#### F-c3d1786-20260903T124638Z-safety-invariants-074

- **標題**：宣告式裝置 provider 的「停用／撤銷」不跨重啟：serial 埠與 MQTT broker 連線在啟動時無條件重開，裝置受器也回到啟用
- **維度**：safety-invariants
- **Severity**：high（原報 high）
- **file:line**：crates/interaction-runtime/src/providers.rs:275 (loop start); build/spawn at crates/interaction-adapter-declarative/src/lib.rs:1206,1254,1307; sensitivity default at crates/interaction-adapter-declarative/src/link_caps.rs:74; registry enable default at crates/interaction-registry/src/lib.rs:76

**主張**：人類把宣告式硬體 provider transition 成 Disabled 或 revoke 之後，只要 daemon 重啟，`init_providers` 就會無條件重新載入 `~/.adaptive-interaction/config/adapters/*.yaml` 並呼叫 `register_declarative_spec`，在 build 期就重新 spawn 實體連線（serial 開埠、MQTT 帶憑證連 broker 並 subscribe `.../from-device`、BLE），同時把該 spec 的受器重新註冊進 registry 並以預設值啟用。provider 記錄雖然停在 Disabled／Revoked，但沒有任何執行／讀取路徑檢查 provider 狀態，所以撤銷後的裝置在重啟後可以再被讀（外部連線也已經自己接回去）。這牴觸 CLAUDE.md「高風險能力在重啟後不得自動恢復」、spec §2「外部資料傳送需明確授權」，也牴觸這段程式自己寫下的規則。

**證據**：providers.rs:244-272 步驟 2 只把 `is_operational()` 的 provider 降成 Disabled（`desc.detail = "re-armed on restart requires explicit enable"`），沒有關連線、也沒有把該 provider 的受器／動器設回 disabled；Revoked 連降級都不觸發。providers.rs:275-307 步驟 3 走訪 `config/adapters` 每個 yaml 並 `register_declarative_spec(&spec)`，完全沒有讀 provider 狀態；providers.rs:358「A persisted record wins over a fresh one」分支只 `attach_capabilities` 後 return，同樣不看狀態。lib.rs:1206 `serial::SerialRawLink::spawn(...)`（serial.rs:120-155 立刻起 supervisor 執行緒開埠、退避重連）、lib.rs:1254 `mqtt::MqttRawLink::spawn(...)`（mqtt.rs:100-150 立刻連 broker，ConnAck 後 `subscribe(&topic_from)`）、lib.rs:1307 `ble::BleRawLink::new(...)` 都在 `build()` 裡。關連線的唯一入口是 providers.rs:729-732 `transition_provider` 與 771 `revoke_provider` 裡的 `close_declarative_links`，只有人類再按一次才會執行。受器啟用預設：link_caps.rs:74 `.sensitivity(Sensitivity::Internal, self.spec.requires_consent)`（spec 的 `requiresConsent` serde 預設 false，官方 ESP32 範例 firmware/esp32-companion/README.md:322-335 的 `env` 受器就沒有寫），配上 interaction-registry/src/lib.rs:76 `enabled: !manifest.requires_consent` → 重啟後啟用。provider 狀態在執行期完全沒被檢查：`is_operational()` 的 doc 寫「Only these states may execute/observe anything」（interaction-core/src/provider.rs:134-140），但全 repo 只有 providers.rs:261 一處呼叫它。crates/interaction-runtime/tests/providers_loop.rs 只測了 revoke 當下（213-247）與 operational→Disabled（296），沒有 revoke/disable 後重啟的回歸。

**建議修法**：init_providers 步驟 3 之後，依 persisted provider 狀態收斂：非 operational 的 provider 立刻 `close_declarative_links` 並把它的受器／動器 `set_*_enabled(false)`；或更好，在 `register_declarative_spec` 內先查 persisted 狀態，Disabled/Revoked/Expired/Closed 就只註冊 manifest 而不 build link（lazy 開連線）。另外把 `is_operational()` 真的接到 `observe_fresh`／`execute` 前的閘門上，讓 doc 的宣稱有牙齒。

**回歸測試**：Start a runtime with a declarative device spec, revoke_provider the device (assert receptor Availability != Available and provider state == Revoked), shutdown, Runtime::start again on the same home dir, then assert the receptor's Availability is still != Available and observe_fresh(receptor_id) returns Err — currently it returns Ok with fresh data and Availability::Available. / In providers_loop.rs, write a device-spec-backed provider (a yaml under `config/adapters` producing at least one receptor, e.g. reuse `runtime_with_device_spec()`'s fixture), call `revoke_provider` (or `transition_provider(..., Disabled)`) on it, `rt.shutdown()`, restart a fresh `Runtime` on the same home dir, then assert that `rt.registry.receptor(&id)` errors (or that `rt.observe_fresh(&id)` fails) instead of succeeding — today it would succeed because `register_declarative_spec` re-registers the receptor as enabled regardless of the persisted Disabled/Revoked provider state.

#### F-c3d1786-20260903T124638Z-mobile-server-061

- **標題**：緊急停止中連上／重連的 iPhone 不會被要求停止感測（只收到純 UI 的 character.present emergency）
- **維度**：mobile-server
- **Severity**：high（原報 high）
- **file:line**：crates/interaction-runtime/src/mobile.rs:3046 (unchanged)

**主張**：emergency stop 生效後才完成 auth 的手機，永遠不會收到 `stop-all{sensors:true}`；桌面只 spawn `mobile_project_estop_device`，那只改手機上的角色文字標籤，不會停手機的麥克風／位置／BLE gateway。因此「緊急停止 → 手機端也必須停止感測」對這一條路徑不成立：手機在 estop 全程繼續擷取音訊，而桌面的 `status.activeSensors`／tray 是空的（受器已被停用，line 2313 會把它濾掉），使用者在桌面看不到任何仍在感測的跡象。

**證據**：mobile.rs:3043-3053 `auth` 成功後只做：`if self.is_estopped() { tokio::spawn(async move { rt.mobile_project_estop_device(&device_id, "reconnect-during-estop").await; }); }`——整個 handler 內沒有第二處送 `stop-all`（grep `stop-all` 只出現在 `stop_all_inner` 與 `stop_sensors_for`，兩者都只由 estop／stop-sensors 那一次掃描呼叫）。iOS 端 `ActuatorCenter.handleCharacterPresent`（ActuatorCenter.swift:429-436）只做 `characterState.state = state`，不碰感測；只有 `stopAll(sensors:true)`（ActuatorCenter.swift:451-490 → `stopSensorsOnStopAll`）才會關 mic／location／BLE gateway。既有測試 `phone_connecting_during_estop_receives_emergency`（tests/mobile_loop.rs:2444-2472）也只斷言收到 `character.present{state:emergency}`，沒有斷言 stop-all。

**建議修法**：auth 成功且 `is_estopped()` 時，除了角色投影，另外對這一台送 `stop-all{sensors:true, reason:emergency}` 並走 `stop_sensors_for(Some(device_id), …)` 的有界等待（結果照樣寫 stopped/unknown/unreachable audit）；同時把 `mobile_active_sensors` 對「受器停用但手機自報仍在串流」的情況改成顯示 stop-unknown，而不是整筆濾掉。

**回歸測試**：在既有 phone_connecting_during_estop_receives_emergency 之後，接住下一則訊息並斷言其為 `{"type":"stop-all","sensors":true,...}`（而不是只讀一則 act 就結束）：目前實作下這個 assert 會逾時／收不到，證明該路徑缺少 stop-all。

### medium（35 項）

#### F-c3d1786-20260903T124638Z-companion-gameplay-032

- **標題**：玩具把互動框撐成聯集包圍盒，製造大片「既不穿透桌面、也不反應」的死區
- **維度**：companion-gameplay
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/rig/stage.ts:604

**主張**：`interactiveBounds()` 回傳「角色 hit-rect ∪ 所有非游標玩具」的**包圍盒**（不是聯集形狀），這個框整個被 Rust 端當成「游標在角色身上」而關掉點擊穿透；但 WebView 端 `pointerDown` 只認 `charHitRect()` 與玩具本體，框內其餘空白一律回 `"none"` 並直接 return。結果：只要把一顆毛球丟到遊玩場另一側，角色與玩具之間那一大條區域既不會把點擊傳給底下的桌面，點下去也完全沒有反應（不拖視窗、不開選單）。

**證據**：stage.ts:604-620 `interactiveBounds()`：`let x0=r.x; ... for (const t of solid) { x0=Math.min(x0,(t.x-14)*s); x1=Math.max(x1,(t.x+14)*s); ... } return {x:x0,y:y0,w:x1-x0,h:y1-y0}` —— 只排除了 `isCursorToy` 的光點／逗貓棒（companion-gameplay-004 的修法），一般玩具全部併進包圍盒。stage.ts:527 `reportHitRect()` 把它送給 host。src-tauri/src/lib.rs:2196-2208 `clickthrough_want_ignore()`：`let on_character = cx>=rx && cx<rx+rw && cy>=ry && cy<ry+rh; !on_character` —— 整個矩形都算「在角色身上」。CompanionApp.tsx:1650 `if (hit === "none") return; // 擴大互動框內的空白：不反應`。實算（scale=1、logical 320×170、ground=164、char.x=160）：`charHitRect` = x134..186；毛球落在 x=300 → bounds = x134..314，比原本的 52px 寬成長到 180px，x186..286 這 100×130 的區域變成死區。既有測試 regressions-run2-companion.test.ts:464 只釘了游標玩具那一種情形，沒有覆蓋這個。

**建議修法**：host 端改回報多個矩形（角色一個＋每個可抓玩具一個），或在 WebView 端把 `pointerDown` 的 `"none"` 改成「落在包圍盒空白 → 當成一般視窗互動（拖曳／開選單）」，並讓 Rust 端對每個矩形分別判定 on_character。最小修法：`interactiveBounds` 只在「玩具真的離角色很近」時併入，其餘玩具用獨立矩形回報。

**回歸測試**：Extend the companion-gameplay-004 test: after dragging a `yarn` toy far from the character, assert `stage.pointerDown(x, y)` at a point that is inside `interactiveBounds()` but outside both `charHitRect()` and any toy's grab radius returns something other than `"none"` (e.g. falls back to allowing window-drag / click-through), not silently `"none"`.

#### F-c3d1786-20260903T124638Z-companion-gameplay-033

- **標題**：Reduced Motion 開啟時使魔狀態永久凍結：愛心永遠掛在頭上，Roll Call 說「在散步／在打招呼」
- **維度**：companion-gameplay
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/playfield.ts:643

**主張**：`stepFamiliars` 在 `inputs.reducedMotion` 時直接 early return，**在 `active` 閘門之後、狀態機推進之前**，所以使魔會停在切換當下的 `state`／`vx` 上永遠不再更新。若切換 Reduced Motion 時剛好有使魔處於 `greet`，`drawFamiliar` 會無限期畫出打招呼愛心；處於 `walk`／`chase` 的使魔則永遠帶著 `vx` 卻不移動，而 `rollCall` 只對 `frozen` 做了誠實降級、對 reducedMotion 沒有，於是誠實地寫著「在散步」「在追朋友」——實際上牠們一動也不動。

**證據**：playfield.ts:643 `if (inputs.reducedMotion || w.familiars.length === 0) { return char === w.char ? w : {...w, char}; }`（`f.stateUntil` 的重擲在 647 行之後，永遠到不了）。stage.ts:1138 `if (f.state === "greet" && !this.flags.frozen && statusOverlay(...) !== "takeover") this.drawGreetHeart(...)` —— 只擋 frozen，不擋 reducedMotion。playfield.ts:778 `const label = opts.frozen ? ... : f.state === "walk" ? "在散步" : ...` —— `rollCall` 的凍結殘影守則（776-777 行註解「不能拿它報『在散步』」）只套用在 frozen。實測（node 直跑 playfield.ts）：兩隻 idle 使魔以 rng=0.65 走一步 → 兩隻都進 `greet`（stateUntil=1002500）；接著以 `reducedMotion:true` 跑 2000 步／10 分鐘 → 仍然 `['a','greet'],['b','greet']`，rollCall = `[{小樞,在休息},{A,在跟大家打招呼},{B,在跟大家打招呼}]`。walk 案例同理：10 分鐘後 `['walk',-30,x=60]`，rollCall 仍說「在散步」。CompanionApp.tsx:817-830 的 `onMotionChange` 讓使用者在執行期切換系統 Reduce Motion 即可觸發。

**建議修法**：把 reducedMotion 的處理從「early return」改成「先把所有使魔收斂到 idle／vx=0／greetWith=null（等同 `!active` 分支），再 return」；並讓 `rollCall` 接收 `reducedMotion` 旗標，比照 frozen 回報「停下來了」。

**回歸測試**：在 `src/test/playfield.test.ts` 的「使魔與 Roll Call」describe 下新增一個 case：以固定 rng 讓使魔進入 `greet`（或手動設定初始 `state:"walk", vx:25`），接著用 `reducedMotion:true` 跑上千步／模擬數分鐘，斷言 `familiar.state`／`x` 完全不變（凍結）且 `rollCall(...).activity` 對該使魔輸出的仍是「在跟大家打招呼」/「在散步」——先紅（證明現狀撒謊）；修法應是讓 `rollCall` 與 `drawFamiliar` 對 reducedMotion 比照 `frozen` 做同款「停下來了」式誠實降級，或讓 `stepFamiliars` 在 reducedMotion 時仍推進到下一個穩定/idle 狀態而非原地凍結在動態狀態上，然後測試轉綠。

#### F-c3d1786-20260903T124638Z-companion-gameplay-034

- **標題**：玩具已達上限時丟光點／逗貓棒：什麼都沒生成，卻回報成功、送出 toy-thrown 事件並寫進互動記憶
- **維度**：companion-gameplay
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/rig/stage.ts:552

**主張**：`StageRenderer.spawnToy()` 用 `this.world.toys.length > before || isCursorToy(kind)` 判定成功。`isCursorToy` 那一項本來是為了「光點已存在→替換」（數量不變但確實生成）而加的，但它同時把「已達 MAX_TOYS＝4 且場上沒有同種游標玩具」這個**完全沒生成**的情形也判成成功。於是 UI 什麼都沒出現，卻仍然對 Runtime 送出 `character.toy-thrown` 輸入事件，並把「玩過光點」寫進 `companionInteractionMemory` 持久化——違反誠實階梯（沒發生的事不得當成發生）。

**證據**：stage.ts:552-558 `spawnToy(kind){ if(this.flags.frozen) return false; const before=this.world.toys.length; this.world=spawnToy(this.world,kind,this.now()); return this.world.toys.length > before || isCursorToy(kind); }`。playfield.ts:169-189 `spawnToy()`：先 `toys.filter(t=>t.kind!==kind)`（游標玩具），再 `if (world.toys.length >= MAX_TOYS) return world;`（MAX_TOYS=4，playfield.ts:137）。character/adapters/shu.ts:440-446 `spawnToy: (kind)=>{ ... if(!this.stage.spawnToy(kind)) return null; this.emitInput({kind:"character.toy-thrown",payload:{toyId:kind}}); return kind; }`（註解自稱「玩具已滿時拒絕：沒生成就不說生成了」）。CompanionApp.tsx:1851-1855 `if (gameplay.spawnToy(kind) === null) return; const mem = notePlay(memoryRef.current, kind, Date.now()); ... desktop.prefsPatch({companionInteractionMemory: mem})`。實測（node 直跑 playfield.ts）：丟 yarn/paper/plane/yarn 得 4 顆 → 再丟 light → `{before:4, after:4, lightPresent:false}`，而 stage 的回傳式在此為 `false || true = true`。

**建議修法**：讓 `playfield.spawnToy` 回傳是否真的生成（例如回 `{world, spawned:boolean}` 或比較 `world.toys` 是否含有該 id），`StageRenderer.spawnToy` 依此回報；不要用 `isCursorToy(kind)` 當成無條件成功。

**回歸測試**：在 stage.ts 的測試中：先用 stage.spawnToy() 依序丟滿 4 顆非游標玩具（如 yarn/paper/plane/yarn）使 world.toys.length===4，接著呼叫 stage.spawnToy("light")，斷言其回傳值為 false 且 stage.toyCount() 仍為 4（目前實際回傳 true，違反預期）；再於 shu.ts adapter 層斷言此情境下 gameplay.spawnToy("light") 回傳 null 且未 emitInput 一個 character.toy-thrown 事件。

#### F-c3d1786-20260903T124638Z-companion-gameplay-035

- **標題**：主角與使魔的「互相注意」是單向的：小樞永遠只被打招呼，從不主動靠近、打招呼或追逐使魔
- **維度**：companion-gameplay
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/playfield.ts:355

**主張**：規格 §5.1 要求「多角色能互相注意、打招呼、出現愛心、追逐及玩耍」。實作中使魔↔使魔確實是雙向的（greet 會讓對方也 greet；chase 會讓對方逃跑或回頭），但主角↔使魔只有單向：`char.attendTo`／`greetBackUntil` **只**在 `stepFamiliars` 裡由使魔發起 greet 時被設定，`stepChar` 從頭到尾不認識 `w.familiars`（除了拿來決定 `facing`），沒有任何路徑讓主角主動走向、打招呼、追逐或和使魔玩。主角的「互動」上限是原地轉頭＋35% 機率冒一顆愛心。

**證據**：playfield.ts:355-595 `stepChar()`：唯一引用 familiars 之處是 368-371 行 `const f = w.familiars.find(x=>x.id===c.attendTo); if (f) c.facing = f.x >= c.x ? 1 : -1;`；`CharPlayMode` 的所有分支（free/stroll/sniff/chase/pounce/return/refuse）目標都只有 `targetToy`，沒有 familiar 目標。playfield.ts:679-687：`char = {...char, attendTo: f.id, attendUntil: now+2500, greetBackUntil: rng()<0.35 ? now+1800 : char.greetBackUntil}` —— 主角狀態只在此處（使魔那一側）被寫入。playfield.ts:698-725 的 chase／flee／look-back 只在 `list`（使魔）之間發生。docs/acceptance-evidence.md:490 的已知限制第 8 項只涵蓋「跨螢幕漫遊／坐視窗邊緣／探頭／躲視窗後」，沒有涵蓋這一項。

**建議修法**：在 `stepChar` 的 `free` 分支加入一個以 familiars 為目標的 hazard 抽樣（新增 `greet-familiar`／`chase-familiar` 模式：走近、停下、冒愛心、偶爾互追），沿用既有的 canPlay 閘門與冷卻；或在 CHANGELOG／acceptance-evidence 誠實記為已知限制（目前兩處都沒有）。

**回歸測試**：在 world 中放一隻靜止的使魔（無 sleep）與主角，跑多幀直到 `stepFamiliars` 抽樣不到 greet／chase 分支也不觸發；斷言：多次執行不會有任何一次 `char.mode` 因為使魔而非玩具被設成非 free（或新增一個 char 主動 attendTo 使魔的分支後，斷言 `char.targetFamiliar`/`char.mode==="chase"` 且目標是某 familiar id 時能成立）——目前程式碼下此斷言必然失敗，證明「主角不會主動接近/追逐使魔」。

#### F-c3d1786-20260903T124638Z-rig-renderer-058

- **標題**：stand↔sit 姿勢硬切造成 10.00px 單幀頭部瞬移，影響 8 個常駐 ambient 表情
- **維度**：rig-renderer
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/rig/params.ts:393

**主張**：blendPose 明確排除不含 lie 的姿勢切換（註解：「stand↔sit 只差 10px，硬切看不出來」），但 layoutFor 的 sit 版面整體下移 10px，且 lerpParams 的字串通道在 ease 中點硬切，實測 idle→sit 的頭中心在單幀從 hy=46.00 跳到 56.00（Δ=10.00px）。128px 邏輯畫布上，站姿頭頂到地面只有 78px，10px 是 13%；scale=2 時等於一幀內位移 20 CSS px。受影響的是 Director 最常排的休息類 ambient：sit、doze、legswing、tailhug、caught-slacking、await-player、player-back、quiet 全部量到 10.00px。專案自己對 lie 過場的回歸門檻是 <12px，這條被排除的路徑就貼在門檻上而且完全沒有測試覆蓋。

**證據**：params.ts:392-393 `if (from.pose === to.pose) return params; if (from.pose !== "lie" && to.pose !== "lie") return params;`；draw.ts:52-57 `const drop = sit ? 10 : crouch ? 6 : 0; hy: 46 + drop`；draw.ts:91-100 layoutFor 只有 pose==='lie' 或 poseBlend<1 時才 mixLayout。實測（16.7ms 取樣）idle→sit：+184ms pose=stand hy=46.00 d=0.00 ／ **+200ms pose=sit hy=56.00 d=+10.00** ／ +217ms hy=56.00 d=0.00。單一表情內部掃描（setAnimation 後 6 秒）最大單幀位移：sit/doze/legswing/tailhug/caught-slacking/await-player/player-back/quiet 皆為 10.00px @200ms。

**建議修法**：把 blendPose 的條件由「是否為 lie」改為「兩個 layout 的 hy 差是否 > 阈值（例如 3px）」，讓 stand↔sit、stand↔crouch 也走連續 poseBlend；mixLayout 的另一端要用實際的來源姿勢而不是硬寫 stand。

**回歸測試**：在 regressions-phase7.test.ts 仿照既有「lie-flat → idle 連續兩幀頭部 y 位移 < 12px」測試，新增一則對 idle→sit（或 8 個 ambient 表情之一）跑 `ExpressionTimeline.paramsAt` + `layoutFor` 逐幀掃描，斷言 `maxJump < 12`（目前會在 200ms 附近量到 10.00 而非失敗，因為根本沒人寫這個斷言）——藉此把這條刻意排除的路徑的位移量鎖進回歸門檻，未來若 drop 值加大會被抓到。

#### F-c3d1786-20260903T124638Z-director-pipeline-044

- **標題**：clear-transient 的安全白名單漏掉 requesting-consent：runtime 真的在等使用者確認時，連戳角色或 presentation cancel 就把「在等你確認」的真相狀態抹掉
- **維度**：director-pipeline
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/machine.ts:157

**主張**：machine.ts:157 的 SAFETY_TRANSIENTS 只有 blocked/failed/unknown，沒有 requesting-consent。因此非 force 的 clear-transient（reduce() case "clear-transient", machine.ts:278-283）會清掉正在台上的 requesting-consent（ask 姿勢）。這條路有三個實際觸發點，其中「連戳角色」完全本機、不需要任何競態：CompanionApp.tsx:1746 的連戳分支先 apply({type:"clear-transient"}) 再套 performing，繞過 PRIORITY 階梯（requesting-consent 80 vs performing 25，正常競爭本來會 keep）。結果是 runtime 仍在 waiting-consent／waiting-input，角色卻已回到玩鬧姿勢，送回 runtime 的 rollCall 也從「在等你確認」變成 null（CompanionApp.tsx:1163 只在 t.kind 非 performing 時給標籤）。專案內部對這個狀態的定位是明確的安全類：behavior.ts:189 把 CLASS_OF 的 "waiting-confirmation"（machine.ts:73 = requesting-consent）算成 isSafety「安全事件永不歸零」；rig/expressions.ts:1090 的 ask 標 truthState:true 並註明「代表 runtime 真的在等 consent／輸入」；intent.rs:149 給 RequestConsent floor 80。只有 machine.ts 的 SAFETY_TRANSIENTS 與它們不一致。

**證據**：machine.ts:157 `const SAFETY_TRANSIENTS = new Set<TransientKind>(["blocked", "failed", "unknown"]);`
machine.ts:281 `if (!event.force && t && t.untilMs > nowMs && SAFETY_TRANSIENTS.has(t.kind)) return state;`
CompanionApp.tsx:1745-1748 連戳分支：`if (plan.kind === "rapid") { apply({ type: "clear-transient" }); apply({ type: "transient", kind: "performing", ... }); return; }`
CompanionApp.tsx:1077 presentation cancel：`if (cancelPlan.transient === null) apply({ type: "clear-transient", force: effects.forceClear });`（cancelEffects("cancel") → forceClear:false，presentationCommands.ts:80-84）
CompanionApp.tsx:1090 舊 daemon 路徑 animation-play "idle"/"quiet"（PLAYABLE_ART 中 art===null）→ `apply({ type: "clear-transient" })`，同樣不帶 force，且 companion.animation.play 是 AI 可呼叫的 actuator。
對比證據（behavior.ts:189）`const isSafety = cls === "emergency" || cls === "sensor-safety" || cls === "waiting-confirmation";` 與 machine.ts:73 `"requesting-consent": "waiting-confirmation"`。
實測（把未修改的 machine.ts／presentationCommands.ts 複製到 scratchpad 用 node --experimental-strip-types 跑）：
  before: {"kind":"requesting-consent","untilMs":1012000} → pose { animation: 'ask', ambient: false }
  plan: {"outcome":"completed","transient":null,"bubble":null} effects: {"forceClear":false}
  after cancel: null → pose { animation: 'idle', ambient: true }
  blocked after cancel: {"kind":"blocked","untilMs":1004500}（對照組沒被清掉）
CHANGELOG.md 與 docs/acceptance-evidence.md grep 「requesting-consent／clear-transient／SAFETY_TRANSIENTS」皆 0 筆，未列為已知限制。

**建議修法**：把 "requesting-consent" 加進 machine.ts:157 的 SAFETY_TRANSIENTS（它同時也讓 estop 進場時不再保留它——需另行決定：estop 時本來就該清掉，可改成兩個集合：CLEAR_PROTECTED = blocked/failed/unknown/requesting-consent 給 clear-transient 用，ESTOP_KEEP = blocked/failed/unknown 給 base:emergency 用）。連戳分支（CompanionApp.tsx:1746）不要先 clear-transient，改成讓 transientCompetition 的優先階梯決定（performing 25 本來就搶不過 80）；若確實需要清掉自己的 ambient，改成只在目前 transient.kind === "performing" 時清。並補一條回歸：requesting-consent 在台上時，連戳與 presentation cancel 都不得把它清掉。

**回歸測試**：In machine.ts's reduce(): seed state with an active `requesting-consent` transient (untilMs in the future), call `reduce(s, {type:"clear-transient"}, now)` with no `force`, and assert `result.transient?.kind === "requesting-consent"` (mirroring the existing `for (const kind of ["blocked","failed","unknown"])` loop in regressions-run2-companion.test.ts but adding `"requesting-consent"` to that list) — plus an end-to-end CompanionApp test asserting the rapid-click branch and the `cancel` presentation-command branch do not clear an active requesting-consent transient.

#### F-c3d1786-20260903T124638Z-character-protocol-040

- **標題**：on_receipt 丟棄 adapter 誠實回報的降級 resolution，回執出現 status=unsupported 但 resolution=exact
- **維度**：character-protocol
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-character/src/gateway.rs:1117

**主張**：§7 規定回執的 resolution「只能比協商結果更差」，程式碼註解也寫「unsupported 更差 → 採用」，但 `.filter(|r| *r <= Resolution::Reduced)` 把 adapter 回報的 `Unsupported`／`Failed` 過濾掉，改用協商當時的值。結果 Gateway 對外發出自相矛盾且偏樂觀的回執（狀態說沒演成、resolution 說 exact），Runtime 的 audit／`character.receipt` 事件因此高估實際呈現品質。TS 端行為相反（`p.effective = worse(...)` 會採用 unsupported），兩份實作對同一輸入產生不同回執。

**證據**：gateway.rs:1112-1120：`let effective_resolution = if receipt.status == ReceiptStatus::Failed { Resolution::Failed } else { receipt.resolution.filter(|r| *r <= Resolution::Reduced).map_or(negotiated_resolution, |r| r.max(negotiated_resolution)) };`，其中 capability.rs:342-348 的 Ord 為 `Exact < Substituted < Reduced < Unsupported < Failed`。實跑探針：協商 exact 的 `notice`，adapter 回 `{status:Started, resolution:Unsupported}` → `RECEIPT m4 status=Started res=Some(Exact)`；回 `{status:Unsupported, resolution:Unsupported}` → `RECEIPT s1 status=Unsupported resolution=Some(Exact)`。tests/gateway.rs:1437 的註解宣稱涵蓋此情境，但該測試只斷言了 substituted 的情況，沒有實際驗 unsupported。對照 apps/interaction-desktop/src/character/gateway.ts:1111-1113（`worse()` + `if (status === "unsupported") p.effective = "unsupported"`）。

**建議修法**：把 filter 改成只擋「比協商結果更好」的值（`r.max(negotiated_resolution)` 已足夠），讓 Unsupported／Failed 直接被採用；並在 tests/gateway.rs 針對 status=unsupported 補 `assert_eq!(resolution, Some(Resolution::Unsupported))`。

**回歸測試**：In Gateway::on_receipt, dispatch an Exact-capable intent, then feed a receipt with status=Unsupported (or Started) and resolution=Some(Unsupported) directly (bypassing the Failed-status special case); assert the emitted CommandReceipt.resolution equals Unsupported (or at least > negotiated), not the pre-negotiation Exact value — i.e. extend receipt_resolution_may_degrade_but_never_upgrade with an actual Unsupported-resolution case instead of only asserting it in a comment.

#### F-c3d1786-20260903T124638Z-character-protocol-042

- **標題**：AI 呈現命令由「任一角色實例最先回的終態回執」結案，外部 adapter 可代桌面角色決定結果
- **維度**：character-protocol
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/character.rs:1842

**主張**：`character_ai_present` 對所有已連線 instance 用同一個 messageId（= actionId）派送，而 `take_ai_command` 是 remove 語意：第一個回終態回執的 instance 就結算該 presentation action，其餘 instance 的回執一律被丟成「already settled」。只持有 adapter token 的第三方外部 adapter（role familiar）因此能替桌面角色把 AI 的 `companion.state.present`／`animation.play` 標成 completed（桌面其實還沒演或演失敗），或反過來搶先回 unsupported／failed 把成功的呈現記成失敗。§8.2 明訂 adapter token「不能呼叫 actuator」，這條路徑等於讓它決定 actuator receipt 的終態。

**證據**：character.rs:1705-1735 `let message_id = action_id.clone();` 後 `for id in &targets { ... gw.dispatch(&InstanceId(id.clone()), envelope, now) }`，targets 來自 character.rs:548-555 `connected_instance_ids()`（只過濾 connected+negotiated，不分 role）。character.rs:684-689 `fn take_ai_command(...) { let taken = guard.by_message.remove(message_id)?; ... }`。character.rs:1838-1850：`if receipt.is_terminal() { if let Some((action_id, _command)) = hub.take_ai_command(&receipt.message_id) { settlements.push(Settlement { outcome: settlement_outcome(receipt.status), ... }) } }`。character.rs:1905-1913 顯示後到的結算被吞成 debug log「presentation command already settled or expired」。

**建議修法**：把 AI command 的追蹤鍵改成 (messageId, instanceId)，只讓桌面 primary-companion（或發起該 actuator 的表面）的回執推進 presentation receipt；其他 instance 的回執只進 audit／事件。或改成「等所有目標終態後彙總，任一 uncertain 即記 uncertain」。

**回歸測試**：在測試中對同一 `action_id`／`companion.state.present` 讓 desktop 與一個已註冊 role=familiar 的外部 adapter 同時 connected+negotiated；先由外部 adapter 對該 messageId 回 `ReceiptStatus::Completed`（經 `hub.character_receipt` 或等效路徑），斷言 `presentation_ack` 已把該 action 的持久化 `ActionReceipt` 寫成 completed；接著讓桌面對同一 messageId 回 `ReceiptStatus::Failed`，斷言該筆真正的失敗回執被吞（`take_ai_command` 回 None／`settle_character_receipts` 落入 NotFound debug 分支），且持久化 receipt 仍停留在外部 adapter 搶先寫入的 completed，而非反映桌面的真實失敗結果。

#### F-c3d1786-20260903T124638Z-character-protocol-043

- **標題**：外部 adapter 的輸入事件繞過桌面表面閘門，卻寫進同一組 companion.* 受器並解除主動對話的未回覆守則
- **維度**：character-protocol
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/character.rs:2117

**主張**：`character_input_to_observation` 對 external origin 直接關掉表面閘門（`enforce_surface_gate = origin != InstanceOrigin::External`），但事件仍被寫進 `companion.click`／`companion.text-input`／`companion.quick-action`／`companion.drag-drop`／`companion.pointer` 這些「隱藏角色即停用」的表面受器。結果：桌面角色已隱藏／視窗已關（UI 與 status 宣稱其視窗內感官已停止）時，只持有 adapter token 的第三方 adapter 仍可持續產生這些受器的 observation，並因此觸發 `proactive_note_reply()`（把「使用者其實沒回」偽裝成回覆，解除不追問守則）與 `evaluate_recipes()`。這牴觸「隱藏角色停用視窗內受器」的確定性承諾與誠實階梯（非人類輸入被當成人類互動）。

**證據**：character.rs:2116-2118 `// 桌面角色的視窗內受器受隱藏／斷線閘門管制；外部 adapter 沒有桌面表面。 let enforce_surface_gate = origin != InstanceOrigin::External; self.ingest_with_gate(receptor, facts, ...)`；受器 id 由同函式 2009-2099 產生，全部是 companion.* 表面受器。presentation.rs:915-927 `is_companion_surface_receptor` 明列這些 id；runtime.rs:617-624 只有在 `enforce_surface_gate` 為 true 時才回 `Unavailable("companion window hidden or not connected; its receptors are stopped")`。runtime.rs:645-654：命中 companion.click／text-input／quick-action／drag-drop 即呼叫 `self.proactive_note_reply()`；656 行接著 `self.evaluate_recipes(Some(receptor_id))`。外部 adapter 走 routes.rs:1716 `character_events`（lib.rs:395-401 adapter token 明確允許此路由）即可送達。

**建議修法**：外部 instance 的輸入改寫入獨立受器（例如 `character.external.click`／`character.external.text-input`）或在 facts 加上不可偽造的 `origin: "external-adapter"`，並讓 `proactive_note_reply()` 只接受 origin 為可信 host 的互動；同時在 status／UI 顯示外部角色仍在送輸入，維持「感測不靜默」。

**回歸測試**：在測試 runtime 中：註冊一個桌面 character（hello, visible=false 或不 hello），另外用 `character_adapter_add`＋`character_ws_attach`／`character_event` 對同一 instance 送一則 `companion.click`（TextSubmitted 亦可）；斷言 (a) 呼叫成功回 `InputOutcome::Observed` 而非 `Unavailable`，(b) 呼叫前後 `registry.receptor_manifests()` 中 `companion.click` 的 `availability` 仍是 `Offline`（因為只看桌面 bridge），(c) 呼叫後 `proactive_dialogue.last_answered == true`——三者同時成立即證明「status 說 Offline，但這個 receptor 剛剛真的觀察到一筆並解除了未回覆守則」。

#### F-c3d1786-20260903T124638Z-ia-settings-013

- **標題**：活動紀錄收件匣在一般模式直接印出原始裝置／受器識別碼
- **維度**：ia-settings
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/pages/ActivityPage.tsx:294

**主張**：`InboxSection` 的次要資訊列無條件輸出 `裝置 {item.deviceId}`，沒有 `advanced` 閘門。後端的 deviceId 對 action-result 是原始 actuator id（如 `builtin.notification`），對 safety-event 是原始感測／手機 id（如 `iphone-a1b2c3d4`、`iphone.mic-level`），違反規格 §15.5「一般模式不暴露不必要的 UUID、Token…」，也與同一 repo 內 SensorBanner 的紀律（原始 startedBy 只在進階模式以 title 顯示）不一致。

**證據**：ActivityPage.tsx:291-297 該 `<div className="muted small">` 內：`{item.deviceId ? `・裝置 ${String(item.deviceId)}` : ""}`，其上一行的 agentId 有走 `agentDisplayLabel`、下一行 advanced 區塊才印原始狀態碼（ActivityPage.tsx:298-302），唯獨 deviceId 兩種模式都印原值。來源：crates/interaction-runtime/src/activity.rs:391 `device_id: Some(receipt.actuator_id.as_str().to_string())`；activity.rs:247-252 `let device_id = event.payload.get("deviceId").or_else(|| event.payload.get("sensor"))…`；mobile.rs:2959 `let device_id = format!("iphone-{}", &token_hex(8)[..8]);`。對照 App.tsx:181 `<span title={advanced ? s.startedBy : undefined}>{sensorStartedByLabel(s.startedBy)}</span>`。

**建議修法**：把該行改成人話投影（既有的 `findCard("actuator", id).name` / `sensorKindLabel` / mobile 裝置名稱），原始 id 併入既有的 `{advanced && …原始狀態…}` 區塊。

**回歸測試**：在 vitest 中對 InboxSection 用一筆 `kind: "safety-event", deviceId: "iphone-a1b2c3d4"` 的假資料、`advanced={false}` render，斷言輸出文字（`screen.getByText` 或 container textContent）不包含 `"iphone-a1b2c3d4"`（或更廣義地不包含符合 `^iphone-[0-9a-f]{8}$`／原始 actuator id 樣式的字串），且 `advanced={true}` 時允許出現。

#### F-c3d1786-20260903T124638Z-ia-settings-015

- **標題**：活動紀錄的「待決定 N」忽略後端的 pendingCountExact，把下限講成總數
- **維度**：ia-settings
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/pages/ActivityPage.tsx:242

**主張**：`InboxSection` 標題 `統一收件匣（待決定 ${pending}／共 ${total}）` 只讀 `pendingCount`，沒有讀 `pendingCountExact`。後端在撞到掃描上限時會回 `pendingCountExact: false`（該值只是下限），其他所有介面都改口成「至少 N 項」並顯示 PENDING_INCOMPLETE_NOTE，唯獨這一頁——也就是所有其他頁面叫使用者「前往活動紀錄查看」的目的地——用肯定的精確數字呈現。

**證據**：ActivityPage.tsx:238 `const pending = Number(inbox.data?.pendingCount ?? 0);`、:242 `<Section title={`統一收件匣（待決定 ${pending}／共 ${total}）`}>`；import（:9）不含 `isPendingCountExact` / `pendingCountLabel` / `PENDING_INCOMPLETE_NOTE`。後端 crates/interaction-runtime/src/activity.rs:342 `let pending_count_exact = pending_receipts_exact && candidates_exact;`、:349 `"pendingCountExact": pending_count_exact`。對照誠實作法：App.tsx:654-666（`inboxBadgeText` 回「至少 N」）、HomePage.tsx:486/542-550、ConnectPage.tsx:525-539。statusProjection.ts:390-392 明寫「這種情況下**絕不可以**說『目前沒有待決定事項』」，而 PENDING_INCOMPLETE_NOTE 正是把人導到這一頁。

**建議修法**：標題改用 `pendingCountLabel(pending, isPendingCountExact(inbox.data))`，並在 exact 為 false 時於清單上方加一條 PENDING_INCOMPLETE_NOTE。

**回歸測試**：Render InboxSection (or ActivityPage) with inbox.data = {pendingCount: 5, pendingCountExact: false, totalBeforeLimit: 5, items: [...]}; assert the rendered Section title/heading contains \"至少\" (or otherwise signals inexactness) and that PENDING_INCOMPLETE_NOTE text is shown, failing today since the title renders the bare \"待決定 5／共 5\" with no qualifier.

#### F-c3d1786-20260903T124638Z-ia-settings-016

- **標題**：感測開始／停止的收件匣標題外洩原始手機裝置 id（同檔案的 stop-uncertain 已會解析人話名稱）
- **維度**：ia-settings
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/activity.rs:247-259（外洩實際成形於標題組字 :269-270/:278）

**主張**：`SensorStarted` / `SensorStopped` 的標題以 `sensor_display_name(device_id)` 組成，而該函式對認不得的值「照原樣顯示」，所以 iPhone 感測事件會產生 `感測開始：iphone-a1b2c3d4` 這種帶原始裝置 id 的標題，直接進到一般模式的通知中心、「現在」頁待我決定清單與活動紀錄。同一個 impl 裡的 `stop_uncertain_device_label` 已經會用 `self.mobile.device_name(device_id)` 換成人話名稱，證明資訊拿得到、只是這條路徑沒用。

**證據**：activity.rs:247-252 `let device_id = event.payload.get("deviceId").or_else(|| event.payload.get("sensor"))…`；:257-260 `let sensor_label = device_id.as_deref().map(sensor_display_name).unwrap_or_else(|| "感測器".into());`；:269-271 `SensorStarted => (event_type.clone(), format!("感測開始：{sensor_label}"))`；:398-405 `fn sensor_display_name` 只認 microphone/camera，`other => other.to_string()`。事件 payload 來源 crates/interaction-runtime/src/mobile.rs:2705-2712 `json!({"sensor": "iphone.mic-level", "deviceId": device_id, "source": "iphone"})`，device_id 形狀見 mobile.rs:2959 `format!("iphone-{}", &token_hex(8)[..8])`。對照同檔 :69-80 `stop_uncertain_device_label` 已呼叫 `self.mobile.device_name(device_id)`。前端 statusProjection.ts:521-529 的 `sensorKindLabel` 也明訂認不得的種類要說「其他感測器」、不得外洩原始字串。

**建議修法**：SensorStarted/Stopped 也走 `stop_uncertain_device_label` 那套解析（先 `mobile.device_name`，再 sensor 種類人話），認不得時回「感測器」而不是原始字串。

**回歸測試**：在 activity.rs（或等效的 runtime 整合測試）中，emit 一筆 EventType::SensorStarted 與 SensorStopped，payload 同時帶 "sensor": "iphone.mic-level" 與 "deviceId": "iphone-a1b2c3d4"（模擬 mobile.rs 實際送出的 shape），呼叫 rt.activity_inbox(..)，斷言回傳項目的 title 不含子字串 "iphone-a1b2c3d4"、且以「感測開始：iPhone」/「感測停止：iPhone」（或走 self.mobile.device_name 解析出的人話名稱）呈現，而非原始 deviceId。

#### F-c3d1786-20260903T124638Z-ia-settings-017

- **標題**：互動結果在一般模式顯示 runtime 原始機器 intent（既有的 receiptIntentLabel 只有 HomePage 在用）
- **維度**：ia-settings
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/pages/ActivityPage.tsx:69

**主張**：ActivityStory 的區塊標題與全域搜尋的結果標籤都直接串接 `receipt.intent`（`emergency-stop`／`companion-test`／`presence`／`celebration` 這類 runtime 內部 id）。HomePage 為此寫了 `receiptIntentLabel`，註解明說「Runtime 的原始 id 不該出現在一般模式的第一層文字裡」，但全 repo 只有 HomePage 自己呼叫它。

**證據**：ActivityPage.tsx:69 `title={`${time}　${receipt.intent}`}`（Section 標題，無 advanced 閘門）；components/GlobalSearch.tsx:205 `label: `${advanced ? "結果收據" : "互動結果"}：${receipt.intent}``。helper 定義與唯一呼叫點：HomePage.tsx:403-415 `export function receiptIntentLabel(intent: string)` 與 HomePage.tsx:695 `receiptIntentLabel(receipt.intent)`；`grep -rn receiptIntentLabel src --include=*.tsx | grep -v /test/` 只有這兩處。intent 實際值：crates/interaction-runtime/src/runtime.rs:954 `intent: "emergency-stop"`、orchestrator.rs:305/325/334/355/367 `SemanticIntent::new("celebration"|"presence"|"progress"|"success"|"warning")`。

**建議修法**：把 `receiptIntentLabel` 移到共用模組（statusProjection.ts），ActivityPage 標題與 GlobalSearch 標籤都改走它，原始 intent 只在 advanced 的技術區塊出現。

**回歸測試**：在 advanced=false 情境下渲染 ActivityPage／GlobalSearch，餵入 `receipt.intent = "emergency-stop"`，斷言輸出文字不含原始字串 `"emergency-stop"`（而是走 receiptIntentLabel 或等價投影後的「緊急停止」之類人話）。

#### F-c3d1786-20260903T124638Z-ia-settings-018

- **標題**：首次設定精靈的 commit 非原子，但失敗時只說「套用失敗」
- **維度**：ia-settings
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/human.rs:658

**主張**：`commit_onboarding` 依序 policy patch → 受器 → 動器 → starter recipes → preferences，每一步都用 `?` 直接返回，任何中途失敗都會留下已經寫入的部分（例如政策已改、部分受器已開，但偏好沒寫、onboarding 也沒標記完成）。前端把這個 Err 直接算成整體失敗，畫面只顯示「套用失敗：<error>」，沒有任何「部分設定可能已生效」的誠實提示，而 Onboarding.tsx 檔頭還宣稱「所有選擇最後一次性套用到後端…不會留下半套用狀態」。

**證據**：human.rs:658-730：`if let Some(patch) = &commit.policy_patch { self.update_policy(patch.clone()).await?; }` → `self.registry.set_receptor_enabled(…).await?` → `self.registry.set_actuator_enabled(…).await?` → `self.upsert_recipe_text(yaml).await?` → `if let Some(prefs) = commit.preferences { self.update_ui_preferences(prefs).await?; }` → 最後才 `set_meta(ONBOARDING_META_KEY, …completed:true…)`，中間沒有交易或補償。前端 apps/interaction-desktop/src/pages/Onboarding.tsx:475-481 `try { await api.onboardingCommit(approved.commit); } catch (e) { setError(String(e)); setCommitting(false); return; }`，訊息渲染在 Onboarding.tsx:708 `套用失敗：{error}`；檔頭宣稱在 Onboarding.tsx:2-3。

**建議修法**：要嘛讓 commit 真的可回復（先算 plan、失敗時回滾已套用的步驟），要嘛讓 commit 回傳「已完成到哪一步」並讓精靈誠實顯示「以下已套用、以下未套用」，不要用單一的「套用失敗」蓋掉半套用事實。

**回歸測試**：In commit_onboarding, pass a commit whose enable_receptors contains an id not present in the registry's live component map (but which plan_onboarding treats as 'changed' via its unwrap_or(false) default) alongside a valid policy_patch; assert the call returns Err, then assert the policy was nonetheless persisted (via runtime.policy()) and the onboarding-completed meta key is still absent — proving a partial write survives the failed commit — and assert the equivalent HTTP path's error body carries no partial-application indicator.

#### F-c3d1786-20260903T124638Z-memory-ui-003

- **標題**：「備份與還原」的「匯出全部」只匯出記憶、硬上限 1000 條且靜默截斷，不含知識、素材與角色互動記憶
- **維度**：memory-ui
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/pages/BackupSection.tsx:78-90 (button), report at line 82

**主張**：按鈕文字「匯出全部」與回報「已匯出 N 條」讓使用者以為拿到完整備份，但實際只呼叫 memoryExport（10 層記憶），完全不含知識節點、素材與衍生資料、知識收據，也不含 spec §11 第一類的角色互動記憶（存在 DesktopPrefs）。而且 memory_export 內部只取 1000 筆且無截斷回報，記憶超過 1000 筆時會靜默漏掉最舊的部分，卻仍宣稱「已匯出 N 條」。

**證據**：BackupSection.tsx:76-89：
```
const out = (await api.memoryExport()) as Record<string, unknown>;
report(`已匯出 ${String(out.count)} 條（內容已在下方顯示，可自行複製保存）。`, true);
… 匯出全部
```
apps/interaction-desktop/src/api.ts:199 是整個 api 介面唯一的資料匯出（`memoryExport: () => invoke("memory_export")`；grep `Export` 只另有 toolsExport）。
crates/interaction-runtime/src/memory.rs:166-188 `memory_export`：`let bodies = self.store.list_memory(None, 1000)?;` → 只回記憶、`count = items.len()`，沒有 total 或 truncated。
crates/interaction-storage/src/lib.rs:990 `let limit = limit.clamp(1, 1000);`，SQL 為 `ORDER BY updated_at DESC LIMIT ?1` → 超過 1000 筆時最舊的被丟掉且無訊號。
角色互動記憶只存在 apps/interaction-desktop/src/companion/interactionMemory.ts（DesktopPrefs），沒有任何後端匯出路徑（`grep -rn "CharacterMemory|character-memory" crates` 只命中 enum 定義與 default_retention，無寫入者）。

**建議修法**：把按鈕改名為「匯出記憶」或讓匯出涵蓋知識／素材／互動記憶；memory_export 加 keyset 分頁與 `truncated`/`total` 欄位，前端在截斷時明說「只匯出了最近 1000 條」。

**回歸測試**：寫入 1200+ 筆記憶後呼叫 memory_export，斷言回傳需標示 truncated:true 或 totalCount>count（目前會直接失敗，因欄位不存在）；並可加前端測試斷言「匯出全部」流程涵蓋 knowledge_list／knowledgeReceipts 與角色互動記憶，目前也會失敗因為沒有對應呼叫。

#### F-c3d1786-20260903T124638Z-memory-ui-004

- **標題**：stale 記憶的「重新確認（再保留 90 天）」對 agent 建立的 user-memory 只延 30 天，而且把它降級成「等待確認」並踢出 Context Bundle
- **維度**：memory-ui
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/pages/MemoryKnowledgePage.tsx:253

**主張**：按鈕明確宣稱「再保留 90 天」，但對 created_by 為 Agent 的 user-memory，後端的 apply_actor_rules 會把 reviewAfter 壓回 now+30 天，並因為 wants_long_term 成立而把 kind 改成 Candidate（UI 標籤「等待確認」）。使用者按下「重新確認」得到的是「保留期只有 1/3」加上「這條記憶反而被降級、從此不再進 Context Bundle」，UI 卻回報成功、不顯示任何差異。

**證據**：apps/interaction-desktop/src/pages/MemoryKnowledgePage.tsx:238-256：
```
const next = new Date(Date.now() + 90 * 24 * 3600 * 1000).toISOString();
await api.memoryPatch(String(item.memoryId), { retention: { ...(retention ?? {}), reviewAfter: next } });
… 重新確認（再保留 90 天）
```
crates/interaction-runtime/src/memory.rs:76 `apply_actor_rules(&mut updated, patched_at);`（PATCH 也重套）。
crates/interaction-core/src/memory.rs:254-287：created_by 為 Agent 時，`review_cap = now + 30 天`；`earliest_checkpoint = now+90d > review_cap` → `wants_long_term = true`；`if item.layer == MemoryLayer::UserMemory && wants_long_term { item.kind = MemoryKind::Candidate; item.retention.review_after = Some(review_cap); }`，其後 `defaults.review_after`（UserMemory = 30 天，core/memory.rs:92-96）再取 min。
降級後果：crates/interaction-runtime/src/memory.rs:260-264 `if item.kind == MemoryKind::Candidate { excluded_unreviewed_candidates += 1; continue; }`。
UI 標籤對應：MemoryKnowledgePage.tsx:25-31 `candidate: { text: "等待確認" }`。

**建議修法**：按鈕文案改為依 layer/creator 計算的真實延長（或先向後端查可延長上限再顯示），並在 patch 回應後比對實際 retention.reviewAfter 與 kind，若被壓縮或降級就明說「只能再保留 30 天，且需要你確認後才會再提供給 AI」。

**回歸測試**：Create a MemoryItem via new_memory_item(UserMemory, Preference, ..., MemoryActor::Agent("x"), now) with retention.review_after = now-1day (already stale); call runtime.memory_update(id, json!({"retention": {"reviewAfter": now+90days}})); assert the returned item.kind == MemoryKind::Candidate and item.retention.review_after <= now+30days (not the requested 90), then assert the item's id is absent from memory_context_bundle's `includes` and only bumps excluded.unreviewedCandidates.

#### F-c3d1786-20260903T124638Z-memory-ui-005

- **標題**：「清除短期記憶」等按鈕沒有 try/catch，後端刻意寫的誠實失敗訊息被靜默吞掉
- **維度**：memory-ui
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/pages/MemoryKnowledgePage.tsx:141

**主張**：後端特地在 session-context 清不乾淨時回 `Err("session-context 清除未完成：已刪 N 筆，仍殘留至少 M 筆無法清除")`，但前端 onClick 沒有 try/catch，專案也沒有任何全域 unhandledrejection／ErrorBoundary，因此失敗時使用者看不到任何訊息、清單也不會刷新——一個明確設計成「清不完就誠實回報」的路徑在 UI 上變成完全靜默。同樣模式出現在知識的「不採用」與素材的匯入／解析／刪除。

**證據**：crates/interaction-runtime/src/memory.rs:150-161：
```
if deleted_this_round == 0 { break; }
…
Err(DomainError::Internal(format!("session-context 清除未完成：已刪 {n} 筆，仍殘留至少 {remaining} 筆無法清除")))
```
apps/interaction-desktop/src-tauri/src/lib.rs:1147-1154 `memory_clear_session` 以 `.map_err(err_s)?` 原樣往上拋。
apps/interaction-desktop/src/transport.ts:75-88：非 2xx `throw new Error(...)`（HTTP 模式）。
apps/interaction-desktop/src/pages/MemoryKnowledgePage.tsx:141-147 無 try/catch：
```
onClick={async () => { const out = await api.memoryClearSession(); setNotice(`已清除 …`); retry(); }}
```
同檔 381-386 的「不採用」也沒有 catch（相對地 368-380 的「採用」有）。
`grep -rn "unhandledrejection|componentDidCatch|ErrorBoundary" apps/interaction-desktop/src` 無任何結果 → 沒有全域接手。

**建議修法**：比照同檔「採用」按鈕，替 memoryClearSession／knowledgeReview(reject)／assetImport／assetDerive／assetDelete 的 onClick 加 try/catch 並把後端訊息原文顯示在 role="status"/"alert" 區塊；或加一個全域 unhandledrejection 通知。

**回歸測試**：在 vitest 中 mock `api.memoryClearSession` reject 一個 Error，render MemoryKnowledgePage 並點擊「清除短期記憶」，斷言頁面上出現使用者可見的錯誤提示（而非目前的靜默無反應/僅 console 未處理 rejection）。

#### F-c3d1786-20260903T124638Z-link-transports-049

- **標題**：serial supervisor 只在「開埠失敗」時退避；開得起來但立刻斷的連線會無退避地重連churn
- **維度**：link-transports
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-adapter-declarative/src/serial.rs:200 (Ok 分支起點；收尾於 308 行，緊接 310 行 Err 分支的 interruptible_sleep，中間全程無延遲)

**主張**：supervisor 的指數退避只寫在 open_port 的 Err 分支。Ok 分支在 session 結束（reader EOF／寫入錯誤）後直接回到迴圈頂端再次 open_port，中間沒有任何 sleep。對「開得起來但馬上 EOF」的節點（file fallback 開到一般檔案、/dev/null、peer 已關閉但節點仍可開的裝置）會形成每 ≤200 ms 一輪的持續重連：每輪 spawn 一條 reader thread、generation 連續遞增、drain_stale_queue 把剛排進去的命令一律丟掉。後果不只是 CPU／thread churn——health 會在 Connecting 與「已連線但未握手」之間跳動、永遠不會 offline，UI 因此從不誠實顯示這台裝置不可用。模組註解宣稱的行為與程式碼不符。

**證據**：serial.rs:3-5 模組註解：「斷線＝alive=false → supervisor 以指數退避重開（1s→2s→…→15s cap），世代 +1」。實際 serial.rs:199-320：`let mut backoff = BACKOFF_START_MS; while !shutdown.load(..) { match open_port(&port, baud) { Ok(halves) => { backoff = BACKOFF_START_MS; ... /* reader spawn、writer 迴圈、drop(writer)、join */ }  /* 284-308，無 sleep */  Err(e) => { ...; interruptible_sleep(&shutdown, backoff); backoff = (backoff * 2).min(BACKOFF_MAX_MS); } } }`。open_port 對一般檔案會走 ENOTTY fallback 成功開檔（serial.rs:86-92，並由 serial.rs:726-751 的測試證實 macOS 上一般檔案確實走 fallback），pump_lines 立即 `Ok(0) => break`（serial.rs:348），alive=false，寫入迴圈 200 ms 內退出（serial.rs:259-283），然後無延遲重開。generation 每輪 +1（serial.rs:234），排隊命令每輪被 drain（serial.rs:226）。

**建議修法**：在 Ok 分支的 session 結束後也套用退避（例如 session 存活時間 < 1 s 才算「立刻斷」，套用同一組指數退避；存活夠久才重置 backoff），並讓連續 N 次立刻斷的埠把 state 標成 STATE_DISCONNECTED（health offline）而不是 CONNECTING。

**回歸測試**：用 SerialRawLink::spawn 指向一個內容為空的一般檔案（或 /dev/null，走 79-93 行的 ENOTTY file fallback），在極短時間窗（如 300-500ms）內對 generation() 取樣多次，斷言 generation 遞增次數遠高於「有退避時應有的上限」（例如 >5 次而非 backoff 下應有的 0-1 次），且全程 link_state() 從未回報 LinkState::Disconnected（對應 readiness() 從未變成 Disconnected/offline），藉此同時釘死「無退避重連churn」與「health 永遠不落到 offline」兩點。

#### F-c3d1786-20260903T124638Z-link-transports-050

- **標題**：LinkReceptor::read 在「一個 fact 都沒解析出來」時仍回成功觀察，provider 因此被標成「已測試」
- **維度**：link-transports
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-adapter-declarative/src/link_caps.rs:87

**主張**：read() 只要 read_state 成功就回 Observation，即使 spec 的每一個 json-pointer 都指不到東西（韌體欄位改名、spec pointer 打錯、裝置回 facts:{}），obs.facts 會是空的卻仍算 Ok。observe_fresh 據此把 provider 記成「已測試」，UI 顯示「感知來源『X』讀取成功」，並把一筆零 fact 的觀察寫進 store 與事件流。這與 runtime.rs:573 自己寫的規則「真的讀到資料才算，只有 metadata 不算」直接矛盾，也讓規格 §9.3 的「已測試」狀態變成不可信。HTTP receptor（lib.rs:797-830）有同一個問題。

**證據**：link_caps.rs:87-105：`let state = self.link.read_state(timeout).await.map_err(...)?; let mut obs = Observation::now(...); for (fact, pointer) in &self.spec.facts { if let Some(v) = state.pointer(pointer) { obs.facts.insert(fact.clone(), v.clone()); } } Ok(obs)` —— 迴圈之後沒有任何「至少要有一個 fact」的檢查。runtime.rs:558-579：`let mut obs = receptor.read().await?; ... self.store.insert_observation(&obs)?; self.publish_observation_event(&obs); // 「已測試」證據（spec §9.3）：真的讀到資料才算，只有 metadata 不算。 self.note_capability_tested(TestedCapability::Receptor, receptor_id.as_str()).await;`。validate_link_cap（lib.rs:410-415）只要求 facts 這個 map 非空，不驗證 pointer 真的解得到。

**建議修法**：read() 在 obs.facts 為空時回 ReceptorError::Unavailable（訊息點名哪些 pointer 解不到、裝置實際回了哪些鍵），或至少讓 observe_fresh 只在 obs.facts 非空時 note_capability_tested。加 regression test：裝置回 {"facts":{}} 時受器回 Unavailable 且 provider 不被標已測試。

**回歸測試**：用一個回傳 read_state Ok(json!({"deviceId":"dev1","facts":{}})) 的假 RawLink 建 LinkReceptor（spec.facts 非空但 pointer 全部指向不存在欄位），呼叫 read()：目前會得到 Ok(obs) 且 obs.facts.is_empty()==true；修好後應改為對『一個 fact 都沒命中』回傳 Err（例如 ReceptorError::Unavailable("device replied but no declared fact resolved")），並斷言 observe_fresh 在此情況下不會呼叫 note_capability_tested／不會把 provider 標成已測試。

#### F-c3d1786-20260903T124638Z-link-transports-051

- **標題**：pair-fail 的 reason／retryAfterMs 與 hello.pairingLocked 被丟棄，鎖定期內把正確的配對碼報成「配對碼被裝置拒絕」
- **維度**：link-transports
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-adapter-declarative/src/protocol.rs:41

**主張**：DeviceMsg::PairFail 是無欄位的 unit variant、Hello 也沒有 pairingLocked 欄位，所以裝置明講的 reason:"pair-locked" 與 retryAfterMs 在 host 端整組蒸發。參考韌體與模擬器在配對鎖定期內對任何 pair（含正確碼）一律不比對就回 pair-locked，host 卻把它翻成「pairing code rejected by device」→ 收據原因 device-identity-or-pairing。使用者被告知配對碼錯誤，而碼其實是對的、真正的原因是暴力猜測鎖定，且可以再試的時間點（裝置已經算好給你）被丟掉。這是把 unknown/refused 的原因演成另一個確定結論，違反誠實階梯，且該限制沒有記進 CHANGELOG.md 或 docs/acceptance-evidence.md 的已知限制清單（只寫在 firmware README）。

**證據**：protocol.rs:41-42 `PairOk, PairFail,`（unit variant，內部標籤 enum 會把 reason／retryAfterMs 靜默吃掉）；protocol.rs:29-40 Hello 沒有 pairingLocked 欄位；protocol.rs:475-484 `let ok = wait_for(...) { DeviceMsg::PairOk => Some(true), DeviceMsg::PairFail => Some(false), ... }; if !ok { return Err(LinkError::Refused("pairing code rejected by device".into())); }`。裝置端：firmware/esp32-companion/esp32-companion.ino:716-722 `out["type"]="pair-fail"; out["reason"]="pair-locked"; out["retryAfterMs"]=pairRetryAfterMs(link, now);`、:532 `doc["pairingLocked"] = pairingEnabled() && pairLocked(link, millis());`；scripts/esp32-serial-sim.py:344-363、:383 鏡射同一規則。tests/esp32_sim_conformance.rs:534-540 只斷言「解析得出 DeviceMsg::PairFail」，等於把資訊丟棄釘死成期望值。firmware README.md:278-280 承認「host 端目前只認 pair-fail 的 type」，但 docs/acceptance-evidence.md 的已知限制 1-14 與 CHANGELOG 都沒有這一項。

**建議修法**：PairFail 加上 `#[serde(default)] reason: Option<String>` 與 `retry_after_ms: Option<u64>`、Hello 加 `pairing_locked: bool`；ensure_ready 對 pair-locked 回一個獨立的 LinkError（訊息說明「裝置因連續錯碼鎖定，N 秒後再試；碼可能是對的」），link_caps 的 refusal_reason 再多一個 pairing-locked 分支；並把此限制同步記進 CHANGELOG 與 acceptance-evidence（或直接修掉）。

**回歸測試**：在 DeviceMsg::PairFail 加上 `reason: Option<String>` 與 `retry_after_ms: Option<u64>`（Hello 加 `pairing_locked: Option<bool>`），握手失敗時斷言 LinkError::Refused 訊息／收據攜帶「pair-locked」與剩餘毫秒數，且 refusal_reason() 對此情形回傳有別於 device-identity-or-pairing 的新分類（例如 "pairing-locked"），而非固定字串「pairing code rejected by device」。

#### F-c3d1786-20260903T124638Z-link-transports-053

- **標題**：read_state／cancel 無條件認領無 id 的裝置錯誤，且 in_flight 只計 command，歸屬守衛在有並行 read/cancel 時失效
- **維度**：link-transports
- **Severity**：medium（原報 low）
- **file:line**：crates/interaction-adapter-declarative/src/protocol.rs:718

**主張**：command() 花了大段註解說明「無 id 的錯誤只有在這條 link 上只有我一個在等時才算我的」，用 in_flight 計數守衛；但 (1) read_state（:718）與 cancel（:672）對 Err{id:None} 一律認領，沒有任何守衛；(2) in_flight 只在 command() 遞增（:605），read_state／cancel／stop_all 都不計入。因此一條 link 上同時有 receptor 輪詢與 actuator 命令時，command 看到的 in_flight 仍是 1，守衛失效而照樣認領那則可能屬於 read 的錯誤；反向地，一次 cancel 會把別的請求的 not-paired 認領成自己的結果，回報 ActuatorError::NotFound("device reports no cancellable effect")——對一個正在跑的震動／蜂鳴效果，這是在安全路徑上給出確定卻錯誤的結論。同一個 DeviceLink 被該 adapter 的所有能力共用，並行是常態。

**證據**：protocol.rs:605 `let in_flight = InFlightGuard::enter(&self.in_flight);`（只在 command）；:620-629 `DeviceMsg::Err { id: None, reason } => { if self.in_flight.load(Ordering::SeqCst) <= 1 { Some(m.clone()) } else { ... None } }` 與 :615-619 的註解；:672 cancel 的 picker `DeviceMsg::Err { id: None, .. } => Some(m.clone()),`（無守衛）；:718 read_state 的 picker `DeviceMsg::Err { id: None, reason } => Some(StateReply::Refused(reason.clone())),`（無守衛）。link 共用：lib.rs:1185-1215（serial）／1219-1268（mqtt）／1273-1315（ble）都把同一個 DeviceLink clone 給該裝置的每個 capability。韌體的無 id 錯誤來源：esp32-companion.ino:758-760（未配對的 read）、:790（pollSerial 的 bad-json）、:830（unknown-type）。既有測試 protocol_honesty.rs:1258 只覆蓋 command 對 command 的情況。

**建議修法**：把 InFlightGuard 套到 read_state／cancel／stop_all（讓 in_flight 是這條 link 上所有等待者的真實計數），並讓 read_state／cancel 套用與 command 相同的「只有我一個在飛才認領」規則；無法歸屬時回逾時＋在 detail 記下那則匿名錯誤。

**回歸測試**：Spawn a `read_state()` and a `command()` concurrently on the same mock `DeviceLink` (device never acks the command), then emit one id-less device error; assert the `read_state()` call does NOT resolve as `Refused` while the command is genuinely in flight (it currently does, unconditionally) — and symmetrically, spawn a `cancel("a1", …)` alongside a concurrent `command("a2", …)` that never acks, emit an id-less error, and assert `cancel` does not resolve to a definite "no cancellable effect" result when it cannot actually attribute the error to `a1`.

#### F-c3d1786-20260903T124638Z-agent-honesty-024

- **標題**：gateway session 的委派 receipt 永遠停在 Dispatched：gateway_deliver 的 ack 是 no-op，action.acknowledged 從不發出
- **維度**：agent-honesty
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/gateway.rs:737（原引用 713 已因周邊程式碼變動而漂移；其餘引用行號 executor.rs:621/650/686、agents.rs:464/760/768 均與現行檔案相符）

**主張**：`gateway_deliver` 在真的寫進 stdin 後呼叫 `acknowledge_delegated_action_public`，但此時 executor 還沒把 driver receipt merge 進去，receipt 的狀態是 `Accepted` 而不是 `Dispatched`；`acknowledge_delegated_action` 的守衛只在 `Dispatched` 時動作，所以對 codex／claude-code 這條 v0.5 主路徑，委派 receipt 永遠不會推進到 `Acknowledged`，`EventType::ActionAcknowledged` 也從不發出。程式碼註解「轉送即送達：補 delivered 戳記＋委派 receipt ack」與實際行為不符。

**證據**：executor.rs:621 `receipt.transition(ActionStatus::Accepted, ...)` → `persist_receipt` → 650 `actuator.execute(action.clone())` → 686 `merge_driver_receipt(...)`（Dispatched 是在 execute **回來之後**才寫入）。DelegateActuator::execute（agents.rs:1466-1531）在 execute 內部呼叫 `runtime.mailbox_send(..., Some(action_id))` → agents.rs:665-668 `self.gateway_deliver(id, &message).await?` → gateway.rs:713-726 取得 `action_id` 後呼叫 `acknowledge_delegated_action_public`（agents.rs:464）→ agents.rs:760-772 `if receipt.current_status == interaction_core::ActionStatus::Dispatched { ... }`，此刻是 Accepted，整段被跳過，錯誤還被 `let _ =` 吞掉。非 gateway session 沒這個問題（ack 發生在稍後的 `mailbox_fetch`，agents.rs:748-752），也正是唯一被測到的路徑：tests/agents_loop.rs:37 用的是 `agent.coder`（非 gateway），gateway_loop.rs:1186 的 fetched 測試則用 `action_id = None`。

**建議修法**：讓 `acknowledge_delegated_action` 接受 `Accepted | Dispatched`（或在 DelegateActuator 內先把 receipt 推到 Dispatched 再送信箱），並補一個以 gateway fixture＋`agent.delegate` action_id 走完 dispatched→acknowledged 的回歸測試。

**回歸測試**：建一個假 codex/claude-code binary fixture（沿用 gateway_loop.rs 現有機制），透過 `agent.delegate` actuator 走完整 governor 流程（帶真實 action_id）委派給該 gateway session，等 stdin 真寫入成功後，斷言該 action 的權威 receipt（`rt.get_action(&action_id)`）最終狀態為 `ActionStatus::Acknowledged`、且事件流中出現 `EventType::ActionAcknowledged`；目前這個斷言會失敗（receipt 卡在 `Dispatched`，事件從未發出）。

#### F-c3d1786-20260903T124638Z-agent-honesty-025

- **標題**：「已送達 Agent」是固定文案：stdin 已關閉與非 gateway session 兩種未送達情況下 mailbox_send 仍回 Ok，UI 照樣宣稱送達
- **維度**：agent-honesty
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/pages/AiPage.tsx:598

**主張**：`mailbox_send` 對 gateway session 呼叫 `gateway_deliver(...).await?`，但 `gateway_deliver` 在「stdin 已關閉」時 return `Ok(false)`（不是 Err），在非 gateway session 也 return `Ok(false)`。兩種情況訊息都留在信箱、`delivered_at` 是 null，runtime 甚至剛把 session 記成 failed，`mailbox_send` 仍回 Ok(message)、HTTP 200，桌面則印出固定字串「已送出（已送達 Agent，尚未完成）。」——把 queued 冒充成 delivered，正是誠實階梯禁止的 dispatched≠acknowledged。

**證據**：gateway.rs:684-691（`GatewayError::Closed` 分支）：`let _ = self.report_agent_session(session_id, "failed", json!({"error": "無法把訊息送進 agent 子程序（stdin 已關閉…）；這則訊息未送達"})).await; return Ok(false);`。gateway.rs:604-613：非 gateway session `return Ok(false)`。agents.rs:665-668 `if direction == MailboxDirection::ToSession { self.gateway_deliver(id, &message).await?; } Ok(message)` — `Ok(false)` 不是錯誤，直接回傳成功。AiPage.tsx:595-599 `await api.agentSessionSend(record.sessionId, "task", { task }); ... onNotice("已送出（已送達 Agent，尚未完成）。")`，且 SessionCard 對任何 agentId 的 open session 都渲染這顆送出按鈕，不限 gateway agent。TaskComposer.tsx:485 也是同樣寫死的「已送達、尚未完成」。

**建議修法**：讓 `mailbox_send` 回傳（或在 message DTO 明示）是否真的 delivered；UI 依 `deliveredAt` 決定文案：有戳記才說「已送達」，否則說「已排入信箱，尚未送達 Agent」。

**回歸測試**：對一個已知會回 `GatewayError::Closed`（或非 gateway agentId 且無 managed handle）的 fixture session 呼叫 `mailbox_send(..., MailboxDirection::ToSession, ...)`，斷言：要嘛回傳 `Err`，要嘛回傳的 `MailboxMessage.delivered_at` 為 `None` 且呼叫端（Tauri command／前端）能以此區分「排隊中」與「已送達」，而不是無條件顯示「已送達 Agent」。

#### F-c3d1786-20260903T124638Z-protocol-conformance-030

- **標題**：裝置停用配對時對任何碼都回 pair-ok，host 不檢查 hello.pairing 就把它記成已配對的 handshake 證據
- **維度**：protocol-conformance
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-adapter-declarative/src/protocol.rs:465

**主張**：`ensure_ready` 只檢查一個方向：「裝置說要配對但 spec 沒有 pairingCode」→ Refused(465-470)。反方向完全不檢查——spec 有 pairingCode、但裝置的 `PAIRING_CODE` 是空字串（config.h.example:38 明文允許）時，韌體對**任何** code 都直接 `g_linkPaired[link]=true` 並回 `pair-ok`（esp32-companion.ino:708-715，模擬器 esp32-serial-sim.py:339-343 鏡射），`hello.pairing` 誠實回報 false，但 host 在 `self.pairing_code.is_some()` 分支(471-484)只看 `PairOk/PairFail`，把 `hello.pairing` 丟棄。結果：配對碼從未被驗證，host 卻標記握手完成，providers.rs:335-337 據此把該 provider 的每次讀取／命令記成 handshake 等級證據。配對碼是這套線協定唯一的共享秘密（deviceId 是裝置自報的、埠／topic 不是身分），host 手上明明已有能分辨的欄位卻不用。

**證據**：protocol.rs:465-470 只有 `if device_wants_pairing && self.pairing_code.is_none()`；471-484 送 pair 後只判斷 PairOk/PairFail，`device_wants_pairing` 為 false 時不做任何降級。firmware esp32-companion.ino:707-715：`if (!pairingEnabled()) { g_linkPaired[link] = true; out["type"]="pair-ok"; sendDoc(...); return; }`——完全不比對 `doc["code"]`。config.h.example:38「設成空字串 "" 代表停用配對」。scripts/esp32-serial-sim.py:339-343 同樣行為。providers.rs:335-337 註解：「有實體連線（serial/mqtt/ble）的 provider：之後任何一次成功的讀取或命令都必然通過 hello 身分＋pair-ok 握手，證據等級記為 handshake」。

**建議修法**：在 471 之前加：`if self.pairing_code.is_some() && !device_wants_pairing { … }` → 至少把「裝置未強制配對，配對碼未被驗證」記進 link 狀態並讓 providers.rs 不得把證據等級記成 handshake（降到 claim/unverified），或直接 Refused 要求使用者移除 spec 的 pairingCode（讓「我以為有配對」不能靜默成立）。

**回歸測試**：mock 一台裝置：hello 回 `{pairing:false}`（模擬 PAIRING_CODE 為空），host adapter spec 設定 pairingCode="1234"；ensure_ready() 送出 pair 後裝置回 pair-ok；斷言 host 要嘛拒絕/降級這次握手，要嘛至少記錄一個「配對碼未經比對」的警告，而不是把它當成一般 handshake 級證據寫入 ProviderTested。

#### F-c3d1786-20260903T124638Z-perf-claims-007

- **標題**：Reduced Motion 下 StageRenderer 仍每幀清空並重繪整個透明視窗（SpriteRenderer 已有的靜態短路沒做進主渲染器）
- **維度**：perf-claims
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/rig/stage.ts:712

**主張**：小樞（reference adapter）實際使用的 StageRenderer 在 Reduced Motion 下畫面是逐幀完全相同的，但 loop() 沒有任何 dirty／static 短路，仍以 rAF 頻率（實測 headless 8.3ms／幀，真機 60–120Hz）clearRect＋重畫場景／使魔／玩具／角色，等於在透明、always-on-top 的視窗上持續合成同一張圖。同一個 repo 的另一個渲染器 SpriteRenderer 已為此明確修過並寫下要求；StageRenderer 沒有跟上，而且 perf rig 從頭到尾沒有量過 Reduced Motion，所以「Reduced Motion 已有（真靜態）」這句話只涵蓋視覺，不涵蓋工作量，且沒有任何數字。

**證據**：stage.ts:712-727 `private loop = () => { ... this.raf = requestAnimationFrame(this.loop); ... this.renderFrame(start); ... }`：唯一的跳幀條件是 `shouldDrawFrame(this.budget, ...)`（幀預算），沒有任何「畫面未變」判斷。stage.ts:837-864 renderFrame 每次都 `ctx.clearRect(0,0,canvas.width,canvas.height)` 後重畫 drawScene／familiars／toys／drawRig。畫面在 Reduced Motion 下確實是靜止的：timeline.ts:393-394 `if (this.reducedMotion) { target = base; }`（參數固定）、timeline.ts:364-366 粒子歸零、playfield.ts:305-311 玩具直接安放地面且 vx/vy=0、playfield.ts:365 `const canPlay = ... && !inputs.reducedMotion` → playfield.ts:378-387 `c.vx = 0; c.mode = "free"`、playfield.ts:643 `if (inputs.reducedMotion || w.familiars.length === 0)` 使魔不動、stage.ts:958-960 drawToy 自己也吃 reduced。對照組 renderer.ts:195-204（SpriteRenderer）：「Static representation: hold the first frame of the state — drawn once, not on every rAF (the picture does not change; Reduced Motion must reduce work, not redraw a transparent window at full refresh rate)」＋`staticDrawn` 旗標。量測缺口：`grep -n "setReducedMotion\|reducedMotion" src/companion/rig/perf-entry.ts src/companion/rig/perf-soak-entry.ts scripts/shu/perf-rig.mjs` → 0 命中；docs/acceptance-evidence.md:426-443 的效能表沒有任何 Reduced Motion 列。文件宣稱見 docs/v05-capability-gap-matrix.md:120「Reduced Motion→已有（執行中可切換、真靜態）」。

**建議修法**：在 StageRenderer.loop／renderFrame 加上與 SpriteRenderer 同構的靜態短路：Reduced Motion 且（world 靜止＋timeline 參數未變＋無 pointer／表情事件）時只畫一次，之後只有狀態改變（setAnimation／setMachineFlags／setMicroMotion／pointer／spawnToy／resume）才把 dirty 旗標打開；並在 perf-entry.ts 加一個 reducedMotion=true 的量測列（drawn frames 與每秒繪製成本），讓「Reduced Motion 減少工作」變成有數字的宣稱而不是推論。

**回歸測試**：在 headless（如 perf-rig 或 vitest + jsdom canvas mock）中：setReducedMotion(true) 後，spy ctx.clearRect／ctx.drawImage 呼叫次數，跑 N 個 rAF tick，斷言 loop 啟動後第一幀之後不再呼叫 clearRect／重繪（呼叫次數應恆為 1 而非 N），作為 StageRenderer 的 Reduced Motion 靜態短路回歸測試（呼應 renderer.ts 的 staticDrawn 測試模式）。

#### F-c3d1786-20260903T124638Z-perf-claims-008

- **標題**：30fps 降級的觸發量測的是 renderFrame 的 JS 成本（實測 0.24ms vs 門檻 12ms），與真正掉幀的原因脫鉤，等於實務上不可觸發
- **維度**：perf-claims
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/gameFeel.ts:72

**主張**：規格 §14「低效能裝置允許 30fps 降級」在程式裡唯一的實作是 frameBudgetPolicy，而它的輸入是 `renderFrame` 前後的 CPU 時間差——不含 raster／合成，也不看 rAF 實際間隔。專案自己的量測顯示這個量的中位數 0.240ms、最大 0.40ms、迴圈窗平均 0.188ms，門檻卻是 12ms（約 50 倍餘裕），且 perf rig 每次跑出來都是 skipEveryOther=false。真正會讓使用者掉幀的情境（透明 always-on-top 視窗的 GPU 合成貴、系統節流 rAF、其他行程搶 GPU）完全不會反映在這個輸入上，所以降級路徑在真實低效能裝置上不會啟動；現有唯一證據是把合成 ms 直接餵給純函式的單元測試，沒有任何「迴圈自己降級」的量測或測試。

**證據**：gameFeel.ts:71-76 `FRAME_DEGRADE_MS = 12` / `FRAME_RECOVER_MS = 8` / `FRAME_WINDOW = 60`；gameFeel.ts:98-109 frameBudgetPolicy；stage.ts:722-726 `const start = this.now(); this.renderFrame(start); this.budget = frameBudgetPolicy(this.budget, this.now() - start);`（餵的是繪製指令錄製成本）。perf-entry.ts:280-283 自承 note：「幀預算餵的是 renderFrame 成本（不含 raster flush）」。實測值 docs/acceptance-evidence.md:434「全舞台一幀 median 0.240 ms／p95 0.280／max 0.40」、:436「ticks=362 drawn=362 skipEveryOther=false，視窗平均成本 0.188 ms」、:574「stage loop : ticks=362 drawn=362 skipEveryOther=false lastWindowAvgCost=0.247 ms」。測試覆蓋 src/test/gameFeel.test.ts:77-124 全部是 `frameBudgetPolicy(s, ms)` 純函式餵合成值（20／9／5／16.67ms），沒有一個測試讓 StageRenderer 的 loop 真的降級。文件宣稱 docs/v05-capability-gap-matrix.md:121「30fps 降級→**已有**（遲滯政策）」、docs/v05-phase7-final-report.md:185「30fps 降級遲滯政策」。

**建議修法**：把觸發訊號改成「實際幀節奏 vs 顯示器目標」而不是純 JS 成本：以 rAF 回呼間隔的移動中位數與該螢幕的基準間隔（第一秒取樣得到的 min gap，60Hz→16.7ms、120Hz→8.3ms）相比，連續 N 個窗超過 1.5× 才降級、低於 1.1× 才恢復（保留現有遲滯），這樣既避開 perf-claims-017 的「16.67 > 12 永遠降級」錯誤，也能對 raster／合成瓶頸有反應；並在 perf-entry.ts 加一個人為加重負載（放大 canvas／多實例）直到 loop 真的 skipEveryOther=true 的量測列，讓降級路徑有實跑證據。

**回歸測試**：在 vitest 裡對真的 StageRenderer 用假 canvas + mock `renderFrame`（或注入一個人工拖慢的繪製 stub，例如在 renderFrame 內忙迴圈到 >12ms）跑滿 60 個 rAF tick，斷言 `stage.frameBudget().skipEveryOther === true` 且之後 `drawnFrames ≈ ticks/2`——證明 loop() 整合路徑真的能被觸發降級，而不是只餵合成 ms 給純函式。

#### F-c3d1786-20260903T124638Z-perf-claims-009

- **標題**：記憶體浸泡證據只涵蓋 StageRenderer，完全不含 CompanionApp／CharacterGateway／Director／intervals，但被當成 §18-20 的記憶體交付
- **維度**：perf-claims
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/companion/rig/perf-soak-entry.ts:62

**主張**：規格 §18-20 要求交付「記憶體」量測，acceptance-evidence 的記憶體證據（60s 浸泡、GC 後 +231 KB、三次重跑終值差 <1 KB）全部來自一個只 new 了 StageRenderer 的頁面。實際角色視窗裡所有會長期累積的結構——CharacterGateway 的 pending／seen／grants Map、Director 的 decisions／recent、interactionMemory、React 樹、5 個 setInterval、SSE 500 筆事件環——沒有任何一個被浸泡到。文件與已知限制只說「只有 60 s」，沒有說「只量了畫布層」，讀者會把它當成角色視窗整體的無洩漏證據。

**證據**：perf-soak-entry.ts:62-72 只建立 `new StageRenderer(c, "maid-classic", 1, { autoStart: false, rng: () => 0.37 })`＋setFamiliars／spawnToy，之後 `stage.renderFrame(await nextFrame())` 迴圈；整個檔案沒有 import CompanionApp／CharacterGateway／Director（`grep -n "Gateway\|Director\|CompanionApp" src/companion/rig/perf-soak-entry.ts` → 0 命中）。scripts/shu/perf-rig.mjs:57-58 只打包 `src/companion/rig/perf-entry.ts` 與 `src/companion/rig/perf-soak-entry.ts` 兩個 entry。文件呈現：docs/acceptance-evidence.md:441「JS heap 浸泡（60.0 s 真 rAF、7205 幀、29 次抓放玩具、6 次 spawn、每 5 s 取樣）… 三次 60 s 重跑 GC 後終值差 <1 KB … 未判定無洩漏」；已知限制 docs/acceptance-evidence.md:502-503 只提「只有 60 s 浸泡」，未提範圍不含 app／gateway。

**建議修法**：在 acceptance-evidence 的浸泡列與已知限制明寫「範圍＝StageRenderer 畫布層；CompanionApp／CharacterGateway／Director／intervals／SSE 緩衝未浸泡」；若要真的覆蓋，再加一個以 jsdom＋假計時器長跑 CompanionApp pump／gateway.sweep 的浸泡測試，斷言 pending.size／seen.size／grants.size／decisions.length／recent.length 在數萬次事件後仍在上限內。

**回歸測試**：在 perf-rig.mjs 新增第三個浸泡 entry（或擴充現有 soak entry），實際 mount CompanionApp／CharacterGateway／Director 並跑滿其 5 個 setInterval＋SSE 500 筆環（用假 EventSource 灌事件）60 秒以上，斷言 GC 後 heap 差值與 pending/seen/grants Map、decisions/recent 陣列長度都在 bounded 範圍內；並在 acceptance-evidence.md 與已知限制第 16 項明確加註「僅涵蓋 StageRenderer 畫布層，不含 CompanionApp/Gateway/Director/interval/SSE 環」。

#### F-c3d1786-20260903T124638Z-docs-claims-070

- **標題**：iOS XCTest 宣稱 21/21（Protocol 13），實際測試檔有 25 個 test 方法，4 個緊急停止誠實性測試從未有執行紀錄
- **維度**：docs-claims
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-ios/README.md:36

**主張**：iOS README、CHANGELOG 與 test-matrix 一致宣稱「XCTest 21/21 通過（MotionClassifier 8 + Protocol 13）」，但 repo 內 ProtocolTests.swift 現有 17 個 test 方法（合計 25），多出的 4 個正是驗證 stop-all 緊急狀態誠實性的 async 測試，沒有任何文件記錄它們跑過。

**證據**：文件：apps/interaction-ios/README.md:14、:36、:57-58（「ProtocolTests.swift … XCTest:13 個 test 方法」）、:141、:378（「8 + 13 = 21 個 test 方法」）、:382、:400（「Executed 21 tests, with 0 failures」）；CHANGELOG.md「XCTest 21/21」；docs/releases/v0.5.0-test-matrix.md iOS 列「XCTest 21 passed / 0 failed（MotionClassifier 8＋ProtocolTests 13）」。實際：`grep -c "func test"` → MotionClassifierTests.swift = 8、ProtocolTests.swift = **17**（合計 25）。多出的四個都在 ProtocolTests.swift:128、:143、:158、:174：testEmergencyStopAllSetsTheCharacterStateEvenIfCharacterPresentIsLost、testUserStopAllDoesNotFakeAnEmergencyCharacterState、testActuatorOnlyStopAllTouchesNeitherSensorsNorCharacterState、testOnlyTheRuntimeClearsTheEmergencyCharacterState。commit c3d1786 的訊息自己寫「XCTest: 25 tests」，與所有文件的 21 不一致。這 4 個測試守的是「只有 runtime 能清除 emergency」「使用者停感測不得偽裝成緊急停止」這類不變量，卻沒有任何一次記錄在案的執行。

**建議修法**：在 iPhone 17 模擬器重跑 xcodebuild/simctl 注入的 XCTest，把 README:14/36/57-58/141/378/382/400、CHANGELOG、test-matrix 的 21（8+13）一律更新為實跑到的 25（8+17）；若無法重跑，至少改寫成「repo 內 25 個 test 方法，最後一次記錄在案的執行是 21 個（2026-09-03，缺 4 個 stop-all 角色狀態測試）」。

**回歸測試**：A doc-lint check (or simple shell assertion in a test/CI step) that runs `grep -c "func test" apps/interaction-ios/InteractionCompanionTests/*.swift`, sums the counts, and fails the build if that sum doesn't match the count string asserted in apps/interaction-ios/README.md / CHANGELOG.md / docs/releases/v0.5.0-test-matrix.md, plus actually executing the 25-test suite via xcodebuild/simctl injection (as README §2026-09-03 describes) and recording the real pass/fail count including the four new async stop-all tests.

#### F-c3d1786-20260903T124638Z-docs-claims-071

- **標題**：新增的 65 項 Playwright user-task 套件與 examples/fake_iphone.rs 模擬 iPhone fixture 完全沒有進任何發布文件
- **維度**：docs-claims
- **Severity**：medium（原報 medium）
- **file:line**：CHANGELOG.md:168

**主張**：commit fb52a44 新增 8 個 Playwright spec（含 iphone／sensors／estop／a11y）、共享 helpers、三個有序 project 與 CI 變更，commit 6ec0528 新增 examples/fake_iphone.rs 程序內模擬 iPhone；CHANGELOG [0.5.0]、gap-matrix §11、acceptance-evidence 與 DESKTOP-GUIDE 都沒有一句提到，讀者無從得知這些「模擬 iPhone（fixture）」證據的存在與等級。

**證據**：`grep -n "fake_iphone|work-delegate|home-state|a11y|helpers.ts|65 tests|estop-last" CHANGELOG.md docs/releases/*.md docs/v05-capability-gap-matrix.md docs/acceptance-evidence.md docs/DESKTOP-GUIDE.md` 只命中 docs/acceptance-evidence.md:161 一個不相干的舊詞（v0.2 審查的「a11y 焦點圈養」）。實際檔案：apps/interaction-desktop/e2e/{a11y,agent-not-installed,character,estop,home-state,iphone,sensors,work-delegate}.spec.ts（8 個新檔）＋ helpers.ts；playwright.config.ts:27-35 定義 first-run→main→estop-last 三個相依 project；crates/interaction-runtime/examples/fake_iphone.rs 存在。CHANGELOG 對 Playwright 只在 :187 提了一句「Playwright 跑瀏覽器版」；docs/releases/v0.5.0-release-readiness.md:20（關卡 9）也只列 app/evidence/narrow 三個舊 spec「皆已同步修改」，未提 8 個新 spec。文件描述的 Playwright 覆蓋面（35 tests、4 個 spec）與 repo（65 tests、12 個 spec）不符。

**建議修法**：在 CHANGELOG [0.5.0] 補一段 Added — 測試（Playwright user-task 套件 12 spec/65 test、三個有序 project、examples/fake_iphone.rs 程序內模擬 iPhone、CI 建置 fake_iphone），並在 gap-matrix §11／acceptance-evidence 的證據等級總結明確標示「iPhone 相關 e2e 一律為程序內【模擬 iPhone】fixture，非真機」。

**回歸測試**：在 docs/releases/v0.5.0-release-readiness.md 第 9 關與 v0.5.0-test-matrix.md 的 Playwright 列中加入對 `examples/fake_iphone.rs`、8 個新 spec 檔名（或至少型式化列出檔名清單）與三 project 排序的具體提及後，重跑 `grep -nE "fake_iphone|work-delegate|home-state|helpers.ts|estop-last" CHANGELOG.md docs/releases/*.md docs/v05-capability-gap-matrix.md docs/acceptance-evidence.md docs/DESKTOP-GUIDE.md` 應能命中對應文件，而非只命中舊詞。

#### F-c3d1786-20260903T124638Z-docs-claims-072

- **標題**：README 與 FEATURES 仍以舊的、綁定「小樞」的字眼描述精靈步驟與五個一級入口，與程式碼不符
- **維度**：docs-claims
- **Severity**：medium（原報 medium）
- **file:line**：README.md:47

**主張**：README:47-48 寫「三步設定精靈（認識小樞／要讓小樞幫忙工作嗎／安全預設）」與「5 個一級入口：現在／小樞／工作／連接與權限／更多」，FEATURES.md:106 同樣寫「三步（認識小樞／AI 幫手／安全預設）」；程式碼的步驟名與導覽第二項標籤都已改為角色無關。

**證據**：實際步驟名：apps/interaction-desktop/src/pages/Onboarding.tsx:77 `const STEPS = ["選擇角色與陪伴方式", "選擇 AI 工作方式", "確認安全與權限預設"]`（:559、:602 直接渲染 STEPS[0]／STEPS[2]），docs/DESKTOP-GUIDE.md 已同步為這三個名稱。實際導覽：apps/interaction-desktop/src/App.tsx:64-70 SIMPLE_NAV 第二項 label 是 `characterNameFallback`，:73-77 `simpleNavFor()` 於執行期換成目前角色的名字與 icon，DESKTOP-GUIDE 亦寫「角色（顯示目前角色的名字）…角色還沒載入前顯示中性的『角色』」。README／FEATURES 把「小樞」寫成固定入口名與固定精靈步驟名，與 CLAUDE.md「小樞只是 Reference Adapter，Runtime／頁面不得再引用小樞的部位、表情名或 pack id」的角色無關化方向相反，也與同一份 README:123「小樞是第一個 Reference Adapter，不是唯一角色」自相矛盾。

**建議修法**：README:47-48 改為「三步設定精靈（選擇角色與陪伴方式／選擇 AI 工作方式／確認安全與權限預設）」與「5 個一級入口：現在／〔目前角色〕／工作／連接與權限／更多（第二項顯示你目前角色的名字，預設小樞）」；FEATURES.md:106 同步。

**回歸測試**：A docs-lint/grep check asserting README.md and FEATURES.md do not contain the literal step names 「認識小樞」／「要讓小樞幫忙工作嗎」or the literal nav list 「現在／小樞／工作／連接與權限／更多」, and instead reference the actual STEPS array values from Onboarding.tsx and the runtime-substituted character label from App.tsx/characterName.ts.

#### F-c3d1786-20260903T124638Z-safety-invariants-075

- **標題**：L4 高敏感能力的同意對話預設給「整個工作階段」，沒有 per-use 選項——與它自己顯示的 L4 政策文字相反
- **維度**：safety-invariants
- **Severity**：medium（原報 medium）
- **file:line**：apps/interaction-desktop/src/pages/SafetyPage.tsx:321 (state init), :369-374 (fixed select options), :383-386 (session→undefined mapping)

**主張**：`GrantDialog` 對所有風險分級共用同一個「有效期間」選單（30 分鐘／2 小時／整個工作階段），初始值硬寫成 `"session"`，而 `"session"` 會以 `expiresMinutes = undefined` 呼叫 `POST /v1/session/consent`，產生沒有 `expiresAt` 的無期限 consent。攝影機、持續麥克風、定位、Agent 寫入等 L4 能力因此預設就拿到整個 session 的授權，既不是「每次」也不是「短效」，違反 spec §2 風險分級表 L4「每次或短效授權」。更明確的是同一個對話框在上方就印出 tier 4 的政策文字「每次使用都要你同意（或只給短效授權）」，畫面文字與預設行為互相矛盾。

**證據**：SafetyPage.tsx:321 `const [expires, setExpires] = React.useState<string>("session");`；:349-352 `const risk = riskTierOfCard(selected)` 後印出 `risk.policy`；:368-374 選單只有 30／120／session 三個固定選項，不隨 `risk.tier` 變化；:382-386 `await api.consentGrant(scope, expires === "session" ? undefined : Number(expires))`。riskTier.ts:92-97 tier 4 的 policy 字串為「攝影機、持續麥克風、定位、Agent 寫入檔案：每次使用都要你同意（或只給短效授權），使用中會持續顯示指示。」。後端 interaction-core/src/session.rs:65-73 `grant(scope, now, expires_at)` 在 `expires_at = None` 時就是永久（`Consent::is_active` 只看 revoked 與 expires_at），且沒有任何 single-use／max_uses 欄位，所以「每次」在資料模型上也不存在。

**建議修法**：依 `riskTierOfCard(selected).tier` 決定選單：tier ≥ 4 預設 30 分鐘、拿掉「整個工作階段」（或標為需二次確認）；若要真正支援「每次」，在 `Consent` 加 `max_uses`／one-shot 並讓 `Session::has_consent` 在用掉後失效。

**回歸測試**：Render GrantDialog with an L4-risk HumanCard (e.g. camera/microphone), select it without touching the 有效期間 select, click 同意, and assert api.consentGrant was called with expiresMinutes !== undefined (or assert the resulting Session's Consent has expiresAt set) — it currently fails because expires defaults to "session" regardless of risk.tier.

#### F-c3d1786-20260903T124638Z-safety-invariants-076

- **標題**：外部 character adapter 在緊急停止期間（重）連線時拿不到 emergency 投影，且被禁止讀 /v1/status，會回到 idle 演出
- **維度**：safety-invariants
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/character.rs:1235

**主張**：`character_project`（含 `character_project_emergency`）只投影給「投影當下已連線」的 instance；`character_ws_attach` 與 `character_hello` 都沒有在 attach 後補投影目前的 estop／pause 真相，`Hello` 訊息本身也不帶任何安全狀態。外部 adapter 又被 `adapter_request_allowed` 明確禁止讀 `/v1/status`，所以它沒有第二條路可以自己查。結果：估停中斷線重連（或估停期間才第一次連上）的外部角色會停在 idle／play，直到下一次估停或 clear 才被更新。同一個洞在手機路徑上已經被明確修掉，桌面角色則靠輪詢 `/v1/status` 補足——只有外部 adapter 兩者皆無。

**證據**：character.rs:1500-1540 `character_project`：`let targets = hub.connected_instance_ids();`，targets 空就只走 system.text，之後只對當下 targets `gw.dispatch`；character.rs:1633-1645 `character_project_emergency` 只是包一層 Projection，沒有任何「補投影給新連線」的路徑。character.rs:1235-1301 `character_ws_attach` 從註冊 instance 到 `tx.try_send(WireMessage::Hello(hello))` 全程沒有 `is_estopped()` 檢查；character.rs:803-916 `character_hello` 同樣沒有。gateway.rs:475-491 `hello_for` 回傳的 `Hello` 只有 protocolVersion／runtimeVersion／role／locale／reducedMotion／requires／limits，沒有 truthState。對照 mobile.rs:3044-3052：`if self.is_estopped() { tokio::spawn(... mobile_project_estop_device(&device_id, "reconnect-during-estop") ...) }`；對照 apps/interaction-desktop/src/companion/machine.ts:391-392「安全狀態不靠這條路：emergency／paused 由 `/v1/status` 輪詢決定」。api/src/lib.rs:398-401 `adapter_request_allowed` 只允許 POST `/v1/character/receipts|events`，`/v1/status` 一律 403（同檔 auth_scope_tests 也斷言了這點）。crates/interaction-runtime/tests/character_loop.rs 有估停投影測試（597-605、1432-1433），但沒有任何「估停期間連上／重連」的測試。

**建議修法**：在 `character_ws_attach` 與 `character_hello` 完成註冊後，若 `is_estopped()`（或 proactive paused）就對這一個 instance 直送 emergency／rest 投影，做法比照 `mobile_project_estop_device`；或在 `hello_for` 帶一個由 Runtime 決定的 `truthState` 快照欄位。

**回歸測試**：In character_loop.rs: engage estop (drive runtime to is_estopped()==true), then call rt.character_adapter_add + rt.character_ws_attach + send Negotiate; assert the adapter's session.rx receives an emergency-intent envelope (or that the resulting instance/character state reflects TruthState::Emergency) before any subsequent estop-transition event is fired — currently it receives none.

#### F-c3d1786-20260903T124638Z-safety-invariants-077

- **標題**：外部 character adapter 可以偽造人類輸入觀察（companion.click／text-input／quick-action），觸發 recipe 並重置主動對話退避
- **維度**：safety-invariants
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/character.rs:2117

**主張**：只有 adapter token 的第三方 character adapter，POST `/v1/character/events`（唯一允許它走的兩條路由之一）送出 `clicked`／`textSubmitted`／`actionRequested` 事件後，會被正規化成與桌面真實使用者完全同 id 的受器觀察（`companion.click`／`companion.text-input`／`companion.quick-action`），而且明文跳過桌面隱藏／斷線閘門。這些觀察接著會 (a) 呼叫 `proactive_note_reply()`——讓「使用者沒回覆就不追問」的退避被假的「使用者有回應」解除，(b) 呼叫 `evaluate_recipes(Some(receptor_id))`，而 recipe 觸發只比對 receptor id，沒有任何來源／origin 判斷。預設 `initiative = Suggest` 會把自主動作限制在低風險本機通道（通知／音效／log／對話），所以預設影響面是「第三方 adapter 可以無聲地讓桌面替它發通知、發聲、寫紀錄並影響主動對話節奏」；若使用者把 initiative 調成 active，範圍就是使用者已啟用並同意的一切。這與「角色呈現層沒有權限主權」相衝突：呈現層不該有合成人類輸入的權力。

**證據**：character.rs:2117-2120 `let enforce_surface_gate = origin != InstanceOrigin::External; self.ingest_with_gate(receptor, facts, BTreeMap::new(), 1.0, enforce_surface_gate).await?;`（外部 adapter 的 origin 就是 External，見 character.rs:1269-1276 `origin: InstanceOrigin::External`）。character.rs:2010-2035 把 `Clicked/DoubleClicked → "companion.click"`、`TextSubmitted → "companion.text-input"`、`ActionRequested → "companion.quick-action"`，與桌面路徑共用同一組 id。runtime.rs:645-656：`if matches!(receptor_id, "companion.click" | "companion.text-input" | "companion.quick-action" | "companion.drag-drop" | "session.input") { self.proactive_note_reply().await; }` 之後 `self.evaluate_recipes(Some(receptor_id)).await;`。runtime.rs:1492-1503 recipe 候選過濾只做 `e.recipe.trigger.steps.iter().any(|s| s.receptor == r)`，沒有 origin／instanceId 條件。interaction-core/src/policy.rs:27 `InitiativeLevel::Suggest` 為預設，policy/src/lib.rs:151-170 在 Suggest 下擋掉 `risk_class > Low || external_side_effect` 的自主動作（這是唯一的限縮，不是來源檢查）。

**建議修法**：讓外部 origin 的輸入落到獨立的受器命名空間（例如 `companion.external.click`），或在 facts 之外把 origin 帶進 Observation 的一等欄位，並讓 recipe trigger 預設只接受 origin=builtin 的輸入；`proactive_note_reply()` 只在 origin 為桌面本尊時呼叫。

**回歸測試**：整合測試：以 human token 註冊一個外部 character adapter 取得 adapter token，建立一條 trigger 為 `companion.quick-action` 的 recipe 並設 `initiative=Suggest`／`no_follow_up=true` 且先耗盡一次主動對話額度；接著僅用 adapter token（不模擬任何桌面輸入）POST `/v1/character/events` 送 `actionRequested`；斷言 (a) recipe 確實 fire（低風險本機通道有副作用/log）、(b) 下一次主動對話評估時 `no_follow_up` 退避已被解除（即 `last_answered` 已為 true）——證明外部 adapter 單靠 event API 就能達成這兩個效果，不需任何桌面端輸入。

#### F-c3d1786-20260903T124638Z-mobile-server-062

- **標題**：mobile_ble_scan 沒有 emergency stop 閘門（同檔的 mobile_test／mobile_present_verified 都有）
- **維度**：mobile-server
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/mobile.rs:3240

**主張**：`Runtime::mobile_ble_scan` 在緊急停止生效時仍會把 `ble.scan` 指令送到手機並啟動一次 BLE 掃描（一個對外的無線探測／裝置發現動作）。同檔案裡風險更低的 `mobile_test`（只送一個 WebSocket Ping，mobile.rs:2541）與 `mobile_present_verified`（mobile.rs:2429）都先檢查 `is_estopped()` 並回 `PolicyBlocked`，本機麥克風 `begin_mic_listen`（sensors.rs:111-115）也是；只有 BLE 掃描這條漏掉。

**證據**：mobile.rs:3240-3252：`pub async fn mobile_ble_scan(&self, duration_ms: u64, device_id: Option<&str>) -> DomainResult<Value> { let duration_ms = duration_ms.clamp(500, 8_000); let id = format!("ble-scan-{}", token_hex(4)); let msg = json!({"type":"ble.scan",...}); ... self.mobile.dispatch(target, &id, msg).await`——函式全文（3240-3288）沒有任何 `is_estopped()`。對照 mobile.rs:2541-2545 `mobile_test`：`if self.is_estopped() { return Err(DomainError::PolicyBlocked("emergency stop engaged; nothing is sent to the iPhone".into())); }`。路由 `POST /v1/mobile/ble/scan`（api/src/routes.rs:2275-2288）也沒有補這道閘。

**建議修法**：在 `mobile_ble_scan` 開頭加上與 `mobile_test` 相同的 `is_estopped()` → `PolicyBlocked` 檢查（並補一則回歸測試：estop 期間 ble-scan 必須被拒、不得有訊息送到手機）。

**回歸測試**：

#### F-c3d1786-20260903T124638Z-mobile-server-063

- **標題**：一台 iPhone 斷線就停用全域 iphone.mic-level 受器，讓另一台仍在串流的手機從 activeSensors／tray 消失（感測靜默）
- **維度**：mobile-server
- **Severity**：medium（原報 medium）
- **file:line**：crates/interaction-runtime/src/mobile.rs:2180

**主張**：`mobile_disable_high_risk_receptors` 停用的是全域受器 id `iphone.mic-level`，不是「這一台」的能力。兩台手機同時連線時，A 斷線會把受器整個關掉；此時仍連線、仍在串流、仍自報 `micLevel:true` 的 B 會在 `mobile_active_sensors` 被 line 2313 濾掉，於是 `status.activeSensors` 變空、tray 的感測指示燈熄滅，而且不會為 B 發任何 `sensor.stopped` 事件——B 的麥克風其實還在錄。這正是同檔 `mobile_sensors_stop`（mobile.rs:2519-2523）註解自己指認為不可接受的失效模式（「為了停一台而停用它，會讓另一台仍在串流的手機從 activeSensors 消失（＝感測靜默）」），卻在斷線路徑上發生。

**證據**：mobile.rs:2180-2199：`for spec in MOBILE_RECEPTOR_SPECS.iter().filter(|s| s.high_risk) { let id = ReceptorId::new(spec.id); ... self.registry.set_receptor_enabled(&id, false)`——鍵是 receptor id，`device_id` 只寫進 audit payload。呼叫端 mobile.rs:3195-3200 的斷線收尾：`None if was_active => { self.mobile_disable_high_risk_receptors(&device_id, "disconnected").await }`。效果端 mobile.rs:2285-2314：`let enabled = self.registry.receptor(&ReceptorId::new("iphone.mic-level")).await.is_ok();` … `if !enabled && state == crate::sensors::SENSOR_STATE_ACTIVE { return None; }`。多台同時連線是已支援情境（tests/mobile_loop.rs:2700 `with_two_phones_an_act_must_name_its_target_and_the_receipt_records_it`）；tray 讀的就是 `status.activeSensors`（src-tauri/src/tray.rs:263、host_safety.rs:95）。

**建議修法**：斷線時只在「沒有其他手機仍在使用該高風險受器」時才停用全域受器；否則保留啟用並只清掉斷線那台的 `mic_since`。或者讓 `mobile_active_sensors` 在受器被停用但仍有手機自報串流時輸出 `stop-unknown` 狀態（誠實：桌面不再收資料 ≠ 手機停了），使指示燈不會無聲熄滅。

**回歸測試**：Pair and connect two iPhones A and B, enable+consent iphone.mic-level, have both send observation micLevel:true (mic_since set on both conns); disconnect A only; assert rt.mobile_active_sensors() (or status.activeSensors) still contains B's entry with state \"active\", and assert an audit/event log contains no false claim that B stopped — currently it will incorrectly vanish from the list with zero SensorStopped event for B while B's iPhone is still recording.

### low（20 項）

#### F-c3d1786-20260903T124638Z-companion-gameplay-036

- **標題**：「生命底層微動作排程」是死碼：scheduleMicroAction／SHU_MICRO_ACTIONS 只有測試在呼叫
- **維度**：companion-gameplay
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/companion/behavior.ts:232

**主張**：behavior.ts 的模組註解宣告 Behavior Runtime 有三層，第一層是「生命底層：微動作排程（眨眼與各種閒置小動作；動作清單由角色 tables 注入）——本模組」，並由 `scheduleMicroAction()` 實作、`SHU_MICRO_ACTIONS`（8 個微動作，含 frameSlice 局部動作）提供資料。但整個 production 路徑沒有任何地方呼叫它：CompanionApp 只用了 `layeredMicroMotion()`（連續視線／耳朵疊加）與 `InteractionDirector.tick()`（ambient 變體）。這一層等於「只有資料結構與測試、沒有行為」，而 behavior.test.ts 的 6 個微動作測試會讓人以為它在跑。

**證據**：`grep -rn "scheduleMicroAction\|SHU_MICRO_ACTIONS" src/` 的非定義處全部落在 `src/test/behavior.test.ts`（124/127/135/147/164/178/179/191/203 行）與 `src/test/character-shu-adapter.test.ts:243`；`src/companion/CompanionApp.tsx`、`src/character/adapters/shu.ts` 皆無引用。behavior.ts:1-14 模組註解列出三層並標「本模組」。CompanionApp.tsx:1332-1339 只呼叫 `layeredMicroMotion(...)`；1390-1402 只呼叫 `directorRef.current.tick(...)`。

**建議修法**：二擇一：(a) 在 500ms pump 裡把 `scheduleMicroAction(behaviorState, ctx, microRng, tables.microActions)` 接上（產出 `performing` transient，含 frameSlice），讓「耳先動」這類局部微動作真的會出現；(b) 刪除 `scheduleMicroAction`／`SHU_MICRO_ACTIONS` 與其測試，並修正 behavior.ts 的三層註解，避免測試數字給出不存在的覆蓋率印象。

**回歸測試**：在 CompanionApp 的整合測試中 mock/spy scheduleMicroAction（或改以行為觀測：長時間 ambient idle 且無互動時，斷言渲染出的微動作序列中出現至少一個 SHU_MICRO_ACTIONS 定義的 id/animation），目前無此類斷言即會通過（因為根本沒被呼叫），一旦真正接上排程器則需更新該測試以覆蓋呼叫路徑。

#### F-c3d1786-20260903T124638Z-companion-gameplay-037

- **標題**：連戳（poked-rapid）這個高頻反應只有 1 個變體，不符 §5.2「每個高頻反應至少 3～6 個變體」
- **維度**：companion-gameplay
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/character/adapters/shuTables.ts:74

**主張**：§5.2 要求「每個高頻反應至少 3～6 個變體，具防重複與冷卻」。`SHU_REACTIONS` 只給了 `poked`（3 個）與 `lifted`（3 個）變體池，而同樣由高頻輸入觸發的 `poked-rapid`（1.4 秒內第 3 次以上點擊）只有單一表情；因為 `planClickReaction` 對 rapid 沒傳 `cooldownMs`，它吃 8 秒的預設冷卻，所以連續連戳時第一次之後全部退回 `poked` 池——使用者連戳 30 秒只會看到同一個 `poked-rapid` 演出（每 8 秒一次）。

**證據**：shuTables.ts:72-74 `poked: ["poked","poked-flinch","poked-grin"], lifted: ["lifted","lifted-curious","lifted-wriggle"], "poked-rapid": "poked-rapid"`；同檔 56-57 行註解自述「高頻反應（單擊 poked、拖起 lifted）各有 3 個變體（spec §5.2）」，未把連戳算進高頻。gameFeel.ts:160 `input.director.reactDetailed("poked-rapid", input.nowMs, 2_200, input.rng)` —— 沒有 `opts.cooldownMs`，於是 director.ts:272 套用 `REACTION_COOLDOWN_MS = 8_000`（director.ts:129）。director.ts:252 的防重複 `ready.length > 1 && last` 對單元素池不生效。

**建議修法**：把 `poked-rapid` 擴成 3～6 個變體（例如 poked-rapid／pretend-not-hear／poked-flinch 的連戳版），或在 `planClickReaction` 對 rapid 傳一個較短的 `cooldownMs` 並在冷卻中改播不同的既有變體；同時更新 shuTables 的註解，把連戳列入高頻反應清單。

**回歸測試**：對 director.reactDetailed 寫一個測試：在 8 秒窗內以 rapid=true 持續呼叫 reactDetailed("poked-rapid", ...) 多次（間隔小於冷卻），斷言每次 action!==null 時回傳的 expression 有 ≥3 個相異值可輪替；目前實作因 SHU_REACTIONS["poked-rapid"] 只有單一字串變體而必然失敗。

#### F-c3d1786-20260903T124638Z-rig-renderer-059

- **標題**：Reduced Motion 不是文件宣稱的「真靜態」：8 個表情在 reduced 下仍每 2.2–5.4 秒自動眨眼
- **維度**：rig-renderer
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/companion/rig/timeline.ts:440

**主張**：paramsAt 的自動眨眼區塊只看 `this.expr.autoBlink`，沒有被 `!this.reducedMotion` 包住（enter/loop/exit/crossfade/微動作/粒子都有被關掉）。Reduced Motion 下 idle、sit、lie-flat、spaced-out、legswing、tailhug、await-player、quiet 這 8 個表情仍會週期性把 eyeOpen 從 1.0 壓到 0.05 再回來（150ms 一次、間隔 2.2–5.4 s）。timeline.ts 檔頭自己寫「Reduced Motion：只保留 hold（無 enter/loop/exit）」，docs/DESKTOP-GUIDE.md:324「真靜態，不是慢動作」、docs/v05-phase7-final-report.md:96「Reduced Motion 真靜態」、docs/v05-capability-gap-matrix.md:120「執行中可切換、真靜態」都與實際行為不符。

**證據**：timeline.ts:440-454 `if (this.expr.autoBlink) { if (now >= this.nextBlinkAt) {…} … params = clampParams({ ...params, eyeOpen: params.eyeOpen * (1 - dip * 0.95) }) }`（對照 :364 particles 歸零、:393 target=base、:407 略過 crossfade、:416 略過 blendPose、:424 略過微動作都有 reducedMotion 守門）。實測 setReducedMotion(true)+setAnimation("idle")，20 秒掃描唯一會變動的通道是 eyeOpen（最大變化 0.95）：3800ms:1.000 → 3817:0.788 → 3834:0.577 → 3850:0.365 → 3867:0.154。autoBlink 表情清單：idle,sit,lie-flat,spaced-out,legswing,tailhug,await-player,quiet。

**建議修法**：二選一並讓文件與程式一致：(a) 在 :440 加上 `&& !this.reducedMotion` 讓 Reduced Motion 真的完全靜止；或 (b) 承認眨眼是刻意保留（SHU_AMBIENT_VARIANTS 的 blink 就標了 reducedMotionOk:true），把 DESKTOP-GUIDE/phase7 report/capability matrix 的「真靜態」改成「只保留原地眨眼」，並補一條回歸測試釘死「reduced 下除 eyeOpen 外所有通道恆定」。

**回歸測試**：In ExpressionTimeline, call setReducedMotion(true), setAnimation("idle", 0), then sample paramsAt(t) across a 20s range at small steps and assert every param (including eyeOpen) stays exactly equal to paramsAt(0)'s hold values — i.e. wrap the autoBlink block at timeline.ts:440 in `if (!this.reducedMotion) { ... }` and assert the test now shows zero variation in eyeOpen.

#### F-c3d1786-20260903T124638Z-rig-renderer-060

- **標題**：behavior.scheduleMicroAction 與 SHU_MICRO_ACTIONS 在生產路徑完全沒有呼叫端（死碼），且其 frameSlice 在 rig 管線無效
- **維度**：rig-renderer
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/character/adapters/shuTables.ts:126

**主張**：shuTables.ts:125 註解宣稱 SHU_MICRO_ACTIONS 是「生命底層微動作（behavior.scheduleMicroAction 的候選清單）」，但 scheduleMicroAction 在整個 src/ 只有 test/behavior.test.ts 呼叫，生產程式沒有任何呼叫端——這一層行為在執行期不存在（實際的 ambient 由 InteractionDirector 的 SHU_AMBIENT_VARIANTS 排程，而該表不含 notice/routing）。此外表中 ear-flick／gaze-around 用 `frameSlice: [0,1]` 表示「只播前兩幀」，但 rig 管線的 frameSlice 只在 name==="success" 時被使用（resolveRigAnimation 只用它區分 claimed/verified），其他名稱一律播完整表情；若哪天接上，gaze-around 會播完整的 routing「找資料」表情（hold coreGlow 0.7、tailTip 0.8＝§4.2 的核心／工具狀態燈），在沒有任何 agent 工作時點亮工作狀態通道。

**證據**：`grep -rn "scheduleMicroAction" src/` 只命中 companion/behavior.ts:232（定義）與 test/behavior.test.ts:124-191；shuTables.ts:126-135 SHU_MICRO_ACTIONS 內容；timeline.ts:167-173 `resolveRigAnimation(name, frameSlice)` 只有 `if (name === "success") wanted = frameSlice ? "success-claimed" : "success-verified"` 會讀 frameSlice；expressions.ts:976-991 routing.hold = { coreGlow: 0.7, tailTip: 0.8, armPose:"pocket", … }。

**建議修法**：要嘛把 scheduleMicroAction 真的接進 CompanionApp 的 tick（並先把 gaze-around 換成非狀態表情、或讓 rig 支援真正的段落切片），要嘛刪掉這張表與 scheduleMicroAction 並在 CHANGELOG 已知限制補一行，不要讓註解宣稱一個執行期不存在的分層。

**回歸測試**：在 `director.ts` 或任一生產程式路徑中，斷言 `SHU_MICRO_ACTIONS`／`scheduleMicroAction` 有非測試呼叫端（例如靜態掃描 import graph，或直接 `grep -c` 生產檔案中的引用數應 > 0）；若要接線，另加一條測試：對非 "success" 的 `animation` 傳入 `frameSlice`，斷言 `resolveRigAnimation` 產出的表情確實只播放切片幀，而不是完整表情（目前會失敗，因為 timeline.ts 只在 name==="success" 讀取 frameSlice）。

#### F-c3d1786-20260903T124638Z-director-pipeline-045

- **標題**：CompanionApp 硬寫死角色表情 id "blink"，違反「頁面不得引用角色表情名」不變量，且對非 shu 角色會退化成安靜時段的整段表演
- **維度**：director-pipeline
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/companion/CompanionApp.tsx:1405

**主張**：安靜時段（quiet hours／勿擾／本機安靜期）Director 只允許「就地眨眼」，CompanionApp.tsx:1405 用 `action.expression === "blink"` 判斷是否走 rig 的 blinkNow() 就地眨。但 Director 的眨眼表情是由角色 adapter 經 DirectorTables.blink 注入的（director.ts:68 `blink: { expression: string; durationMs: number } | null`），host 不該知道它叫什麼。任何一個 blink.expression 不是字面 "blink" 的角色，安靜時段的眨眼會落到 else 分支被套成一般 performing transient——正是同一段註解（CompanionApp.tsx:1401-1403）說要避免的結果：「套成一般表演的話，角色會從安靜陪伴的坐姿彈回中性站姿」。這也直接違反 CLAUDE.md 的「Runtime／頁面不得再引用小樞的部位、表情名或 pack id」與 director.ts:11-14 的 engine-neutral 宣告。目前只有 shu 提供 tables 且它的眨眼 id 剛好是 "blink"（shuTables.ts:89 SHU_BLINK），所以是潛伏缺陷而非現行可見錯誤。

**證據**：CompanionApp.tsx:1404-1406
```
const blinkedInPlace =
  gate.quiet && action.expression === "blink" && rig?.blinkNow?.() === true;
```
`grep -n '"blink"' src/companion/{CompanionApp.tsx,director.ts,machine.ts,behavior.ts,renderer.ts}` → 只有 CompanionApp.tsx:1405 一筆（director/machine/behavior/renderer 都沒有任何角色表情 id，符合 engine-neutral）。
director.ts:67-68 註解與型別：「安靜時唯一允許的『就地眨眼』（null＝安靜時完全不動）」`blink: { expression: string; durationMs: number } | null;`
shuTables.ts:89 `export const SHU_BLINK = { expression: "blink", durationMs: 400 } as const;`
CLAUDE.md 不變量：「Runtime／頁面不得再引用小樞的部位、表情名或 pack id」。

**建議修法**：把 tables 的 blink 表情名帶到 host：例如在 CompanionApp 保存一份 `blinkExprRef`（wireAdapter 時從 `shu?.directorTables.blink?.expression` 取），判斷改成 `gate.quiet && action.expression === blinkExprRef.current`；或更乾淨地讓 DirectorAction 帶一個 `kind: "blink"` 標記，host 只看標記不看 id。

**回歸測試**：Register a second fake DirectorTables (e.g. `{ ...EMPTY_DIRECTOR_TABLES, blink: { expression: "eyes-shut", durationMs: 300 }, ambient: [{expression:"eyes-shut", ...}], isPlayable: () => true }`) as the active character's tables, drive the quiet-hours ambient tick until Director returns that blink action, and assert the companion's pose/base stays at the quiet sitting state (not reset to a neutral "performing" transient) — this fails today only in intent (can't literally fail via the current test harness since no second adapter exists yet), but a unit test directly on the `blinkedInPlace`-equivalent logic extracted with an injectable "current blink expression id" would catch the literal-`"blink"` coupling.

#### F-c3d1786-20260903T124638Z-director-pipeline-046

- **標題**：InteractionDirector.score()（規格 §6 管線的 Utility Scoring 段）是死碼，Director 的決策完全沒有經過效用評分
- **維度**：director-pipeline
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/companion/director.ts:186

**主張**：規格 §6 的 Director 管線是「… → Attention Manager → Character Context → Utility Scoring → Behavior Intent → Action Scheduler → …」，director.ts:185-188 提供 `score(cls, ctx)` 作為這一段的入口，但整個 repo 沒有任何呼叫端。Director 的 tick()／reactDetailed() 只用 hazard 抽樣、冷卻與防重複做決定，完全不看 scoreEvent 的重複懲罰、alreadyResponded、doNotDisturb 或 relevance/novelty。scoreEvent 唯一的真實用途在 machine.ts:151-152 的同優先 transient 平手判定，而那裡的 ctx 是硬寫死的常數（recentSameClass:0、doNotDisturb:false、relevance:1），退化成「同優先一律換人」的固定結果。所以「Interaction Director 具備 Utility Scoring」這個規格項目在實作上沒有落地。

**證據**：director.ts:185-188
```
/** 事件效用評分（供上層決定是否值得反應；安全類不受壓制）。 */
score(cls: EventClass, ctx: EventScoreContext): number {
  return scoreEvent(cls, ctx);
}
```
`grep -rn "\.score(" apps/interaction-desktop/src apps/interaction-desktop/e2e` → 0 筆（含所有測試）。
`grep -rn scoreEvent apps/interaction-desktop/src` → 只有 behavior.ts 定義、director.ts:16/187、machine.ts:10/151-152、behavior.test.ts。
machine.ts:143-153 的 base 常數：`{ recentSameClass: 0, alreadyResponded: false, interruptible: true, doNotDisturb: false, relevance: 1, novelty: 0 }`，next 只多 novelty:1 → 同優先永遠 replace。
Director 的實際決策路徑：director.ts:277-350 tick()（hazard/minRelax/cooldown/recent）與 director.ts:231-274 reactDetailed()（playable/cooldown/lastVariant），兩者都沒有呼叫 score/scoreEvent。

**建議修法**：二選一並讓文件與程式一致：(a) 真的接上——CompanionApp 在把事件變成 behaviorIntent 前先用 director.score() 過濾（把 recentSameClass、alreadyResponded、doNotDisturb=gate.quiet、relevance/novelty 餵進去，<=0 就不反應），machine.ts 的平手判定也改用真實 ctx；或 (b) 刪掉 director.score()，並在 docs/CHANGELOG 誠實說明 Utility Scoring 目前只用於 machine 的同優先平手，Director 的節流靠 hazard＋冷卻＋防重複。

**回歸測試**：Instrument scoreEvent with a call counter, run a scripted sequence of director.tick()/reactDetailed() calls across varied DirectorContext states (quiet, doNotDisturb, high recentInterruptions, repeated same-class events), and assert the counter stays 0 — proving Director's real decisions never invoke Utility Scoring.

#### F-c3d1786-20260903T124638Z-ia-settings-019

- **標題**：活動紀錄的「重新驗證」是無回饋的 floating promise
- **維度**：ia-settings
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/pages/ActivityPage.tsx:131

**主張**：`onClick={() => api.verifyAction(receipt.actionId)}` 沒有 await、沒有 catch、也沒有任何結果回報。驗證失敗時使用者看不到任何訊息（只有一個未處理的 promise rejection），成功時也沒有任何確認——這正是誠實階梯要求「結果未知不得靜默」的反面。同頁的「取消」按鈕（:117-128）就有 try/catch＋`setCancelMsg`，證明本頁已有正確樣板。

**證據**：ActivityPage.tsx:130-132 `{["acknowledged", "uncertain", "completed"].includes(receipt.currentStatus) && (<button onClick={() => api.verifyAction(receipt.actionId)}>重新驗證</button>)}`；對照 ActivityPage.tsx:116-128 的「取消」按鈕有 `try { await api.cancelAction(...); setCancelMsg("已要求取消。") } catch (e) { setCancelMsg(`取消失敗：${e}`) }`。

**建議修法**：比照「取消」按鈕加 try/catch 與狀態訊息（成功也要說「已重新查驗，結果如下／仍不確定」，不得靜默）。

**回歸測試**：Render ActivityStory for a receipt with currentStatus "acknowledged", mock api.verifyAction to reject, click 重新驗證, assert the UI shows an error/rejection message (or that the click handler is awaited/caught) rather than leaving an unhandled rejection and unchanged UI.

#### F-c3d1786-20260903T124638Z-ia-settings-020

- **標題**：「現在」頁詳細狀態的工作階段開始／結束沒有失敗回報
- **維度**：ia-settings
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/pages/HomePage.tsx:315

**主張**：「開始工作階段」`await api.sessionStart("desktop", [])` 在 async onClick 裡沒有 try/catch，「結束工作階段」`api.sessionStop()` 是裸的 floating promise。任一失敗都不會出現在畫面上（同頁其他操作如暫停、停止感測都有 error state），使用者會以為狀態已改變。

**證據**：HomePage.tsx:312-323：`<button onClick={async () => { await api.sessionStart("desktop", []); onNavigate("home"); }}>開始工作階段</button>` 與 `<button onClick={() => api.sessionStop()}>結束工作階段</button>`；對照同檔 :63-72 的 `tryPause` 與 :76-92 的 `stopSensors` 都有錯誤路徑並寫進 `pauseError` / `sensorNotice`。

**建議修法**：兩顆按鈕都包 try/catch，失敗寫進既有的 `pauseError`（或新增 sessionError）並以 role="alert" 顯示。

**回歸測試**：Mock api.sessionStart to reject (e.g. `jest.spyOn(api, "sessionStart").mockRejectedValue(new Error("network"))`), render HomePage in the no-session state, click "開始工作階段", and assert the component renders a visible error affordance (e.g. a `role="alert"`/error text) instead of only producing a console unhandled-rejection with no DOM change.

#### F-c3d1786-20260903T124638Z-ia-settings-021

- **標題**：全域搜尋在一般模式印出知識更新的原始 triggeredBy 值
- **維度**：ia-settings
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/components/GlobalSearch.tsx:216（輔助證據中的 :157 應為 :196，pack.id 的顯示）

**主張**：知識收據項目的標籤在一般模式是 `知識更新：${String(receipt.triggeredBy)}`，直接顯示 runtime 的 kebab-case 原始值（`user-correction`／`review-overdue`／`conflict-detected`／`task-experience`／`human-review`）。HomePage 已有 `knowledgeTriggerLabel` 專門翻譯這個欄位並註明「認不得的值不外洩原始字串」，此處沒用。

**證據**：GlobalSearch.tsx:172-181 的 `label: `${advanced ? "知識收據" : "知識更新"}：${String(receipt.triggeredBy)}``；helper 在 HomePage.tsx:387-401（`KNOWLEDGE_TRIGGER_LABEL` 與 `export function knowledgeTriggerLabel`），唯一呼叫點是 HomePage.tsx:367。同函式區塊 GlobalSearch.tsx:157 也把 `String(pack.id)` 當一般模式的 detail 直接顯示。

**建議修法**：把 `knowledgeTriggerLabel` 提到共用模組並在此使用；pack id 只在 advanced 顯示。

**回歸測試**：在 advanced=false 模式下對 GlobalSearch 的 receipts 分支塞入 triggeredBy="user-correction" 的假資料，斷言產生的 label 字串不含 "user-correction"（應含「你的更正」或等價人話翻譯，而非原始 kebab-case）。

#### F-c3d1786-20260903T124638Z-memory-ui-006

- **標題**：一般模式的「關於我的記憶」暴露完整 10 層記憶 taxonomy，並預設把所有分層混列在同一個標題下
- **維度**：memory-ui
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/pages/MemoryKnowledgePage.tsx:128

**主張**：spec §11 要求一般 UI 只顯示「關於我的記憶／{角色}學會的知識／素材與來源」三項、技術分層移到進階。分頁確實只有三個，但「關於我的記憶」區塊在 advanced=false 時仍無條件渲染一個列出全部 10 層的下拉（含「Skill」「Agent 交接」「對話暫存」「領域 Know-how」「角色核心」「角色經歷」），且預設 layer="" 會把所有分層的項目都列在「關於我的記憶」標題下——既外洩後端 taxonomy，也讓「領域知識」「Agent 交接」等非「關於我」的項目被貼上「關於我的記憶」的標籤；下拉裡的「角色經歷」更與正上方文案「角色記憶…不會混進這裡」互相打架。

**證據**：apps/interaction-desktop/src/pages/MemoryKnowledgePage.tsx:12-23 LAYER_LABEL 定義 10 層（含 `skill: "Skill"`、`"agent-handoff": "Agent 交接"`、`"character-memory": "角色經歷"`）。
同檔 125-136（MemorySection 內、沒有任何 `advanced &&` 條件）：
```
<label className="field-label">分層
  <select value={layer} …>
    <option value="">全部</option>
    {Object.entries(LAYER_LABEL).map(([id, label]) => (<option key={id} value={id}>{label}</option>))}
```
預設 `const [layer, setLayer] = React.useState<string>("")`（MemoryKnowledgePage.tsx:98）→ `api.memoryList(undefined, 200)` 取全部分層；MemoryCard 以 `LAYER_LABEL[item.layer]` 顯示（同檔 195-197）。
相鄰文案（113-118）：「{name}跟你玩耍、互動累積的角色記憶…不會混進這裡」。
現有一般模式測試（regressions-v05.test.tsx:411-427）只斷言不出現「候選／收據／Context Bundle」，不涵蓋這些分層字樣。

**建議修法**：一般模式把分層下拉收進 advanced，或改成只提供人話的兩三個群組（我說過的偏好／工作脈絡／暫存），並讓一般模式預設只列 user-memory 相關分層；「角色經歷」層若無寫入者就不要出現在選單。

**回歸測試**：Render `<MemoryKnowledgePage advanced={false} />` with `memoryList` mocked, open the "關於我的記憶" tab, and assert `container.textContent` contains none of the LAYER_LABEL technical values ("Skill", "Agent 交接", "對話暫存", "領域 Know-how", "角色核心", "角色經歷", "世界觀", "任務記憶", "領域知識") — currently fails; fix by gating the layer `<select>` behind `advanced` (or restricting the default/general listing to the `user-memory` layer) and gating the cross-layer merge accordingly.

#### F-c3d1786-20260903T124638Z-link-transports-052

- **標題**：wait_reply 靜默吞掉 broadcast lag，把「答覆可能被丟掉」講成「裝置沒回」
- **維度**：link-transports
- **Severity**：low（原報 low）
- **file:line**：crates/interaction-adapter-declarative/src/protocol.rs:553

**主張**：cmd／cancel／read／stop-all 四條請求路徑用的 wait_reply 對 RecvError::Lagged 直接 continue，完全不記錄丟了幾則；逾時訊息因此宣稱「no ack for action X」「device did not answer read」。但 WaitError 的文件（同檔 838-848）明寫 lagged>0 表示「答覆可能已經送到卻被丟了，呼叫端不該把它講成『裝置沒回』」，而握手路徑用的 wait_for 確實有做這件事。同一份收據 detail 會被人與 AI 讀成「裝置沒反應」，實際可能是 host 端 64 格 broadcast 追不上（裝置每 5 s 主動推 state、按鈕邊緣還會加推）。

**證據**：protocol.rs:547-557 `match tokio::time::timeout(remaining.min(RESET_POLL), rx.recv()).await { ... Ok(Err(broadcast::error::RecvError::Lagged(_))) => continue, ... }`；產生的訊息 protocol.rs:644-646 `"no ack for action {action_id} — outcome UNKNOWN (not retried: ...)"` 與 :726 `LinkError::Timeout("device did not answer read".into())`。對照 protocol.rs:889-921 wait_for_with 累加 lagged 並回 WaitError::TimedOut{lagged}，:853-857 Display 產生「timed out (N message(s) dropped while waiting; the reply may have been lost)」，:925-932 handshake_wait_error 把它寫進 detail。BROADCAST_CAP=64（serial.rs:24、mqtt.rs:19、ble.rs:23）。

**建議修法**：讓 wait_reply 也累加 lagged，並在 WaitEnd::TimedOut 帶出數量；命令／讀取的逾時文案在 lagged>0 時改成「等待期間有 N 則訊息被丟棄，答覆可能已到但遺失」，不要說 device did not answer。

**回歸測試**：

#### F-c3d1786-20260903T124638Z-link-transports-054

- **標題**：serial reader thread：drop(writer) 解不開 dup fd 上的 blocking read，pty／file fallback 每次重連洩漏一條 detach 的執行緒
- **維度**：link-transports
- **Severity**：low（原報 low）
- **file:line**：crates/interaction-adapter-declarative/src/serial.rs:289

**主張**：reader 與 writer 是同一個 fd 的 try_clone（dup(2)）。POSIX 下關掉其中一個 dup 不會讓另一個 dup 上的 blocking read 返回，所以 `drop(writer); // 關掉 fd，讓 reader 的 blocking read 解除` 這個前提不成立。serialport 路徑有 200 ms 讀取逾時所以還好，但 ENOTTY file fallback（pty 模擬器與 CLI E2E 走的就是它）的 read 沒有逾時：裝置沉默時 reader 會一直卡著，500 ms 寬限後被 detach，每次重連／每次 shutdown 就留下一條永遠不會結束的執行緒。RawLink::shutdown 的契約寫的是「停止重連、回收執行緒／task」，實際回收不到。

**證據**：serial.rs:214-221 `PortHalves::File(file) => match file.try_clone() { Ok(r) => (Box::new(r), Box::new(file)), ... }`（std File::try_clone = dup，兩個 File 共用同一個 open file description，各自持有獨立 fd）；serial.rs:36-38 常數註解自己承認「pty fallback 的 read 沒有逾時，裝置完全沉默時可能還卡在 read」；serial.rs:289-307 `drop(writer); // 關掉 fd，讓 reader 的 blocking read 解除` 之後是 500 ms 輪詢 is_finished()，逾時就 `tracing::debug!(... "detaching it")`；serial.rs:204-206 的 RawLink trait 契約（protocol.rs:204-206）「主動關閉連線：停止重連、回收執行緒／task」。既有測試 pty_link_opens_and_shutdown_stops_the_supervisor 只斷言 supervisor 收工，沒有斷言 reader 執行緒被回收。

**建議修法**：file fallback 改用非阻塞 fd（O_NONBLOCK ＋ poll/select 帶逾時）或以 shutdown flag 可中斷的讀法；至少把「pty fallback 的 reader 可能被 detach 且不會回收」寫進 RawLink::shutdown 的契約與已知限制，並在 detach 時計數以便偵測洩漏。

**回歸測試**：On the pty-fallback path, force two reconnect generations while the simulator stays silent and its master fd stays open, then after the second connect assert (e.g. via a named-thread probe for `serial-read-<port>`, or an instrumented completion counter in pump_lines) that only one reader thread from the current generation is alive — proving the prior generation's reader was actually reclaimed rather than merely detached.

#### F-c3d1786-20260903T124638Z-agent-honesty-026

- **標題**：非 gateway session 真的被 agent fetch 時不發 fetched taxonomy 事件，角色一直停在 queued
- **維度**：agent-honesty
- **Severity**：low（原報 low）
- **file:line**：crates/interaction-runtime/src/agents.rs:708-758 (mailbox_fetch)

**主張**：規格 §7.4 要求 queued／fetched／working… 每一態都由真實事件驅動。`fetched` 目前只在 gateway 路徑（gateway.rs:714）發出；v0.3 輪詢流程的 agent 以 `MailboxReader::Agent` 呼叫 `mailbox_fetch` 真的取走任務、蓋了 `delivered_at`、也推進了委派 receipt，卻不發 `agent.session.state = fetched`。角色與 UI 因此停在 created/queued（「準備中」），直到 agent 自己回報 task-started 才跳到 working——fetched 這一態對非 gateway agent 等於不存在。

**證據**：agents.rs:706-757 `mailbox_fetch`：`m.delivered_at = Some(now); newly_delivered = true; ... acked.push(...)`，整段沒有任何 `emit_agent_session_state` 呼叫；唯一的 fetched 發射點是 gateway.rs:714 `self.emit_agent_session_state(session_id, &entry.record.agent_id, "fetched")`（`grep -rn emit_agent_session_state crates/` 只有 created/fetched/verified/taxonomy/timed-out/cancelled|closed/unknown 七個站點）。character.rs:387 的投影表已經備好 `"fetched" => (I::Think, T::Working)`，只是沒有事件餵它。

**建議修法**：在 `mailbox_fetch` 的 `newly_delivered` 分支（人類 peek 不算）補發 `fetched`，與 gateway 路徑一致。

**回歸測試**：Create a non-gateway agent session (agent_id not "codex"/"claude-code"), mailbox_send a task, call rt.mailbox_fetch(&sid, ToSession) as the Agent reader (assert delivered_at becomes Some), then assert the session's AgentSessionState taxonomy events (filter EventType::AgentSessionState by agentSessionId) contain "fetched" — currently they contain only "created".

#### F-c3d1786-20260903T124638Z-protocol-conformance-031

- **標題**：README 指令參數段寫 servo.move angle 0..180，與硬限制表及兩端實作的 10..170 矛盾
- **維度**：protocol-conformance
- **Severity**：low（原報 low）
- **file:line**：firmware/esp32-companion/README.md:170

**主張**：README〈協定訊息一覽〉後的「指令參數」一行寫 `servo.move {angle 0..180}`，但同一份 README 的〈韌體硬限制表〉(222) 寫 `angle clamp 10..170`，韌體與模擬器實作也都是 10..170。照第一段寫 adapter spec 的人會以為 0 與 180 可用，實際被靜默 clamp 成 10／170（ack.applied 才看得出來）——硬限制表與協定表在同一份文件裡自相矛盾。

**證據**：firmware/esp32-companion/README.md:169-170 「指令參數：… `servo.move {angle 0..180}`」 vs README:222 「| `servo.move` | angle clamp **10..170**；每 **300ms** 最多一次 |」。實作：esp32-companion.ino:95-96 `SERVO_MIN_ANGLE = 10` / `SERVO_MAX_ANGLE = 170`（用於 656-658 的 readIntParam）；scripts/esp32-serial-sim.py:65-66 相同常數（用於 327）。

**建議修法**：README:170 改成 `servo.move {angle 10..170}`，或註明「超界會被 clamp，實際值見 ack.applied 與硬限制表」。

**回歸測試**：非程式碼問題，無法用自動化測試釘死；建議文件審閱腳本（如 grep 比對 README 內所有 "0..180" 與 "10..170" 提及 servo.move 之處）確保一致，或直接把第170行改成與222行一致的 10..170。

#### F-c3d1786-20260903T124638Z-perf-claims-011

- **標題**：StageRenderer.pause() 的契約說「不再回報互動框」，但 500ms pump 無條件 reportHitRect(true)，隱藏／suspend 後仍持續 IPC
- **維度**：perf-claims
- **Severity**：low（原報 low）
- **file:line**：apps/interaction-desktop/src/companion/rig/stage.ts:417

**主張**：pause() 的 doc comment 宣稱「取消 rAF、物理與粒子都停下，也不再回報互動框」，但 reportHitRect() 本身沒有任何 paused 判斷，而 CompanionApp 的 500ms pump 每一拍都以 force=true 呼叫它。因此角色被隱藏／CPP suspend 之後，WebView 仍每 500ms（頁面隱藏時瀏覽器節流後 ≥1s）發一次 Tauri IPC `companion_hit_rect`，並在 Rust 端取窗、上鎖、重評點擊穿透。這與「隱藏後計時器真的停」的預期不符，也讓 pause() 的契約敘述不成立。

**證據**：stage.ts:417-419 註解「暫停主迴圈（CPP §7 hide／suspend）：取消 rAF、物理與粒子都停下，**也不再回報互動框**」；stage.ts:525-534 `reportHitRect(force = false)` 只檢查 `this.hitRectCb` 與節流政策，沒有 `if (this.paused) return;`。CompanionApp.tsx:1344-1346 在 500ms pump 內 `const stage = stageRef.current; // 心跳：rAF 被系統節流／視窗隱藏時，至少每 500ms 還有一次互動框回報。 stage?.reportHitRect(true);`，且該 pump 的 cleanup 只在 effect 卸載時執行（CompanionApp.tsx:1417-1419），不隨 suspend 停止。落地端 src-tauri/src/lib.rs:2335-2366 `companion_hit_rect` 會取窗、clamp、上鎖寫入並呼叫 apply_companion_clickthrough（後者於 lib.rs:2236-2238 才因 `!win.is_visible()` 提早返回，所以 IPC 與鎖仍每拍發生）。

**建議修法**：兩者擇一並讓文件與程式一致：(a) 在 reportHitRect 開頭加 `if (this.paused) return;`，並把 500ms 心跳改成只在未暫停時送；或 (b) 保留心跳但修正 pause() 的註解，明說暫停只停 rAF／物理，互動框心跳仍在，並在 pump 內以 `stageRef.current?.isPaused()` 跳過。

**回歸測試**：建 StageRenderer 並注入假 hitRectCb，呼叫 pause() 後直接呼叫 reportHitRect(true)，斷言 callback 未被觸發（目前會被觸發，測試會失敗，證明缺陷存在）。

#### F-c3d1786-20260903T124638Z-docs-claims-073

- **標題**：FEATURES.md 把讀者指向不存在的 CHANGELOG [Unreleased] 章節，且 CHANGELOG 檔尾的 Unreleased 比較連結仍指向 v0.3.0
- **維度**：docs-claims
- **Severity**：low（原報 low）
- **file:line**：docs/FEATURES.md:129

**主張**：FEATURES.md 的「測試」列仍寫 v0.4.1 基線數字，並要讀者去看「CHANGELOG [Unreleased]」；CHANGELOG 已無 [Unreleased] 章節，僅剩檔尾一個過期的比較連結。

**證據**：docs/FEATURES.md:129「v0.4.1 基線 Rust 336、Vitest 94、Playwright 23、Tauri 4、CLI E2E 51（v0.5 最新數字見 `docs/v05-recovery-matrix.md` 與 CHANGELOG [Unreleased]）」；FEATURES.md:181 又寫「以 docs/acceptance-evidence.md v0.5 最新章節（Phase 8）的實跑數字為準」。CHANGELOG.md 第 9 行的章節標題已是「## [0.5.0] — 2026-09-03（準備中）」，全檔唯一的 Unreleased 出現在 :846 `[Unreleased]: https://github.com/miles990/adaptive-interaction/compare/v0.3.0...HEAD`——比較基準是 v0.3.0，而 `git tag` 顯示最新發布為 v0.4.1，:846-849 的連結定義也缺 v0.4.0／v0.4.1 兩列。

**建議修法**：FEATURES.md:129 改指「CHANGELOG [0.5.0]」與 docs/releases/v0.5.0-test-matrix.md，並在最終數字填回後同步更新該列；CHANGELOG 檔尾補上 [0.5.0]／[0.4.1]／[0.4.0] 連結定義並把 [Unreleased] 的比較基準改成 v0.4.1（或整段移除）。

**回歸測試**：

#### F-c3d1786-20260903T124638Z-safety-invariants-078

- **標題**：SSE 把 agent.session.state 事件送給 agent token，違反 sse.rs 自己宣告的「SSE 必須與 REST 同界線」
- **維度**：safety-invariants
- **Severity**：low（原報 low）
- **file:line**：crates/interaction-api/src/lib.rs:436-449 (GET branch of agent_request_allowed, was cited 432-445); crates/interaction-api/src/lib.rs:465-467 (interrupt allow-list, was cited 458-460); crates/interaction-runtime/src/agents.rs:788-791 (emit_agent_session_state, was cited 789-792) — minor line drift only, content matches exactly; crates/interaction-api/src/sse.rs:22 matches exactly as cited

**主張**：`agent_request_allowed` 明確禁止 agent token GET 任何 `/v1/agent-sessions*`，但 `event_allowed` 的排除清單漏了 `EventType::AgentSessionState`，所以 legacy agent token 只要開 `GET /v1/events` 就能持續收到所有 agent session 的 `{agentSessionId, agentId, state}`。這正是該函式註解說要避免的「SSE 變成被 REST 拒絕的 payload 的側通道」。搭配同檔允許的 `POST /v1/agent-sessions/{id}/interrupt`（`agent_request_allowed` 對任何 id 都放行，無擁有權檢查），agent 平面可以枚舉並中斷不屬於自己的 session。

**證據**：sse.rs:22-48 `event_allowed` 的 `!matches!(...)` 清單列了 KnowledgeUpdated／ConsentChanged／PolicyChanged／Sensor*／Presentation*／AiAssist*／Character*，但沒有 `AgentSessionState`；同檔 25-27 註解：「The legacy Agent token has no knowledge/memory/presentation/sensor authority. SSE must obey the same boundary as REST instead of becoming a side channel for payloads rejected by those route families.」。api/src/lib.rs:432-445 `agent_request_allowed` 的 GET 分支含 `&& !path.starts_with("/v1/agent-sessions")`；同檔 458-460 `if path.starts_with("/v1/agent-sessions/") && path.ends_with("/interrupt") { return true; }` 沒有比對 session 擁有者。事件內容見 agents.rs:789-792 `self.events.emit(EventType::AgentSessionState, json!({"agentSessionId": id, "agentId": agent_id, "state": state}))`。

**建議修法**：把 `EventType::AgentSessionState` 加進 `event_allowed` 的排除清單（或改成白名單制），並在 sse.rs 既有的 `legacy_agent_sse_cannot_read_...` 測試加上這個 case；順帶考慮讓 `/v1/agent-sessions/{id}/interrupt` 對 AgentSession principal 之外的 agent token 也做擁有權比對。

**回歸測試**：Unit test asserting `event_allowed(&AuthContext{principal: AuthPrincipal::LegacyAgent}, &RuntimeEvent{event_type: EventType::AgentSessionState, ...}) == false`, paired with an `agent_request_allowed` change that scopes `POST /v1/agent-sessions/{id}/interrupt` to sessions the legacy token is entitled to (or removes blanket allow), asserting a cross-session interrupt attempt is now refused.

#### F-c3d1786-20260903T124638Z-mobile-server-064

- **標題**：撤銷已連線手機時，連線收尾的 was_active 永遠是 false：不發 sensor.stopped、不收斂 stop 等待者、不結掉在途 act
- **維度**：mobile-server
- **Severity**：low（原報 low）
- **file:line**：crates/interaction-runtime/src/mobile.rs:2153

**主張**：`mobile_revoke` 在取消 CancellationToken 之前就先把 `conns` 表項移除，因此 handler 收尾時 `conns.get(&device_id)` 一定是 `None`，`was_active` 恆為 false。結果是撤銷路徑跳過了斷線路徑做的三件事：(a) 該手機的麥克風正在串流時不補發 `sensor.stopped` 事件（只有 4 秒輪詢的 status 會讓指示燈熄滅，事件面與 status 不一致）；(b) 不呼叫 `stop_sensors.mark_disconnected()`，並行中的停止等待會等滿逾時後回報 `unknown` 而不是 `unreachable`；(c) 不呼叫 `fail_pending_for_device`，送給該手機的在途 act 要等滿 4 秒 ACT_TIMEOUT 才收斂，`status.pendingActs` 期間仍 >0。

**證據**：mobile.rs:2153-2157（`mobile_revoke`）：`let conn = self.mobile.conns.write().await.remove(device_id); let was_connected = conn.is_some(); if let Some(conn) = conn { conn.close.cancel(); }`。mobile.rs:3150-3161（收尾）：`let (was_active, was_streaming) = { let mut conns = self.mobile.conns.write().await; match conns.get(&device_id) { Some(c) if c.conn_id == conn_id => { … (true, streaming) } _ => (false, false) } };`，其後 `if was_active { stop_sensors.mark_disconnected(); let ended = self.mobile.fail_pending_for_device(&device_id); … if was_streaming { self.events.emit(EventType::SensorStopped, …) } }`。現有測試 `revoke_disconnects_live_connection_immediately`（tests/mobile_loop.rs:773-836）沒有覆蓋「撤銷時麥克風正在串流」或「撤銷時有在途 act」。

**建議修法**：`mobile_revoke` 不要自己移除 conns 表項，改成只 `close.cancel()`（讓 handler 走同一條收尾邏輯），或在 revoke 內直接呼叫 `mark_disconnected()`／`fail_pending_for_device()`／補發 `sensor.stopped`。

**回歸測試**：在 `tests/mobile_loop.rs` 新增測試：pair 一台手機、送一則 `observation` 讓 `iphone.mic-level` 進入 mic_since（或直接送 `status` 帶 `sensors.micLevel:true`）使其被視為 streaming，同時對該裝置 `dispatch` 一個尚未 ack 的 act 使 pending_acts 非空，接著呼叫 `rt.mobile_revoke(&device_id)`；斷言（1）短時間內 events 流出現 `EventType::SensorStopped`（reason=disconnected/revoked），（2）`rt.mobile_status()["pendingActs"]` 幾乎立即變 0（不必等 4 秒 ACT_TIMEOUT），（3）該 act 的呼叫端立即收到 err/disconnected 而非等滿 timeout。

#### F-c3d1786-20260903T124638Z-mobile-server-065

- **標題**：iphone.motion 的 at 事實永遠到不了 runtime：iOS 送在訊息頂層，Rust 只過濾 facts
- **維度**：mobile-server
- **Severity**：low（原報 low）
- **file:line**：crates/interaction-runtime/src/mobile.rs:128 (spec), 209-222 (filter fn), 3078 (call site)

**主張**：`iphone.motion` 的 manifest 宣告 `provides: ["event", "at"]`，`filter_mobile_facts` 也把 `at` 列入 facts 白名單，但 iOS App 把 ISO8601 時間戳放在 observation 訊息的**頂層**欄位而不是 `facts` 裡，Rust 只讀 `v["facts"]`。因此真機／模擬器送來的 motion 觀察永遠只有 `event` 一個事實，`at` 這個白名單分支是死碼，而能力 manifest（也是 UI「可以提供」卡與 HumanMeta `fact_fields` 的來源）對外宣稱了一個實際上永遠不會出現的欄位，語意事件自己的時間戳也被丟掉（只剩伺服器的 received_at）。

**證據**：mobile.rs:125-134 `MobileReceptorSpec { id: "iphone.motion", … provides: &["event", "at"], … }`；mobile.rs:209-222 `filter_mobile_facts` 只對 `facts.as_object()` 過濾；mobile.rs:3070 呼叫端 `filter_mobile_facts(receptor, &v["facts"])`。iOS 端 Protocol.swift:315-323：`case .observation(let receptor, let facts, let at): var object = ["type":…, "receptor":…, "facts": .object(facts)]; if let at { object["at"] = .string(at) }`（`at` 在頂層，不在 facts）；SensorCenter.swift:166-170 `.observation(receptor: "iphone.motion", facts: ["event": .string(kind.rawValue)], at: WireTime.nowISO8601())`；iOS 測試 ProtocolTests.swift:60 `XCTAssertEqual(motionObject["at"] as? String, "2026-08-28T10:00:00.000Z")` 把頂層形狀釘死。Rust 測試 tests/mobile_loop.rs:343-353 只用手寫的 `{"event":…}` facts，沒有比對真實 wire 形狀。

**建議修法**：擇一：(a) Rust 端在過濾前把頂層 `at` 併進 facts（受白名單約束）；或 (b) 把 `at` 從 `provides` 拿掉並在協定文件說明時間戳是訊息層欄位。同時補一個以 iOS ProtocolTests 的實際 JSON 為輸入的 Rust 回歸測試，避免兩端形狀再次漂移。

**回歸測試**：在 tests/mobile_loop.rs 建構一則完整 wire 訊息 json!({"type":"observation","receptor":"iphone.motion","facts":{"event":"lifted"},"at":"2026-08-28T10:00:00.000Z"})，跑過連線處理流程後斷言產生的 Observation 帶有等於該 at 值的時間戳（而不是伺服器 received_at）——目前程式碼下此斷言會失敗，證明缺陷成立。

#### F-c3d1786-20260903T124638Z-mobile-server-066

- **標題**：失敗的 auth（未知裝置／錯 token／已撤銷）完全沒有 audit
- **維度**：mobile-server
- **Severity**：low（原報 low）
- **file:line**：crates/interaction-runtime/src/mobile.rs:3024-3027（auth-fail send 在 3025）

**主張**：`auth` 驗證失敗時只送 `auth-fail` 並關閉連線，沒有寫任何 audit。相對地，配對失敗（`mobile.pair-failed`／`mobile.pair-burned-by-peer`）、未認證逾時（`mobile.unauthenticated-timeout`）、入站速率超限（`mobile.rate-limited`）都有 audit。結果是「已撤銷的手機持續嘗試重連」或「區網上有人拿猜測的 deviceId/token 試」在 audit 與 status 上完全無跡可循——撤銷後的效果無法被使用者驗證，也沒有偵測面。

**證據**：mobile.rs:3014-3027：`Some("auth") if authed.is_none() => { … let ok = { … d.token_hash == sha256_hex(token.as_bytes()) … }; if !ok { send(&out_tx, json!({"type":"auth-fail","reason":"unknown device or bad token (possibly revoked)"})).await; break; }`——`if !ok` 分支只有 send + break，沒有 `self.store.audit(...)`。對照 mobile.rs:2867-2878（`mobile.unauthenticated-timeout`）、2905-2920（`mobile.rate-limited`）、2944-2958（`mobile.pair-failed`／`mobile.pair-burned-by-peer`）。`mobile_status` 的 heartbeat 區塊也只有 `refusedConnections`，沒有 failedAuth 計數。

**建議修法**：在 `if !ok` 分支加 `mobile.auth-failed` audit（peer 位址、送來的 deviceId、是否為已撤銷的 id），並在 `mobile_status().heartbeat` 加一個 `failedAuths` 計數，讓「撤銷後仍有人重連」看得見。

**回歸測試**：在 mobile_loop.rs 新增測試：配對後送 `{"type":"auth","deviceId":<known-or-unknown-id>,"token":"wrong"}`，斷言收到 `auth-fail` 後，透過測試專用的 audit 查詢介面（或 store 的記憶體 log）能找到一筆 event 類型如 `mobile.auth-failed` 且帶 deviceId/peer 資訊；目前該斷言會失敗，因為根本沒有寫入。

## 已於審查期間修復（fixed-meanwhile）（0 項）

## 已駁回（refuted）（4 項）

- F-c3d1786-20260903T124638Z-rig-renderer-055 — sprite adapter 的 presentationHints.variant 是開放詞彙：AI 可讓角色假演緊急停止／被擋下／離線／結果未知，並用 variant="paused" 強制清掉真正的安全訊息

- F-c3d1786-20260903T124638Z-rig-renderer-057 — shu 參考 adapter 的 variant 把 Runtime 對 AI 的 wait→think 降級又還原回「等待結果」，AI 也能演出「裝置離線」

- F-c3d1786-20260903T124638Z-ia-settings-014 — 活動紀錄收件匣用原始 title，繞過 inboxItemTitle 的人話投影

- F-c3d1786-20260903T124638Z-perf-claims-010 — 交付報告 §20 引用 acceptance-evidence 明文宣告「不得引用」的量化桶值與 repo 內沒有任何跑法能產生的 8.7 ms

## 未驗證（unverified）（0 項）
