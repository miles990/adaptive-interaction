# v0.8.0 發布與安裝驗證

版本來源固定為 main `1fa69b8fafed54687119747c652a0869cb991f89`。後續 main 的純文件提交不移動 tag，也不冒充重新建置過的發布來源。

## 1. 提交、CI 與不可變 tag

| 階段 | 實際結果 |
|---|---|
| 起點 | main `622c8bf70963e343e51f92f5ecdf7fce4b37f516`，本機/遠端一致、clean，沒有其他worktree或使用者修改被清理 |
| 候選 | feature/semantic-recovery-convergence，`c15873acbaaed870d2c90fa95c12255f532ce782` |
| 合併 | [PR #5](https://github.com/miles990/adaptive-interaction/pull/5)，GitHub rebase；match-head-commit，不使用admin／force |
| 候選 CI | [34017584720](https://github.com/miles990/adaptive-interaction/actions/runs/34017584720)，四項success |
| 實際 main | `1fa69b8fafed54687119747c652a0869cb991f89`；tree `22f1ebcb2a1fac9cb1744444589c7028709ce036` 與受測候選相同 |
| main push CI | [34017910631](https://github.com/miles990/adaptive-interaction/actions/runs/34017910631)，四項success |
| 完整本機發布關卡 | 同SHA `bash scripts/release-verify.sh 0.8.0 --run-tests`，165.78s，0skip；Rust1278/0、Tauri78/0及所有其他gate成功 |
| annotated tag | `v0.8.0`；object `5bca3660afa1d74d7f0844eeb7832e1e25ff9375`；本機/遠端object與解參照SHA一致 |
| tag push | `bash scripts/release-tag.sh 0.8.0 --push`，15.81s，exit0；受驗tree未改 |
| Release workflow | [34018696536](https://github.com/miles990/adaptive-interaction/actions/runs/34018696536)，tag來源同SHA，11個job全部success |
| 已發布 Release | [v0.8.0](https://github.com/miles990/adaptive-interaction/releases/tag/v0.8.0)；publishedAt `2026-09-06T07:35:55Z`，draft=false／prerelease=false |

首次main完整verify為exit1（206.75s），只有workspace gate失敗，原腳本丟棄其底層輸出。相同SHA捕獲診斷1278/0（126.72s）與完整再驗165.78s成功；原因未知，狀態 **not-reproduced**，不能稱fixed。再驗只用repo外Cargo forwarder保存stdout/stderr，未改命令參數、可執行Cargo、exit code或repo。第一次失敗、forwarder與逐Cargo結果均保留。
Tag helper的靜態再查本身skip一次full matrix；它不獨立構成完整驗證。完整證據是前一步相同SHA的0skip驗證。獨立稽核另列原始記錄。

## 2. 資產與來源

全部23個Release資產實際下載；9/9個`.sha256`逐個綁定同名binary asset並核對，GitHub提供的digest共23個相符。Skill ZIP所有檔案及QUICKSTART/install.sh/CHANGELOG與tag內來源逐byte一致。App tar.gz若無sidecar，只依其GitHub asset digest，不冒稱有獨立sidecar。

完整檔名、bytes、asset ID、SHA-256、sidecar與source對照見[資產驗證JSON](evidence/2026-09-06-convergence/publication/release-asset-verification.json)。這證明下載內容與該Release一致，不是簽章、公證、SBOM或可重現建置attestation。

## 3. 可用平台安裝與啟動

| 平台／路徑 | 結果與範圍 |
|---|---|
| macOS arm64 CLI | 下載的`install.sh --version v0.8.0 --cli-only --bin-dir <task-owned>` exit0；installed binary與驗證archive一致，`--version`=0.8.0；隔離home/loopback daemon ready、authenticated status、SIGINT exit0、port closed；外層總耗時4.58s，內層runner 4.471s |
| macOS桌面 | 已驗DMG唯讀掛載，以ditto複製App到暫存安裝目錄後detach；bundleVersion0.8.0，binary SHA-256 `c47057b14b99954a9d076bb247c01aa2dd6b3ad32b774459928730d5b41f42f5` |
| 發布App原生走查 | 指定上列下載App執行既有AX runner，9completed＋1人工解除needs-environment；90.51s；App未變、cleanup port0 |
| macOS x86_64 | needs-environment：本機沒有Rosetta，x86_64成品未啟動；本輪未安裝Rosetta，也不算Intel硬體驗收。[CLI smoke JSON](evidence/2026-09-06-convergence/publication/release-install-cli.json)保留實際結果 |
| Linux／Windows | Release建置與下載checksum成功；本macOS環境沒有執行它們的安裝／啟動，狀態not-run |

DMG初次staging因`/tmp`與`/private/tmp`的同目錄別名字串比較失敗（14.71s），當次finally已卸載且未啟動App；實際probe確認別名後以resolved path比對，新的自有v2目錄重跑成功（10.91s）。原失敗與獨立負向回驗保留，不把這個harness錯誤當成發布App缺陷。

原生走查來源：App為tag `1fa69b8fafed54687119747c652a0869cb991f89`；driver commit `4f5992933cde634a242f114f39575b278e9bb834`，只比tag多發布進度文件，driver/AX/fixture blob與tag一致。腳本用AX操作真Tauri視窗，但不是真人耗時；22個成功點擊與其餘精確metrics見[原始結果](evidence/2026-09-06-convergence/publication/release-native-smoke/tauri-ax-walkthrough.json)。未改系統Applications、shell rc、使用者既有設定或權限。直接bundle executable啟動不等於Finder quarantine／Gatekeeper批准已驗收。

## 4. 審查、限制與可續接資料

發布驗證harness另有2項P2及1項P3被獨立發現並修正：sidecar名稱綁定、執行installer前綁定成功報告與目前檔案hash、初次version命令timeout20s。獨立20/0（17負向＋3正向控制）；不把這些in-memory probes當成實際安裝。
[工程30列Find→Verify](evidence/2026-09-06-convergence/final/review-closeout/convergence-find-verify-final-map.md)與publication harness審查分列，原始紅燈不刪除。

真iPhone／ESP32、MQTT/BLE專屬applied閉環、非開發者、真人VoiceOver及人工解除緊急停止仍未執行，下一動作見[known limitations](v0.8.0-known-limitations.md)。隔離自動測試中的合成安全狀態轉移不是人類授權驗收；本輪原生與安裝走查不代人授予consent或解除latch。

本頁所有結論的原始來源見[publication facts](evidence/2026-09-06-convergence/publication/publication-facts.json)與[artifact manifest](evidence/2026-09-06-convergence/publication/artifact-manifest.json)。manifest分開原始與封存SHA-256：文字log僅正規化尾端空白/空行、`.log`存成`.txt`；既知artifact連結改為相對路徑。保留失敗與未執行界線；runtime home/token與大型發布binary不進版控。

發布後文件PR #6的main CI另檢出rebind測試完成條件競態；test-only修正 `857f009acf0b2c217f12ec041766ad7de2a2c7cf` 已完成受控紅綠、獨立Find→Verify與完整Rust1278/0。此後main可多出測試維護；v0.8.0產品來源與tag不變。原失敗、修正與整合查核入口见[CI後記](v0.8.0-ci-followup.md)。
