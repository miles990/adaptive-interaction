# v0.5.1 發布就緒清單

> 狀態值只有三種：**met**（這一關本身的條件已滿足，證據見備註）、**pending final run**（工作已完成，
> 但要等最後一次全套跑／CI／發布流程實際確認才能蓋章）、**blocked-by-human**（需要使用者或人工環境
> 介入，非工程可解）。**沒有一格是猜測**；沒有實跑證據的一律標 `pending final run`，不標 met。
>
> 基準：v0.5.0（tag `v0.5.0` ＝ `8b713c7`）。本清單對應分支 `release/v0.5.1-product-hardening`
> 上的 30 個修復 commit（20 個規格修復＋10 個對抗審查修復；`git log --oneline v0.5.0..HEAD`）。數字來源見
> `docs/releases/v0.5.1-test-matrix.md`；已知限制見 `docs/releases/v0.5.1-known-limitations.md`。
>
> 對抗審查 `0c845e0-20260903T185130Z` 已回填關卡 11／16；發布後才存在的事實（CI run、tag、Release、checksum）在發布段落回填。

## 20 道關卡

| # | 關卡 | 狀態 | 證據／備註 |
|---|---|---|---|
| 1 | 本機回歸全套綠（Rust／Tauri／前端／CLI E2E／Playwright／ESP32／iOS） | **met** | 2026-09-04 最終一次全套實跑：`cargo fmt --check` exit 0、`cargo clippy --workspace --all-targets -- -D warnings` 0 warning、`cargo test --workspace` **827 passed / 0 failed / 0 ignored（66 個 test target）**、`cargo build --workspace` 成功、CLI E2E **82 passed / 0 failed**、Tauri backend **50 passed / 0 failed**（clippy 乾淨）、前端 typecheck 乾淨、vitest **1168 passed / 0 failed（60 檔）**、`pnpm build` 成功、Playwright **65 passed / 0 failed（2.0 分）**、ESP32 兩組態 exit 0、iOS 模擬器 XCTest **46 passed / 0 failed**。vitest 最初全量跑有 2 個案例只在全量跑失敗、單獨跑通過，根因是 `src/characterName.ts` 的刷新沒有世代概念（測試輔助 `resetCharacterNameForTests`／`primeCharacterNameForTests` 之後遲到的舊刷新會把已解析的「小樞」蓋回中立的「角色」並更新節流時間戳；正式執行期沒有人換世代，行為不變）；修法是刷新帶世代編號、reset／prime 換代、舊世代落地即作廢（commit `a6e289e`，新增 3 個 characterName regression test，`regressions-v05` 的 `stubShellApis` 自己先 reset）。修後全量連跑 **6 次全綠**，typecheck 乾淨。對抗審查修復（10 個 commit）後於 `957332e` 再跑一次完整全套（05:05–05:14）：上列數字即為該次結果；vitest 在修復期間全量跑 2 次，第一次唯一失敗是 memory-ui-002 預期中的舊斷言（改為進階模式斷言），第二次 1168/0。iOS XCTest 46/46 為對抗審查修復前的實跑，iOS 原始碼自那之後未變。 |
| 2 | CI 綠（GitHub Actions 全 job） | pending final run | 本分支尚未 push，CI 從未對它跑過。本輪已把 `desktop-backend` job 從 `cargo check` 升為 `cargo clippy -D warnings`＋`cargo test`（commit `e70ef28`），該組合在本機為 50 passed / 0 failed、clippy 乾淨；**CI 上的實際結果未知**。 |
| 3 | Release workflow 可重現（`scripts/release.sh` → tag → Release CI → 產物） | pending final run | `scripts/release.sh` 本輪修好一個靜默失效：CHANGELOG 既沒有 `## [<version>]` 也沒有 `## [Unreleased]` 時直接失敗，不再靜默跳過（`e70ef28`）。`## [Unreleased]` 區段已存在，插入點可被腳本找到。**尚未執行過 release.sh、未打 tag、未觸發 Release CI**。 |
| 4 | v0.5.0 五個刻意保留的 partial finding 全數處理 | **met** | 五項各一個 commit，皆附「舊行為下先紅燈」的回歸測試：`ia-settings-012`（`f3a9eea`）、`safety-invariants-078`（`af8d1f1`）、`companion-gameplay-032`（`73f0694`）、`protocol-conformance-030`（`ddbab07`）、`link-transports-054`（`4029316`）。範圍與殘留見 known-limitations §1。 |
| 5 | agent session interrupt 擁有權（`safety-invariants-078`） | **met** | `POST /v1/agent-sessions/{id}/interrupt` 對 legacy 共享 agent token 一律 `403 token_scope_forbidden`；session-scoped token 只能中斷自己的 session（middleware 比對＋handler `interrupt_principal_allowed` 雙層）；human token 保留管理能力；runtime 緊急停止走內部呼叫不受影響。回歸：`api_e2e.rs`。**刻意的安全收斂**（Breaking，見 migration）。 |
| 6 | provider 生命週期狀態接進執行期（`ProviderGate`） | **met** | `observe_fresh`、push 受器 ingest、`Executor::run_step`、`simulate_plan` 都查擁有 provider 的狀態；Disabled／Expired／Revoked／Closed 一律拒絕（observe→`Unavailable`；execute／simulate→`Blocked{rule:"provider.not-operational"}`）；共用能力只在所有擁有者都停下時才擋。回歸：`providers_loop` 22/0、`runtime_loop` 28/0、`interaction-registry` 單元。 |
| 7 | 首次設定 commit 原子性 | **met** | 兩段式提交：Phase 1 純計算與驗證；Phase 2a 檔案 temp+rename＋SQLite 單一 `Store::transaction`（交易失敗回寫檔案舊 bytes）；Phase 2b 記憶體旗標只在 2a 提交後翻、失敗全部翻回並寫 `onboarding.partial` audit。故障注入測試涵蓋五條路徑（`human_layer` 27/0）。**殘留**：檔案寫入與 SQLite 提交之間的程序崩潰沒有 journal（known-limitations）。 |
| 8 | TaskComposer 顯示真實送達狀態 | **met** | 新增 `src/work/delivery.ts`：依後端訊號（mailbox `deliveredAt` 戳記、`DomainError` 前綴）投影成六態（已送達／尚未送達（已放進信箱）／排隊中／Agent 不可用／傳送失敗／結果不確定），只有後端蓋戳才說「已送達」；TaskComposer 與工作卡片「再交代」共用同一份投影。回歸：`delivery.test.ts` 16 條＋Playwright `work-delegate.spec.ts` 改為斷言投影後的句子。 |
| 9 | 一般模式不外洩技術分層標籤 | **met** | `GlobalSearch` 改走共用的 `memoryLayerLabel(layer, advanced, name)`，一般模式看不到 `agent-handoff`／`skill`／「Agent 交接」（`5ad1262`，附正反向斷言）。原本兩條「一般模式不外洩治理術語」的斷言只在全量跑失敗，已查明是測試輔助造成的 product-race（見關卡 1），由 `a6e289e` 修好——**不是**一般模式真的外洩術語。vitest **1168 passed / 0 failed（60 檔）**，全量連跑 6 次全綠。 |
| 10 | serial pty/file fallback 讀取執行緒不再洩漏 | **met** | unix 上 fallback reader 改為獨立 `O_NONBLOCK` fd＋`poll(2)`＋per-session self-pipe，shutdown 在 `READER_JOIN_GRACE_MS` 內真的 join 回 reader。回歸：沉默 pty 寬限期內回收、50 圈 open/shutdown soak 零 detached（最差關閉 203 ms）、reader 睡在 poll 裡（mutation 驗證）。**範圍**：非 unix 平台仍是 bounded-join＋detach＋計數。 |
| 11 | 無新的 blocker／high 對抗審查 finding | **met** | `.claude/workflows/adversarial-review-v05.js` 對 `0c845e0` 跑出 62 送審／55 confirmed（high 7／medium 27／low 21）；**7 個 high 全部已修**，55 個 confirmed 已修 52／部分修 3／未修 0，殘留 17 條逐條記在 known-limitations §4.1。報告：`docs/reviews/adversarial/0c845e0-20260903T185130Z.md`。修復後未再跑第二次審查（記為已知限制）。 |
| 12 | claimed ≠ verified 在 UI 與 API 都成立 | **met** | 真 Tauri 視窗驗收：工作卡片顯示「對方說已完成，尚未經過檢查」＋「標記為已驗證（我確認過結果）」按鈕，**沒有綠勾**；工作狀態標籤新用語保留 warn 徽章與待你裁決。`character.present` 的 `verified-success`／`emergency` 仍是 Runtime 專屬真相狀態（v0.5.0 起，本輪未放寬）。 |
| 13 | 緊急停止與「停止所有感測」端到端 | **met** | 真 Tauri 視窗驗收：Tray「緊急停止」→ `/ready` `emergencyStop:true`、可信「安全狀態」覆蓋視窗出現並顯示「緊急停止中」、角色視窗顯示固定文字、Tray 狀態同步；`POST /v1/emergency-stop/clear` 後覆蓋視窗與角色文字消失。Tray「停止所有感測」→ `activeSensors` 清空、覆蓋視窗消失、fixture 手機收到 2 則 stop-all（含 auto-ack）。等級：真 Tauri 視窗＋fixture 手機。 |
| 14 | 390px 窄視窗可用 | **met**（browser 等級） | Playwright `narrow.spec.ts` 在最終一次跑（65 passed / 0 failed）內通過；`docs/assets/v05-evidence/narrow-*.png` 由本輪 evidence spec 重新產生。**等級標示**：瀏覽器（Chromium），非真 Tauri 視窗——390px 未在真視窗尺寸下重測。 |
| 15 | 原生資料夾選擇器在真 Tauri 視窗可用 | **met** | 真 Tauri 視窗驗收四列：取消（NSOpenPanel 出現→Escape→欄位仍空、無錯誤文案）、選擇（⌘⇧G 指定暫存目錄→欄位顯示實際路徑、預覽「不會修改：這次只看不改」）、唯讀不取得 write scope（API `allowWrite:false`、`dataScope` 只含選定資料夾、`toolScope:[]`）、寫入需第二次確認（預覽改「會修改：只限 \<完整路徑\>」＋第二個核取方塊，不勾就無法以寫入模式開始）。 |
| 16 | 文件與程式碼一致 | **met** | docs-claims-050／052／054、perf-claims-013／014／015、protocol-conformance-043 已修（CHANGELOG 不存在的條目移除、README／CLAUDE.md／FEATURES 改為 v0.5.0 已發布、iOS README 46/46、能力矩陣 <16ms 改為未達標、韌體 README pair-locked 描述改正）；character-protocol README §3.4／§6／§8 與 adapter-authoring §9／§10 已跟上安全 fallback 守衛、輸入能力閘門與 outbound 佇列實際行為；對抗審查結果已回填五份文件。 |
| 17 | worktree 乾淨（無未提交變更、無未追蹤殘留） | pending final run | 30 個修復 commit 已在分支上，但本批次文件變更與重新產生的 `docs/assets/v05-evidence/*.png` 目前**未提交**。發布前必須提交或明確排除。 |
| 18 | 版本一致性（四處版本號相同） | pending final run | 目前四處皆為 `0.5.0`（workspace `Cargo.toml`／`src-tauri/Cargo.toml`／`tauri.conf.json`／`package.json`）——彼此一致但尚未 bump 到 `0.5.1`。版本號一律由 `./scripts/release.sh 0.5.1` 一次更新，本清單不代為手動改動。 |
| 19 | `v0.5.1` tag 尚不存在（不覆蓋既有 tag） | **met** | `git tag -l v0.5.1` 無輸出；現有 tag 為 v0.2.0／v0.3.0／v0.4.0／v0.4.1／v0.5.0。 |
| 20 | 乾淨 checkout 可重建（tag 產物與 checksum 可重現） | pending final run | 尚未從乾淨 checkout 重跑全套，也尚未產生 Release 資產。發布後必須依 §「發布後」重跑並比對 checksum。 |

**統計**：**met 13 項**（關卡 1、4、5、6、7、8、9、10、12、13、14、15、19）、**pending final run 7 項**
（關卡 2、3、11、16、17、18、20）、關卡層級 blocked-by-human 0 項（另有兩項非關卡的人工阻塞，見下）。

## 不在 20 關卡內、但確實 blocked-by-human 的兩件事

| 項目 | 狀態 | 說明 |
|---|---|---|
| iPhone 真機驗收（v0.5.1 新增的冷啟動自動重連／位址變更提示） | **blocked-by-human** | 2026-09-04 的嘗試停在 macOS 鑰匙圈授權對話框（`apps/interaction-ios/README.md`「真機」章節的 **H3** 人工步驟）：`codesign` 等待私鑰存取授權，`xcodebuild` 停住約 9 分鐘無進展，**App 從未裝上手機、冷啟動測試 0 次執行**。AI 不得代按該對話框（等同代替使用者授予同意）。詳見 `docs/releases/v0.5.1-iphone-device-evidence.md`。v0.5.0 的真機證據（`v0.5.0-iphone-device-evidence.md`）仍然有效，未被本輪推翻。 |
| ESP32 真板驗收 | **blocked-by-human** | 本環境無實體 ESP32 裝置。本輪兩種組態以 `arduino-cli` 實際編譯，皆 exit 0——**非真板驗收**。 |

## 發布前建議順序（給整合者）

1. ~~修完兩個 vitest 全量跑失敗的斷言~~ **已完成（2026-09-04）**：根因是測試輔助造成的 product-race（見關卡 1），已由 commit `a6e289e` 修好；`cd apps/interaction-desktop && pnpm test` 為 **1168 passed / 0 failed（60 檔）**，全量連跑 6 次全綠、typecheck 乾淨，數字已回填本文件與 `docs/releases/v0.5.1-test-matrix.md`／`CHANGELOG.md`／`docs/acceptance-evidence.md`／`docs/v05-capability-gap-matrix.md`。
2. ~~等對抗審查跑完~~ **已完成**：55 confirmed → 已修 52／部分修 3，結果已回填關卡 11／16 與 final-report／known-limitations／acceptance-evidence／gap-matrix。
3. 重跑一次完整回歸（可重跑命令，全部從 repo 根目錄執行）：
   ```bash
   cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings
   cargo test --workspace
   cargo test --manifest-path apps/interaction-desktop/src-tauri/Cargo.toml
   cd apps/interaction-desktop && pnpm typecheck && pnpm test && pnpm build && pnpm test:e2e
   ./scripts/v03-cli-e2e.sh
   ./firmware/esp32-companion/compile.sh && ./firmware/esp32-companion/compile.sh --ble
   ```
4. 提交本批次文件與重新產生的證據截圖，確認 `git status` 乾淨（關卡 17）。
5. push 分支、開 PR，等 CI 全綠（關卡 2）。
6. 使用者明確授權後執行 `./scripts/release.sh 0.5.1`（處理版本一致性、golden schema 重生、打 tag、觸發 Release CI），關卡 3／18／19 於此結清。
7. 發布後從乾淨 checkout 重跑第 3 步全套並比對 Release 產物 checksum（關卡 20）。
8. iPhone 真機（blocked-by-human）：請使用者在 Mac 上對鑰匙圈對話框按「**總是允許**」，再重跑
   `apps/interaction-ios/scripts/device-build.sh --no-launch`，之後才能補跑冷啟動矩陣。**在此之前
   不得**把 v0.5.1 的 iPhone 冷啟動／位址變更提示寫成「已在真機驗收」。
