# v0.8.0 工程收斂報告（已發布）

本輪已完成語意狀態契約、可協商套用回執、感測摘要重啟恢復與 host 擁有的設定恢復實作。
工程實作、獨立原版缺陷驗證、原生任務與乾淨 checkout 五演練已完成；發布 gate 另記，不以中途測試代替main/tag驗證。
續接入口：[進度](next-convergence-progress.md) §5／§6；唯一發布事實來源：[evidence index](evidence-index.json)。

## 1. 起點與變更歸屬

起點 main `622c8bf70963e343e51f92f5ecdf7fce4b37f516`，遠端同 SHA；v0.7.0 tag 指向
`630b4291f6a59444cfb1d8185f757f9dcda9ecc4`。初始工作樹乾淨，無其他 worktree，無使用者修改被清理。
本輪分支 `feature/semantic-recovery-convergence`；主要實作 checkpoint
`30ff852f5d893215c019d982774c8b59ab010659`，證據與演練 patch 整理 checkpoint
`a32258ea161df537f2ad6c1f25cd70d86910d0da`。停止通知修正 `87b0d03173cdefce68bad2403b7f76be59b738e1`；驗收 checkpoint `3b375bde4188e4a039852881d48d82ea9b619ce1`。候選 `c15873acbaaed870d2c90fa95c12255f532ce782` 已經由 PR #5 rebase 至 main `1fa69b8fafed54687119747c652a0869cb991f89`，兩者tree一致。該main四項CI成功，完整release-verify通過後已建立遠端annotated v0.8.0；Release workflow、全部資產/checksum及可用macOS安裝smoke已完成；詳見[發布驗證](v0.8.0-publication.md)。

## 2. 需求差異與 production 呼叫路徑

| ID | 本輪實作及 canonical owner | 保護與契約 | 狀態及證據範圍 |
|---|---|---|---|
| N1 | 純 `interaction-session::semantic_contract` 匯出獨立 schema →既有 aip-codegen → TS/Swift DTO＋production validator；raw HTTP/SSE/Tauri文字→number-source→SessionClient→renderer projection | DTO不作hash輸入；未知欄位/數值字面保留；完整合併結果先驗再採用；frozen v0.7.0 corpus與current golden分離；AIP/Session純依賴不變 | fixed；共享44 state＋4 patch cases，Rust/TS/Swift實跑；optional欄位clean HEAD演練21/0（含6預期負向） |
| N2 | `StateAppliedTracker`共用於DeviceLink與MobileBridge；send記有界expectedstate→可信link context驗receipt→runtime diagnostic→characterSync投影 | `aip.applied/1`選配協商；peer/link/session/epoch/rev/hash/nonce綁定，32 outstanding/5s期限；後續patch立即待確認。對端自報套用不等於真實呈現或物理效果 | fixed；production Serial pty及Mobile程序內TLS client。default/BLE firmware只compile，MQTT/BLE專屬套用閉環未跑 |
| N3 | raw capture開始→`sensor_journal` write-ahead→同scope與currentgeneration停止確認→SQLite meta＋audit；restart恢復unknown；正常shutdown先有界停止再markclean | active/unknown/audit分離，32明細＋可見overflow；future/corrupt parked；新連線不能清舊提醒，讀鎖busy保守保留；高風險仍預設off，consent不復活 | fixed；81 mobile＋38sensor＋5獨立scope回歸；非consent受器keptDisabled為verified-existing；Removed host hook為experimental |
| N4 | React呼叫 `preset_service` →持久marker/prefs→Runtime conditional revision＋operation ID→讀回→清marker；一般prefs寫入同hostgate；匯入先schema/meta驗證 | 讀回一致才完整預設；新runtime/prefs選擇阻止舊恢復覆蓋；marker版本未知不自作處理；不稱ACID。匯入部分失敗說明保存/套用界線；檔案0600/自有temp清理 | fixed；Tauri78與TS回歸；同一原生App10個fault/restart情境completed；backup走查clean/legacy各4completed＋4correctly-blocked，精確時間見逐任務表 |
| N5 | committed package/template/optional patch/lifecycle runners＋architecture-checks統一入口 | 角色沿既有adapter只加package；移除走真hostport，不以rm當UI；每run暫存資料保留與獨立輸出；不需wt/drills或聊天 | fixed；3b375bd clean HEAD四runner五演練全部通過，含package production host port；不是原生移除UI像素證據 |
| N6 | 五入口與一般模式保留；Dialog初始Tab/Shift+Tab修復；native AX與Browser分開，實際download/filepicker及fixtureprocess取消 | 未確認不消失；腳本不替人解除estop或授予consent。390px Chromium與nativeAX分列；沒有VoiceOver真人/真機結果 | fixed（工程子集）／needs-environment（真機與真人）；完整Browser92/0，native clean/legacy逐任務完成，人工解除仍not-run |

## 3. 獨立 Find → Verify

指定workflow的`agent/parallel/phase` runtime在本環境沒有callable工具；採相同五維度與反駁步驟，
由獨立協作agent交叉審查，整合者修正與統一證據。没有宣稱五人完整平行執行。最後[30列映射與五面向影響範圍](evidence/2026-09-06-convergence/final/review-closeout/convergence-find-verify-final-map.md)包含3列使用者指定契約變更；product/harness open、待獨立驗證與必要範圍缺口均0。作者測試與獨立執行分列。
逐finding有觸發、作者、Finder、Verifier、紅/綠與限制：
[N1/N4/N6](evidence/2026-09-06-convergence/review/n1-n4-n6-findings-handoff.json)、
[N3](evidence/2026-09-06-convergence/review/n3-convergence-handoff-20260906.json)、
[N2/N5及跨區複核](evidence/2026-09-06-convergence/review/n2-n5-review-handoff-20260906.json)。

特別保留歸屬界線：N3 scope03是作者延伸發現＋另一agent獨立Verify；N1原始缺鍵/null的紅是作者執行，
後由非作者state_receipts在未改動的622c8bf原版與3b375bd各執行同一批獨立probes：TS原版2controls/5fail→7/0；Rust原版2controls/1fail→3/0，反駁與raw log另存closeout，不改寫作者原始紀錄。
原先`semantic_state_never_serializes_a_null`及欄位hash fixture覆蓋為verified-existing；
它們只保護序列化/覆蓋，不等於本轮新增的接收runtime驗證，歷史矛盾已修正。

## 4. 測試與環境

Apple M2 Pro / macOS、Rust/Cargo1.94.0、Node24.5.0、pnpm10.27.0、Xcode26.6。
基線1245 Rust /63Tauri /1816TS /158iOS；中途完整整合1278 Rust /78Tauri /163iOS；最新提交TS1916/0、Browser92/0，均0failed。
CLI96/0。原始命令、時長、環境與來源階段見[evidence](evidence/2026-09-06-convergence/README.md)。
中途log中的HEAD不是未提交改動的SHA；已在index明示，不會拿它冒充正式candidate通過。

## 5. 資料與版本

產品版本判定為minor0.8.0：增加可協商套用回執與持久恢復能力，沒有把舊peer新增訊息變必填。
AIP1.0、device wire既有版本、semantic-state1.0與snapshot格式各自管理；sensor journal1與preset marker1是獨立本機格式。
詳細[遷移與回退](v0.8.0-migration.md)、[已知限制](v0.8.0-known-limitations.md)、[deprecation ledger](../aip/deprecation-ledger.md)。

## 6. 發布完成與保留限制

工程及效能gate完成；[同條件性能比較](v0.8.0-performance.md)8runs未越事前調查預算，範圍與未測路徑分列。工程驗收結果見[完整測試矩陣](v0.8.0-test-matrix.md)、[一般模式逐任務](v0.8.0-general-mode-tasks.md)、[五演練](v0.8.0-drills.md)。
已完成版本準備、精確候選CI、rebase main、該main必要CI與完整release verify、annotated tag。Release workflow 34018696536 全部success，23資產/9sidecars驗證相符；下載的macOS CLI與App啟動走查完成。[逐平台發布事實](v0.8.0-publication.md)與raw artifacts另列。

完整main release-verify首輪workspace非0，但原腳本丟棄測試細項；同SHA捕獲診斷重跑1278/0，保留stdout/stderr的完整重跑0skip成功（165.78s）。原因未定位，狀態not-reproduced，不宣稱已修復。
真iPhone/ESP32/非開發者/VoiceOver真人與人工解除緊急停止維持needs-environment/not-run；
既有發布政策容許的範圍與下一動作保存在known-limitations，沒有把它們改標為已修。

發布後文件PR #6的main CI另檢出rebind測試完成條件競態；test-only修正 `857f009acf0b2c217f12ec041766ad7de2a2c7cf` 已完成受控紅綠、獨立Find→Verify與完整Rust1278/0。此後main可多出測試維護；v0.8.0產品來源與tag不變。原失敗、修正與整合查核入口见[CI後記](v0.8.0-ci-followup.md)。
