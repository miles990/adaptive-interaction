# v0.6.0 後續：可維護性與一般模式收斂 — 需求矩陣與進度（工作文件）

> 這是一份**工作中的**進度文件，目的是讓任何新的 session 能從檔案恢復，而不依賴聊天上下文。
> 版本號尚未決定（見 §0.3）；本文件不是發布文件，發布文件另建。所有數字都是本輪實際執行的結果，
> 未執行的項目標 `not-run`／`needs-environment`，不以文件宣稱替代。

## 0. 起點事實（2026-09-05）

### 0.1 Repository 狀態

| 項目 | 值 |
|---|---|
| 分支 | `feature/v0.6.x-maintainability`（自 `main` 8f52837 建立） |
| 起始 HEAD | `8f528374fd0b1490eacb623f6e299339bfd565e0`（＝`origin/main`，worktree 乾淨） |
| 最新 tag | `v0.6.0`（commit `4bd55fe`，Release 已公開：23 個 assets，非 draft／pre-release，2026-09-04T21:06:58Z） |
| main 最新 CI | run `33920312398`（8f52837）success；`33919774319`（ea7de59）failure 為 Windows checksum CRLF，已由 8826656 修正 |
| 保留 | 不 reset、不改既有 tag、不 force-push；本輪只在 feature 分支提交 |

### 0.2 基線（修改前，本機 macOS／Apple Silicon）

| 套件 | 命令（取自 `.github/workflows/ci.yml`／`CLAUDE.md`） | 結果 |
|---|---|---|
| Rust workspace | `cargo test --workspace --no-fail-fast` | 1040 passed／0 failed／0 ignored（85 個測試二進位），exit 0 |
| Rust lint | `cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings` | 未在基線重跑（CI 8f52837 為綠；整合里程碑再跑） |
| Tauri backend | `cargo clippy --manifest-path apps/interaction-desktop/src-tauri/Cargo.toml --all-targets -- -D warnings`／`cargo test --manifest-path …` | clippy 乾淨；56 passed／0 failed |
| 前端 | `pnpm aip:check`／`pnpm typecheck`／`pnpm test`／`pnpm build` | 同步／通過／1416 passed（70 檔）／build 2.63 s |
| CLI e2e | `./scripts/v03-cli-e2e.sh` | 96 passed／0 failed |
| Playwright | `pnpm test:e2e`（`INTERACT_AI_MOBILE_ADVERTISE=0`） | 71 passed（2.2 min） |
| 磁碟 | 使用者要求 `cargo clean`：釋放 38.6 GiB，之後剩 44 GiB | — |

註：Playwright 基線會重寫 `docs/assets/v05-evidence/*.png`／`v06-evidence/*.png`（像素級差異），已 `git checkout` 還原，不列入本輪變更。

### 0.3 版本政策（決定）

- 相容修補（M1 全部、M3 的 UI 收斂、M4 文件）與**新增公開能力**（M2 的第二裝置 AIP profile、SensorSource port、
  diagnostics／snapshot 新欄位）以**不同 commit 群**提交，讓維護者可以分別安排 patch／minor。
- 不預先把所有變更叫 v0.6.1；本輪**不打 tag、不建 Release**。建議（非決定）：M1＋M3＋M4 → patch 候選；M2 → minor 候選。

## 1. 需求矩陣（狀態：implemented／verified／partial／not-reproduced／needs-environment）

證據等級：`code-read`＝讀到程式碼、`test-exists`＝找到並讀過對應測試斷言、`doc-claim`＝只有文件說、
`test-run`＝本輪實際執行。行號以起始 HEAD 8f52837 為準。

### M1 同步接收、持久化與協定演進

| # | 需求 | 現有實作（起始） | 呼叫路徑 | 測試 | 已知限制 | 證據 | 起始狀態 | 本輪動作 |
|---|---|---|---|---|---|---|---|---|
| 1.1a | 同 epoch 較舊 snapshot 不得覆蓋 | `CharacterSyncCard.tsx:118-121` snapshot 分支無條件接受 | SSE `character.session.state` → `alignSession` | 無（`character-sync-card.test.tsx:365` 只測 revision 上升） | 無記錄 | code-read | **partial（confirmed 缺陷）** | 抽成純 reducer `src/aip/sessionClient.ts`，鏡射 Rust `accept_state_with_epoch`（patch.rs:92-154） |
| 1.1b | 慢 HTTP 回應不得覆蓋較新 SSE 狀態 | Effect A（:221-262）resolve 後無條件 `commitSession` | GET `/v1/character-session` | 無 | 無 | code-read | **partial（confirmed）** | request generation＋appliedSinceIssue 判定 |
| 1.1c | 相同 snapshot 重播＝already-applied | 同 1.1a | 同 | 無 | 無 | code-read | partial | reducer |
| 1.1d | 缺失／負數／小數／>2^53 revision 不得變成 0 | `num()`（:90）只驗 finite；缺值預設 0（:101-102） | 同 | 無 | 無 | code-read | **partial（confirmed）** | 嚴格解析：非負安全整數，否則 invalid |
| 1.1e | 合法 reset vs 不合法跨 epoch | Rust／iOS：`reason==session-reset && epoch!=local` 才 Reset；桌面無此規則 | — | Rust `pure_functions.rs::accept_state_handles_snapshots_and_session_reset`；iOS `SessionClientTests` | 無 | test-exists（Rust／iOS） | partial（桌面） | reducer：不同 epoch 無 reason → realign（走 POST resume） |
| 1.1f | 卸載重掛／連線切換／daemon 重啟 | `lastEventSequence`（:316-324）用 RuntimeEvent.sequence 去重；daemon 重啟後 sequence 從 1 重來 → 事件永久被丟 | App.tsx:249/255 連線切換只 bump refreshKey | 無 | 無 | code-read | **partial（confirmed）** | 去重改用協定 (epoch,sequence)／messageId；連線切換觸發 resume |
| 1.2a | 持久化容器格式版本 | `Snapshot`（types.rs:173-185）無版本欄位 | — | 無 | known-limitations #21 | code-read | partial | `format: u32`（0＝v0.6.0 無版本；1＝現行），與 AIP wire version 分開 |
| 1.2b | 新增 default 欄位不誤判損壞 | restore（session.rs:249-300）第二次 canonical 重 hash | `CharacterSessionHost::open` | `session_hardening.rs::restore_rejects_a_poisoned_snapshot_even_with_a_matching_hash` | #21 | test-exists | **partial（confirmed by code）** | 第二次 hash 改為 migration 偵測；未知鍵仍靠 deny_unknown_fields＋Attention 鍵檢查拒絕 |
| 1.2c | 區分舊版本／未來版本／暫時無法讀／損毀／migration 中斷 | load：NotFound／Corrupt／Unavailable 三分；無未來版本概念；Unavailable 只跳過開機 save，之後 persist 仍覆寫 | character_session.rs:223-233, 240-320 | `a_transient_read_failure_never_throws_away_the_stored_snapshot` | — | test-exists | partial | 新增 `PortError::FutureFormat`、parked store、備份檔、五種結果各一測試 |
| 1.2d | 真實 v0.6.0 快照 fixture | 無 checked-in 快照樣本 | — | 無 | — | code-read | not-reproduced | 由 HEAD 產生（session／aip crate 自 v0.6.0 tag 零差異，`git diff --stat v0.6.0..HEAD` 為空） |
| 1.2e | Downgrade 行為 | 無偵測 | — | 無 | — | code-read | partial | 測試證明 format 1 可被只認舊型別的讀者讀取；矩陣寫進文件 |
| 1.3a | 舊 snapshot 不得晚到覆蓋新 revision | `JsonSessionStore::save`（:212-221）無 guard；各 `character_session_apply` 併發 spawn_blocking | Output::Persist → `character_session_persist`（:1333） | `concurrent_persists_never_publish_a_spliced_snapshot`（只驗不拼接） | — | code-read | **partial（confirmed by code；決定性重現＝先 save(6) 再 save(5)）** | (epoch,revision) guard；`SaveOutcome` |
| 1.3b | persist 錯誤可觀測 | `let _ = spawn_blocking(...).await` 吞掉 Result／JoinError | 同上 | 無 | — | code-read | **partial（confirmed）** | 計數＋固定文字 note 進 diagnostics |
| 1.3c | broadcast／persist／ack／重啟可恢復範圍 | outputs 依序：Broadcast 在 Persist 前；每 32 revision／60 s 持久化；resume 補洞 | session.rs persist_if_due | `a_restart_resumes_the_same_session_and_asks_members_to_renegotiate` | — | test-exists | implemented | 文件化，不宣稱 exactly-once |
| 1.4a | hash 涵蓋與 canonical 規則三端一致 | Rust `canonical_json`（aip/lib.rs:62-95）；iOS 逐字保留數字字面；TS 刻意不核對（0.0 vs 0） | — | Rust `canonical_json_sorts_keys_recursively_and_is_stable`；iOS `testSnapshotStateHashMatchesTheRustFixture`（用手寫 intensity 0 的 fixture） | transport-bindings.md:165 | code-read | partial | **已做**：三端共用 `stateHashes` fixtures（9 個，host 真實輸出）＋`stateHashDoublePaths` 漂移 gate（commit 475c7ac、055d638）；TS 依 doublePaths 印 `x.0`；Rust／Swift 消費者測試 |
| 1.4b | -0、0/0.0、整數邊界、巢狀順序、中文、新欄位 | `clamp_unit(-0.0)` 產生 `-0.0`；fixtures 無中文／-0／unsupportedIntents | state.rs:159-166 | 無 | — | code-read | **partial（confirmed：-0.0 可產生）** | clamp 正規化＋violates_limits 拒絕；fixtures 已涵蓋 |
| 1.4c | MAX_UNSUPPORTED_INPUTS 漂移 gate | envelope.ts:652 手寫 32 | — | 無 | — | code-read | partial | 進 golden schema limits（`maxUnsupportedInputs`）→ codegen → TS 讀生成常數 |
| 1.4d | hash ≠ 簽章 | 文件未明說 | — | — | — | doc | — | 文件（M4） |

### M2 通用擴充與安全移除

| # | 需求 | 現有實作 | 測試 | 已知限制 | 證據 | 起始狀態 | 本輪動作 |
|---|---|---|---|---|---|---|---|
| 2.1a | SensorSource port（識別／列舉／停止／deadline／結果／cleanup） | 只有回報 trait `SensorStopOutcome`（sensors.rs:21-32）；停止路徑只有 local＋mobile；宣告式 adapter 從未 `declare()`，高風險受器對 `stop_all_sensors` 不可見 | `sensors_loop.rs::stop_all_sensors_is_honest_about_high_risk_receptors_it_never_asked` | v0.6.0 known-limitations §3 #12（runtime-boundaries-065） | test-exists | partial | 新增 port＋registry；local／mobile／第二來源同一協調器；宣告式 adapter 接 `stop_all`＋宣告 |
| 2.1b | estop／stop-all／provider disable-remove cleanup 一致 | estop 走 `stop_all_sensors`＋`close_declarative_links`；transition/revoke 不碰宣告表（家族級，正確） | providers_loop.rs 多筆 | v0.5.1 #4（指 `interaction_registry::CapabilityRegistry` 旗標，**不是** ProviderCapabilityRegistry） | code-read | implemented（語意不同屬刻意） | 測試釘住三條路徑的 cleanup 清單；澄清兩個 registry 命名 |
| 2.2 | Capability registry owner | `ProviderCapabilityRegistry` 掛 CharacterHub（character.rs:354）；唯二寫入 init_providers；讀者 character/activity/sensors | 6 個測試（見調查） | architecture-boundaries.md:100-106 承認暫掛 | test-exists | implemented（暫掛） | 搬到 RuntimeInner；唯讀 view trait；declare 收成 crate 內單一入口 |
| 2.3 | 第二裝置 AIP profile（Serial fixture） | 宣告式 adapter 零 AIP；DeviceMsg 無裝置→host 任意 payload 型別；身分＝hello.deviceId 字串＋裝置端配對碼（弱於 iPhone） | esp32_sim_conformance.rs、mqtt_loop.rs、protocol_honesty.rs | device-profile.md §6、adapter-development.md §3 | code-read | not-reproduced | 選 Serial：新增 `aip` 裝置→host 訊息（sim＋protocol.rs）、runtime 分派點、身分強度標示、生命週期測試、「停用第二 adapter 不影響桌面／iPhone」 |
| 2.4 | Character 專屬設定 | settingsTransfer.ts:124 用 `SHU_RIG_PALETTES` 全域驗證；:41 PERSONAS 硬編；CompanionPage `isShuRig` 字面判斷 | companion-gateway-wiring.test.ts:391-425 | #17 | test-exists | partial | 驗證綁 adapter meta（characterId→entrypoint→variants／personas）；ShuPlayControls 搬進 shu adapter 模組；hasPlayfield 驅動 |
| 2.5 | 分拆高耦合模組 | runtime.rs 2471／mobile.rs 3936（`mobile_handle_conn` 625 行）／character.rs 2769／character_session.rs 1518／App.tsx 1116／CompanionPage.tsx 1141／statusProjection.ts 1169 | — | — | code-read | — | 一次一個責任：JsonSessionStore→store.rs、CharacterSessionHost→host.rs、mobile AIP binding→mobile/session_binding.rs、App 路由→routing.ts、CompanionPage 主動對話→pages/companion/、statusProjection 依領域拆檔；每步同一組測試通過 |
| 2.6 | Port 盤點 | ports.rs 6 個 trait：只有 SessionStore 有 production 實作；Clock／IdentityVerifier 被繞過；RendererPort／DevicePort 零實作；ConsentVerifier 刻意不接 gate（fail-closed 在 session.rs:901-907） | ports.rs 內 3 個單元測試 | ports.rs:43-50 | code-read | implemented（by design） | 文件加成熟度欄；不因介面存在宣稱授權流程可用 |


### 補充列（完整性批評者指出的盲點，2026-09-05）

| # | 需求 | 現況（code-read） | 起始狀態 | 本輪動作 |
|---|---|---|---|---|
| X1 | `emergency_stop` 與 `stop_all_sensors` 的誠實回報一致 | `emergency_stop`（runtime.rs:988-1131）自行手刻 `StopAllSensorsReport`，**不呼叫** `unreported_high_risk_receptors`；`stop_all_sensors`（sensors.rs:260-294）會呼叫 | **partial（confirmed by code）** | 兩條路徑共用同一個聚合函式（M2 §3.1） |
| X2 | 通用 provider 端點對 mobile provider 的效果 | `POST /v1/providers/{id}/revoke`（`revoke_provider`，providers.rs:1199）不關 WS、不走 mobile 專屬停止；`PATCH state=disabled`（`transition_provider`，:1105）對 mobile 幾乎 no-op（不翻 receptor 旗標） | partial | SensorSource port 落地時讓 revoke／disable 走同一 cleanup；補交叉測試 |
| X3 | 宣告式 adapter（Serial／MQTT／BLE）的高風險受器進宣告表 | `register_declarative_spec` 從未 `declare()`；其 requires_consent 受器對 stop-all 與 no-stop-path 掃描皆不可見 | partial（runtime-boundaries-065） | 註冊時宣告＋實作 SensorSource（DeviceLink `stop_all`＋ack 等待） |
| X4 | deep links／URL 路由無退步 | 未調查 | not-reproduced | M3 補：列出既有 deep link（`LEGACY_ANCHORS`、tray `onNavigate`）並加測試 |
| X5 | 一般模式畫面不外洩 revision／epoch／UUID | 只確認進階模式才有「連接診斷」摺疊；未窮舉一般模式渲染字串 | partial | M3 補測試：一般模式 DOM 文字不含 `revision`／`epoch`／uuid 形狀 |
| X6 | 小樞互動／渲染無退步 | 既有 `rig.test.ts`／`playfield.test.ts`／`pnpm perf`；拖曳／玩耍／使魔正向效果無 e2e（Tauri-only） | partial（needs-environment） | 整合里程碑重跑既有測試＋`pnpm perf` 同條件；真 Tauri 視窗列 blocked |
| X7 | 已知限制 #21 的前提「v0.6.0 尚未發布，沒人會撞到」 | v0.6.0 已發布，前提失效 | doc stale | M1 修復後改寫該條（M4） |
| X8 | 誠實階梯全庫稽核 | 只在三處零星撞見（STORE_NOTE_UNREADABLE 誇稱隔離、no-stop-path、needs-reconfirmation 零裝置文案） | partial | 對抗審查加一個「誠實階梯」維度 |

### M3 一般模式簡化

| # | 需求 | 現有實作 | 測試 | 證據 | 起始狀態 | 本輪動作 |
|---|---|---|---|---|---|---|
| 3.1 | 角色頁首屏收斂 | 8 區塊全展開＋10 張內建角色卡；Tauri 約 48 個控制項（瀏覽器約 28） | characterPage.test.tsx；e2e app.spec.ts:139 只驗 heading | code-read | — | 首屏：目前角色＋預覽＋顯示/暫停＋陪伴摘要＋手機/同步摘要；其餘按需展開；預設（安靜／自然／活潑／自訂）映射既有 `companionExpressiveness`＋主動對話模式，不覆蓋自訂值、不改費用上限 |
| 3.1b | 六組「安靜」語意合併呈現但底層分開 | policy.quietHours（Governor）／companionDoNotDisturb／companionProactiveQuietUntil（只能從桌寵右鍵設）／proactiveDialogueQuiet／dndDefer／安全文字不受影響 | quietHours.test.ts | code-read | — | 一個控制區顯示實際受影響項目；不合成單一布林 |
| 3.1c | 第二入口名稱 | route id 固定 `companion`，label 動態角色名（characterName.ts） | e2e 多支 | test-exists | implemented | 保留；文件說明 |
| 3.2 | 同步卡下一步 | 只有「重新檢查」；`onNavigate` 在 CompanionPage 宣告但未解構、App.tsx:970 未傳；ConnectPage `initial` 只有 devices／safety，到不了 providers 子分類 | statusProjection-session.test.ts 十一態 | code-read | partial | 投影輸出穩定 `action` id；三處接線；ConnectPage `initial` 加 `providers` |
| 3.3 | 撤銷／移除終態 | `revokedDevice && remote.length===0` → needs-reconfirmation（statusProjection.ts:1115-1117）；provider 條目永遠留 revoked | statusProjection-session.test.ts:168 | test-exists | implemented（文件稱刻意） | 改語意：零裝置＋撤銷歷史 → 「目前僅在這台電腦使用」＋歷史紀錄；只有裝置回來才 needs-reconfirmation |
| 3.3b | store-reset 收斂 | load_note 為 host 不可變欄位；重啟才清 | character_session_loop.rs:1391 | code-read | partial | 成功持久化後降為歷史通知；parked／persist 失敗為 active issue |
| 3.4 | 任務驗收 | e2e 覆蓋：首次使用／連手機／緊急停止／取消工作／移除手機／撤銷後重連（兩支 serial）；換角色／陪伴程度／外觀／勿擾為 Tauri-only（只有 vitest mocked）；安靜時段只驗 heading；離線恢復只到 reconnecting | a11y.spec.ts 6 支（手寫，無 axe） | test-exists | partial | 補 e2e＋決策／點擊計數；390px；非開發者腳本（標未測） |

### M4 協定、文件與發布證據

| # | 需求 | 現有實作 | 起始狀態 | 本輪動作 |
|---|---|---|---|---|
| 4.1 | 生成完整性 | Rust→schema→TS/Swift；`aip:check` 漂移 gate；MAX_UNSUPPORTED_INPUTS 手寫 | partial | 見 1.4c；文案測試只保護語意與安全句（不逐字） |
| 4.2 | 文件過期 | README:107-111／146／236 稱 v0.6.0 為候選；general-mode-ux.md:55 稱 Runtime 未投影協商結果（character_session.rs 已輸出 unsupportedIntents）；AGENTS.md 稱已發布 v0.5.1；README §6 說 reset 需 epoch「大於」而 character-session.md §7 與程式碼為「不同」 | partial | 逐項修正並標適用版本與證據 commit |
| 4.3 | release/evidence 索引 | 無 | — | `docs/releases/evidence-index.json`（版本／commit／CI run／證據等級／限制指標）；文件引用它 |
| 4.4 | CHANGELOG claim-check | release-verify.sh 要求最上方段落重述保護行為 | — | 改綁版本＋commit；行為由 executable tests 保證 |
| 4.5 | Release scripts | Windows CRLF 已修（8826656）；管線退出碼待查；crate 版本策略待查；簽章／SBOM／provenance 現況待查 | — | 調查中（維度尚未回報） |
| 4.6 | 真機與性能 | iPhone AIP 層真機零執行；真 Tauri 視窗未走查；perf 60 s soak vs v0.5.1 10 min 不可比（文件已誠實揭露） | needs-environment | 保存 blocked 清單；本機可做：typecheck／模擬器 build／simctl XCTest；`pnpm perf` 同條件重跑 |

## 2. 已完成步驟（依時間）

1. 基線（§0.2）— 2026-09-05。
2. `475c7ac` test(aip)：三端共用 state-hash fixtures（9 個，host 真實輸出；manifest `stateHashes` 段；Swift 內嵌重生）。
3. `055d638` test(aip)：`stateHashDoublePaths` 由 schemars 推導（漂移 gate）。
4. M1 修復 workflow 執行中（A 儲存格式與寫入順序／B 桌面 reducer 與 TS hash／C Rust -0 與 Swift 消費者＋MAX_UNSUPPORTED_INPUTS gate），各由獨立驗證者重跑。

## 3. 下一動作

- M1 完成 → 主 agent 統一 `pnpm aip:codegen`、跑 M1 相關測試、依驗證者 verdict 修補、分 commit 提交。
- M2：依 §1 M2 表逐項（2.2 → 2.1 → 2.3 → 2.4 → 2.5），每項獨立 workflow（find→fix→verify）。
- M3、M4 依序。整合里程碑跑全套（§0.2 同一組命令）並記錄實際數字。
- 對抗審查：`.claude/workflows/adversarial-review-adaptive-interaction.js`／`adversarial-review-v06` skill（find／verify 分開）。

## 4. Blockers（需要人）

- iPhone 真機：Apple Developer Team 簽章、Developer Mode、鑰匙圈授權、實體操作（README §H1–H6）。
- ESP32 真板：無實體裝置。
- 真 Tauri .app 視窗走查：本 session 為無頭環境，未執行。
- `xcodebuild -destination`：需下載 iOS 26.5 平台元件；本機只有 iOS 26.2 模擬器 runtime（可用 `-sdk`＋simctl 注入替代）。
