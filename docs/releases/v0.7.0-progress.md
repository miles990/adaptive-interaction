# v0.7.0收斂（第二輪）— 差異矩陣與進度（工作文件）

> 恢復點：任何新 session 先讀 §0（起點與基線）、§4（已完成步驟）、§5（下一動作）。
> 第一輪（M1–M4 可維護性）的矩陣在 `docs/releases/v0.6.x-maintainability-progress.md`；本輪不重做
> 那裡已標 verified 的項目。所有數字都是本輪實跑；未跑的標 `not-run`／`needs-environment`。
> 模擬器／fixture／程序內 client 逐列標示，真機／真板零執行除非另有證據。
> 尚未產生的數字一律寫佔位字樣（發布前只剩【發布回填】），不以估計替代。

## 0. 起點事實（2026-09-05 晚）

| 項目 | 值 |
|---|---|
| 分支 | `feature/v0.6.x-maintainability`（PR #4 draft，標題含「do not merge yet」） |
| 起始 HEAD | `f5b8e2fc9e86d8fa8067f91303dbbcb8d04cf42f`（＝origin 同名分支；worktree 乾淨；無未追蹤檔） |
| `origin/main` | `8f528374fd0b1490eacb623f6e299339bfd565e0`（CI 33920312398 success） |
| 最新 tag／Release | `v0.6.0` → `4bd55fe`（Release 23 assets，2026-09-04T21:06:58Z）；無 `v0.6.1` |
| PR #4 CI | run 33959613144（`f5b8e2f`）四個必需 job 全 success |
| Branch protection／rulesets | 無（`gh api …/branches/main/protection` 404；rulesets 空） |
| 合併形狀 | PR #1／#2／#3 皆為線性（rebase merge，單 parent） |
| 版本決定 | **0.7.0**（minor）：本輪對外新增向後相容能力（新 HTTP 路由與 CLI 子指令、diagnostics／status 新欄位、裝置線 v1.1→v1.2 分片、AIP `reason:"recovery"`、provider 停用／重新啟用語意）；無 breaking |
| 磁碟 | 4.7 GiB → `cargo clean`（使用者同時下令）釋放 39.2 GiB |
| 隔離 worktree | `../ai-wt-m1`／`../ai-wt-m3`／`../ai-wt-swift`／`../ai-wt-ts`，各自從 `f5b8e2f` 或鏈上某點分出；主 worktree 只做 TS／src-tauri／文件 |

### 0.1 Candidate 基線（`f5b8e2f`，本機 macOS Apple Silicon，`cargo clean` 後冷建）

| 套件 | 命令 | 結果 | 時間 |
|---|---|---|---|
| Rust workspace | `cargo test --workspace --no-fail-fast` | **1126/0/0**（89 二進位），exit 0 | 4:04 |
| Tauri backend | `cargo clippy --manifest-path apps/interaction-desktop/src-tauri/Cargo.toml --all-targets -- -D warnings`／`cargo test --manifest-path …` | clippy 乾淨；**57/0** | — |
| 前端 | `pnpm aip:check`／`pnpm typecheck`／`pnpm test`／`pnpm build` | 同步／0／**1622/0（78 檔）**／built 3.14 s | — |
| CLI e2e | `./scripts/v03-cli-e2e.sh` | **96/0** | — |
| Playwright | `INTERACT_AI_MOBILE_ADVERTISE=0 pnpm test:e2e` | **88/0** | 3.5 min |
| iOS XCTest（**模擬器** iPhone 17；simctl 注入流程） | `apps/interaction-ios/README.md` 四步 | **Executed 126 tests, 0 failures** | 19.9 s |
| ESP32 韌體 | `./firmware/esp32-companion/compile.sh` | EXIT=0（bin 939 536 bytes；arduino-cli＋ctags-shim） | 52.9 s |

註：Playwright 會重寫 `docs/assets/v06-evidence/*.png`（像素差），已 `git checkout -- docs/assets/` 還原。
完整基線紀錄：本輪 scratchpad `baseline/SUMMARY.md`（不入 repo）。

## 1. 差異矩陣（需求｜現有實作｜production 呼叫路徑｜測試｜剩餘限制｜本輪結果）

證據等級：`code-read`（讀到程式碼）、`test-exists`（讀過對應測試斷言）、`test-run`（本輪實跑）、`doc-claim`（只有文件說）。
狀態欄是**本輪結果**：`done`（已落地且有實跑測試）／`partial`（主要缺口關掉、範圍寫在剩餘限制）／`deferred`（本輪不做，理由在該列）／`not-run`／`needs-environment`。
起始讀碼由 10 位唯讀讀者（sonnet）＋完整性批評者完成（workflow `v07-understand-gap-matrix`，11 agents，21 min），主 agent 逐條裁決。

### M1 三端協定決策

| # | 需求 | 本輪動作與結果 | 剩餘限制 | 狀態 |
|---|---|---|---|---|
| 1.1 | 同 epoch 新／舊／相同 revision 重播 | 三端改讀同一張表（`crates/interaction-session/src/receive.rs`／`sessionClient.ts`／`SessionReceive.swift`），45 個 `receiveDecisions` fixtures 逐筆對答案 | — | done（test-run） |
| 1.2 | 不同 epoch 合法／不合法 reset | 統一為 realign（`epoch-changed`）；host resume 對 epoch 不同回 `session-reset`，故一次收斂 | — | done |
| 1.3 | host 重啟／重灌／重新配對 | fixtures 覆蓋（epoch 變小＝重灌、sessionId 變＝重新配對） | — | done |
| 1.4 | 舊連線延遲到達的 reset | 規則 0：連線／請求世代先於 epoch；三端都把世代帶進決策 | SSE 事件流本身不帶世代，只擋得住飛行中的 GET／resume 回覆 | partial |
| 1.5 | sessionId／身分不符 | 規則 1 `reject-identity`；`dbeed51` 放寬為「local sessionId 已知才比對」 | 不採用狀態的決策不補記身分（下一次成功套用即收斂） | done |
| 1.6 | patch base 不符 | 三端 realign；fixtures 釘住 | — | done |
| 1.7 | resume 混合已套用＋新 patch | `decide_resume_batch` 純函式（良性舊項跳過不中止、第一個帶 effect 的決策中止整批） | 超過上限的**客戶端**分支無法從 wire 驗（513 則超過 typed boundary），只有純函式測試涵蓋 | done |
| 1.8 | HTTP 回應晚於較新 SSE | 取消 `allowRegression`／`hostRegressed` | — | done |
| 1.9 | 請求發出前本地 revision 已領先 host | 新 `reason:"recovery"`（host 只在同 epoch、只對該成員）＋規則 6 `recover` | — | done |
| 1.10 | 缺 hash／缺 sessionId／衝突 baseRevision | 規則 2：snapshot 缺 hash 或缺 state → `reject-invalid`（三端一致） | typed boundary 三端都**不**要求 `payload.hash`（缺 hash 的 patch 依規則 14「兩邊都知道才算不符」）；裁決為維持三端同一位置、同一結論 | done |
| 1.11 | 超大回應／錯誤格式／無限 realign | `MAX_RESUME_PATCHES=512`／`MAX_REALIGN_ATTEMPTS=3` 進 golden schema＋codegen，三端讀生成值 | — | done |
| 1.12 | 共用 fixtures | `manifest.json` 第 11 段 `receiveDecisions` 45 案例＋產生器＋三個獨立消費者 | fixture 期望值由 Rust 產生器算出；守門是只讀 JSON 的第二消費者＋突變驗證＋三端交答案 | done |

### M2 Serial 能力邊界

| # | 需求 | 本輪動作與結果 | 剩餘限制 | 狀態 |
|---|---|---|---|---|
| 2.1 | 實際 UTF-8 wire frame 量測 | 實測表寫進 `docs/aip/device-profile.md` §6.2（capability 318／snapshot 814–1019／含 members patch 686–810／不含 members patch 515）；BLE 上限文件 512→**480** 更正 | 量測來自 `declarative_session_loop::the_measured_wire_sizes_…`，重跑要看該測試的 `MEASURED wire line bytes:` 輸出 | done（test-run） |
| 2.2 | snapshot／含 members patch 超限 | 由分片解決（2.4） | 只對宣告 `aip.frag/1` 的裝置 | partial |
| 2.3 | 成員模式分類 | Runtime `derive_sync_profile` 推導 `full-state`／`intent-only`／`event-source`，投影到 diagnostics／status／稽核；桌面接線（`bc6d834`）非 full-state 不顯示綠勾 | 依協商好的 `role` 而非逐項 intents；`docs/DESKTOP-GUIDE.md` 同步狀態表尚未補 `partial-sync` 列 | done |
| 2.4 | 有界分片與重組 | 裝置線 v1.2 `aip-frag`（`fragment.rs`：1 in-flight／8 KiB／64 片／2 s 逾時／cancel／crc32；重複亂序缺片截斷惡意 total 整筆丟棄＋稽核）；模擬器雙向分片；參考韌體忽略且不宣告 | 出站中途失敗不重送；入站對未宣告的裝置一律拒絕（刻意嚴格）；真板零 | done（test-run，pty 模擬器） |
| 2.5 | 局部投影 scope／revision／hash | **本輪不做**；設計方向記在 `docs/aip/transport-bindings.md` §8.1 | 含 members 的 patch 仍整段重送 | deferred |
| 2.6 | pty production adapter 走 snapshot→patch→重連；韌體編譯 | production `DeviceLink`＋pty 模擬器測試；`compile.sh` 兩組態 EXIT=0（含 `--ble`） | 真板零；MQTT 有 rebind 測試（`b6d849c`），BLE 無 | done（模擬器） |

### M3 可恢復的裝置生命週期

| # | 需求 | 本輪動作與結果 | 剩餘限制 | 狀態 |
|---|---|---|---|---|
| 3.1 | Disabled→Available 不重啟 | `declarative_lifecycle.rs`（`Bound`／`Rebinding{gen}`／`Unbound{reason}`，上限 64）＋八步有界 rebind（3 s 收斂／20 s 握手／40 s 總預算）；握手 Ready 才 `Available` | rebind 失敗後 supervisor 仍退避重連；預算固定不可設定 | done（test-run） |
| 3.2 | disabled／disconnected／revoked／removed 四態 | rebind 期間誠實 `Disconnected`；revoked／removed 不得復活 | `Removed` 沒有 production 設定路徑（只有 `note_declarative_removed()` 明確表態入口） | done |
| 3.3 | 反覆 enable/disable、重入、timeout、拔線、舊 callback、enable 中 revoke、A 不影響 B | 新測試涵蓋；registry 每 provider id 一把鎖；世代守衛丟棄晚到回呼 | MQTT 只有一支 rebind 閉環測試，BLE 無 | done |
| 3.4 | orphan Q1：TTL 到期是否變「全部正常」 | 到期轉入不受 TTL 影響的 `unresolvedStops` 摘要 | 只在記憶體、不跨重啟 | done |
| 3.5 | orphan Q2：同 ID 新來源是否錯誤解除 | 登記帶單調世代；新來源不清舊世代 | — | done |
| 3.6 | orphan Q3：即時／未解決／歷史分開 | 三層＋`GET /v1/sensors/unresolved`／`POST …/dismiss`／CLI／status／桌面三處呈現＋二段確認＋人話 `sourceLabel` | 解除只記人的決定（`confirmedStopped:false`）；tray 文字隨刷新週期更新 | done |

### M4 陪伴預設與設定恢復

| # | 需求 | 本輪動作與結果 | 剩餘限制 | 狀態 |
|---|---|---|---|---|
| 4.1–4.4 | 兩段寫入的交易化與恢復 | `applyPresetPlan.ts` 純函式＋marker 與第一段同一次原子寫入＋第二段讀回確認＋mount 一次補送 | 恢復流程只有 vitest（mock host）＋src-tauri 單元證據，無真桌面實跑；清 marker 是第三次寫入 | done |
| 4.5 | 兩視窗 | single-instance 已擋，不另設計 | 只寫在程式註解與本文件 | done |
| 4.6 | 途中使用者自訂修改 | select／勿擾／進階主動對話區在交易期間停用；`patch()` 世代守衛 | 使用者若在停用前已改，補送會覆蓋他手改的模式（需人主動按） | done |
| 4.7 | 有效設定讀回失敗 | `unverified` 狀態＋「無法確認目前生效值」；Tauri 下 `prefsGet` 失敗不再誤報「瀏覽器檢視」 | — | done |
| 4.8 | `desktop_prefs_patch` persist 失敗不回滾記憶體 | 先持久化成功才寫回記憶體，失敗回滾（persist 可注入） | 其他呼叫端（`close_decision` 等）仍是舊順序 | done |
| 4.9 | 不動費用／權限／AI 幫手 | `companion-presets.test.ts` 一字未動 | — | done |

### M5 一般模式真實任務

| # | 任務 | 本輪動作與結果 | 狀態 |
|---|---|---|---|
| 5.1–5.3、5.5、5.6 | 新安裝／更換角色／陪伴預設／勿擾／顯示隱藏 | 分類寫進 `TASK_MANIFEST` 斷言；Tauri-only 四項由 `scripts/tauri-ax-walkthrough.sh` 真視窗 AX 走查（9/9 completed，連跑兩次） | done |
| 5.4 | 暫停主動對話 | 新增第 14 個任務＋量測 | done |
| 5.7–5.11 | 手機、取消工作、緊急停止、390px | 不動（fixture／browser 等級） | done（既有） |
| 5.12 | 可及性 | 補 onboarding 確認框／配對對話框／陪伴預設按鈕群 | done |
| 5.13 | 完成率／時間／求助／失敗恢復 | `taskMetrics` 新增 `durationMs`／`helpRequested`／`recoveredFromFailure`＋誤讀警告 | done |
| 5.14 | 真機受測者 | 五份腳本保留，**沒有受測者** | not-run |

### M6 可維護性

| # | 需求 | 本輪動作與結果 | 狀態 |
|---|---|---|---|
| 6.1 | AGENTS.md 必讀順序 | 獨立入口檔（107 行，七節；不變量仍只有 `CLAUDE.md` 一份） | done |
| 6.2 | 能力歸屬表 | `docs/MAINTAINERS-MAP.md`（10 列 × 七欄） | done |
| 6.3 | 可執行架構檢查 | `scripts/tests/architecture-checks.sh`（`--list`／`--docs`／`--ts`／`--rust`；未跑＝SKIP，swift 列驗存在但不執行） | done（`--rust` 於整合 `a1015a4` 實跑 PASS：179/0＋新增測試；見 final-report §7） |
| 6.4 | 相容路徑退場登記 | `docs/aip/deprecation-ledger.md`（12 條，10 條完整七欄，2 條刻意散文並說明） | done |
| 6.5 | 演練 | 五個演練各實跑一次＋兩支腳本＋一份 spec 範例；缺口 F1–F10 記在 `docs/releases/v0.7.0-drills.md` §7 | done |
| 6.6 | Session 續接 | 本檔 §4／§5 | done |

## 2. 裁決摘要（主 agent）

- **M1**：決策表 canonical 於 `docs/aip/character-session.md` §7.2；三端統一 realign；新增 `reason:"recovery"`（AIP 1.0 澄清，wire 不變）；取消 `allowRegression`；上限常數 canonical 化並進 golden schema。規則 1 採「local sessionId 已知才比對」的放寬解。
- **M2**：分片只進 transport binding（adapter-declarative＋模擬器＋韌體），核心零變更；成員 `syncProfile` 由 Runtime 推導，不新增宣告欄位；scope 局部投影不做。
- **M3**：rebind 複用既有 `DeviceLink` 建立路徑（自然 bump 連線世代，不自創第二套）；orphan 三層分離；未知不因 TTL 變成「已停止」。
- **M4**：use case 純函式＋marker 與第一段同一次原子寫入；persist 回滾先於 marker。
- **M5**：分類（completed／correctly-blocked／failed／not-run）寫成斷言；真 Tauri 用 AX 走查；受測者 not-run。
- **版本 0.7.0（minor）**；PR #4 ready 後 rebase merge；tag 從 main 通過 CI 的 SHA 打。

## 3. 波次

| 波次 | 內容 | 狀態 |
|---|---|---|
| W1 | M1 Rust＋fixtures｜M3 lifecycle＋orphan｜M4 presets｜M5 e2e | 完成 |
| W2 | M1 TS｜M1 Swift｜M2 Serial 分片＋syncProfile | 完成 |
| W3 | 身分放寬｜桌面 TS 接線｜M6 文件與架構檢查｜Rust／TS 收尾｜演練 | 完成 |
| W3c | 文件收斂（CHANGELOG／progress／known-limitations／migration／final report／evidence-index／acceptance-evidence） | 進行中（本檔） |
| W3d | 整合全套測試 → 對抗審查（`adversarial-review-v06x`，base `f5b8e2f`）→ 修復波 → 回歸 | done（審查 `713f8fe-20260905T181800Z` 54/42/11；五路修復 confirmed；整合全套於 `a1015a4`） |
| W4 | `release-prepare.sh 0.7.0` → PR ready → CI → rebase merge → main CI → `release-verify.sh` → `release-tag.sh --push` → Release 產物驗證 → 文件回填 | 待 |

## 4. 已完成步驟（含每步的本分支 commit 與實跑數字）

> 各 worktree 的原始 SHA 在 rebase 線性化後改變；**以本分支 SHA 為準**，括號內是原始 SHA 以便對帳。

1. 基線（§0.1）。
2. 讀碼 workflow（10 讀者＋完整性批評者）→ §1 矩陣 → 主 agent 逐條裁決（§2）。
3. **W1 M4 陪伴預設交易化** `49eee6c`：`applyPresetPlan.ts` 純函式（`beginPresetOp`／`shouldResumePendingOp`／`projectPresetStatus`）、marker 與第一段原子寫入、第二段讀回確認、mount 一次補送、交易期間停用、`patch()` 世代守衛、`prefsGet` 誠實文案、`desktop_prefs_patch` persist 失敗回滾。vitest **80 檔 1647/0**、src-tauri **60/0**、typecheck／build 通過。獨立驗證 **confirmed**（3 次突變）。
4. **W1 M1 Rust** `d941a9e`（原 `ace4ec9`→`0f99384`）：`receive.rs` 決策表（規則 0–15＋resume 逐則＋`RealignBudget`）、`accept_state_with_epoch` 薄包裝＋`StateDecision::Recover`、host `reason:"recovery"`（並移除 runtime 那段無效的 `session-reset` 改寫）、limits 進 golden schema＋codegen、`receiveDecisions` 43 案例＋產生器＋只讀 JSON 的第二消費者。interaction-session **132/0**、interaction-aip **31/0**、e2e golden **12/0**、runtime **467/0**、api **47/0**、docs-claims **106/0**。獨立驗證 **confirmed**（2 次突變）。
5. **W1 M3 lifecycle＋orphan** `b32ba10`（原 `9202f6f`→`e5fbbab`）：`declarative_lifecycle.rs`、八步有界 rebind、Ready 才 Available、registry 每 provider 一把鎖、停用順序改「先停來源→關連線→翻旗標」、入站 `aip` 綁定閘門、感測來源世代、`unresolvedStops`＋HTTP＋CLI＋status、移除 `needs-restart-to-rebind`。cargo workspace **1142/0**（該 worktree 的加總法）、CLI e2e **96/0**。獨立驗證 **confirmed**（2 次突變；low：GET unresolved 未擋 agent token → 由 `9799b1e` 收緊）。
6. **W1b M5 一般模式** `e7348a8`／`2010852`：任務 14、分類漂移防線、結果摘要 JSON、量測三欄位、對話框可及性、陪伴預設按鈕群 a11y、`scripts/tauri-ax-walkthrough.sh` 真 Tauri 走查 **9/9**（連跑兩次）。Playwright **91/0**（3.7 min）、vitest **81 檔 1662/0**。獨立驗證 **confirmed**（3 次突變）。
7. **W2 M1 TS** `e2179d5`（原 `b1157bb`）：`sessionClient.ts` 依表（規則 0／1／2／6、取消 `allowRegression`、上限改讀 `AIP_LIMITS`）、`receive-decision-fixtures.test.ts` 逐筆、零差異棘輪。vitest **81 檔 1709/0**、typecheck／build／`aip:check` 通過。獨立驗證 **confirmed**（2 次突變）。
8. **W2 M1 Swift** `050bac7`／`a0d3309`／`1c8c2fa`（原 `727da05`／`bbfedf4`／`9e2bfd6`）：`SessionReceive.swift` 規則 0–15、`ReceiveDecisionConformanceTests`、`SocketTransport`＋可注入排程＋五個背景閘門測試，並修掉「resume 補丁攤平形狀從未被解析」的既有 bug。XCTest（**iPhone 17 模擬器**）**144/0**。獨立驗證 **confirmed**（2 次突變）。
9. **線性化一**：鏈 rebase 到 M5 之後並 fast-forward feature（`2010852` → `d941a9e` → `e2179d5` → `b32ba10` → `050bac7`／`a0d3309`／`1c8c2fa`）。
10. **W3 身分放寬** `dbeed51`（原 `dfdc384`）：規則 1 只在 local sessionId 已知時比對；fixtures 43→**45**（新增選填 `expect.sessionIdAfter`）；三端同步。Rust **167/0**（`-p interaction-session -p interaction-aip`）、vitest **1728/0**、XCTest **146/0**（模擬器）。獨立驗證 **confirmed**（2 次突變）。
11. **W3 桌面 TS 接線** `d7199a8`：`projectUnresolvedStops`＋狀態列 banner／連接頁逐筆＋二段確認／tray「感測停止待確認 N」；`projectProviderConnection`（重新連線中／沒有重新連上＋人話原因）；ProactiveSettings 交易期間 disabled；X5 守門加 `generation`／`sourceId`。vitest **83 檔 1750/0**、src-tauri **62/0**。獨立驗證 **confirmed**（3 次突變）。
12. **W3 M6 文件與架構檢查** `3c0fda3`／`ea2aef9`／`2b5c640`／`160d880`／`2da6ebc`／`248e120`／`b3a49f7`：`architecture-checks.sh`、deprecation ledger、MAINTAINERS-MAP、獨立 AGENTS.md、README／ARCHITECTURE／FEATURES 補新模組、docs-claims 加 AGENTS.md。docs-claims **107/0**、release-scripts **54/0**、`--ts` 組 **6 檔 127/0**。獨立驗證 partial→修正→**confirmed**。
13. **W2b M2 裝置線 v1.2＋syncProfile** `9799b1e`（原 `d52c5d7`）：`fragment.rs`、`aip-frag`、模擬器雙向分片、`DeviceOutbound::{max_line_bytes,supports_fragmentation}`＋`derive_sync_profile`、實測 wire bytes、M3 收尾兩項（rebind 保留人類關閉的受器、GET unresolved 進 agent 排除清單）。adapter-declarative **163/0**、runtime **486/0**、api＋cli **80/0**、registry **23/0**、`compile.sh` 兩組態 EXIT=0。獨立驗證 **confirmed**（5 次突變；實測表逐位元組重現）。核心零變更（`interaction-session`／`interaction-aip` diff 為空）。
14. **線性化二**：feature HEAD 依序 `d7199a8` → `dbeed51` → `3c0fda3`…`b3a49f7` → `9799b1e`。
15. **W3 收尾與演練**：
    - `bc6d834` 桌面 syncProfile 接線（`partial-sync` 態）＋「重新啟用這台裝置」按鈕。vitest **83 檔 1769/0**、src-tauri **63/0**、typecheck／build／clippy 乾淨。
    - `4329c1e` `UnresolvedStop` 帶人話 `sourceLabel`（provider 顯示名 → class label；查不到就省略），status 與 `GET /v1/sensors/unresolved` 兩份清單欄位逐欄相同由 API 測試釘住。
    - `5c82671` `architecture-checks.sh` 首次實跑 `--rust`（**179 tests, 0 failed**）並修掉兩條從未執行的錯誤路徑與 swift 列的橡皮圖章。
    - `8159d20` 三份文件把已合併的 v1.2 改寫成實際位置（21 個 file:line 與 4 個測試名逐一核對）。docs-claims **107/0**。
    - `b6d849c` MQTT rebind 閉環測試（in-process broker＋fake device **模擬器**），6 次連跑各 1 passed。
    - `d391f38` 演練 2：event-source 裝置走 production serial adapter（`declarative_session_loop` **26/26**）。
    - `3b0af66` 演練 4：`scripts/drills/provider-disable-reenable.sh`（真 daemon＋pty 模擬器；零程式碼變更）。
    - `75a6f27` 演練 5：`scripts/drills/remove-package-keep-user-data.sh`（真 daemon；`desktop.json` 前後 sha256 相同、稽核筆數不變、epoch 未跳）。
    - `713f8fe` `docs/releases/v0.7.0-drills.md`（五個演練＋§7 十條發現）。docs-claims **107/0**。
16. **W3c 文件收斂**（本步）：CHANGELOG `[Unreleased]` 新增「本輪（v0.7.0）」分組（逐條附本分支 SHA）、本檔、`v0.6.x-maintainability-progress.md` 加第二輪指標、`v0.7.0-known-limitations.md`、`v0.7.0-migration.md`、`v0.7.0-final-report.md`（草稿）、`evidence-index.json` candidate 更新、`acceptance-evidence.md` 本輪證據等級表、`v0.6.x-final-report.md` §11 標註已修三條。

### 4.1 待整合處理（主 agent 記錄）

- **整合全套已於 `a1015a4` 實跑**（依序、同機）：fmt 乾淨／clippy 0 warning／`cargo test --workspace --no-fail-fast` **1245/0（96 二進位）**／Tauri backend clippy 乾淨＋**63/0**／`aip:check` 同步／typecheck 0／vitest **1815/0（84 檔）**（`1a585cc` 後 1816/0）／build ok／CLI e2e **96/0**／Playwright **91/0**（3.6 min）／iOS 模擬器 XCTest **158/0**／`compile.sh` 與 `--ble` EXIT=0／`architecture-checks.sh --rust` PASS／`--ts` PASS／docs-claims 於 `a1015a4` 151/3（計數漂移，`e5e14d4` 修後 **154/0**）／release-scripts **58/0**／release_provenance ok；`pnpm perf` 同條件對照無退步（final-report §9）。各步驟的分項數字（§4）是各自 worktree 當時的實跑值，整合值以本條與 `v0.7.0-final-report.md` §7 為準。
- Rust workspace 的總數在不同 worktree 用不同加總法（`b32ba10` 的 1142 是逐行 `test result` 加總，含 doc-tests），整合里程碑要以同一條命令重跑一次才可對帳。
- M3 限制：rebind 失敗後 supervisor 仍退避重連；`unresolvedStops` 不跨重啟；BLE rebind 無專屬測試；`transition_provider(Available)` 對宣告式裝置回 `Disconnected`（誠實，但是對外可見的行為變更）。
- M4 限制：清 marker 為第三次寫入；恢復流程只有 vitest mock＋src-tauri 單元證據。
- TS：進階診斷 alignment 計數名稱更換；SSE 事件流本身無世代，規則 0 只擋飛行中的 GET／resume。
- 演練 §7 的 F4（`SemanticState` 不在跨語言 schema）、F5（沒有測試擋得住選填欄位寫成 `null`）、F1（`adapter-development.md` §1 把加角色與加 adapter 混成一步）本輪不修，列為已知限制。

## 5. 下一動作

- **已完成（2026-09-06）**：PR #4 merged（main `630b429`，CI run 33993815652 綠）→ `release-verify.sh 0.7.0 --run-tests` 全過 → annotated tag `v0.7.0`（`b2952bf` → `630b429`）→ Release workflow run 33994408124（success，23 個資產；checksum 9/9 .sha256 本機驗證相符（CLI 四平台壓縮檔＋dmg／AppImage／deb／msi／exe）；aarch64 .app.tar.gz 依設計無 checksum；CLI smoke interact-ai-v0.7.0-aarch64-apple-darwin.tar.gz 解壓後 interact-ai --version → 0.7.0）。
- 後續（下一版本）：真機／真板／受測者（§6）；`pending-full-state` 的 wire 回執（AIP 契約層）；MQTT／BLE 更多閉環；docs 的絕對數字 lint。

## 6. Blockers（需要人或環境）

| 項目 | 狀態 | 需要什麼 |
|---|---|---|
| iPhone 真機（AIP／Character Session 層） | needs-environment | Apple Developer Team 簽章、Developer Mode、鑰匙圈授權、實體操作（`apps/interaction-ios/README.md` §H1–H6） |
| ESP32 真板（含 v1.2 分片路徑） | needs-environment | 實體板子；另需讓參考韌體宣告 `aip.frag/1`（目前刻意不宣告） |
| 非開發者受測者（M5 五份腳本） | not-run | 受測者與觀察紀錄 |
| 真 Tauri AX 走查進 CI | needs-environment | macOS 桌面工作階段＋輔助使用權限（本機可跑，CI 不行） |
| push／PR ready／tag／Release | 待授權 | 使用者明示；本輪一律不自行 push |
