# v0.6.0 已知限制

> 本文件做兩件事：(1) 把對抗審查 `6683403-20260904T161327Z`（對象 commit `6683403`，規格
> `docs/aip/README.md`）的 **73 個 confirmed finding 逐一列出處置**，另附 refuted 7 則（其中 1 則
> 報告本身的 verdict 欄位其實是 `unverified`，本文件依 verdict 欄位歸類，見 §2.2 說明）；
> (2) 列出 v0.6.0 本輪新增的窄限制，以及 v0.5.1 已知限制清單（8 項保留＋14 項窄限制）在 v0.6.0
> 是否仍然成立。
>
> **分類用字**：**已修** ＝ 根因消除且有回歸測試（含跨組收尾補齊、已完成獨立覆核的項目，見
> §2.1 備註欄）；**部分修** ＝ 主要缺口關掉、剩下的範圍在備註欄寫明並指出負責範圍；**deferred** ＝
> 修復者自己判斷根因不在自己擁有的檔案清單內，誠實不動並轉交；**保留** ＝ 本輪未動（含刻意保留
> 的設計決定）。真機／模擬器／fixture／browser／真 Tauri 視窗／單元字樣照原始紀錄，不美化。
>
> **跨組收尾已全部完成**：先前撰寫本文件時仍在進行的 4 項——`general-mode-ux-022`／
> `capability-consent-052`（Runtime 把協商結果 `NegotiatedCapabilities` 投影進 `MemberView`／
> diagnostics 的 `members[].unsupportedIntents` 欄位：`#[serde(default)]`，由
> `negotiated.intents` 過濾；restore 後未重新協商的成員全部標 unsupported）、
> `reconnect-recovery-044`（`crates/interaction-runtime/src/mobile.rs` 斷線收尾對已協商裝置送
> `interaction_session::Presence::Reconnecting`，逾時後才由 session tick 轉 `Offline`，撤銷仍是
> `leave`）、`identity-binding-009`（`character_session_device_query` 對去重命中直接回
> `result{accepted,duplicate:true}`，不重跑 resume／snapshot）——現在都已收斂為**已修**。
> 另有兩則已修但曾有跨組鏡射瑕疵同步收尾的 finding，該伴生瑕疵現在也已收斂：
> `pairing-migration-001`（Rust 端 `crates/interaction-session/src/patch.rs::accept_state_with_epoch`
> 改為 `epoch != local_epoch` 即接受，與 iOS 端及契約 §7 第 4 步一致；測試在
> `crates/interaction-session/tests/pure_functions.rs::accept_state_handles_snapshots_and_session_reset`，
> 涵蓋 host 重灌後 epoch 從 1 起跳仍視為 Reset、無 `reason: session-reset` 不享例外）與
> `session-integrity-056`（`Runtime::start` 在 `estop_engaged` 時補送
> `RuntimeFact::Emergency{engaged:true}` 進 session，且排在 `restore_agent_sessions` 之前；測試
> `an_engaged_emergency_stop_is_replayed_into_the_session_on_startup`）。已同步更新這裡、
> `CHANGELOG.md`、`docs/releases/v0.6.0-migration.md` §13 與 `docs/acceptance-evidence.md`。
>
> **對抗審查 `6683403-20260904T161327Z` 的 73 個 confirmed finding 已處置（最終狀態，與
> `CHANGELOG.md` `[Unreleased]` 的「對抗審查 `6683403-20260904T161327Z`」段一致）：
> 已修 68／部分修 5／deferred 0**；high 22 全部已修（無一項保留為未動，也沒有任何一項仍在
> 跨組收尾中）。完整報告：`docs/reviews/adversarial/6683403-20260904T161327Z.md`（＋同名
> `.json`）。修復依檔案歸屬分成 7 組（`aip-cross-language`／`session-crate`／`runtime-host`／
> `ios`／`release-and-docs`／`desktop-and-tauri`，另加一個跨組收尾 agent），每組先寫「舊行為下
> 紅燈」的回歸測試再修，各自的獨立 verifier 覆核（`perFinding`）；journal 來源：
> `/Users/user/.claude/projects/-Users-user-Workspace-claude-lab-adaptive-interaction/a156fa79-234c-44cb-9073-2c41e7d88901/subagents/workflows/wf_39d322e6-ab3/journal.jsonl`。
> **12 個 finding 原本只有 fixer 自報、尚未有獨立 verifier 覆核，現已由 `runtime-host` 組的
> 獨立稽查 agent 逐一補上覆核**（`capability-consent-051`／`identity-binding-006`／
> `identity-binding-008`／`pairing-migration-002`／`pairing-migration-003`／
> `reconnect-recovery-041`／`reconnect-recovery-046`／`reconnect-recovery-047`／
> `runtime-boundaries-065`／`runtime-boundaries-066`／`runtime-boundaries-068`／
> `session-integrity-059`）：11 項確認**已修**、`runtime-boundaries-065` 確認為**部分修**
> （誠實回報已做，`SensorSource` port 未做），與 fixer 原本自報的結論一致；下表不再標示
> 「僅自報、尚無獨立驗證」。

## 1. 分類統計

| 指標 | 數量 |
|---|---|
| reviewed（審查總數，報告原文） | 80 |
| confirmed（已確認） | 73 |
| refuted（已駁回，verdict 精確為 `refuted`） | 6 |
| unverified（未驗證） | 1 |
| confirmed → 已修 | 68 |
| confirmed → 部分修 | 5 |
| confirmed → deferred | 0 |
| confirmed → 保留（未動） | 0 |

### confirmed 依 severity

| severity | 數量 | 已修 | 部分修 |
|---|---|---|---|
| high（高） | 22 | 22 | 0 |
| medium（中） | 33 | 29 | 4 |
| low（低） | 18 | 17 | 1 |

`session-integrity-056` 與 `pairing-migration-001`（皆為 high，計入上表的「已修」）先前各自有一個
**跨組鏡射的伴生瑕疵**同步收尾中，那個伴生瑕疵不是這兩則 finding 本身引用的缺陷（見上方「跨組
收尾已全部完成」段落），現在也已收斂完成，直接計入上表的「已修」，不再需要單獨追蹤。

## 2. 對抗審查處置表

> id 為完整 finding id 去掉共同前綴 `F-6683403-20260904T161327Z-`（例：`aip-protocol-034`
> 對應 `F-6683403-20260904T161327Z-aip-protocol-034`）。「回歸測試／備註」欄優先引用 fixer 回報的
> 回歸測試名稱與檔案，部分修的項目前置一句範圍說明。

### 2.1 confirmed（73 則，依 severity 分節）

### Severity: high（高） — 22 則

| id | 標題 | 處置 | 回歸測試／備註 |
|---|---|---|---|
| `aip-protocol-034` | iPhone app aborts (fatal trap) inside AIPEnvelope.validate() on a state envelop… | 已修 | crates/interaction-aip/tests/fixtures/invalid-state-revision-out-of-range.json (payload.revision = 1e30) 與 invalid-state-revision-beyond-u6… |
| `identity-binding-004` | member 重送 capability 會繞過 lastSeenAt 投影格線，讓已配對裝置把 revision／廣播打成無界成長 | 已修 | crates/interaction-session/tests/session_hardening.rs::capability_reannounce_does_not_flood_revisions |
| `pairing-migration-001` | iPhone 端把 host 的 session-reset 快照當成 rollback 丟掉，然後宣稱「已同步」（換桌面／換 epoch 後永久失聯） | 已修 | iOS SessionClient.swift 的 epoch!=／ignoredRollback／pairingDidChange 已修並經 verifier 覆核；Rust 端 crates/interaction-session/src/patch.rs::accept_state_with_epoch 已改為 `epoch != local_epoch` 即接受（與 iOS 端、契約 §7 第 4 步一致，跨組鏡射瑕疵已收斂）；apps/interaction-ios/InteractionCompanionTests/SessionClientTests.swift::testASessionResetIsAppliedWheneverTheEpochDiffersEvenIfItWentBackw…；crates/interaction-session/tests/pure_functions.rs::accept_state_handles_snapshots_and_session_reset（host 重灌後 epoch 從 1 起跳仍視為 Reset；無 `reason: session-reset` 不享例外） |
| `session-integrity-056` | 任何 task.* 真相轉錄都會清掉 emergency 守衛，緊急停止中互動重新被接受 | 已修 | session-crate 的雙重 emergency 守衛（submit_runtime＋director::on_fact）已修並經 verifier 覆核；`Runtime::start` 已在 `estop_engaged` 時補送 `RuntimeFact::Emergency{engaged:true}`（排在 `restore_agent_sessions` 之前，跨組鏡射瑕疵已收斂）；crates/interaction-session/tests/session_hardening.rs::task_truth_cannot_clear_an_emergency；crates/interaction-runtime/tests/character_session_loop.rs::an_engaged_emergency_stop_is_replayed_into_the_session_on_startup |
| `session-integrity-057` | 非目標成員可以用 correlationId 冒領 intent：假 observed 進計數器並吃掉 intent-expired 稽核 | 已修 | crates/interaction-session/tests/session_hardening.rs::a_non_target_member_cannot_settle_or_count_an_intent |
| `session-integrity-058` | 非乾淨重啟後 host 的 revision 會退回，成員依 §6 rollback 規則忽略權威 snapshot 而永久停在舊狀態 | 已修 | crates/interaction-session/tests/session_hardening.rs::restore_never_moves_the_revision_backwards 與 ::a_member_cannot_rebuild_a_live_sessio… |
| `session-integrity-059` | 裝置的第一則 capability frame 完全繞過 §8 安全管線的 schema／limits／sessionId 檢查 | 已修 | crates/interaction-runtime/tests/character_session_loop.rs::an_oversized_first_capability_frame_is_refused_before_it_can_join、::a_first_cap… |
| `capability-consent-049` | capability 重新協商把 token bucket 重設為滿，速率上限可被無限繞過 | 已修 | crates/interaction-session/tests/session_hardening.rs::capability_reannounce_does_not_refill_the_rate_bucket |
| `capability-consent-050` | 沒有收到過該 command 的成員可以把 Behavior Intent 結清成 observed，intents.observed 可被單一成員重複灌值 | 已修 | crates/interaction-session/tests/session_hardening.rs::a_non_target_member_cannot_settle_or_count_an_intent 與 ::a_target_that_already_repor… |
| `capability-consent-051` | 持久化檔壞到救不回 epoch 時，quarantine 產生的新 epoch 會與原本的 epoch 相同（0+1=1），成員收不到 session-res… | 已修 | crates/interaction-runtime/src/character_session.rs::tests::a_rebuilt_session_never_reuses_the_epoch_members_remember, ::the_next_epoch_con… |
| `character-package-017` | 匯入的 builtin:shape 角色被冒名成內建 ref-shape：adapter 丟掉 ctx.manifest／ctx.characterId | 已修 | apps/interaction-desktop/src/test/regressions-v06-round2-desktop.test.ts :: 「character-package-017：builtin:shape 的匯入角色不得冒名 ref-shape」 |
| `renderer-lifecycle-028` | sprite adapter 的 suspend() 會對 host machine 送 force clear-transient，抹掉受保護的安全 tra… | 已修 | regressions-v06-round2-desktop.test.ts :: 「028：suspend() 不得清掉受保護的安全 transient（blocked）」；companion-gateway-wiring.test.ts 的 MixerRenderer 門面… |
| `renderer-lifecycle-029` | MixerRenderer 不轉送 pause()／resume()，sprite 角色 suspend／hide 從未真的停掉 rAF | 已修 | regressions-v06-round2-desktop.test.ts :: 「029：MixerRenderer 要把 pause()／resume() 轉給真正的 renderer」與「029b：正式接線（MixerRenderer 包真 SpriteRenderer… |
| `renderer-lifecycle-030` | sprite adapter 對「混音器根本沒讓它上台」的 intent 仍回 started→completed | 已修 | regressions-v06-round2-desktop.test.ts :: 「030：混音器沒讓它上台時，sprite 要回 cancelled{preempted}，不得回 started／completed」 |
| `general-mode-ux-022` | 「部分能力目前不可用」看的是 role，不是協商結果：協商出 unsupported intent 的裝置在一般模式顯示綠色「已同步」 | 已修 | 桌面端已改用協商結果判定＋新增 capability-unknown 保守狀態；Runtime 端已把 `NegotiatedCapabilities` 投影進 `MemberView`／diagnostics 的 `members[].unsupportedIntents`（`#[serde(default)]`，由 `negotiated.intents` 過濾；restore 後未重新協商的成員全部標 unsupported）；apps/interaction-desktop/src/test/regressions-v06-round2-general-mode.test.tsx :: describe「capability-consent-052／general-mode-ux-022：綠勾只給真…」；crates/interaction-session/tests/session.rs::negotiated_unsupported_intents_are_projected_into_members；crates/interaction-runtime/tests/character_session_loop.rs::negotiated_unsupported_intents_reach_the_shared_state_and_diagnostics |
| `reconnect-recovery-041` | 壞檔隔離後 epoch 沒有真的 +1：重建的 session 與舊 session 同號，手機把 host 的新狀態當 rollback 永久忽略，卻顯示「… | 已修 | crates/interaction-runtime/src/character_session.rs::tests::a_rebuilt_session_never_reuses_the_epoch_members_remember（依 finding 的 REGTEST：e… |
| `reconnect-recovery-042` | 幾分鐘前排隊的觸碰事件可在重連後被當成新鮮互動套用：成員自報的 expiresAt 無上界，occurredAt 從不與 host 時鐘比對 | 已修 | crates/interaction-session/tests/session_hardening.rs::a_self_reported_deadline_cannot_outlive_the_interaction_ttl |
| `release-provenance-071` | release-tag.sh 在 macOS 預設 bash 3.2 下，只有「不跳過 CI」的正常路徑會 crash，--skip-ci 反而能通過 | 已修 | scripts/tests/release-scripts.sh（「release 腳本沒有未保護的 ${ARR[@]} 展開」+ 用 /bin/bash 3.2 跑 release-tag.sh 不帶 --skip-ci 的三條行為斷言）；crates/interaction… |
| `release-provenance-072` | tag→Release 全鏈沒有任何 CI 通過的強制關卡：ci.yml 不在 tag 上跑，release.yml 也不檢查 CI，--all-in-one… | 已修 | crates/interaction-cli/tests/release_provenance.rs::release_is_gated_on_ci_and_published_only_after_every_build_succeeds；scripts/tests/rele… |
| `release-provenance-073` | release-verify.sh 在跳過 CI／測試關卡時仍宣稱「✔ all gates passed」，違反誠實階梯 | 已修 | scripts/tests/release-scripts.sh（合成 repo 實跑 release-verify.sh --skip-ci：必須出現「跳過」、必須出現「測試未跑」、收尾不得是無限定詞 all gates passed、必須是 passed-with-skip… |
| `release-provenance-074` | self update / install.sh 的 sha256 驗證是 fail-open：抓不到 .sha256 就照裝，但 README／INSTAL… | 已修 | crates/interaction-cli/src/selfmgmt.rs::tests::missing_checksum_is_fail_closed_unless_explicitly_allowed（純函式 missing_checksum_policy）；crate… |
| `release-provenance-075` | 桌面安裝包完全沒有完整性驗證：無 .sha256、無簽章、無 SBOM／provenance，install-desktop 下載後直接 open | 已修 | crates/interaction-cli/tests/release_provenance.rs::desktop_bundles_are_verified_and_have_published_checksums（cmd_install_desktop 必須先 verif… |

### Severity: medium（中） — 33 則

| id | 標題 | 處置 | 回歸測試／備註 |
|---|---|---|---|
| `aip-protocol-035` | specVersion parsing disagrees across the three implementations: TS/Swift accept… | 已修 | Rust: crates/interaction-aip/src/version.rs::tests::version_syntax_has_no_fuzzy_edges（前後空白／換行不得被 trim、major/minor 溢出 u32 → None、u32 邊界仍可解析、… |
| `aip-protocol-036` | offline_policy classifies the only real approval-request name as state-reconcil… | 部分修 | 排序面已修（is_approval_name 排在 character.session.* 前）；offline_policy 無 production 呼叫者、§8「重連用它決定」的宣稱未接線，留給 session／文件組；Rust: crates/interaction-aip/src/offline.rs::tests::approval_names_need_a_fresh_human_decision（character.session.approval → RequireReconfir… |
| `identity-binding-005` | capability 訊息會把該成員的 rate-limit token bucket 重置成滿桶，成員可自行解除 §8 第 10 關的速率上限 | 已修 | crates/interaction-session/tests/session_hardening.rs::capability_reannounce_does_not_refill_the_rate_bucket |
| `identity-binding-006` | iPhone transport 的身分不符稽核與錯誤 envelope 直接回顯未驗證、無長度上限的攻擊者可控字串 | 已修 | crates/interaction-runtime/tests/character_session_loop.rs::a_forged_frame_never_echoes_unbounded_attacker_text |
| `identity-binding-007` | 遠端成員自報的 role 未經任何驗證即被採信，一台 iPhone 宣稱 host-renderer 就能讓一般模式顯示「已同步」，但它永遠收不到任何 Beh… | 部分修 | role 冒領已修（依 party.kind 夾住＋role-corrected 稽核，收得到 command）；「布林投影進 MemberView」未做，屬跨組 JSON 形狀變更；crates/interaction-session/tests/session_hardening.rs::a_device_may_not_claim_the_host_renderer_role |
| `pairing-migration-002` | character session 檔案救不回 epoch 時新 epoch 固定為 1，與成員記得的 epoch 相同（註解宣稱不會發生） | 已修 | crates/interaction-runtime/src/character_session.rs::tests::a_rebuilt_session_never_reuses_the_epoch_members_remember 與 crates/interaction-… |
| `pairing-migration-003` | 被新連線取代（superseded）的收尾略過高風險受器強制停用，重連在 45 s idle timeout 內即可保住 iphone.mic-level | 已修 | crates/interaction-runtime/tests/mobile_loop.rs::a_superseded_connection_still_forces_the_high_risk_receptor_off |
| `session-integrity-060` | host 自己送出的 capability 協商回覆可以超過 AIP payload 上限，且 unsupported_inputs 無上限存進成員紀錄 | 已修 | crates/interaction-session/tests/session_hardening.rs::a_capability_reply_never_exceeds_the_payload_limit；上游（`interaction-aip`）同步補上界：新增 `limits::MAX_UNSUPPORTED_INPUTS=32`，`negotiate_capabilities` 確定性截斷（取前 N 個）並回報 `unsupported_inputs_truncated`（`#[serde(skip)]`／`#[schemars(skip)]`，wire schema 不變）；crates/interaction-aip/src/capability.rs::unsupported_inputs_are_bounded_and_truncation_is_recorded；TS `negotiateCapabilities`（apps/interaction-desktop/src/aip/envelope.ts）同上限、同截斷邏輯 |
| `session-integrity-061` | restore() 只驗 snapshot 自洽的 hash，攜帶未知欄位的被污染 state 會被原樣重新廣播給所有成員 | 已修 | crates/interaction-session/tests/session_hardening.rs::restore_rejects_a_poisoned_snapshot_even_with_a_matching_hash |
| `capability-consent-052` | 一般模式「已同步／部分能力目前不可用」用的是成員自報的 role，不是協商後的 intent 支援度 | 已修 | 同 `general-mode-ux-022`——桌面資料路徑與 Runtime 投影皆已完成；regressions-v06-round2-general-mode.test.tsx 的 `capability-consent-052` describe，含「協商結果說有 unsupported intent → 部分能力目前不可…」；crates/interaction-session/tests/session.rs::negotiated_unsupported_intents_are_projected_into_members |
| `capability-consent-053` | 沒有任何目標的 Behavior Intent 照樣掛進 pending，TTL 到期被記成 intents.expired 與 intent-expired… | 已修 | crates/interaction-session/tests/session_hardening.rs::an_intent_with_no_target_is_dropped_not_expired |
| `capability-consent-054` | 緊急停止只清掉 host 自己的 pending，不對已經拿到 command 的 renderer 送 character.behavior.cancel | 已修 | crates/interaction-session/tests/session_hardening.rs::emergency_cancels_the_intents_already_sent_to_renderers |
| `character-package-018` | host 接線層仍用小樞專屬 helper 決定 variant，架構守門測試抓不到 | 已修 | regressions-v06-round2-desktop.test.ts :: describe「character-package-018：初始 variant／舊 pack 由 adapter 宣告，host 不猜」（3 個 case）；architecture-no-… |
| `character-package-019` | 匯入上限 32 MB 但讀取上限 8 MB：8–32 MB 的 sprite 角色匯入成功卻永遠載不起來 | 已修 | apps/interaction-desktop/src-tauri/src/character_store.rs :: tests::import_never_accepts_an_asset_it_can_never_read_back |
| `renderer-lifecycle-032` | adapter contract 的「資源清理」測試是空的：四個 case 都不會排任何 timer／rAF，斷言恆真 | 已修 | apps/interaction-desktop/src/test/adapter-contract.test.ts :: describe.each「adapter 自己擁有 renderer 時的資源清理」（shu-rig 自建 StageRenderer／sprite 自… |
| `renderer-lifecycle-033` | CharacterGateway.handshake 對 adapter.onInput() 沒有 try/catch，違反「任何 adapter 例外都不會… | 已修 | regressions-v06-round2-desktop.test.ts :: 「onInput 擲例外：例外不穿出 Gateway，實例不會卡在 negotiating，同 id 可以重新註冊」 |
| `general-mode-ux-024` | 出貨路徑（live 模式）讀不到權威狀態時完全沒有自動重試：卡片永遠停在 pending 的「同步尚未完成」，契約 §7.5 的「無法恢復，請重新連接」只能靠… | 已修 | regressions-v06-round2-general-mode.test.tsx :: 「連續讀不到會自己重試到契約 §7.5 的『無法恢復，請重新連接』，不必使用者按三次」 |
| `general-mode-ux-025` | 連接頁的手機卡只看 presence：online 一律寫成「角色同步：已同步」，與角色頁對同一台裝置的 partial-capability 自相矛盾 | 已修 | regressions-v06-round2-general-mode.test.tsx :: describe「general-mode-ux-025：連接頁手機卡與角色頁不得互相矛盾」（3 個 case） |
| `general-mode-ux-026` | 多裝置時「連上但尚未同步／已撤銷」的事實被綠色「已同步」蓋掉 | 已修 | regressions-v06-round2-general-mode.test.tsx :: describe「general-mode-ux-026：另一台裝置尚未同步／已撤銷時不得給綠色」（3 個 case，含「只是曾經有裝置被撤銷過（現在沒連著）不得壓掉一台真的在線的裝… |
| `evidence-honesty-010` | 成員可重複灌爆 intents.observed／rejected／failed 計數，甚至為從未收到的 command 回報「已呈現」 | 已修 | crates/interaction-session/tests/session_hardening.rs::a_target_that_already_reported_cannot_count_twice |
| `evidence-honesty-011` | iOS 開啟 Reduced Motion 時 react-happily-to-touch 完全沒有任何呈現，卻仍回報 result{observed} | 已修 | apps/interaction-ios/InteractionCompanionTests/SessionClientTests.swift::testReducedMotionStillHasSomethingToShowForReactHappilyToTouch |
| `evidence-honesty-012` | CHANGELOG [Unreleased] 明文宣稱 wave 2 的四項「尚未落地」，但四項都已在 HEAD 上 | 已修 | scripts/tests/docs-claims.sh（CHANGELOG [Unreleased] 不得寫「在落地前不會出現在這裡」；已落地的三個模組必須各有條目） |
| `evidence-honesty-013` | ARCHITECTURE.md 與 v0.6.0-migration.md 仍以「rg 搜尋不到對應實作」為由宣告桌面同步 UI 與 iOS Session… | 已修 | scripts/tests/docs-claims.sh（ARCHITECTURE.md 不得含「搜尋不到對應實作」；CHARACTER_SYNC_PROJECTION 存在時不得寫「尚未出現在 statusProjection.ts」） |
| `reconnect-recovery-043` | 沒有遠端 renderer 時，每一個 Behavior Intent 都被記成「沒有人回覆」而過期，drop-if-offline 被誤報成 intents… | 已修 | crates/interaction-session/tests/session_hardening.rs::an_intent_with_no_target_is_dropped_not_expired |
| `reconnect-recovery-044` | Presence::Reconnecting 沒有任何產生者：契約 §11 與桌面 UI 的「iPhone 正在重新連線」永遠不會出現 | 已修 | session 側契約測試已補齊；`crates/interaction-runtime/src/mobile.rs` 斷線收尾已對已協商裝置送 `interaction_session::Presence::Reconnecting`，逾時後才由 session tick 轉 `Offline`，撤銷仍是 `leave`；crates/interaction-session/tests/session_hardening.rs::reconnecting_is_a_transport_fact_the_session_projects_faithfully；crates/interaction-runtime/tests/character_session_loop.rs::a_disconnected_member_is_reconnecting_before_it_is_offline；crates/interaction-runtime/tests/mobile_loop.rs::a_legacy_app_that_never_negotiated_creates_no_session_member_on_disconnect |
| `reconnect-recovery-045` | iOS：resume 失敗計數不隨重連歸零，且第 3 次 resume 根本沒送出就宣告「無法恢復，請重新連接」 | 已修 | apps/interaction-ios/InteractionCompanionTests/SessionClientTests.swift::testEveryCountedResumeAttemptIsActuallySentAndReconnectingRestores… |
| `runtime-boundaries-063` | 依賴邊界測試只釘住 tokio：FORBIDDEN 宣告 9 個套件，遞移檢查只斷言 1 個 | 已修 | tests/e2e/tests/dependency_boundaries.rs：遞移檢查改名為 pure_crates_do_not_pull_transport_or_runtime_crates_transitively 並改用 forbidden_reached() 對… |
| `runtime-boundaries-065` | `ProviderCapabilityRegistry::high_risk_receptors()` 在正式程式碼中無任何呼叫端；stop-all-sens… | 部分修 | 已誠實回報（stopped=false、SensorStopUncertain{no-stop-path}）；未做 SensorSource port 結構性修復，非 mobile provider 仍收不到真正停止請求；crates/interaction-runtime/tests/sensors_loop.rs::stop_all_sensors_is_honest_about_high_risk_receptors_it_never_asked |
| `runtime-boundaries-066` | builtin entrypoint 白名單在 Rust 與 TS 各有一份，無跨語言一致性測試 | 已修 | tests/e2e/tests/builtin_whitelist_consistency.rs::the_rust_and_typescript_builtin_whitelists_are_the_same_set、::the_architecture_guard_test… |
| `release-provenance-077` | CI 關卡只要求「HEAD 現有的 check-runs 全綠」，不要求四個必需 job 都在場 | 已修 | scripts/tests/release-scripts.sh 的四條 ci-required-checks 斷言（全綠→通過、e2e 缺席→失敗、有 job 非 success→失敗、空清單→失敗）；crates/interaction-cli/tests/release_… |
| `release-provenance-078` | interaction-agent-gateway 的版本號脫離 release 流程，並以 0.3.0 洩漏給外部 Codex agent | 部分修 | interaction-agent-gateway 版本已跟 workspace；interaction-adapter-declarative／adapters/media 仍 0.2.0 版本漂移，release-verify.sh 以 ⚠ 白名單列出；crates/interaction-cli/tests/release_provenance.rs::agent_gateway_version_follows_the_workspace；scripts/release-verify.sh 新增關卡「every crate… |
| `release-provenance-079` | Release 先建立非草稿 release 再開始建置，任一平台失敗就留下資產不全的已發布版本，且無任何後置檢查 | 已修 | scripts/tests/release-yml-embedded.py（把 finalize 的資產盤點腳本抽出實跑六種情境）；scripts/tests/release-scripts.sh 的 workflow 結構檢查；crates/interaction-cli/t… |
| `release-provenance-080` | Linux aarch64 被安裝器與 CLI 宣告為支援平台，但 release.yml 從未建置該 target | 已修 | crates/interaction-cli/tests/release_provenance.rs::every_triple_the_installer_claims_is_actually_built_by_release_yml 與 ::install_desktop_… |

### Severity: low（低） — 18 則

| id | 標題 | 處置 | 回歸測試／備註 |
|---|---|---|---|
| `aip-protocol-037` | MAX_ID_CHARS and MAX_STRING_CHARS are counted in grapheme clusters on iOS but i… | 已修 | apps/interaction-ios/InteractionCompanionTests/AIPConformanceTests.swift::testIdentifierAndStringLimitsAreCountedInUnicodeScalarsLikeRustAn… |
| `aip-protocol-038` | Swift (and TS) silently corrupt large integers in unknown top-level fields, bre… | 已修 | src/test/aip-conformance.test.ts 的 round-trip 區塊改成對**原始位元組**比對：每則 ok fixture 都用 scanNumberLiterals 掃原文與 encodeEnvelope 輸出，逐個 JSON Pointer 比… |
| `aip-protocol-040` | Integer-typed envelope fields written as `1.0` are rejected by Rust but accepte… | 已修 | crates/interaction-aip/tests/fixtures/invalid-state-integers-written-as-floats.json（sequence: 2.0、baseRevision: 1.0、payload.revision: 2.0），… |
| `identity-binding-008` | 未成為成員的裝置送出的第一則 capability frame 完全跳過 §8 第 1 關（schema／payload 上限／深度／字串長度） | 已修 | crates/interaction-runtime/tests/character_session_loop.rs::an_oversized_first_capability_frame_is_refused_before_it_can_join, ::a_capabili… |
| `identity-binding-009` | 重複 messageId 的 query 會被再次執行（去重環對 resume／snapshot 失效） | 已修 | session 側 `duplicate:true` 訊號已提供且未被破壞；runtime 的 `character_session_device_query` 已在去重命中時直接回 `result{accepted,duplicate:true}`，不重跑 resume／snapshot、不消耗 sequence；crates/interaction-runtime/tests/character_session_loop.rs::a_replayed_resume_query_is_deduped_instead_of_re_executed |
| `session-integrity-062` | 去重環在 emergency 關卡之前就被消耗：被拒的訊息重送會拿到 accepted{duplicate:true} 卻從未被套用 | 已修 | crates/interaction-session/tests/session_hardening.rs::a_rejected_message_does_not_consume_its_dedupe_slot |
| `capability-consent-055` | ConsentVerifier port 宣告為安全管線的一環卻從未接線，inbound envelope 的 consentGrantId 完全不驗證 | 已修 | crates/interaction-session/tests/session_hardening.rs::member_messages_may_not_carry_a_consent_grant |
| `character-package-020` | Rust 與 TS 的 §2.1 assets 驗證雙向分岔：mediaType 必填性與 asset id 字元集不一致 | 部分修 | TS 側 mediaType 必填＋MEDIA_TYPE_RE 已修；Rust manifest.rs 對 asset id 無字元集規則（反向分岔）未修；regressions-v06-round2-desktop.test.ts :: describe「character-package-020：TS 鏡射的 assets 驗證要和 Rust／golden schema 一致」（4 個 case，含 golden schema… |
| `character-package-021` | character_import 在任何數量檢查之前就解碼全部 IPC 資產 | 已修 | character_store.rs :: tests::ipc_asset_decoding_is_bounded_by_the_host_not_the_caller |
| `general-mode-ux-027` | e2e「鍵盤到得了五個主入口／Reduced Motion」的斷言幾乎不可能失敗，證據強度低於測試名稱的宣稱 | 已修 | apps/interaction-desktop/e2e/character-session.spec.ts :: 「角色同步：鍵盤到得了五個主入口與同步卡；減少動態時卡片仍讀得到」 |
| `evidence-honesty-014` | threat-model.md 的 13 步安全管線行號整體漂移約 55 行，指向 diagnostics() 等無關程式碼 | 已修 | threat-model.md 的行號錨點已改函式名；`docs/aip/renderer-adapter.md` 原本漂移的行號引用也已改成註解錨點／函式名（例如「決定 `verified`」一列改引用 `crates/interaction-session/src/session.rs::gate` 的第 8 關註解錨點，不再是硬編行號）；scripts/tests/docs-claims.sh（threat-model.md §3 不得出現硬編行號 `:NNN`；session.rs 必須仍有 `fn gate(` 與 `// 1.`…`// 13.` 十三個步驟註解錨點） |
| `evidence-honesty-015` | DESKTOP-GUIDE 的同步狀態表與 narrow.spec 的允許清單都漏掉「角色同步紀錄曾損毀」這一態 | 已修 | regressions-v06-round2-general-mode.test.tsx :: 「DESKTOP-GUIDE 的表格列出 CHARACTER_SYNC_PROJECTION 的每一句 headline」（drift lint） |
| `evidence-honesty-016` | AIP §10 宣稱 EvidenceClass「用於 diagnostics」，但它在整個 repo 沒有任何生產端或消費端 | 已修 | scripts/tests/docs-claims.sh（掃 crates/interaction-{runtime,api,cli}/src 是否有 EvidenceClass/evidence_class 的生產或消費端；沒有時 §10 必須含「尚未接進」或「impleme… |
| `reconnect-recovery-046` | presence 逾時的成員在同一個 tick 內就被 leave 掉，§11 的「iPhone 暫時離線」在逾時路徑上看不到 | 已修 | crates/interaction-runtime/tests/character_session_loop.rs::a_timed_out_member_is_offline_before_it_is_evicted |
| `reconnect-recovery-047` | 活連線被新連線取代（superseded）的分支仍然沒有任何測試——v0.6.0 恢復矩陣第 11 條列為必補，本輪仍為 0 命中 | 已修 | crates/interaction-runtime/tests/character_loop.rs::attaching_while_a_connection_is_live_supersedes_it_with_a_goodbye、crates/interaction-ru… |
| `runtime-boundaries-064` | `cargo metadata` 失敗時遞移邊界檢查靜默通過（綠燈≠已驗證） | 已修 | tests/e2e/tests/dependency_boundaries.rs::cargo_metadata()：兩條原本 eprintln!("SKIPPED …") + return 的路徑改成 panic!／assert!，訊息以 'dependency bounda… |
| `runtime-boundaries-067` | 架構守門測試的 entrypoint 分岔偵測只涵蓋 `===`／`case`，且例外規則會遮蔽真正的分岔 | 已修 | apps/interaction-desktop/src/test/architecture-no-entrypoint-switch.test.ts :: describe「架構守門本身：偵測器抓得到繞道寫法」（6 個繞道 case ＋ 2 個「不得誤判」case） |
| `runtime-boundaries-068` | `providers.rs` 引入 `COMPANION_CAPABILITY_PREFIX` 後，同檔四處仍寫死 `"companion."` 字面值 | 已修 | crates/interaction-runtime/src/providers.rs::tests::the_companion_prefix_has_exactly_one_literal_in_this_file |

### 2.2 refuted 與 unverified

> 報告原文的頂層統計寫「refuted 7、unverified 1」，但 `refuted` 陣列裡有一則（`release-provenance-076`）
> 自己的 `verdict` 欄位其實是 `unverified`，與另外存在的獨立 `unverified` 陣列（同一則）重複。
> 本文件依每則自己的 `verdict` 欄位歸類：`refuted` 表只列 verdict 精確等於 `refuted` 的 6 則，
> `release-provenance-076` 只出現在 unverified 表，不重複計入，因此本文件的 6＋1＝7，
> 與報告頂層「refuted 7」在算法上不同但涵蓋同一組 finding，無遺漏。

### refuted（已駁回）— 6 則

| id | 標題 | 駁回理由摘要 |
|---|---|---|
| `aip-protocol-039` | §11 payload bounds (depth, per-string length, payload bytes) are enforced only on `payloa… | 已親自讀 crates/interaction-aip/src/envelope.rs 全部相關程式碼，finding 對程式碼行為的描述是準確的：validate() 只對 self.payload 呼叫 check_payload（envelope.rs:174），parse()（envelope.rs:112-121）只檢查整體 bytes.len() 對 MAX_MESSAGE_BYTES，extra 是 #[serde(fl… |
| `capability-consent-048` | 崩潰後 restore 落後成員 revision，host 的 resume 回答被成員自己的 rollback 防護忽略 → 永久靜默分歧 | 已重讀 session.rs:577-618（resume）與 patch.rs:88-146（accept_state_with_epoch），並實際寫一個 out-of-tree Rust harness（依 path 相依直接呼叫 interaction-session 的公開 API，模擬「member 領先 6 個 revision、host 從落後 snapshot restore、epoch 不變」）跑出來，事實與 fi… |
| `renderer-lifecycle-031` | sprite adapter 的 resume() 會把 lastPlayed 重放進共享 machine，可在無 Runtime envelope 的情況下重新升起 verif… | 逐一核對後，此 finding 的因果鏈在三個關鍵點上都站不住： 1) `resume()`（sprite.ts:117-123，內容與引用一致）只呼叫 `this.renderer?.setAnimation(...)`，完全沒有呼叫 `sink()`、沒有產生回執、也沒有呼叫 gateway 的任何方法。它不構成「對 Runtime 重放一則 transient 事件」——沒有 IntentEnvelope 被重新處理，Runti… |
| `general-mode-ux-023` | 連接與權限頁每收到一則 runtime 事件就 GET /v1/character-session，用唯讀畫面推著權威 session 的 sequence 前進 | All cited evidence verified against current file contents, no drift: 1. ConnectPage.tsx:294-300 — `useAsync(async () => { try { return await api.characterSessionSnapshot(); } catch { return null; } }, [refreshKey])`. No… |
| `runtime-boundaries-069` | `ProviderCapabilityRegistry` 是無界集合，但註解與文件宣稱「有界」 | 親自讀過 providers.rs:57-233（含 declare()/is_presentation_surface/class_label_of_receptor/high_risk_receptors）並用 rg 追蹤了 declare_provider_capabilities 的所有呼叫點與外部暴露面，finding 的機制描述（BTreeMap 無容量檢查、三個 Vec 無截斷、三個查詢函式對 .values() 做線性… |
| `runtime-boundaries-070` | 可信 host overlay 仍用子字串 `"mic"` 判斷麥克風感測，未接上 provider 的高風險受器宣告 | 親自讀了 host_safety.rs、tray.rs、overlay/hostSafety.ts、sensors.rs、mobile.rs、providers.rs，並跑了現有測試，finding 的核心安全主張站不住： 1. 「不含 mic 字串的麥克風類感測會不亮指示器,違反感測不靜默」——這是錯的。host_safety.rs 的 `sensor_text()`（line 143-166）對「非 mic、非 camera」的… |

### unverified（未驗證）— 1 則

| id | 標題 | 未驗證理由 |
|---|---|---|
| `release-provenance-076` | secret 掃描的四條 regex 漏掉現行主流憑證格式（github_pat_／gho_／sk-proj-／AKIA／AIza／xox*） | 報告本身標為 `unverified`（未指派 verifier 覆核，claim／evidence／fix 見報告原文：發布前唯一的 secret 關卡只認四種形狀，且都是舊格式。GitHub 的 fine-grained PAT（`github_pat_...`）、Actions/OAuth token（`gho_`／`ghs_`／`ghr_`）、現行 OpenAI project key（`sk-proj-...`，`sk-` 後第 5 個字元就是 `-`，撞不到 `[A-Za-z0-9]{40,}`）、AW…）。原始報告的「refuted」清單也把此則一併列入，但其 verdict 欄位是 `unverified` 而非 `refuted`——本文件依 verdict 欄位歸類，不重複計入 refuted 表。**已順手修正（不計入 confirmed 統計）**：整合者已把 `scripts/release-verify.sh` 的 secret 掃描正則擴充到 `gh[pousr]_`／`github_pat_`／`sk-ant-`／`sk-proj-`／`AKIA`／`AIza`／`xox[abprs]-` 等現行格式，並在 `scripts/tests/release-scripts.sh` 新增 13 個正反案例（42/0） |

## 3. v0.6.0 新增的窄限制（每一項都是本輪修復或新功能刻意劃定的邊界）

1. **iPhone 真機仍是 implemented-unverified**（含 haptic、Reduced Motion 真機行為、背景重新連線、
   `docs/aip/pairing-security.md` 的 QR 配對流程）：程式碼與模擬器／fixture 測試存在，但 v0.6.0
   新增的 AIP／Character Session 這一層在真機上**零執行**（`docs/aip/iphone-companion.md` §10；
   `docs/releases/v0.6.0-test-matrix.md` §6）。v0.5.0／v0.5.1 的舊協定真機證據
   （`docs/releases/v0.5.0-iphone-device-evidence.md`／`v0.5.1-iphone-device-evidence.md`）仍然
   有效，但**不涵蓋**這層新協定，不得引用它們證明 AIP 層已在真機驗收。
2. **ESP32 真板零驗收**：本輪未新增任何真板證據，兩種組態仍只是 `arduino-cli` 編譯 exit 0
   （fixture 等級）。固定句沿用 v0.5.1：ESP32 firmware compiled and simulator-tested; not
   validated on a physical ESP32 board。
3. **真 Tauri 視窗對「同步卡」（`CharacterSyncCard`）本身未驗**：v0.6.0 新增的同步卡、十種狀態
   文案、五入口是否仍成立只有 Playwright（Chromium）與 vitest（jsdom）覆蓋，本輪沒有重跑一次真
   `.app` 視窗走查（`docs/releases/v0.6.0-test-matrix.md` §6）。
4. **真 `codex`／`claude` 二進位未跑**：本輪 agent 相關回歸沿用既有 fixture agent
   （`fake_claude.sh`／`fake_codex.sh`），與 v0.6.0 新協定的整合仍是 unverified，非隱藏缺口，
   與 v0.5.1 已知限制一致（`docs/releases/v0.6.0-test-matrix.md` §6）。
5. **iOS 尚未送 AIP `heartbeat`，靠舊 `status` 心跳撐存活證明**：v1 的 `status` 心跳（iOS App
   已在送）在 `mobile.rs` 收到時對已協商手機呼叫 `Runtime::character_session_touch_presence`，
   讓 `lastSeenAt` 前進、避免 45 秒 presence timeout 誤判離線（`docs/aip/transport-bindings.md`
   §1.4；回歸：`character_session_loop.rs::a_negotiated_phone_that_only_sends_legacy_status_stays_a_member`）。
   文件仍建議之後補上每 **15 秒**一則 AIP `heartbeat`（比 45 秒逾時有更大餘裕）——**目前尚未實作**，
   這是刻意留給 App 端下一輪的窄限制，不是本輪的缺陷。
6. **burst 超過 30 msg/s 時，是既有 iPhone 線協定 v1 的速率限制關閉連線，不是 AIP
   `rate-limited` outcome**：`perf-session-after.json` 的 burst100 量測顯示前 30 則被接受並套用後，
   既有 30 msg/s 速率窗把連線關掉，其餘 70 則因連線已斷而未送達——是既有速率限制與量測腳本互動
   的結果，不是 Session 層邏輯錯誤，列為觀察項（`docs/releases/v0.6.0-test-matrix.md` §5.2）。
7. **桌面同步卡刻意不做接收端 hash 核對**：理由是 JS 的 `number` 留不住原始數字字面值，重新
   序列化後與 Rust 端逐位元組比對的 hash 一定對不上，會變成永遠亮著的假警報；桌面端改以
   `patch.baseRevision` 遞增與宣稱的 revision 相符判斷一致性，不是靠 hash
   （`apps/interaction-desktop/src/components/CharacterSyncCard.tsx:25-29`、
   `apps/interaction-desktop/src/aip/envelope.ts:330-333`）。
8. **`timeline`／`realtime` 兩個 syncClass 目前只有介面／邊界定義，沒有實作**：AIP 1.0 只定義
   `timeline`（`performanceId`／`startAt`／`durationMs`／`seed`／`offsetMs`／`lateJoinPolicy`
   等欄位形狀）與 `realtime`（連續拖曳／傾斜／姿態／AR 的邊界宣告：不得成為角色狀態核心依賴、
   不要求跨平台 deterministic lockstep），目前 Character Session 只用 `semantic` 這一類
   （`docs/aip/character-session.md` §8；`docs/aip/README.md` §8）。
9. **`ConsentVerifier` 未接進安全管線，因此目前沒有任何機制允許帶 `consentGrantId` 的 inbound
   envelope 通過**（`capability-consent-055` 已修，但修法是 fail-closed 全拒）：gate 新增第 8.1
   關，任何非空 `consentGrantId` 一律 `rejected{scope-denied}`，不問任何驗證器。實務後果是
   **AIP 1.0 的 session 成員目前不能靠 consent grant 送出需要額外同意的 command**——這條路要打通
   需要真正接上 `ConsentVerifier` 或 1.1 的 approval 流程，本輪只做到「不能被繞過」，沒有做到
   「能夠使用」（`crates/interaction-session/src/ports.rs:42`；回歸：
   `session_hardening.rs::member_messages_may_not_carry_a_consent_grant`）。
10. **stdio JSON Lines transport 仍未實作**（沿承 v0.5.0 #13／v0.5.1 §3-14）：host 端仍不 spawn
    子程序、沒有 stdio fixture，唯一真的外部 transport 仍是 WebSocket。
11. **宣告式 adapter（ESP32／BLE／Serial）與韌體尚未接上 AIP Device Profile**：`rg -n "AIP"
    crates/interaction-adapter-declarative/src` 零命中，`docs/aip/device-profile.md` §6 定義的
    profile 目前只有 iPhone Mobile Provider 一個實作，沒有排定的落地時間
    （`docs/releases/v0.6.0-migration.md` §13）。
12. **`runtime-boundaries-065` 只做到誠實回報，沒有做結構性修復**：`stop_all_sensors` 現在會問
    `capability_declarations().high_risk_receptors()`，把「provider 宣告過、仍啟用、但這次掃描
    沒有任何來源回報涵蓋」的受器列為未知（`stopped=false`、`uncertain=true`，補發
    `SensorStopUncertain{outcome:"no-stop-path"}`），但**沒有 `SensorSource` port**：非 mobile
    provider 宣告的高風險受器仍然收不到真正的停止請求。
13. **兩個 crate 的版本仍脫離 workspace**：`crates/interaction-adapter-declarative`（`0.2.0`）與
    `adapters/media`（`0.2.0`）寫死自有版本，`release-prepare.sh` 改不到；`release-verify.sh` 以
    `⚠ 已知版本漂移` 白名單列出，不算通過（`release-provenance-078` 的剩餘部分）。
14. **Linux aarch64 從支援宣告中移除**（而非加進建置矩陣）：`release.yml` 從未建置該 target，
    `get.sh`／`target_triple()`／`desktop_asset_name()` 改為明確指向「需從原始碼建置」，不是讓
    使用者拿到 HTTP 404。若要真正支援需要在 `release.yml` 加 `aarch64-unknown-linux-gnu`
    （cross 或 ARM runner，含 `libasound2-dev`），本輪未做。
15. **沒有程式碼簽章／公證／SBOM／build provenance**：Release 只發布 `.sha256`，證明「位元組與
    Release 一致」，不證明來源。macOS bundle 未簽章未公證、Windows 安裝程式未簽章；
    `release.yml` 的 permissions 沒有 `id-token: write`，沒有 `actions/attest-build-provenance`。
    已在 `docs/INSTALL.md`／README／QUICKSTART／CHANGELOG 誠實揭露，本輪不做簽章。
16. **`release.yml` 的 `ci-gate`／`finalize` 兩個新 job 未經真實 tag push 驗證**：兩者內嵌的
    shell／python 由 `scripts/tests/release-yml-embedded.py` 抽出實跑（資產不齊會擋、check-run
    缺席會擋），但整條 workflow 只有下一次真的推 tag 時才會由 GitHub 執行。**證據等級：unit，
    不是端對端。**
17. **【已於 `feature/v0.6.x-maintainability` 修正】** 匯入／匯出驗證改綁目標角色的 adapter meta（`variants`／`personas`／
    `hasPlayfield`），`settingsTransfer.ts` 不再 import 小樞的配色表、也不再自帶 persona 清單；說話風格（persona）是同類問題一併
    修掉；小樞遊玩場 UI 搬進 `character/adapters/shuPlayControls.tsx`。原文：**`apps/interaction-desktop/src/companion/settingsTransfer.ts:124` 仍以 `SHU_RIG_PALETTES`
    驗證匯入設定裡的「使魔配色」**：這是接線層殘留的角色專屬知識（`character-package-018` 已知
    未涵蓋部分），本輪未硬修——那是 rig 遊玩場的設定結構、不在 adapter 規劃路徑上，改動會牽動
    familiars 設定的驗證語意；已在守門測試裡以註解明記為已知限制，收乾淨需要 adapter meta 或
    registry 提供「所有已註冊 adapter 宣告過的 variant 集合」來驗。
18. **`docs/assets/v06-evidence/{desktop,narrow}-character-sync-synced.png` 兩張截圖需要重拍**：
    `general-mode-ux-025`／`026`／`capability-consent-052` 落地後，同一個 journey 步驟現在顯示
    「iPhone 已連接，能力核對中」（`capability-unknown` 保守狀態），但這兩張圖目前仍是舊的綠色
    「iPhone 已連接，角色狀態已同步」畫面。`docs/acceptance-evidence.md`／
    `docs/releases/v0.6.0-test-matrix.md` §4.1 引用了這兩張圖，發布前需要重跑 evidence spec
    產生新圖並更新引用文字（截圖檔名裡的 "synced" 只是 journey 步驟代號，不必改檔名，但引用文字
    必須跟著改）。
19. **本輪一組修復曾因 Claude Code 程序中斷而接續**：`ios` 組的兩則 finding
    （`aip-protocol-034`／`038`）在被接手時已「Half-done in the working tree」，接手的 agent
    先驗證既有改動再完成——對最終結果無影響（兩者皆已修並經同組 verifier 覆核），但為透明記錄於此。
20. **一次全套回歸因系統記憶體不足被中止，改用單 worker 重跑**：`hardening` 階段第一輪
    `regress.sh`（預設 `pnpm test:e2e`）跑到 CLI E2E／Playwright 附近時，因多個對抗審查 agent
    與文件撰寫並行佔用記憶體而被系統中止；中止前的 log 被隨後重跑覆寫，沒有留下獨立證據檔。
    重跑改用 `regress-tail.sh`，對 `cli-e2e` 與 `pnpm exec playwright test --workers=1`
    重新執行並成功（`docs/releases/v0.6.0-test-matrix.md` §2「Playwright 重跑備註」）。
    `--workers=1` 與專案設定檔（`playwright.config.ts:13` 的 `workers: 1`）本來就一致，
    不是把行為改成單執行緒的權宜之計。
21. **【已於 `feature/v0.6.x-maintainability` 修正】** 快照檔新增格式版本 `Snapshot.format`（與 wire version 分開），
    restore 的第二次 canonical 重 hash 改為遷移訊號、未知鍵改由 `deny_unknown_fields`＋`attention` 允許鍵擋；
    真實 v0.6.0 格式的檔案（`crates/interaction-runtime/tests/fixtures/character-session/`）現在會被遷移而非隔離
    （測試 `a_snapshot_written_before_the_field_existed_is_migrated_not_rejected`、
    `a_v0_6_0_snapshot_is_restored_and_migrated_to_the_current_format`）。下面保留 v0.6.0 當時的原文，其中
    「v0.6.0 尚未發布，沒有人會撞到」的前提在發布後已不成立。原文：**`MemberView` 新增 `unsupportedIntents` 欄位後，此欄位出現前寫下的
    `state/character-session.json` 開發期快照在 restore 時會被判為 `HashMismatch`**：
    `#[serde(default)]` 讓缺欄位的舊快照能正常反序列化，但 restore 的自洽性檢查是對「重新
    序列化後的內容」與快照內附的 `hash` 逐位元組比對——新欄位一出現，任何在它出現前寫下的
    快照重新序列化後都會多出 `unsupportedIntents: []`，導致 hash 對不上，被當成「攜帶未知變更
    的可疑快照」而不是單純的 schema 演進。後果與既有的壞檔處理路徑一致：該檔被隔離成
    `<file>.corrupt`、開一個新 session（epoch 前進），diagnostics 標一個 `storeNote` 說明原因。
    **只影響開發期留下的舊快照**——v0.6.0 尚未發布，還沒有任何生產環境或使用者機器上存在
    「這個欄位出現之前」的快照，所以實際沒有人會撞到；記錄在此是為了讓下一次替 `MemberView`
    加欄位的人知道這個模式（新增欄位＋自洽性 hash 檢查＋沒有 schema 版本號）會如何互相作用。
22. **【已於 `feature/v0.6.x-maintainability` 修正】** `maxUnsupportedInputs` 進 golden schema 的 `limits` 表，TS／Swift 由 codegen 產出、
    `envelope.ts` 改讀 `AIP_LIMITS.maxUnsupportedInputs`；`schema.rs::every_limit_constant_is_published_in_the_schema` 對
    `limits.rs` 的每個 `pub const` 做雙向漂移 gate。原文：**`MAX_UNSUPPORTED_INPUTS` 在 TS 端是手寫的同值常數，沒有跨語言漂移閘門**：Rust 權威值是
    `crates/interaction-aip/src/limits.rs::MAX_UNSUPPORTED_INPUTS = 32`，TS 端
    `apps/interaction-desktop/src/aip/envelope.ts` 另外手寫了一個同值的 `const
    MAX_UNSUPPORTED_INPUTS = 32` 並在註解裡標明「與 Rust 權威實作同值」，但沒有任何測試斷言
    兩邊的數字保持一致——之後如果只改了 Rust 那一份，TS 端會安靜地用舊的上限，直到有人發現
    行為不一致。不是本輪 confirmed finding，是本輪修復 `session-integrity-060` 上游時新引入的
    窄限制。
23. **`crates/interaction-aip/tests/fixtures/state-snapshot.json` 這份跨語言 hash fixture 的
    `members` 陣列沒有 `unsupportedIntents` 欄位**：`general-mode-ux-022`／`capability-consent-052`
    落地後，`MemberView` 多了這個欄位，但用來驗證 Rust／TS／Swift 三方對同一份 snapshot payload
    算出相同 `hash` 的 golden fixture 沒有同步更新——因為 `#[serde(default)]` 讓缺欄位的 payload
    仍可解析、測試依然全綠，但這份 fixture 已經不再逐欄鏡射 `MemberView` 的完整形狀，三方 hash
    conformance 對這個新欄位事實上是**沒有覆蓋**的。

24. **v0.6.0 Release workflow 的 Windows `.sha256` 步驟首次實跑就紅**（run 33918252926）：`finalize` 依設計 skipped、release
    維持 draft。收尾由整合者在本機從已上傳的 MSI／NSIS 資產計算並上傳 `.sha256`、在本機重跑 finalize 的資產盤點後手動
    `gh release edit --draft=false`。tag 未移動；根因（Windows python 文字模式寫出 `\r\n`，`read -r` 留下 `\r`）已在 main 修正並以
    `scripts/tests/release-scripts.sh` 的 CRLF 假 `gh` 案例釘住，但 GitHub 端要等下一次 tag push 才能真正驗證。
    附帶：該修正 commit `ea7de59` 的 main CI（run 33919774319）`Tauri backend` job 紅——`[Unreleased]` 段落非空後
    CHANGELOG claim-check 找不到保護行為敘述，本機檢查因管線吞掉 exit code 而漏看；`8826656` 補回敘述後 run 33919829058 四 job 綠。
    tag 與 Release 資產不受影響。

## 4. v0.5.1 已知限制在 v0.6.0 的沿承狀態

> 逐條檢視 `docs/releases/v0.5.1-known-limitations.md` §1「保留（8 項）」與 §3「v0.5.1 新增的
> 窄限制（14 項）」在 v0.6.0 是否仍然成立。狀態欄：**沿承** ＝ 本輪未觸碰相關程式碼，情況與
> v0.5.1 記錄的一致；**已修** ＝ 本輪的某個 confirmed finding 或功能落地改變了它；**本輪不變** ＝
> 本輪有觸碰到相關量測／程式碼，但結果與 v0.5.1 記錄的方向一致、無實質變化；**未重新核實** ＝
> 本文件撰寫過程沒有找到本輪是否觸碰的直接證據，不確定，不得推定為沿承或已修。

### 4.1 v0.5.1 §1「保留（8 項，源自 v0.5.0）」

| v0.5.0 # | 項目 | v0.6.0 狀態 | 說明 |
|---|---|---|---|
| 6 | ESP32 真板未驗收 | **沿承** | `docs/releases/v0.6.0-test-matrix.md` §6：「ESP32 真板：未執行」，本輪未新增任何真板驗收，固定句沿用。 |
| 7 | iPhone 真機部分驗收 | **沿承（範圍不變＋新增窄限制）** | v0.5.0／v0.5.1 的真機證據快照不變，但**不涵蓋** v0.6.0 新增的 AIP／Character Session 層——這一層在真機是 implemented-unverified（見 §3-1），是本輪對同一顆限制的擴充敘述，不是矛盾。 |
| 8 | BLE 無真機 | **沿承（未重新核實實質變化，但本輪未觸碰 BLE 相關程式碼）** | 本輪未修改 `crates/interaction-adapter-declarative` 或韌體（`rg -n "AIP" crates/interaction-adapter-declarative/src` 零命中，§3-11），沒有理由認為 BLE 真機狀態改變。 |
| 9 | MQTT 重連修復只在內嵌 broker 上驗證 | **未重新核實** | 本輪審查與修復 12 個維度不含 MQTT 專屬項目，未找到本輪觸碰 MQTT reader 的證據，也未主動重新驗證。 |
| 10 | 對抗審查回歸驗證多數用 fake 子程序 | **沿承** | `docs/releases/v0.6.0-migration.md` §13：「本輪的 agent 相關回歸延用既有 fixture agent（`fake_claude.sh`／`fake_codex.sh`）」，真 codex／claude 二進位本輪仍未跑（§3-4）。 |
| 12 | HTTP 逾時分類保守 | **未重新核實** | 本輪 12 個審查維度不含 HTTP 逾時分類專屬項目，未找到相關程式碼改動的證據。 |
| 17 | 外部角色 adapter 不再能合成人類互動觀察 | **未重新核實** | 本輪的 character-package／renderer-lifecycle 相關 finding 都集中在內建 adapter（shu／sprite／shape／text）與 host 接線層，未見觸及外部 WebSocket adapter 的輸入能力邊界；沒有找到證據顯示這條刻意保留的邊界被重新檢視。 |
| 22 | `memory_context_bundle` 卡在第一個裝不下的項目就停 | **未重新核實** | 本輪 12 個審查維度不含記憶／knowledge 子系統，未觸碰 `memory_context_bundle`，沒有找到相關證據。 |

### 4.2 v0.5.1 §3「v0.5.1 新增的窄限制（14 項）」

| v0.5.1 # | 項目 | v0.6.0 狀態 | 說明 |
|---|---|---|---|
| 1 | 受器與 tool-operation 的 consent 仍是 TTL，不是單次 | **未重新核實** | 本輪 `capability-consent` 維度的 finding 全部落在 Character Session（`interaction-session`／`interaction-aip`）的 token bucket 與 `ConsentVerifier`（§3-9 為本輪新增的窄限制），與這條講的受器／tool-operation dispatch 授權計次是不同的子系統，未找到本輪觸碰的證據。 |
| 2 | 首次設定 commit 在「檔案已寫、SQLite 未提交」之間崩潰沒有 journal | **未重新核實** | 本輪未觸碰 onboarding／首次設定精靈相關程式碼。 |
| 3 | 受器讀取路徑拿不到 `pairingUnverified` | **未重新核實** | 本輪的 `pairing-migration` 維度全部是 Character Session 的 epoch／resume 語意，與裝置配對碼 HMAC 驗證的 `pairingUnverified` 旗標是不同機制，未找到本輪觸碰的證據。 |
| 4 | `simulate_plan` 已擋，但 `transition_provider` 不會關掉 registry 旗標 | **未重新核實** | 本輪未觸碰 provider 生命週期轉換相關程式碼。 |
| 5 | Reduced Motion 未在真 Tauri 視窗驗收 | **沿承** | `docs/releases/v0.6.0-test-matrix.md` §6：本輪新增的同步卡等 UI 同樣「只有 Playwright（Chromium）與 vitest（jsdom）覆蓋，沒有重跑一次真 `.app` 視窗走查」，Reduced Motion 的真視窗驗收缺口未被本輪關閉；但 iOS 端的 Reduced Motion **呈現邏輯**本身有一則獨立修復（`evidence-honesty-011`，已修，見 §2.1），與這條講的「macOS 系統設定切換無法在真視窗自動驗收」是不同層面，不算解決本項。 |
| 6 | 快捷選單／玩具／使魔與 adapter 崩潰 fallback 未在真 Tauri 視窗驗收 | **沿承** | 本輪未重跑真 Tauri 視窗走查（§3-3），這條清單本輪未被觸碰。 |
| 7 | iPhone 冷啟動自動重連與位址變更提示只有模擬器證據 | **沿承** | 真機仍是零執行；v0.6.0 新增的 AIP 層重連（resume／epoch）同樣只有模擬器＋fixture 證據（§3-1），與這條性質一致地擴大了範圍。 |
| 8 | 角色效能浸泡的 GC 後保留集合升到 +523 KB（觀察項，來源未定位） | **本輪不變** | `docs/releases/v0.6.0-test-matrix.md` §5.3：本輪 60 秒浸泡 Δ+466 KB（18.4%），對照修改前 Δ+462 KB（18.7%），「差異在量測雜訊範圍內，看不出這輪的小樞脫核心／Session 掛載造成額外保留」；v0.5.1 記錄的「來源未定位」觀察項在 v0.6.0 仍未定位，本輪也沒有做 heap snapshot 比對去定位它。（v0.5.1 記錄的數字是 10 分鐘浸泡 +523 KB，本輪只重跑了 60 秒浸泡，量測窗口不同，數字不直接可比，但方向一致：仍是未定位的固定保留觀察項，非確認洩漏。） |
| 9 | 角色視窗被主視窗遮蔽時會週期性重新 hello（macOS 行為） | **未重新核實** | 本輪未重跑真 Tauri 視窗走查（同 #6），沒有針對這個現象的新證據，也沒有理由認為底層 macOS 行為已改變。 |
| 10 | 故障注入接縫編進正式碼 | **沿承** | 本輪未移除或新增這類故障注入接縫的證據；性質上這些接縫本來就設計成永久保留（doc-hidden、未武裝時 inert），沒有計畫移除。 |
| 11 | legacy 共享 agent token 不能再 interrupt session（Breaking，v0.5.1 已上線的行為） | **沿承** | 這是 v0.5.1 已生效的安全收斂，非本輪待辦限制；本輪未變更 agent session interrupt 的擁有權規則。 |
| 12 | 升級前建立的 gateway session 不可續開（Breaking，v0.5.1 已上線的行為） | **沿承** | 同上，v0.5.1 已生效的行為，本輪未變更。 |
| 13 | 外部 character adapter 的輸入維持拒絕（沿承 v0.5.0 #17） | **未重新核實** | 與 §4.1 的 v0.5.0 #17 為同一件事，見上方說明。 |
| 14 | `stdio JSON Lines` transport 仍未實作 | **沿承** | 本輪 §3-10 明確重申：host 端仍不 spawn 子程序、沒有 stdio fixture，唯一真的外部 transport 仍是 WebSocket。 |

## 5. 相關文件

- `docs/reviews/adversarial/6683403-20260904T161327Z.md`（＋同名 `.json`）——本文件 §2 逐列引用的完整對抗審查報告
- `docs/releases/v0.6.0-test-matrix.md`——每一列的證據等級與實跑數字
- `docs/releases/v0.6.0-migration.md`——API／CLI／IPC／iOS／Breaking 變更與 §13 已知限制
- `docs/releases/v0.6.0-release-readiness.md`——發布關卡
- `docs/releases/v0.6.0-baseline.md`／`v0.6.0-recovery-matrix.md`——修改前基線與受保護行為
- `docs/releases/v0.5.1-known-limitations.md`——被本文件 §4 逐條核對沿承狀態的原始清單
- `CHANGELOG.md` `[Unreleased]`——v0.6.0 段落，「對抗審查 `6683403-20260904T161327Z`」小節與本文件 §1／§2 的
  已修 68／部分修 5／deferred 0 統計一致
- `docs/acceptance-evidence.md`——v0.6.0 驗收證據段落，已同步本文件 §2 的最終統計與 §3 的殘留窄限制
