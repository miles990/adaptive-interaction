# v0.6.0 發布就緒清單

> 狀態值只有三種：**met**（這一關本身的條件已滿足，證據見備註，且證據是可引用的文件／已完成的
> 唯讀核對，不是等待中的猜測）、**pending final run**（工作已完成或大部分完成，但要等最後一次
> 全套跑／CI／發布流程實際確認才能蓋章）、**blocked-by-human**（需要使用者或人工環境介入，非工程
> 可解；本輪為 iPhone 真機與 ESP32 真板）。**沒有一格是猜測**；沒有實跑證據的一律標
> `pending final run`，不標 `met`。
>
> **本次更新（2026-09-05，純文件任務）**：涵蓋全部對抗審查修復的**單一最終整合回歸**已經跑完
> （`scratchpad/final/`，同機實跑，詳見 `docs/releases/v0.6.0-test-matrix.md` §8）——Rust
> 1040/0（85 target）、Tauri 56/0（3 target）、前端 vitest 1416/0（70 檔）、CLI E2E 96/0、
> Playwright 71/0、iOS XCTest 101/0（模擬器），六個套件最終全數 0 failed；過程中出現的兩則失敗
> （CLI E2E 一次、Playwright 一次）都已定位成因（分別是驗收腳本斷言追不上一則已修復的行為變更、
> 與其他並行 agent 競爭資源造成的計時 flake）並經重跑排除，非產品 regression（§8.2／§8.3）。
> 本次更新只讀 `rg`／`sed`／`cat`／`git log` 核對這批既有 log 與既有文件，**沒有自行執行任何
> `cargo`／`pnpm`／daemon／Playwright／git 操作指令**——下表把因此有了實跑證據的關卡（1／3／4／
> 5／6）改判 `met`，其餘仍需要 push／CI／release 腳本才能核對的關卡（2／8／9／10／11／16）維持
> `pending final run`，不因為「已經很接近」就升級。
>
> 基準：v0.5.1（tag `v0.5.1` → `ccf3bfe`，其後 3 個 docs／fix commit 到 `a94e3d5`）。本清單對應
> 分支 `feature/v0.6.0-foundation`（本次更新時 HEAD `7b4b657`，工作樹於撰寫本次更新前為乾淨狀態；
> 本次更新本身會再次讓工作樹產生未提交變更，這是純文件任務的預期副作用，見關卡 1 備註）。數字
> 來源見 `docs/releases/v0.6.0-{baseline,test-matrix}.md`；已知限制見
> `docs/releases/v0.6.0-known-limitations.md`。
>
> 對抗審查 `6683403-20260904T161327Z`（80 送審／73 confirmed／7 refuted（其中 1 則 verdict 實為
> `unverified`）／1 unverified）已處置：**已修 68／部分修 5／deferred 0**（與
> `CHANGELOG.md` `[Unreleased]` 的「對抗審查 `6683403-20260904T161327Z`」段一致），詳見
> `docs/releases/v0.6.0-known-limitations.md` §1／§2。跨組收尾（先前的 4 項「收尾中（本輪）」與
> 2 則跨組鏡射伴生瑕疵）與 12 個原本僅 fixer 自報的 finding 之獨立 verifier 覆核**皆已完成**
> （11 已修、`runtime-boundaries-065` 部分修，與 fixer 自報一致）。

## 16 道關卡

| # | 關卡 | 狀態 | 最終回歸數字 | 證據／備註 |
|---|---|---|---|---|
| 1 | worktree 乾淨（無未提交變更、無未追蹤殘留） | **met**（於 HEAD `7b4b657`） | — | 全部對抗審查修復（7 組＋跨組收尾）已提交，HEAD 為 `7b4b657`（`docs(release): record the review disposition, known limitations and release gates for v0.6.0`），本次更新開始前 worktree 為乾淨狀態（無未提交變更、無未追蹤殘留）。**注意**：本次更新本身（`v0.6.0-final-report.md`／本文件／`test-matrix.md` §8／`CHANGELOG.md` 回歸段）會再次讓 worktree 產生變更，這是純文件任務的預期結果——整合者需在**提交這批文件之後**重新跑一次 `git status --porcelain` 確認為空，才能把這一關的「乾淨」延續到實際發布前那一刻；本欄的 `met` 指的是「HEAD `7b4b657` 那個 commit 本身乾淨」，不是「提交本次文件之後也自動乾淨」。 |
| 2 | 版本一致性（四處版本號相同） | **pending final run** | `<<TBD>>` | 目前 workspace `Cargo.toml`／`apps/interaction-desktop/package.json`／`src-tauri/Cargo.toml`／`src-tauri/tauri.conf.json` 四處**一致皆為 `0.5.1`**（`release-prepare.sh` 尚未執行，符合預期——本輪還沒進入發布腳本的版本 bump 步驟）。整合者跑 `release-prepare.sh 0.6.0` 後需重新核對四處皆變成 `0.6.0`。 |
| 3 | CHANGELOG `[Unreleased]` 完整 | **met** | — | `CHANGELOG.md` 的 `[Unreleased]` § v0.6.0 段落已補上「對抗審查 `6683403-20260904T161327Z`」小節（80 送審／73 confirmed／已修 68／部分修 5／deferred 0，與 `docs/releases/v0.6.0-known-limitations.md` §1／§2 一致）與「已知限制（v0.6.0 進行中，尚未修）」小節；`session-integrity`／`capability-consent`／`identity-binding`／`pairing-migration`／`renderer-lifecycle`／`general-mode-ux` 等維度靠統計＋交叉引用 `known-limitations.md` §2.1 的完整表涵蓋，本次更新已交叉核對兩份文件的統計數字（80／73／68／5／0）逐字一致。段末的「回歸」小節本次已改成最終整合回歸的實跑數字（Rust 1040/0・85 target、Tauri 56/0、vitest 1416/0・70 檔、CLI E2E 96/0、Playwright 71/0、iOS 101/0，含兩則已定位並排除的暫時性失敗），取代先前只記到 wave1 的舊數字。內容具體、與 `known-limitations.md`／`test-matrix.md` §8 逐項一致，故 `met`。 |
| 4 | AIP compatibility tests（Rust／TypeScript／Swift 三方 golden fixture conformance） | **met** | Rust 27 passed（lib 17＋`conformance.rs` 10）／TS `aip-conformance.test.ts` 84 passed／Swift `AIPConformanceTests` 17 passed；`aip:check` exit 0（無 codegen 漂移） | 全部修復落地＋`node scripts/aip-codegen.mjs` 重生完成後的單一整合跑（`scratchpad/final/{rust-test,pnpm-test,ios/xctest}.log`，見 `docs/releases/v0.6.0-test-matrix.md` §8.6）。三方對同一組 golden fixture（本輪新增至少 11 份，含 `invalid-state-revision-out-of-range`／`invalid-state-integers-written-as-floats`／`invalid-spec-version-*`／`roundtrip-big-integer-unknown-field` 等）全數通過，三方數字不同純粹是 TS 把版本／能力協商純函式案例也收進同一個測試檔（`it.each` 展開），對 fixture 目錄本身的逐檔覆蓋三方一致。 |
| 5 | 全部可用測試（Rust workspace／Tauri backend／前端 vitest／CLI E2E／Playwright／iOS XCTest） | **met** | Rust **1040/0**（85 target）；Tauri **56/0**（3 target）；vitest **1416/0**（70 檔）；CLI E2E **96/0**；Playwright **71/0**（2.3 分）；iOS XCTest **101/0**（模擬器） | 涵蓋全部對抗審查修復的單一最終整合回歸，同機同 worktree 實跑（`scratchpad/final/`，詳細命令與逐項數字見 `docs/releases/v0.6.0-test-matrix.md` §8.1）。六個套件最終皆 0 failed；ESP32 真板與 iPhone 真機仍是零執行，不在本清單「可用測試」定義範圍內（見 blocked-by-human 表）。 |
| 6 | 無未解釋 regression | **met** | 兩則暫時性失敗已定位並排除，重跑後全綠 | 最終回歸過程中出現的唯二兩則失敗都已查明成因、非產品 regression：**CLI E2E** 第一次 95/1，失敗是驗收腳本斷言追不上 `reconnect-recovery-044` 修復後的正確行為（斷線先進 `Reconnecting`、逾時才 `Offline`），修正斷言後重跑 96/0；**Playwright** 第一次 65/1/5-did-not-run，是 `character-session.spec.ts` 一個逾時（與其他對抗審查 agent 並行競爭資源造成的計時 flake），先對受影響 3 個 spec 重跑 30/0，再重跑一次完整套件確認 71/0。歷史上（wave1／hardening）出現過的差異（`git diff --check` 檔尾空行、記憶體不足中止）同樣都已解釋，見 `docs/releases/v0.6.0-test-matrix.md` §2／§8.2／§8.3。 |
| 7 | 無未處理 High finding | **met** | — | 對抗審查 22 個 high severity confirmed finding **全部已修**（含 verifier 覆核或 fixer 附具體回歸測試），最後收尾的 `general-mode-ux-022`（Runtime 把協商後的 `NegotiatedCapabilities` 投影進 `MemberView`／diagnostics 的 `unsupportedIntents`）與 `pairing-migration-001`／`session-integrity-056` 的跨組鏡射伴生瑕疵皆已收斂，見 `docs/releases/v0.6.0-known-limitations.md` §2.1 對應列。另外 12 個原本只有 fixer 自報的 confirmed finding（含部分 high：`capability-consent-051`／`identity-binding-006`／`identity-binding-008`／`pairing-migration-002`／`pairing-migration-003`／`reconnect-recovery-041`／`session-integrity-059` 等）已由 `runtime-host` 組的獨立稽查 agent 逐一覆核完成：11 項確認已修、`runtime-boundaries-065` 確認為部分修（與 fixer 自報一致）。CHANGELOG 統計為 73 confirmed → 已修 68／部分修 5／deferred 0，22 個 high 全部落在已修，無一項保留為未動、無一項仍待收尾——本關卡的判定條件（無未處理 High finding）本身已滿足，故 `met`；「有無新增 High finding」仍需最後一次全套回歸／對抗審查複查（見關卡 5／6 與發布前建議順序第 5 步）才能徹底排除，但那是另一件事，不影響本關卡對「本輪已發現的 High finding 皆已處理」的判定。 |
| 8 | candidate CI 全綠（GitHub Actions） | **pending final run** | `<<TBD>>` | 本輪修復尚未推上 remote、未開 PR，沒有任何 CI run 可引用。需等 worktree 乾淨（關卡 1）、commit、push 後才有 CI 結果可核對。 |
| 9 | release dry-run（`release-prepare.sh` → `release-verify.sh` 全程跑過一次，不含 `release-tag.sh`） | **pending final run** | — | 專案的三段式發布流程（`release-prepare.sh 0.6.0` → `release-verify.sh 0.6.0` → `release-tag.sh 0.6.0 --push`）本輪尚未執行任何一段；`release-verify.sh` 本身在本輪對抗審查中新增了多個關卡（CI 必需 job 齊全性、crate 版本白名單、文件誠實度 lint 等，見 `CHANGELOG.md` [Unreleased] 的 Fixed 段），但這些新關卡尚未在 v0.6.0 的實際版本 bump 場景下跑過一次。 |
| 10 | artifact checksum（CLI／桌面 bundle 的 `.sha256` 可重算比對） | **pending final run** | — | 依賴關卡 9 的 Release 產物才能核對；本輪未產生任何 v0.6.0 的 Release 資產。`release-provenance-074`／`075`（sha256 驗證 fail-closed、桌面 bundle 補齊 checksum）已修（見 `known-limitations.md` §2.1），但那是**修好驗證機制本身**，不等於已經對 v0.6.0 的實際產物跑過一次驗證。 |
| 11 | 無 secret（tracked 檔案） | **pending final run** | — | **更新**：對抗審查 `release-provenance-076`（regex 漏掉 `github_pat_`／`gho_`／`sk-proj-`／`AKIA`／`AIza`／`xox*` 等現行主流憑證格式，報告原文標 `unverified`）已被整合者「順手修正」——`scripts/release-verify.sh` 的正則已擴充涵蓋這些格式，`scripts/tests/release-scripts.sh` 新增 13 個正反案例並全數通過（42/0，見 `docs/releases/v0.6.0-known-limitations.md` §2.2）；此修正**不計入**本輪 73 個 confirmed 統計（因為原始 finding 是 unverified，不是 confirmed）。維持 `pending final run` 的理由：這是「掃描規則本身的單元測試通過」，不是「對 v0.6.0 實際 tracked 檔案真的跑過一次 `release-verify.sh` 且該關卡輸出通過」——後者要等關卡 9 的 release dry-run 才會實際執行到；掃描範圍仍只涵蓋 HEAD 的 tracked 檔案、不含 git 歷史，這點本輪未變。 |
| 12 | SBOM／provenance 符合能力宣告（誠實：本輪無） | **met** | — | `docs/INSTALL.md`／README／QUICKSTART／`CHANGELOG.md`／`docs/releases/v0.6.0-migration.md` §13 皆一致誠實揭露：**沒有程式碼簽章、沒有 Apple 公證、沒有 SBOM、沒有 build provenance**（`release.yml` 的 permissions 沒有 `id-token: write`，沒有 `actions/attest-build-provenance`）；Release 只發布 `.sha256`，且文件明確區分「證明位元組與 Release 一致」與「證明來源」的差別，未誇大成後者。此關卡驗證的是「文件宣稱與程式碼能力相符」而非「有沒有簽章」——本輪的宣稱與現況一致，故 `met`。見 `docs/releases/v0.6.0-known-limitations.md` §3-15。 |
| 13 | 升級／回退文件完整 | **met** | — | `docs/releases/v0.6.0-migration.md` §11「升級／回退」完整記載：feature flag `INTERACT_AI_CHARACTER_SESSION=0` 的整批回退路徑（行為退回 v0.5.1，不需刪檔或改資料格式）、`state/character-session.json` 可安全刪除重建的兩種情境（檔案不存在／讀取失敗）、epoch 語意與客戶端行為保證，並引用對應程式碼行號與 iOS 回歸測試名（`SessionClientTests.swift::testSessionResetWithANewerEpochIsAcceptedEvenThoughTheRevisionGoesBackwards`）。§10「Breaking 變更」逐項列出對外部呼叫者／安裝更新流程／Rust API 的三類 breaking 與理由，§12 列出 deprecated 符號與移除時程。內容具體、可操作，故 `met`。 |
| 14 | Known Limitations 誠實（分類正確、不誇大已修、不隱藏未修） | **met** | — | `docs/releases/v0.6.0-known-limitations.md`（本輪與本文件同批產出）逐一處置 73 個 confirmed finding（依 severity 分節列出 id／處置／回歸測試，最終統計已修 68／部分修 5／deferred 0）、6 個 refuted、1 個 unverified，並誠實記錄先前 4 項「收尾中（本輪）」與 12 項「僅 fixer 自報」finding 已全部完成跨組收尾與獨立 verifier 覆核的過程（含 `runtime-boundaries-065` 覆核後仍是部分修，未被灌水成已修），另加 23 項 v0.6.0 新增窄限制（含跨組收尾本身新產生的 3 項窄限制）與 22 項 v0.5.1 沿承項目逐條核對現況（含「未重新核實」而非硬猜的誠實標示）。分類用字與 v0.5.1 版本一致（已修／部分修／deferred／保留），沒有把部分修灌水成已修，也沒有把已完成的收尾寫成仍在進行。 |
| 15 | 真機證據未被模擬器／fixture 冒充 | **met** | — | 逐一核對 `docs/releases/v0.6.0-test-matrix.md` §6／§7、`docs/aip/iphone-companion.md` §10、`docs/releases/v0.6.0-migration.md` §13：全部一致把 iPhone 真機標為「implemented-unverified」／「未執行」，模擬器（iOS Simulator）與 fixture（`fake_iphone.rs`、`fake_claude.sh`／`fake_codex.sh`）在每一處引用都明確標註證據等級，沒有一處把模擬器／fixture 結果寫成真機結果；ESP32 同樣一律標「fixture（arduino-cli 編譯，非真板）」。`docs/releases/v0.6.0-known-limitations.md` §3-1／§3-2 延續同一誠實用字。未發現冒充案例，故 `met`。 |
| 16 | tag 指向 CI 通過的 commit | **pending final run** | — | 尚未建立 `v0.6.0` tag（`release-tag.sh` 尚未執行）。發布腳本本身在本輪對抗審查中新增了 `ci-gate` 強制關卡（`release-provenance-072`／`073`／`077` 已修，斷言被 tag 的 commit 上四個必需 job 全部 `success` 才放行），但這個新關卡本身尚未在真實 tag push 上驗證過（`ci-gate`／`finalize` 兩個新 job 的證據等級是 unit，非端到端，見 `known-limitations.md` §3-16）。 |

**統計（本次更新後）**：**met 10 項**（1／3／4／5／6／7／12／13／14／15——1／3／4／5／6 是本次
由 pending final run 改判，依據是 §8 記載的最終整合回歸；7／12／13／14／15 為既有的文件／誠實性
檢查與此前已修的 High finding 判定）、**pending final run 6 項**（2、8、9、10、11、16，均需要
實際執行 `release-prepare.sh`／push／CI／`release-verify.sh`／`release-tag.sh` 才能核對，純文件
任務無法代勞）、**blocked-by-human 0 項（關卡層級）**——iPhone 真機與 ESP32 真板見下表，兩者本輪
範圍內都不是發布的硬性關卡（v0.5.1 也是同樣處理方式），但發布前建議順序仍列出對應步驟。

## 不在 16 關卡內、但確實 blocked-by-human 的兩件事

| 項目 | 狀態 | 說明 |
|---|---|---|
| iPhone 真機驗收 v0.6.0 新增的 AIP／Character Session 層 | **blocked-by-human** | 需要人類在 Mac 上完成鑰匙圈與信任步驟才能把 App 裝上真機（沿用 v0.5.1 遇到的同一類人工阻塞，見 `docs/releases/v0.5.1-release-readiness.md` 對應段落）。本輪程式碼與模擬器／fixture 測試齊全（`docs/releases/v0.6.0-test-matrix.md` §3.5／§4），但真機上**零執行**，AI 不得代按鑰匙圈授權對話框。v0.5.0／v0.5.1 的既有真機證據（`docs/releases/v0.5.{0,1}-iphone-device-evidence.md`）仍然有效但不涵蓋這層新協定。 |
| ESP32 真板驗收 | **blocked-by-human** | 本環境無實體 ESP32 裝置。本輪兩種組態以 `arduino-cli` 實際編譯，皆 exit 0（`docs/releases/v0.6.0-baseline.md`）——**非真板驗收**，本輪未新增任何真板證據。 |

## 發布前建議順序（給整合者）

1. ~~等所有並行的對抗審查修復 agent 完成收尾~~——**已完成**：先前 4 項「收尾中（本輪）」
   （`general-mode-ux-022`／`capability-consent-052`／`reconnect-recovery-044`／
   `identity-binding-009`）與 `pairing-migration-001`／`session-integrity-056` 的跨組鏡射伴生
   瑕疵都已收斂為已修，見 `docs/releases/v0.6.0-known-limitations.md` §2.1。整合者仍需確認
   `git status --porcelain` 為空（關卡 1 目前仍是 `pending final run`，因為本文件依任務範圍
   不得執行 git 指令核對，且工作樹可能還有這批文件收尾與其他並行 agent 的變更尚未提交）。
2. ~~對 12 個目前只有 fixer 自報的 finding 補上獨立 verifier 覆核~~——**已完成**：`runtime-host`
   組的獨立稽查 agent 已逐一覆核（見 `known-limitations.md` 開頭列表），11 項確認已修、
   `runtime-boundaries-065` 確認為部分修，與 fixer 自報一致。
3. ~~補齊 `CHANGELOG.md` `[Unreleased]` 段落~~——**已完成**（關卡 3 現為 `met`）：
   `CHANGELOG.md`／`docs/acceptance-evidence.md` 已與 `known-limitations.md` §2 的最終統計
   （已修 68／部分修 5／deferred 0）交叉核對一致，段末「回歸」小節已改成第 4 步跑出的最終數字。
4. ~~重跑一次完整回歸~~——**已完成**（關卡 4／5／6 現為 `met`）：
   ```bash
   cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings
   cargo test --workspace                                    # 1040 passed / 0 failed（85 target）
   cargo test --manifest-path apps/interaction-desktop/src-tauri/Cargo.toml   # 56/0（3 target）
   node scripts/aip-codegen.mjs --check                       # exit 0，無漂移
   cd apps/interaction-desktop && pnpm typecheck && pnpm test && pnpm build && pnpm test:e2e
   # vitest 1416/0（70 檔）；Playwright 71/0（見 §8.3 的計時 flake 定位與重跑）
   ./scripts/v03-cli-e2e.sh                                   # 96/0（見 §8.2 的斷言修正）
   ```
   `./firmware/esp32-companion/compile.sh` 與 iOS typecheck／build／XCTest（101/0，模擬器）本輪
   同步跑過，數字與逐項定位見 `docs/releases/v0.6.0-test-matrix.md` §8。真機／真板部分（ESP32
   實體板、iPhone 真機）仍是 blocked-by-human，見下表，不影響本步驟已完成的判定。
5. 用回歸後的乾淨狀態跑一次 `.claude/workflows/adversarial-review-v06.js`（若時間預算允許），
   確認本輪收尾（含 §3 新增的 3 項窄限制：`MemberView` 新欄位造成的舊快照 `HashMismatch`、
   TS 端 `MAX_UNSUPPORTED_INPUTS` 無漂移閘門、`state-snapshot.json` fixture 未含
   `unsupportedIntents`）沒有引入新的 High finding；關卡 7 本身已 `met`，此步驟是最終前的
   雙重確認，不是把關卡 7 蓋為 `met` 的前提。**本次更新未執行此步驟**（依任務範圍不得跑
   workflow），留給下一位整合者視時間預算決定。
6. 提交本批次文件與程式碼變更（含本次 `v0.6.0-final-report.md`／本文件／`test-matrix.md`／
   `CHANGELOG.md` 的更新），確認 `git status` 乾淨（把關卡 1 的「HEAD 乾淨」延續到「提交後仍乾淨」）。
7. push 分支、開 PR，等 CI 全綠（關卡 8）。
8. 使用者明確授權後依序執行 `./scripts/release-prepare.sh 0.6.0` → `./scripts/release-verify.sh 0.6.0`
   （關卡 2／9／11／16 於此結清）→ 確認全部關卡皆 `met` 後才執行
   `./scripts/release-tag.sh 0.6.0 --push`。
9. 發布後從乾淨 checkout 重跑第 4 步全套並比對 Release 產物 checksum（關卡 10）。
10. iPhone 真機（blocked-by-human）：請使用者在 Mac 上對鑰匙圈對話框按「總是允許」後才能補跑
    v0.6.0 AIP／Character Session 層的真機矩陣。**在此之前不得**把本輪的 iPhone 真機驗收寫成
    「已在真機驗收」。
