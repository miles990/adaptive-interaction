# v0.5.0 發布就緒清單

> 狀態值只有三種：**met**（這一關本身的條件已滿足，證據見下）、**pending final run**（工作已完成，
> 但要等整合階段的最終一次全套跑實際確認才能蓋章）、**blocked-by-human**（需要使用者或人工環境介入，
> 非工程可解）。沒有一格是猜測；找不到證據的一律標 `pending final run` 而不是 `met`。

| # | 關卡 | 狀態 | 證據／備註 |
|---|---|---|---|
| 1 | Rust workspace `cargo fmt --check` 乾淨 | **met** | 最終一次全量跑（2026-09-03，所有 commit 就緒後）：exit 0 |
| 2 | Rust workspace `cargo clippy --workspace --all-targets -- -D warnings` 乾淨 | **met** | 最終一次全量跑：0 warning |
| 3 | Rust workspace `cargo test --workspace` 全綠 | **met** | 最終一次全量跑：**736 passed / 0 failed / 0 ignored（63 個 test target）**；基準 595/0/0（58 target）→ +141；`cargo build --workspace` 成功 |
| 4 | 前端 `pnpm typecheck` 乾淨 | **met** | 第二輪修復收尾最終一次跑：`tsc --noEmit` 乾淨 |
| 5 | 前端 `pnpm test`（vitest）全綠 | **met** | 第二輪修復收尾最終一次跑：**988 passed / 0 failed（49 檔）** |
| 6 | 前端 `pnpm build` 成功 | **met** | 第二輪修復收尾最終一次跑：成功 |
| 7 | Tauri backend `cargo test`（src-tauri）全綠 | **met** | 最終一次全量跑：**46 passed / 0 failed**，同路徑 clippy 0 warning；`Cargo.lock`（tauri-plugin-dialog 2.7.3 等）已隨 commit 提交 |
| 8 | CLI E2E `./scripts/v03-cli-e2e.sh` 全綠 | **met** | 最終一次全量跑：**82 passed / 0 failed**（腳本改用獨立暫存 workdir：Runtime 自 0.5.0 起拒絕以自己的狀態資料夾當 agent workdir） |
| 9 | Playwright `pnpm test:e2e` 全綠 | **met** | 最終一次全量跑：**65 passed / 0 failed**（12 spec，1.9 分；含模擬 iPhone fixture）。過程中抓到並修掉一個真缺陷：StateView 在背景刷新時把清單換成「載入中」導致工作卡重掛、核可裁決結果消失（`fix(ui): keep list contents mounted during background refreshes`） |
| 10 | ESP32 韌體編譯檢查（`compile.sh` 兩種組態） | **met** | 2026-09-03 兩種組態（Serial/Wi-Fi/MQTT 與 `--ble`）以 `arduino-cli` 實際編譯，exit 0；**非真板驗收**，見 #11 對照組 |
| 11 | ESP32 真板驗收 | blocked-by-human | 本環境無實體 ESP32 裝置；編譯檢查（關卡 10）2026-09-03 最終覆核跑仍 exit 0；`firmware/esp32-companion/README.md` 已誠實標示「能編譯≠真機閉環」 |
| 12 | iPhone 真機驗收 | **met（核心流程；動作觀察與 BLE 周邊 blocked-by-human）** | 2026-09-03 iPhone 11／iOS 26.3.1 完成 Developer Mode／簽章身分設定後，實際跑過安裝與啟動、區網 TLS 配對、動器 acknowledged（haptic／notify／tts／torch／flash／角色六態）、AI 偽造 emergency／verified-success 被 runtime 擋下、停止所有感測、緊急停止投影＋停感測、解除不自動恢復、撤銷離線裝置、斷線處理；**blocked-by-human**：observe-motion（需人搖手機）、BLE connect／GATT（無測試用 peripheral）。詳見 `v0.5.0-iphone-device-evidence.md` |
| 13 | 兩輪對抗審查 finding 全數處置 | **met**（第二輪 4 項刻意 partial） | 第一輪 44 confirmed（43 fixed／1 already-fixed／1 partial）；第二輪 `c3d1786-20260903T124638Z` 78 reviewed／74 confirmed／4 refuted，63 fixed＋4 partially-fixed＋7 docs-claims 修在文件；4 項第二輪 partial（`safety-invariants-078`／`companion-gameplay-032`／`protocol-conformance-030`／`link-transports-054`）與 `ia-settings-012`（精靈半邊，第二輪覆核仍為真）刻意保留並記錄根因，見 `v0.5.0-known-limitations.md` §1 |
| 14 | 安全不變量未被本輪改動削弱 | **met** | Policy Governor 的 `min()` 鏈本輪只是新增「裝置安全上限」輸入（`limits:`），沒有移除既有比較項；AI 仍不能授予 consent、不能解除 emergency stop、不能提高安全上限（`RUNTIME_ONLY_STATES` 反而收緊了 AI 能投影的真相狀態）；感測器仍預設關閉；emergency stop 仍不因重啟自動恢復——本輪修復方向一致朝「更嚴格、更誠實」，未發現削弱案例 |
| 15 | 誠實階梯（queued≠completed 等）在本輪改動中維持 | **met** | 本輪修復本身就是在補誠實階梯的漏洞（估計逾時不再自動變 Failed、感測停止逐項誠實回報、緊急停止不再偷開模型呼叫、多台手機不再靜默挑第一台等）；每項修復都附「把 bug 放回去→測試變紅」的驗證，見各 `stage1-reports.json` 條目 |
| 16 | 文件與程式碼一致（docs-claims-024/025/027/029/030/032/033，第一輪；067–073，第二輪） | **met** | 第一輪 7 項＋第二輪 7 項共 14 項已核對程式碼後訂正，見 `docs/v05-capability-gap-matrix.md`、`docs/DESKTOP-GUIDE.md`、`docs/character-protocol/{README,reference-adapters}.md`、`docs/acceptance-evidence.md`、`README.md`、`docs/FEATURES.md`、`CHANGELOG.md`、`docs/releases/v0.5.0-*.md`、`apps/interaction-ios/README.md` 變更 |
| 17 | CHANGELOG 反映本次發布 | **met** | `[Unreleased]` 已轉為 `[0.5.0] - 2026-09-03`；「Phase 9：發布硬化」小節含第一輪（Fixed／Added／Known limitations）與第二輪「對抗審查第二輪」區塊；發布時 `release.sh` 不再另建章節 |
| 18 | Golden schema 無漂移（`schemas/`） | **met** | `cargo test -p interaction-e2e --test golden` 6 passed（含 `golden_character_protocol_schema`），新增欄位皆 `#[serde(default)]`；正式重生仍由 `scripts/release.sh` 於版本 bump 時執行 |
| 19 | 版本一致性（四處版本號） | pending final run | `workspace Cargo.toml`／`src-tauri/Cargo.toml`／`tauri.conf.json`／`package.json` 目前仍是 v0.4.1；版本號一律由 `./scripts/release.sh 0.5.0` 一次更新，發布前才會執行，本清單不代為手動改動版本號 |
| 20 | 版控紀律（無 force push、不重寫公開歷史、不覆蓋 tag、不提交秘密） | **met** | 工作分支 `release/v0.5.0-product-hardening` 以原子 commit 提交（Conventional Commits）；Team ID／UDID／token 皆未進 repo（`git grep` 掃描乾淨）；push／PR／tag 依使用者授權於 CI 綠燈後執行 |

## 發布前建議順序（給整合者）

1. 確認所有 untracked 新檔已被納入下一步的合併／提交範圍（清單見 `v0.5.0-test-matrix.md` 「已知會影響最終數字的因素」）。
2. 跑一次 `cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings`（關卡 1、2）。
3. 跑 `cargo test --workspace`（關卡 3）、`cd apps/interaction-desktop && pnpm typecheck && pnpm test && pnpm build`（關卡 4–6）、`cargo test --manifest-path apps/interaction-desktop/src-tauri/Cargo.toml`（關卡 7）。
4. 跑 `./scripts/v03-cli-e2e.sh`（關卡 8）與 `pnpm test:e2e`（關卡 9）。
5. （已完成）實際數字已回填 `v0.5.0-test-matrix.md`、`docs/v05-capability-gap-matrix.md` §11／§12、`CHANGELOG.md`、`docs/acceptance-evidence.md`。
6. 關卡 11、12（ESP32／iPhone 真機）在使用者完成 `v0.5.0-iphone-device-evidence.md` 列出的人工步驟前，維持 `blocked-by-human`——**不得**在此之前把它們標成已驗收。
7. 使用者明確授權後，執行 `./scripts/release.sh 0.5.0`（處理版本一致性、golden schema 重生、打 tag、觸發 Release CI）。
