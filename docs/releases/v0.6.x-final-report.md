# v0.6.0 後續：可維護性與一般模式收斂 — 最終報告（草稿，整合里程碑後定稿）

> 狀態：**候選成果，未發布**。本輪不打 tag、不建 Release、不合併；所有數字皆為本機同機實跑，
> 模擬器／fixture／程序內 client 逐列標示，真機／真板為零執行。恢復點：`docs/releases/v0.6.x-maintainability-progress.md`；
> 已發布版本的 canonical 事實：`docs/releases/evidence-index.json`。

## 1. 起點與終點

| 項目 | 值 |
|---|---|
| 分支 | `feature/v0.6.x-maintainability`（自 `main` `8f52837` 建立） |
| 起始 commit | `8f528374fd0b1490eacb623f6e299339bfd565e0`（＝ origin/main，v0.6.0 已發布：tag `4bd55fe`，Release 23 assets，CI 33920312398 綠） |
| 最終 commit | _整合後填_ |
| PR | _未建立（環境允許時建 draft PR；本輪不合併）_ |
| CI | _推上 remote 後填 run id_ |

## 2. 需求矩陣

見 `docs/releases/v0.6.x-maintainability-progress.md` §1（每列：需求／現有實作／呼叫路徑／測試／已知限制／證據等級／本輪動作，
狀態 implemented／verified／partial／not-reproduced／needs-environment）。

### 2.1 疑點分類（起始靜態疑點 → 本輪結果）

| 疑點 | 分類 | 結果 |
|---|---|---|
| 桌面 snapshot 無 rollback 防護；缺 revision／epoch 變成 0；慢 GET 覆蓋新 SSE；RuntimeEvent.sequence 去重在 daemon 重啟後失效 | **confirmed** | 已修（純 reducer `src/aip/sessionClient.ts`，40＋21＋5 支測試） |
| restore 第二次 canonical 重 hash 讓新增 default 欄位的舊快照被判損毀（v0.6.0 #21） | **confirmed** | 已修（`Snapshot.format`＋migration＋attention 允許鍵；真實 v0.6.0 格式 fixtures） |
| 舊 snapshot 晚到覆蓋新 revision | **confirmed（決定性重現：先 save(6) 再 save(5)）** | 已修（同鎖 (epoch, revision) guard；`SaveOutcome`） |
| persist 錯誤被吞掉 | **confirmed** | 已修（diagnostics `store` 物件） |
| 暫時讀不到的檔案之後仍會被 persist 覆寫 | **confirmed** | 已修（parked store） |
| `clamp_unit(-0.0)` 產生 `-0.0` 字面 | **confirmed** | 已修（正規化＋restore 拒絕） |
| TS 不核對 hash（0.0 vs 0） | **confirmed（設計限制）** | 已修（doublePaths codegen＋三端 fixtures） |
| `emergency_stop` 不掃未回報的高風險受器（X1） | **confirmed** | 已修（單一協調器） |
| 通用 provider revoke／disable 對 mobile 幾乎 no-op（X2） | **confirmed** | 已修（走 request_stop＋release） |
| `transition_provider` 不翻 registry 旗標（v0.5.1 #4） | **confirmed（先重現）** | 已修（重新啟用不自動恢復） |
| 撤銷後零裝置永遠 needs-reconfirmation | **confirmed（文件稱刻意；產品語意改）** | 已改（local-only 終態） |
| settingsTransfer 全域白名單（#17；persona 同類） | **confirmed** | 已修（綁 adapter meta；舊小樞家族誠實忽略） |
| 宣告式 adapter 零 AIP、無 declare、無 stop 路徑 | **confirmed（文件已承認）** | 已修（D1 `e86afb9`＋D2 `600f030`：裝置線 v1.1、production DeviceLink 經 pty 模擬器、declare＋SensorSource、廣播經出站登記表；真板零） |
| iOS heartbeat 靜默忽略；前景 presence 不依生命週期 | **confirmed** | 已修（模擬器 120/120） |
| 文件過期（README 候選、general-mode-ux 未投影、AIP §6「大於」） | **confirmed** | 已修＋lint 擋回歸 |
| Serial／MQTT 身分強度被寫成與 iPhone 相同 | not-reproduced（文件未明講） | 已明定 `transport-hello+device-side-pairing` 弱於 `paired-token`（文件、稽核、diagnostics 三處一致；UI 不得譯成「已驗證身分」） |
| 真機（iPhone AIP 層）、ESP32 真板、真 Tauri 視窗 | **needs-environment** | 未執行（見 §9） |

## 3. 修改理由與檔案（依里程碑；commit 依時間）

| 里程碑 | commit | 內容（理由） |
|---|---|---|
| 基線／矩陣 | `422a5cf`、`830e748` | 需求矩陣、基線數字、完整性批評者補列（X1–X8） |
| M4 §5.2 文件對帳 | `ca26cbe`、`81b89d1` | 過期的「v0.6.0 候選」改成已發布事實；`evidence-index.json`＋docs-claims lint |
| M4 §5.3 release | `d94abac`、`e8a02cf` | release-verify `--run-tests` 涵蓋 Tauri backend＋`aip:check`；CHANGELOG claim-check 綁版本、不強迫重述 |
| M1 §2.4 hash 契約 | `475c7ac`、`055d638`、`7626e8a` | 三端 state-hash fixtures（9 份）、`stateHashDoublePaths` 由 schemars 推導、文件 |
| M1 §2.1 desktop reducer | `0d9c872`、`518d73b` | 純 reducer `sessionClient.ts`（嚴格 revision／epoch、request generation、reset 契約）、同步 SHA-256 核對、`-0.0` 修正 |
| M1 §2.2／§2.3 快照 | `e34ab17`、`02441c1`、`864842b` | `Snapshot.format`＋五種載入結果＋備份、`SaveOutcome` 同鎖 guard、parked store、diagnostics `store` |
| M2 §3.4／§3.5 TS | `8311c05`、`f215068`、`5cd1da1` | settingsTransfer 綁 adapter meta（舊小樞家族誠實忽略）、routing／statusProjection 拆分 |
| M2 §3.2 registry | `3fcf38f`、`d5a1071` | `ProviderCapabilityRegistry` 歸 RuntimeInner＋唯讀 view；provider 停用真的翻旗標、重啟不自動恢復 |
| M4 §5.4 iOS | `6be568f`、`d6e4127` | 前景 presence 生命週期、15 s heartbeat、AIP heartbeat 處理（模擬器 XCTest 120/120） |
| M3 §4.1–§4.3 | `a80f62f`、`e27180a` | 同步卡 12 態＋穩定 action id＋真導覽、移除＝local-only 終態、首屏 40→5、陪伴預設純函式 |
| M2 §3.1 SensorSource | `53fda8a`、`96b02c9` | port＋單一有界協調器＋五態結果；X1／X2／S4 |
| M2 §3.3 D1 | `e86afb9`、`fb097d4` | 裝置線 v1.1＋Serial AIP profile（production DeviceLink、pty 模擬器）；文件與已知限制 |
| M3 §4.4 | `1ab3dc4` | 任務驗收 e2e 13＋4、量測器、非開發者腳本（未執行）；⌘K 面板真 modal |
| S1 交接 | `2450294` | 桌面顯示 `sources[]`（非手機來源的停止結果） |
| 審查工具 | `287ae8d` | `adversarial-review-v06x.js`（12 維度，含誠實階梯稽核） |
| M2 §3.3 D2 | `600f030` | 型別抹除、有界的裝置出站登記表（宣告式裝置真的收到廣播；no-channel 稽核）、稽核 transport／identityStrength 由來源提供（驗證者抓到的殘留一處由主 agent 補修＋突變驗證）、diagnostics `identityStrength`、並行競態根因（測試共用行程層 `PROVIDER_LINKS`）修機具 |

完整檔案清單：`git diff --stat 8f52837..<最終 commit>`（截至 `287ae8d`：172 檔、+19197／−2856）。

## 4. 協定／儲存遷移

- **hash 契約不變**（AIP 1.0 canonical 未改、無 wire 版本變更）：明定數字字面＝serde_json（f64 整數值 `x.0`，`-0.0` host 永不產生），
  iOS 逐字保留、TS 依 `stateHashDoublePaths` 重印；三端 `stateHashes` fixtures 9 份。
- **快照容器格式版本** `Snapshot.format`（0＝v0.6.0 無版本、1＝現行；不進 hash、不上 wire）：五種載入結果、備份 `character-session.json.pre-format-<n>`、
  downgrade 矩陣（v0.6.0 讀 format 1 可；未來 state 新欄位 v0.6.0 讀會隔離）。
- **持久化順序**：`SessionStore::save → SaveOutcome`，(epoch, revision) 同鎖 guard；不宣稱網路 exactly-once。

## 5. Adapter 演練（§6 維護性完成定義）

| 演練 | 觸及模組 | 結果 |
|---|---|---|
| 新增角色只改 package／adapter／組合根 | `character/adapters/*`（T1：小樞遊玩場 UI 進 adapter、頁面層守門測試） | 頁面層不再有 entrypoint 字面（`CharacterPreview.tsx`／`CharacterLibrary.tsx` 棘輪待收斂） |
| 第二裝置走正式 AIP binding | `adapter-declarative/protocol.rs`＋`runtime/declarative_session.rs`（D1，核心零變更由驗證者 `git diff --stat` 核對）；D2 只動 `character_session.rs`／`runtime.rs`／`mobile.rs` 的出站與稽核 | Serial 模擬器經 production adapter 成為成員、收廣播、stop-all 真 ack；含 members 的 patch 放不進 639 bytes（已知限制） |
| optional 狀態欄位可遷移 | `interaction-session`／`character_session.rs` | 真實 v0.6.0 fixtures 遷移通過 |
| 停用／移除 Adapter 全收斂 | `sensor_source.rs`、`providers.rs`、`declarative_session.rs` | X2／S4 測試；宣告式 adapter 撤銷 → leave＋retract＋unregister＋abort＋出站表清空（測試） |
| 第三方高風險受器有真實 stop 路徑或明確不可驗證 | 同上 | no-stop-path 仍誠實；Serial 模擬器 stop-all 取得 ack（`confirmedVia=ack`），靜默＝`unknown` |
| 同 Session 舊 snapshot 不覆蓋 | `sessionClient.ts` | 測試 (a)(c)(e) |
| 舊持久化不晚到覆蓋 | `character_session.rs` | 決定性測試＋併發測試 |
| 一般模式不必理解協定／UUID／revision | X5 DOM 守門測試 | 通過 |
| 主動移除手機是正常終態 | `characterSync.ts` | local-only |
| 小樞互動／安全階梯／設定／配對／deep links 無退步 | deep-links 39、既有測試、整合全套 | _整合後填_ |

## 6. 一般模式前後對照

見 CHANGELOG 與 `docs/releases/v0.6.x-general-mode-tasks.md`（M3c）：首屏控制項 40→5；決策數 調陪伴程度 10→3、暫停主動對話 30→10、
換角色 38→13、設安靜時段 35→15（三個任務各多一次展開點擊）；同步卡有明確下一步的狀態 0/11→9/12。_M3c 的 e2e 計數與 390px 結果填_。

## 7. 測試命令／數量／結果／時間

| 套件 | 基線（8f52837） | 最終 | 命令 |
|---|---|---|---|
| Rust workspace | 1040/0（85 二進位） | _填_ | `cargo test --workspace --no-fail-fast` |
| Tauri backend | 56/0 | 57/0（M1 後） → _填_ | `cargo test --manifest-path apps/interaction-desktop/src-tauri/Cargo.toml` |
| vitest | 1416/0（70 檔） | 1590/0（76 檔，第二波後） → _填_ | `pnpm test` |
| CLI e2e | 96/0 | _填_ | `./scripts/v03-cli-e2e.sh` |
| Playwright | 71/0 | _填_ | `pnpm test:e2e` |
| iOS XCTest（模擬器） | 101/0（v0.6.0） | 120/0 | README 的 simctl 注入流程 |
| docs-claims / release-scripts | — | 105/0、48/0 | `scripts/tests/*.sh` |

## 8. 證據分類

- **真機／真板**：零執行（iPhone AIP 層、ESP32）。
- **模擬器**：iOS XCTest 120/120（iPhone 17、iOS 26.5 runtime）。
- **fixture／程序內**：fake_iphone、esp32-serial-sim（pty）、in-process FakeSensorSource、MemoryStore。
- **browser**：Playwright（Chromium）對真 daemon。
- **unit／contract**：cargo test、vitest、三端 conformance fixtures。

## 9. 性能條件

_整合後以 `pnpm perf`（headless Chromium，800×600@2x，warm-up 2000 ms，soak ≥60 s，GC 後差值）重跑並與 v0.6.0 test-matrix §5.3 同條件對照；
不與 v0.5.1 的 10 min soak 比較。_

## 10. 獨立審查

- 每個修復 workflow 皆有獨立 sonnet 驗證者（find／verify 分開）：M1 A confirmed／B partial（報告數字）／C confirmed；wave A R1／T1／M5a confirmed；
  I1 confirmed；wave B M3a partial（兩個 issue 已收）／M3b confirmed；S1 confirmed；_D1／M3c 填_。
- 對抗審查（repo workflow）：_整合後執行並填 confirmed／refuted 數_。
- 事故：一個驗證者違規 `git stash`，所有 agent 的未提交修改被清空；已全數還原並重驗；已記錄為 agent 行為回饋。

## 11. 剩餘限制與下一版本建議

_整合後填；已知：hub 深連結已接、`CharacterPreview.tsx`／`CharacterLibrary.tsx` 頁面層棘輪、`companionScene` 仍全域白名單、
陪伴預設兩段寫入非原子、parked store 需重啟恢復、mobile 三態→wire 投影損失、iOS 15 s 心跳未量電量、真機零執行。_

版本建議（非決定）：M1／M3／M4（相容修補與 UX）→ patch 候選；M2（SensorSource port、Serial AIP profile、diagnostics／snapshot 新欄位、
provider 停用翻旗標）→ minor 候選。
