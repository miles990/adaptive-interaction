release-scripts tests @ /Users/user/Workspace/claude-lab/adaptive-interaction
  ✔ scripts/drills/ 有 2 支演練腳本
  ✔ bash -n scripts/release.sh
  ✔ bash -n scripts/release-prepare.sh
  ✔ bash -n scripts/release-verify.sh
  ✔ bash -n scripts/release-tag.sh
  ✔ bash -n scripts/ci-required-checks.sh
  ✔ bash -n scripts/get.sh
  ✔ bash -n scripts/tests/release-scripts.sh
  ✔ bash -n scripts/tests/docs-claims.sh
  ✔ bash -n scripts/tests/architecture-checks.sh
  ✔ bash -n scripts/tauri-ax-walkthrough.sh
  ✔ bash -n scripts/drills/provider-disable-reenable.sh
  ✔ bash -n scripts/drills/remove-package-keep-user-data.sh
  ✔ release 腳本沒有未保護的 ${ARR[@]} 展開（bash 3.2 相容）
  ✔ release-tag.sh（不帶 --skip-ci）在 /bin/bash 3.2 下不會 crash
  ✔ release-tag.sh（不帶 --skip-ci）真的呼叫 release-verify.sh
  ✔ release-tag.sh 在 verify 通過後建立 tag
  ✔ release-tag.sh --skip-ci 也仍可執行
  ✔ release-tag.sh --skip-ci 把旗標轉給 release-verify.sh
  ✔ release-verify.sh --skip-ci 在結構關卡全過時 exit 0
  ✔ release-verify.sh --skip-ci 明確輸出『跳過』（CI 關卡未評估）
  ✔ release-verify.sh 未帶 --run-tests 時明確輸出『測試未跑』
  ✔ release-verify.sh --run-tests 跑 src-tauri 的 cargo test（CHANGELOG claim-check）
  ✔ release-verify.sh --run-tests 跑 src-tauri 的 clippy
  ✔ release-verify.sh --run-tests 跑 pnpm aip:check（codegen 漂移）
  ✔ release-verify.sh --run-tests 區塊沒有會吞掉退出碼的管線（tail/grep/tee）
  ✔ 有關卡被跳過時，收尾不是無限定詞的『all gates passed』
  ✔ release-verify.sh 收尾寫出 passed-with-skips（誠實階梯）
  ✔ ci-required-checks：四個必需 job 全 success → 通過
  ✔ ci-required-checks：e2e 缺席 → 失敗（不得報 all success）
  ✔ ci-required-checks：有 job 非 success → 失敗
  ✔ ci-required-checks：完全沒有 check-run → 失敗（fail-closed）
  ✔ release-verify.sh 的 gh api 有 --paginate（check-run 超過 30 筆不得靜默截斷）
  ✔ workflow YAML 可解析，且 draft→build→gate→finalize／sha256／平台宣稱一致
  ✔ get.sh 抓不到 .sha256 時拒絕安裝（fail-closed）
  ✔ get.sh 未驗證時沒有把二進位裝進 bin-dir
  ✔ get.sh 提供明示的逃生門（而不是預設跳過驗證）
  ✔ release.yml 內嵌 shell 可解析，finalize 缺資產會擋、desktop 會算 .sha256
  ✔ secret 掃描認得 ghp_ABCDEFGHIJ…
  ✔ secret 掃描認得 gho_ABCDEFGHIJ…
  ✔ secret 掃描認得 github_pat_11A…
  ✔ secret 掃描認得 sk-ant-api03-a…
  ✔ secret 掃描認得 sk-proj-abcdef…
  ✔ secret 掃描認得 AKIAIOSFODNN7E…
  ✔ secret 掃描認得 AIzaSyA1234567…
  ✔ secret 掃描認得 xoxb-123456789…
  ✔ secret 掃描認得 -----BEGIN OPE…
  ✔ secret 掃描不誤判：ghost_town_30_characters_long_word
  ✔ secret 掃描不誤判：AKIA short
  ✔ secret 掃描不誤判：sk-1234
  ✔ secret 掃描不誤判：xox-not-a-token
  ✔ release.yml 上傳迴圈對 CRLF 清單（Windows python）仍能上傳每個 .sha256
  ✔ CRLF 清單的兩個 .sha256 都被上傳
  ✔ release-verify.sh：寫死自有版本的 crate 讓關卡 exit 非 0（沒有白名單）
  ✔ release-verify.sh 指名漂移的 crate 與它的版本
  ✔ release-verify.sh 不把 version.workspace = true 的 crate 列成漂移
  ✔ release-verify.sh 不再輸出任何『已知版本漂移』白名單字樣
  ✔ repo 內 interaction-adapter-declarative／adapters-media 都跟著 workspace 版本

release-scripts: 58 passed / 0 failed
