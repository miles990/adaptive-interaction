# v0.7.0Migration Notes

> 對象：分支 `feature/v0.6.x-maintainability` 從 `f5b8e2f` 起的 **58 個 commit**（尚未 merge、尚未 tag；
> 其中第 28 個 `713f8fe` 是對抗審查的對象，其後是五路修復與文件收斂；最終數字由整合者回填）。
> 逐條變更與 commit SHA 見 `CHANGELOG.md` `[Unreleased]` 的「本輪（v0.7.0，2026-09-05／06）」段；
> 限制見 `docs/releases/v0.7.0-known-limitations.md`；上一版的遷移文件是 `docs/releases/v0.6.0-migration.md`。
>
> **一句話**：儲存與 wire 都不 breaking，但**有一個欄位對 AI 那一側是移除**。快照容器格式不變
> （仍是 `1`），AIP wire 形狀與 `specVersion` 不變；新增的是一個 `payload.reason` 值、一種裝置線訊息、
> 幾個唯讀欄位與兩條人類層路由。**唯一需要動手改的呼叫端**：用 agent／session token 讀 `/v1/status`
> 的 `unresolvedStops`／`characterSessionSync`——那兩個鍵對非人類層 principal 已經不再出現（§5.14）。
> 其餘對外可觀察的行為變更列在 §5（對抗審查 `713f8fe` 的修復是 §5.14–§5.18）。

## 1. 快照與儲存格式：不變

| 項目 | v0.6.0 | 本輪 | 說明 |
|---|---|---|---|
| 快照容器 `format` | `1` | **`1`（不變）** | 沒有新增／移除任何持久化欄位；`character-session.json` 的 `epoch`／`revision` 語意不變 |
| `SemanticState` 欄位 | — | **不變** | 本輪沒有新增 state 欄位（演練 3 的 `ambient` 只存在於不合併的演練分支） |
| `DesktopPrefs` | — | **新增選填 `companionPendingPresetOp`** | `#[serde(default)]`／camelCase；舊檔讀進來是 `null`＝沒有進行中的交易，不需要遷移。值只在陪伴預設交易期間存在，完成後寫回 `null` |
| `~/.adaptive-interaction/state/` 其他檔案 | — | 不變 | 演練 5 實測：移除角色套件後 `desktop.json` 前後 sha256 相同、稽核筆數不變、session `epoch` 未跳（`scripts/drills/remove-package-keep-user-data.sh`） |

**回退（降級到 v0.6.0）不需要任何資料轉換**：`companionPendingPresetOp` 對舊版桌面是未知鍵，
`DesktopPrefs` 以 `#[serde(default)]` 讀取時會忽略它（代價：降級後一筆半套用的交易不會被補送，
使用者看到的是既有的「自訂」狀態，與 v0.6.0 的行為相同）。

## 2. 新的裝置線訊息：`aip-frag`（裝置線 v1.2）

`proto` 仍為 **1**；`aip-frag` 是**追加**訊息型別，舊韌體忽略、舊 host 丟棄。

- **只在對端宣告能力時使用**：裝置必須在 `hello` 的 `caps` 裡包含 `aip.frag/1`；沒有宣告的裝置
  完全走 v1.1 路徑（超過單行上限就照舊拒絕，`reason: over-line-limit-no-fragmentation`）。
- **入站對稱**：沒有宣告 `aip.frag/1` 的裝置送來 `aip-frag` 一律拒絕（`reason: not-advertised`）＋稽核。
- **frame 欄位**：`xferId`／`seq`／`total`／`bytes`／`crc32`。
- **有界**：每裝置 1 筆 in-flight（新的 xfer 取消前一筆並稽核）、重組上限 8 KiB、片數上限 64、
  自最後一片起 2 s 逾時；`hello`／斷線／revoke／stop-all／rebind 一律取消。
- **失敗一律整筆丟棄**：seq 重複／亂序／缺片／截斷／header 不符／crc 不符／bad JSON／惡意 `total`
  在第一片就拒——並留 `aip.fragment-dropped{xfer,reason,received,total}` 稽核。
- 參考韌體**刻意不宣告** `aip.frag/1`（真板上分片路徑零執行）。

實作位置：`crates/interaction-adapter-declarative/src/fragment.rs`＋`DeviceLink`／`AipChannel`；
核心（`interaction-session`／`interaction-aip`／`character_session.rs`）零變更。
契約：`docs/aip/transport-bindings.md` §8、`docs/aip/device-profile.md` §6.3、
相容承諾登記於 `docs/aip/deprecation-ledger.md` §3.2。

**單行上限（實測）**：serial 639、MQTT 639、**BLE 480**（v0.6.0 文件寫 512，本輪更正為實測值）。

## 3. 新的 `payload.reason` 值：`recovery`

AIP 1.0 的**澄清**，wire 形狀與 `specVersion` 不變。

- host 只在「同一個 session、`epoch` 不變、權威狀態真的比該成員記得的舊」時，對**該成員**的 resume
  回一則 `state{kind:"snapshot", payload.reason:"recovery"}`（`REASON_RECOVERY`；計數
  `counters.snapshots.recovery`）。
- 接收端規則 6：只有 host 明說 `recovery` 且 epoch 相同，才允許 revision 退回並採納。
- **舊接收端相容**：只認得舊 reason 值的實作把它當成「沒有 reason 的 snapshot」＝ v0.6.0 行為
  （被 rollback 防護忽略）。這正是 v0.6.0 卡住的那條路徑，不會比原本更糟。
- 被移除的是 Runtime 那段「把這種 snapshot 改寫成 `session-reset`」的補救——它其實無效
  （同 epoch 的 `session-reset` 一樣被忽略），已先以測試重現無效性再刪。

契約：`docs/aip/character-session.md` §6／§7.2；`docs/aip/README.md` §6。

## 4. 新的路由、指令與欄位（全部向後相容）

### 4.1 HTTP（人類層 token；agent token 一律拒絕）

| 路由 | 用途 |
|---|---|
| `GET /v1/sensors/unresolved` | 「沒有人確認過結果」的感測停止摘要（有界 32） |
| `POST /v1/sensors/unresolved/{sourceId}/dismiss` | 人為解除（body `{"generation": N}`，`generation` 必填）；回應與稽核都寫死 `confirmedStopped:false` |

`GET /v1/sensors/unresolved` 已加進 agent token 的 GET 排除清單（`/v1/sensors/stop` 仍放行）。

### 4.2 CLI

```bash
interact-ai sensors unresolved
interact-ai sensors dismiss <sourceId> --generation N
```

### 4.3 新增的唯讀欄位（都是選填／非空才序列化，舊呼叫端不受影響）

| 位置 | 欄位 | 值 |
|---|---|---|
| `GET /v1/status` | `unresolvedStops[]` | `sourceId`／`generation`／`sensors`／`since`／`lastKnown`／選填 `sourceLabel`（人話名稱；查不到就省略，`sourceId` 不冒充人話名稱）。**只有人類層 principal 看得到**（§5.14） |
| `GET /v1/status` | `unresolvedStopCount` | agent／session／adapter principal 拿到的是這個數字（沒有任何識別碼），不是上面那份清單（§5.14） |
| `GET /v1/status` | `characterSessionSync[]` | 每個成員的 `syncProfile` 與選填 `providerId`。**只有人類層 principal 看得到**（§5.14） |
| `GET /v1/character-session/diagnostics` | `members[].syncProfile` | `full-state`／**`pending-full-state`**／`intent-only`／`event-source`；查不到出站通道就省略（§5.15） |
| `GET /v1/status`／diagnostics | `providerId`（選填） | 由出站通道自己說出來（`DeviceOutbound::provider_id`），說不出來就省略；桌面用它把一台裝置的同步狀態對到正確的 provider 條目，不再從字串前綴猜（§5.16） |
| 稽核 | `aip.member-sync-profile` | `deviceId`／`transport`／`role`／`syncProfile`／`maxLineBytes`／`supportsFragmentation` |
| 稽核 | `aip.fragment-dropped` | `xfer`／`reason`／`received`／`total` |
| 稽核 | `provider.rebinding`／`provider.rebound`／`provider.rebind-failed{reason}`／`provider.rebind-superseded`／`provider.rebind-not-started`／`provider.rebind-not-tracked` | rebind 生命週期 |
| 稽核 | `provider.rebound.keptDisabledReceptors` | rebind 時保留為關閉的非 consent 受器 |
| 稽核 | `aip.outbound-undeliverable.envelopeBytes` | 送不出去那一則的整行 bytes（`reason` 受 64 字元上限截斷，長度改由這個欄位表達） |
| 稽核 | `sensor.unresolved-stop-dropped` | 未解決停止摘要滿 32 筆時丟最舊 |
| 稽核 | `aip.rejected{stage:"provider-binding"}` | 綁定不是 `Bound` 時擋下的入站 `aip` |

`status.unresolvedStops` 與 `GET /v1/sensors/unresolved` 序列化**同一筆記錄**，欄位逐欄相同，由 API 測試釘住。

### 4.4 生成常數（TS／Swift 由 codegen 讀同一個數字）

`schemas/aip-1.0.schema.json` 的 `limits` 表新增：

| 常數 | 值 | 意義 |
|---|---|---|
| `MAX_RESUME_PATCHES` / `maxResumePatches` | **512** | const assert 綁死 ＝ `EVENT_LOG_RING` |
| `MAX_REALIGN_ATTEMPTS` / `maxRealignAttempts` | **3** | 連續 3 次未能套用＝unrecoverable |

`crates/interaction-aip/tests/fixtures/manifest.json` 新增第 11 段 `receiveDecisions`（45 個具名案例，
選填欄位 `expect.sessionIdAfter`）。**下游若消費 manifest**：新增一段不影響既有段；`AIPFixtures.swift`
因此長了約 1.4k 行（codegen 產物，不得手改）。

## 5. 行為變更清單與對呼叫端的影響

| # | 變更 | 誰會看到 | 要做什麼 |
|---|---|---|---|
| 5.1 | **`transition_provider(id, Available)` 對宣告式裝置回傳 `Disconnected`**（不是 `Available`）：重新啟用只代表「允許再試一次」，握手成功才收斂 | 任何呼叫 `PATCH /v1/providers/{id}` 或 Rust `transition_provider` 並假設「回傳狀態＝請求狀態」的程式 | 改成等待狀態收斂（rebind 一般在數百毫秒到 **50 s** 內結束，見 §5.17）或讀 `provider.rebound` 稽核。桌面已改：條目顯示「重新連線中」／「沒有重新連上」＋人話原因 |
| 5.2 | **`MAX_RESUME_PATCHES` 1024 → 512**，且超過上限從「截斷後 realign」改為**整批不處理**＋`realign(resume-too-long)` | 桌面與 iPhone 的 resume 路徑 | 無須動作（host 超限時本來就會改回 snapshot）。自製客戶端若沿用 1024，改讀 `AIP_LIMITS.maxResumePatches` |
| 5.3 | **realign 有界預算：第 4 次 → 第 3 次未能套用即 unrecoverable** | 三端接收機的「無法恢復」文案觸發時機提早一次 | 無須動作；自製客戶端改讀 `maxRealignAttempts` |
| 5.4 | **`epoch` 不同但沒有 `session-reset` 宣告 → realign**（以前 Rust／Swift 直接套用並靜默改寫本地 epoch）；patch 也開始檢查 epoch | 所有接收端 | 無須動作；host 的 resume 對 epoch 不同會回 `session-reset`，一次收斂 |
| 5.5 | **缺 `hash` 或缺 `state` 的 snapshot 一律 `reject-invalid`**（以前 TS／Swift 不核對就套用） | 自製 host／測試替身若送不帶 hash 的 snapshot | 補上 `payload.hash`（`state` snapshot 一律必帶） |
| 5.6 | **取消 `allowRegression`／`hostRegressed`**：同 incarnation 的回退不因「HTTP 回覆最新」而合法 | 桌面接收機 | 合法的回退改用 `reason:"recovery"`（§3） |
| 5.7 | **停用／撤銷的順序改為「先請來源停止 → 再關連線 → 最後翻旗標」** | 觀察稽核順序的測試／工具 | 稽核序列改變（`sensorStop` 出現在 `closedLinks` 之前）；停止結果不再必然是 `unknown` |
| 5.8 | **rebind 保留人類手動關掉的非 consent 受器**（`provider.rebound.keptDisabledReceptors`） | 依賴「重新啟用＝回到預設能力」的流程 | 無須動作；要真的回到預設請重新啟動 daemon |
| 5.9 | **`GET /v1/sensors/unresolved` 是人類層**：agent token 403 | agent 整合 | 用人類層 token；`/v1/sensors/stop` 不受影響 |
| 5.10 | **桌面進階診斷的 alignment 計數改名**（`stale`→`staleConnection`、`invalid`→`rejectedInvalid`、`ignoredRollback`→`ignoredStale`；移除 `hostRegressed`；新增 `recovered`／`rejectedIdentity`） | 讀那些標籤的截圖／文件 | 更新截圖與引用 |
| 5.11 | **同步卡新增 `partial-sync` 態**（info，不是綠勾）：非 `full-state` 的成員不再顯示「已同步」 | 桌面 UI 的既有截圖與 `docs/DESKTOP-GUIDE.md` 的狀態表 | `DESKTOP-GUIDE` 那一列與三種裝置補充句**已補上**；剩下的把關範圍見已知限制 §4.7 |
| 5.12 | **BLE 單行上限文件值 512 → 480**（更正為實測值，程式碼本來就是 480） | 讀文件估算 payload 的人 | 依 480 重新估算 |
| 5.13 | 通用停用不再關掉家族共用的能力旗標——**這是既有行為**（本分支較早的 `f250acf`，v0.6.x 段），本輪未改，rebind 沿用同一語意 | — | 無須動作 |
| 5.14 | **`GET /v1/status` 與 canonical tool `interaction.status` 依 principal 投影**（`33ad1ca`）：agent／session／adapter token 讀到的 status **不再含 `unresolvedStops`，也不再含 `characterSessionSync`**；取而代之的是新欄位 `unresolvedStopCount`（純數字，沒有 `sourceId`／`generation`／`sensors`／`lastKnown`／`sourceLabel`）。人類層 token 拿到的內容完全不變 | 任何用 **agent／session token** 讀 `/v1/status`、或呼叫 `interaction.status` 的 AI 整合與工具；桌面／CLI 用人類層 token，不受影響 | 讀那兩個鍵的 agent 端要改：判斷「是不是還有沒人確認的感測停止」改讀 `unresolvedStopCount > 0`（AI 仍然可以據此拒絕宣稱「所有感測都停了」）；要**逐筆**內容一律走人類層——`GET /v1/sensors/unresolved`（agent token 403，§5.9）。同步模式改讀 `GET /v1/character-session/diagnostics` 的 `members[].syncProfile` |
| 5.15 | **`syncProfile` 新增第四個值 `pending-full-state`**（`a1015a4`）：一台只在 `hello.caps` **宣稱** `aip.frag/1`、還沒有任何一份完整快照真的寫上那條線的裝置，停在 `pending-full-state` 而不是 `full-state`；出站通道解除登記（斷線／撤銷／重新綁定）時證據一併清掉 | 任何把 `syncProfile` 當成封閉三值列舉的呼叫端（switch／match／`Record<…>` 字典） | 補上第四個值，並沿用同一條規則：**只有 `full-state` 可以顯示「已同步」**。桌面對它說「尚未確認能收到完整狀態」（不是「拿不到完整狀態」——把未知講成已知是反方向的謊），卡片層級照舊是 `partial-sync`（info，不是綠勾） |
| 5.16 | **兩份同步投影新增選填 `providerId`**（`a1015a4`）：由出站通道自己說出來，說不出來就省略 | 想把一台裝置的同步狀態對到 provider 清單的呼叫端 | **選填**，所以舊呼叫端不受影響；新呼叫端請直接用它，**不要**從 id 的字串形狀去猜（`provider.adapter.<spec.id>` 與 AIP device id 在 production 永遠不相等，這正是對抗審查 `general-mode-ux-026`）。沒有 `providerId` 時桌面退回「識別碼剛好相同才對得上」的相容路徑，對不上就不掛任何字 |
| 5.17 | **rebind 的時間預算 20 s／40 s → 25 s／50 s**（`a1015a4`）：握手預算現在涵蓋 binding 最壞的一輪重試（退避上限＋握手逾時），由 const 斷言綁死 | §5.1 那些等狀態收斂的呼叫端；跑 rebind 相關測試的 CI | 等待上限由 40 s 改成 **50 s**（§5.1 的敘述同步更新）。`rebind_timeout_is_bounded_and_honest` 這支測試本身要跑約 26 秒 |
| 5.18 | **iPhone 在背景不再送出任何 AIP frame**（`3d3f136`）：以前只有 legacy `status` 心跳被生命週期閘住 | 期待手機在背景仍會回覆的 host／測試替身 | **背景收到 command 不回 result**（被擋下的 frame 計入 `droppedFrames` 並記錄，不靜默吞掉）；桌面因此不會再把一則背景送出的 frame 當成「手機還活著」。回到前景時 `auth-ok` 會重新宣告一次能力 |

## 6. 沒有變的事（明確聲明）

- AIP `specVersion`、12 種 message type 的形狀、Outcome 誠實階梯、錯誤碼集合：不變。
- 裝置線 `proto`：仍為 **1**（v1.2 是同一個 proto 下的追加訊息）。
- CPP（Character Presentation Protocol）契約：不變（本輪未動 `crates/interaction-character`）。
- `interaction-session`／`interaction-aip` 在 M2 分片那一批的 `git diff --stat` 為空：分片完全在
  transport binding 內部，`character_session.rs` 只多了 `derive_sync_profile` 這個純推導。
- 既有 HTTP 路由、SSE 事件型別、CLI 子指令：沒有移除、沒有改名。

## 7. 回退方式

| 想回退什麼 | 怎麼做 | 代價 |
|---|---|---|
| 整個版本 | 裝回 v0.6.0 的二進位；狀態檔不需轉換（§1） | 半套用的陪伴預設交易不會被補送；`unresolvedStops` 消失（它本來就不跨重啟） |
| 只關掉分片 | 讓裝置不要在 `hello.caps` 宣告 `aip.frag/1`（模擬器用 `--no-frag`；參考韌體本來就不宣告） | 回到 v1.1 行為：snapshot 與含 members 的 patch 送不到，成員模式 `intent-only`，稽核 `aip.outbound-undeliverable` |
| 只關掉 Character Session | 既有的 `INTERACT_AI_CHARACTER_SESSION` feature flag（登記在 `docs/aip/deprecation-ledger.md`，真機／真板證據為零之前不得移除） | 桌面同步卡與 AIP 路由整組停用 |
| 只回到「重新啟用需重啟」 | 沒有旗標可關；rebind 是預設行為 | — |
| 陪伴預設交易 | 沒有旗標；把 `companionPendingPresetOp` 從 `desktop.json` 手動刪成 `null` 即回到「沒有進行中的交易」 | 半套用狀態不再被偵測，畫面回到「自訂」 |

## 8. 升級前的檢查清單（給整合者）

1. `pnpm aip:check` 必須 in sync（本輪重生過 `generated.ts`／`AIPGenerated.swift`／`AIPFixtures.swift`／`schemas/aip-1.0.schema.json`）。
2. `bash scripts/tests/docs-claims.sh`、`bash scripts/tests/release-scripts.sh`、
   `bash scripts/tests/architecture-checks.sh --docs --ts --rust` 全綠。
3. 整合全套測試（`docs/releases/v0.7.0-progress.md` §5 第 1 步）數字回填 progress §4 與
   `docs/releases/v0.7.0-final-report.md` §7 —— 已於 `a1015a4` 實跑（Rust 1245/0、vitest 1815/0、Playwright 91/0、iOS 模擬器 158/0）。
4. 對抗審查（`adversarial-review-v06x`，base `f5b8e2f`，對象 `713f8fe`）**已完成**：54 送審／42 confirmed／
   11 refuted／1 fixed-meanwhile，處置 fixed 40／partial 2／deferred 0。逐條處置表見
   `docs/releases/v0.7.0-known-limitations.md` §0。
