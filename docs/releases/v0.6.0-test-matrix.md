# v0.6.0 Foundation：測試矩陣

> 每一列標明**證據等級**：真機／模擬器／fixture／browser（Playwright）／真 Tauri 視窗／單元／integration。
> 沒有實跑的一律寫「未執行」或「未計」，不寫「通過」也不寫「失敗」。
> 環境：macOS 26.2（Darwin 25.2.0）／Apple Silicon／rustc 1.94.0／node v24.5.0／pnpm 10.27.0，
> 2026-09-04～09-05。基準是 v0.5.1（tag `v0.5.1` → `ccf3bfe`，其後 3 個 docs／fix commit 到
> `a94e3d5`）；本文件是**純文件任務**產物——只讀 `rg`／`sed`／`cat` 與
> `scratchpad/{baseline,wave1,wave2,wave3,hardening}/` 下 2026-09-04～09-05 的實跑 log，
> 未另外執行任何 `cargo`／`pnpm`／daemon／Playwright 指令（另有對抗審查 agent 群並行負責重跑與驗證）。
> HEAD（撰寫本文件時）：`6683403`（`fix(session): count every authenticated frame as liveness, settle
> intents on member results, and harden the sync card`），分支 `feature/v0.6.0-foundation`。

## 1. 基線（v0.5.1＋3 個 docs／fix commit，`a94e3d5`）

完整基線見 [`docs/releases/v0.6.0-baseline.md`](v0.6.0-baseline.md)（同機實跑，2026-09-04）。摘要：

| 套件 | 結果 | 證據等級 |
|---|---|---|
| Rust workspace tests | **827 passed / 0 failed（66 個 test target）** | 單元＋真 runtime＋fixture |
| Tauri backend | **50 passed / 0 failed（3 target）** | 單元 |
| 前端 vitest | **1168 passed / 0 failed（60 檔）** | 單元（jsdom） |
| CLI E2E | **82 passed / 0 failed** | 真 daemon＋mock HTTP／serial pty／模擬 adapter fixture |
| Playwright | **65 passed / 0 failed** | browser（Chromium＋真 daemon；iPhone 相關 spec 對接**模擬 iPhone（fixture）**） |
| ESP32（預設／BLE） | 兩組態 exit 0，939 520／1 190 368 bytes | fixture（arduino-cli 編譯，非真板） |
| iOS typecheck／build／XCTest | 0 error／BUILD SUCCEEDED／**46 passed**（MotionClassifier 8＋ProtocolTests 17＋ReconnectHintTests 21） | 單元／模擬器 |
| iPhone 真機／ESP32 真板 | 未執行 | — |

`docs/releases/v0.6.0-recovery-matrix.md` 額外核對了九個子系統的「文件宣稱 vs 程式碼／測試實際狀態」，
記錄下重構後必須仍成立的受保護行為（本文件 §6 引用其中與 v0.6.0 直接相關的缺口）。

## 2. 各 wave 後的數字表

> 四個 wave 依序疊加（wave 之間不是獨立重測，是同一個分支持續推進；每個 wave 的 log 都是同一台機器、
> 同一個 worktree 的實跑結果）。每個套件的通過數只應上升；`git-diff-check` 例外見下方註記。

### wave 1（`ab0b7b3` 前後：AIP envelope、Character Session 領域 crate、小樞脫核心第一批）

| 套件 | 命令 | 結果 | 證據等級 |
|---|---|---|---|
| Rust fmt | `cargo fmt --all --check` | exit 0 | — |
| Rust clippy | `cargo clippy --workspace --all-targets -- -D warnings` | exit 0（17 s） | — |
| Rust workspace tests | `cargo test --workspace --no-fail-fast` | **945 passed / 0 failed（81 個 test target）**；基線 827／66 → **+118／+15** | 單元＋真 runtime＋fixture |
| Tauri backend | `cargo test --manifest-path .../src-tauri/Cargo.toml` | **52 passed / 0 failed**（基線 50 → +2） | 單元 |
| `pnpm aip:check`（AIP codegen drift） | `node scripts/aip-codegen.mjs --check` | exit 0 | — |
| 前端 typecheck | `pnpm typecheck` | exit 0 | — |
| 前端 vitest | `pnpm test` | **1302 passed / 0 failed（65 檔）**；基線 1168／60 → **+134／+5** | 單元（jsdom） |
| 前端 build | `pnpm build` | exit 0 | — |
| `git diff --check` | — | **exit 2**：`docs/aip/character-session.md:167: new blank line at EOF` | — |
| CLI E2E／Playwright／iOS | — | 未在本 wave 重跑（下一 wave 才動到相關程式碼路徑） | — |

wave 1 的 `git diff --check` exit 2 是一則新文件檔尾多一行空白的格式問題，非程式碼缺陷，
之後的 wave 已修正（wave 2 起 `git-diff-check` 皆 exit 0）。

### wave 2（`e71ab45` 前後：Runtime 掛載權威 Character Session、綁定 iPhone wire／HTTP／SSE／CLI）

| 套件 | 命令 | 結果 | 證據等級 |
|---|---|---|---|
| Rust fmt／clippy | — | exit 0（20 s clippy） | — |
| Rust workspace tests | `cargo test --workspace --no-fail-fast` | **965 passed / 0 failed（82 個 test target）**；wave1 945／81 → **+20／+1** | 單元＋真 runtime＋fixture |
| Tauri backend | — | **52 passed / 0 failed**（同 wave1） | 單元 |
| `aip:check`／typecheck／build | — | 皆 exit 0 | — |
| 前端 vitest | `pnpm test` | **1312 passed / 0 failed（65 檔）**；wave1 1302／65 → **+10／+0 檔** | 單元（jsdom） |
| CLI E2E | `./scripts/v03-cli-e2e.sh` | **96 passed / 0 failed**；基線 82 → **+14**（新增「Character Session（模擬 iPhone（fixture））」段，見 §3） | 真 daemon＋真 wss＋模擬 iPhone fixture |
| `git diff --check` | — | exit 0 | — |
| Playwright／iOS | — | 未在本 wave 重跑 | — |

### wave 3（`012ff69` 前後：小樞脫核心第二批＋`ref-shape`、iOS AIP Character Session client、桌面同步 UI）

| 套件 | 命令 | 結果 | 證據等級 |
|---|---|---|---|
| Rust fmt／clippy | — | exit 0（19 s clippy） | — |
| Rust workspace tests | `cargo test --workspace --no-fail-fast` | **974 passed / 0 failed（82 個 test target）**；wave2 965／82 → **+9／+0 target** | 單元＋真 runtime＋fixture |
| Tauri backend | — | **54 passed / 0 failed**；wave2 52 → **+2** | 單元 |
| `aip:check`／typecheck／build | — | 皆 exit 0 | — |
| 前端 vitest | `pnpm test` | **1355 passed / 0 failed（68 檔）**；wave2 1312／65 → **+43／+3 檔** | 單元（jsdom） |
| CLI E2E | `./scripts/v03-cli-e2e.sh` | **96 passed / 0 failed**（同 wave2） | 真 daemon＋fixture |
| Playwright | `INTERACT_AI_MOBILE_ADVERTISE=0 pnpm test:e2e` | **71 passed / 0 failed**（132 s，1 worker——`playwright.config.ts` 預設 `workers: 1`）；基線 65 → **+6** | browser（Chromium＋真 daemon；iPhone 相關對接**模擬 iPhone（fixture）**） |
| iOS typecheck | `xcrun swiftc -typecheck`（16 個 .swift，含新 `AIPEnvelope.swift`／`AIPGenerated.swift`／`CharacterSemantic.swift`／`SessionClient.swift`） | 0 error（EXIT=0） | 單元（編譯） |
| iOS build | `xcodebuild -target InteractionCompanion／InteractionCompanionTests -sdk iphonesimulator` | **BUILD SUCCEEDED** | 模擬器 |
| iOS XCTest | simctl 安裝＋注入（iPhone 17 模擬器） | **Executed 92 tests, 0 failures**＝ `AIPConformanceTests` 14（新）＋`MotionClassifierTests` 8＋`ProtocolTests` 21（基線 17 → **+4**）＋`ReconnectHintTests` 21（不變）＋`SessionClientTests` 28（新）；基線 46 → **+46** | 模擬器 |
| `git diff --check` | — | exit 0 | — |

### hardening（HEAD `6683403`：liveness／intent 結算／同步卡硬化，目前分支狀態）

| 套件 | 命令 | 結果 | 證據等級 |
|---|---|---|---|
| Rust fmt | `cargo fmt --all --check` | exit 0（1 s） | — |
| Rust clippy | `cargo clippy --workspace --all-targets -- -D warnings` | exit 0，0 warning（20 s） | — |
| Rust workspace tests | `cargo test --workspace --no-fail-fast` | **985 passed / 0 failed（82 個 test target）**；wave3 974／82 → **+11／+0 target**；基線 827／66 → **+158／+16 target** | 單元＋真 runtime＋fixture |
| Tauri backend | `cargo test --manifest-path .../src-tauri/Cargo.toml` | **54 passed / 0 failed（3 target）**；基線 50 → **+4** | 單元 |
| `aip:check` | `node scripts/aip-codegen.mjs --check` | exit 0（0 s，無漂移） | — |
| 前端 typecheck | `pnpm typecheck` | exit 0（4 s） | — |
| 前端 vitest | `pnpm test` | **1366 passed / 0 failed（68 檔）**；wave3 1355／68 → **+11／+0 檔**；基線 1168／60 → **+198／+8 檔** | 單元（jsdom） |
| 前端 build | `pnpm build` | exit 0（8 s） | — |
| CLI E2E | `./scripts/v03-cli-e2e.sh` | **96 passed / 0 failed**（43 s）；基線 82 → **+14** | 真 daemon＋真 wss＋模擬 iPhone fixture |
| Playwright | `pnpm exec playwright test --workers=1`（見下方備註） | **71 passed / 0 failed**（141 s，1 worker）；基線 65 → **+6** | browser（Chromium＋真 daemon；模擬 iPhone fixture） |
| `git diff --check` | — | exit 0（1 s） | — |
| iOS | — | **未在本 wave 重跑**；`git diff 012ff69..HEAD -- apps/interaction-ios` 為空（hardening 之後兩個 commit 都沒動到 iOS 檔案），故 wave3 的 92/0 在 HEAD 仍然有效 | — |
| ESP32／iPhone 真機 | — | 未執行（本輪未動硬體 adapter 或韌體檔） | — |

**Playwright 重跑備註（誠實記錄）**：hardening 的 `scratchpad/hardening/progress.log` 顯示
`cli-e2e` 這一步在 16:07:53 啟動、沒有對應的 summary 行就被下一個時間戳（16:08:59）的 `cli-e2e` 取代——
即第一輪全套回歸（`regress.sh`，用預設 `pnpm test:e2e`）在跑到 CLI E2E／Playwright 附近時，
**因系統記憶體不足被中止**（多個對抗審查 agent 與本文件撰寫並行佔用記憶體）；中止前的 log 檔
被隨後的重跑覆寫，沒有留下獨立證據檔。重跑改用 `regress-tail.sh`，對 `cli-e2e` 與
`pnpm exec playwright test --workers=1` 重新執行並成功（`cli-e2e` 43 s、`pnpm-e2e` 141 s，即上表數字）。
`--workers=1` 與專案設定檔（`apps/interaction-desktop/playwright.config.ts:13` 的 `workers: 1`）本來就一致，
是重跑時的顯式保險，不是把行為改成單執行緒——wave3 用預設指令跑出的 71/0 也已經是 1 worker
（`wave3/pnpm-e2e.log:12` 顯示 `Running 71 tests using 1 worker`）。

## 3. v0.6.0 新增測試清單（HEAD `6683403`，逐一以 `rg` 核實檔案存在與數量）

### 3.1 Rust（依 `cargo test --workspace --no-fail-fast`，`scratchpad/hardening/rust-test.log` 逐行核對）

| crate／測試檔 | 數量 | 說明 |
|---|---|---|
| `interaction-aip`（lib unit） | 14 | envelope／profile／outcome／版本協商／能力協商純函式 |
| `interaction-aip/tests/conformance.rs` | 10 | golden fixture conformance（valid／invalid／version／unknown type／oversized／deadline／cancel／duplicate／identity-mismatch／offline policy） |
| `interaction-session`（lib unit） | 14 | 純函式：語意狀態、Director 決策、patch／hash |
| `interaction-session/tests/pure_functions.rs` | 13 | Director 反應規則、priority、投影 |
| `interaction-session/tests/security_matrix.rs` | 7 | 十三關安全管線 |
| `interaction-session/tests/session.rs` | 43 | session 生命週期、resume、rate limit、membership（hardening 這輪新增 liveness／intent 結算回歸測試） |
| **`interaction-session` 小計** | **77**（14+13+7+43） | 契約：`docs/aip/character-session.md` |
| `interaction-runtime/tests/character_session_loop.rs` | 17 | Runtime 掛載 Session Host、綁定 iPhone wire／HTTP／SSE／CLI 的 integration |
| `interaction-runtime/tests/mobile_loop.rs` | 68 | 基線 64 → **+4**（iPhone `aip` frame 綁定新增案例） |
| `interaction-runtime/tests/character_host_registry.rs` | 4 | Runtime 端內建角色白名單（shu-rig／shape／sprite／text）注入 |
| `interaction-character/tests/migration_registry.rs` | 10 | `PackMigrator` trait＋有界 `MigrationRegistry`（sprite 遷移留核心，小樞遷移移出） |
| `interaction-character-shu`（lib unit） | 0（無 lib 層測試，邏輯在下列整合測試） | 新 crate：`ShuRigPack`／`RigPackMigrator` |
| `interaction-character-shu/tests/conformance.rs` | 1 | |
| `interaction-character-shu/tests/rig_pack.rs` | 6 | |
| **`interaction-character-shu` 小計** | **7**（1+6） | 契約：`docs/aip/reference-character.md` |
| Tauri backend（`apps/interaction-desktop/src-tauri`） | +4（50→54） | 內建角色 id 由 `index.json` 解析、壞索引降級、匯入撞名擋 `ref-shape` 等 |

### 3.2 TypeScript（vitest，`scratchpad/hardening/pnpm-test.log` 實跑數字——不是靜態 `rg -c "it("`，
`it.each` 展開會讓靜態計數低估，`aip-conformance.test.ts` 即一例：檔內字面 `it(`／`test(` 僅 11 處，
實跑展開後是 73 個 case）

| 檔案 | 實跑測試數 | 內容 |
|---|---|---|
| `src/test/aip-conformance.test.ts` | 73 | 與 Rust／Swift 共用 golden fixture 的三方 conformance 之一 |
| `src/test/aip-envelope.test.ts` | 22 | envelope／profile／outcome TS 鏡射 |
| `src/test/adapter-contract.test.ts` | 31 | 四個內建 adapter（shu／sprite／text／shape）共同生命週期契約 |
| `src/test/architecture-no-entrypoint-switch.test.ts` | 4 | 讀原始碼鎖住 `adapterRegistry.ts` 取代 entrypoint if-chain 後不再有字面分岔 |
| `src/test/character-ref-shape.test.ts` | 9 | 第二個 Reference Character `ref-shape` 的 manifest／adapter／20 個 CPP intent |
| `src/test/statusProjection-session.test.ts` | 24 | 角色同步十態文案投影（`CHARACTER_SYNC_PROJECTION`） |
| `src/test/character-sync-card.test.tsx` | 16 | `CharacterSyncCard` 元件呈現 |
| `src/test/regressions-v06-general-mode.test.tsx` | 10 | 五入口不變、`SIMPLE_NAV` 鎖死等一般模式回歸 |
| **8 個新檔小計** | **189** | |

vitest 總數基線 1168（60 檔）→ HEAD 1366（68 檔），**+198／+8 檔**；上述 8 個新檔合計貢獻 189，
其餘 +9 分散在既有檔案新增的案例（例如 `statusProjection.test.tsx`／`character-shu-adapter.test.ts`
等既有回歸檔補的案例，未逐一列舉）。

### 3.3 CLI E2E（`scripts/v03-cli-e2e.sh`，`scratchpad/hardening/cli-e2e.log`）

「Character Session（模擬 iPhone（fixture））」段（`sed -n '525,670p' scripts/v03-cli-e2e.sh`）：
11 組 `ok`／`bad` 二擇一斷言＋3 個 `check` 斷言＝**14 個獨立斷言**，涵蓋：配對連線、capability 協商、
snapshot、touch→applied、Behavior Intent 送達、revision 前進、diagnostics 誠實列出 online 成員、
重複 messageId 不重套用、斷線→offline、重連、resume（fixture 與 CLI 兩層）、未知 message type 誠實拒絕。
CLI E2E 總數基線 82 → **96（+14）**，與此段斷言數一致。

### 3.4 Playwright（`apps/interaction-desktop/e2e/`）

| 檔案 | 測試數 | 備註 |
|---|---|---|
| `character-session.spec.ts` | 4 | 全新：配對→已同步→摸一下→離線→重連→撤銷（桌面寬度）／同一條路 390px／緊急停止中觸摸被拒／鍵盤可達＋Reduced Motion 讀得到卡片 |
| `narrow.spec.ts` | 5（基線 4 → **+1**） | 新增：「角色頁的『同步』卡看得到、不溢出，而且空狀態是中性的」 |
| `a11y.spec.ts` | 6（基線 5 → **+1**） | 新增：「螢幕閱讀器：角色頁每個可按的東西都有名字，『同步』卡是一個有名字的區域」 |

Playwright 總數基線 65 → HEAD 71（**+6** = 4 個全新檔案測試＋narrow +1＋a11y +1）。

### 3.5 iOS XCTest（`apps/interaction-ios/InteractionCompanionTests/`，wave3 `xctest.log`，HEAD 未變）

| Test Suite | 數量 | 備註 |
|---|---|---|
| `SessionClientTests` | 28 | 全新：AIP Character Session client 純函式決策（snapshot／patch／hash／resume 上限／duplicate intent／verified 不可自建等） |
| `ProtocolTests` | 21（基線 17 → **+4**） | 既有線協定測試＋AIP frame 相容案例 |
| `AIPConformanceTests` | 14 | 全新：與 Rust／TS 共用 golden fixture 的三方 conformance 之一 |
| `MotionClassifierTests` | 8（不變） | |
| `ReconnectHintTests` | 21（不變） | |
| **總計** | **92**（基線 46 → **+46**） | |

## 4. 閉環證據（Desktop ⇄ 模擬 iPhone（fixture）；iPhone 真機未涵蓋）

> 全部路徑都經過**模擬 iPhone（fixture）**（`crates/interaction-runtime/examples/fake_iphone.rs`，
> 程序外假手機，真 wss＋真配對）或 **iOS 模擬器**（iPhone 17，Xcode 26.6）。**沒有任何一條路徑
> 在 iPhone 真機上跑過**——這是本節與 §6 反覆強調的邊界，不得引用本節證明真機閉環。

### 4.1 iPhone（模擬 fixture）→ Desktop

- **配對→協商→摸一下→反映到桌面權威狀態**：CLI E2E「Character Session」段（§3.3，14 斷言）——
  fixture 送 `aip-capability` → host 回 `capability(negotiated)`＋`state{snapshot}`；fixture 送
  `aip-touch` → `result{applied}`、`character session status` 的 `revision` 前進、`diagnostics`
  誠實列出該裝置 `presence: online`。
- **同一條路徑在 Playwright 走一遍並留截圖**：`character-session.spec.ts` 第一個 test（見 §3.4）產出
  `docs/assets/v06-evidence/desktop-character-sync-synced.png`；390px 版本
  `narrow-character-sync-synced.png`。
- **Rust integration**：`interaction-runtime/tests/character_session_loop.rs`（17 passed）＋
  `interaction-runtime/tests/mobile_loop.rs`（68 passed）覆蓋 iPhone wire→Session Host 的綁定路徑。

### 4.2 Desktop（Runtime 真相）→ iPhone（模擬 fixture／模擬器）：`task.verified` → celebrate

- Runtime `EventType::AgentSessionState{verified}` 轉成 AIP `event{name:"task.verified"}`（§13 相容表，
  `docs/aip/README.md`）→ `interaction-session` 的確定性 Director 產生 `celebrate` Behavior Intent →
  投影成 CPP intent 送到已協商的 adapter；同時 iPhone `aip` frame 收到同一個 intent。
  單元證據：`interaction-session/tests/pure_functions.rs`（Director `task.verified→proud+celebrate`
  案例，屬於 13 個案例之一）；iOS 端純函式證據：`SessionClientTests` 內
  `testTheAppCanNeverBuildAVerifiedResult`（確認手機端**無法自行合成** `verified`，只能呈現 Runtime
  推來的 intent，不得反向宣稱驗證）與 `testTheSameIntentMessageIsNeverPlayedTwice`。
- **`claimed-completed ≠ verified` 在 Session 層同樣成立**：`interaction-session/tests/security_matrix.rs`
  與 `docs/aip/README.md` §3 的不變量（`verified` 只能由 Runtime 送，adapter／device／renderer 送來的
  一律 `rejected{scope-denied}`）；本輪未新增額外的端到端截圖（沒有 UI 場景可以直接誘發
  celebrate 並截圖，屬於 §6 的已知缺口之一）。

### 4.3 重連（disconnect → reconnect → resume）

- CLI E2E：斷線→`presence offline`→重連→`resume` 回補（fixture 端與 `interact-ai character session
  resume` CLI 端各驗一次）——§3.3 的 14 斷言中 4 個直接對應。
- Playwright：`character-session.spec.ts` 第一個 test 的離線／重連段落，產出
  `desktop-character-sync-offline.png`／`narrow-character-sync-offline.png`；撤銷後產出
  `desktop-character-sync-needs-reconfirmation.png`／`narrow-character-sync-needs-reconfirmation.png`。
- 緊急停止中觸摸被拒：`character-session.spec.ts` 第三個 test，產出
  `desktop-character-sync-emergency.png`（390px 版本未產出對應截圖，屬於 §6 的缺口）。
- **重連 resume 效能**（fixture＋真 daemon，非真機）：`scratchpad/baseline/perf-session-after.json`
  （§5）：`reconnectMs` 27.2 ms、`reconnectToResumedMs` 5.7 ms、`reconnectResumeKind: "patches"`
  （回補的是 delta patch 而非整包 snapshot）。
- **iOS 模擬器截圖**：`docs/assets/v06-evidence/ios-sim-character-session-synced.png`、
  `ios-sim-character-session-advanced-diagnostics.png`（iPhone 17 模擬器，非真機）。

### 4.4 未涵蓋的閉環路徑

- iPhone **真機**送 touch → Desktop 反映：零執行（見 §6）。
- Desktop 真 Tauri 視窗（非 Playwright／jsdom）對同步卡的呈現：未驗（見 §6）。
- 兩台裝置**同時**連線同一 session 的多裝置語意衝突：`interaction-session` 的 members 表設計上支援，
  但本輪測試與 Playwright 場景都只驗證單一 fixture 裝置，多裝置併發**未覆蓋**。

## 5. 效能前後對照表

> 全部**非真機**：daemon／session 段是「fixture 模擬 iPhone＋真 daemon」（`scratchpad/baseline/
> perf-daemon.py`／`perf-session.py`，隔離 home、127.0.0.1）；角色渲染段是「headless Chromium，非
> Tauri WKWebView」（`pnpm perf`）。

### 5.1 daemon／iPhone 連線段

| 指標 | 修改前（`perf-daemon-baseline.json`，`a94e3d5`） | wave3 後（`perf-daemon-after.json`） | hardening 後（`perf-session-after.json`） |
|---|---|---|---|
| daemon 啟動 → `/ready` | 287.2 ms | 183.8 ms | 239.9 ms |
| 閒置 RSS（10 s 後） | 33.2 MB | 32.0 MB | 40.9 MB |
| 配對＋連線 | 49.2 ms | 35.0 ms | 43.4 ms |
| `/v1/mobile/status` 反映 connected | 2.1 ms | 1.8 ms | 未計（`perf-session-after.json` 沒有這個獨立欄位，改用下方 `joinToNegotiatedMs`） |
| 斷線偵測 | 1.8 ms | 4.7 ms | 4.9 ms |
| 重連 | 23.7 ms | 33.9 ms | 27.2 ms |
| 活動 RSS | 38.8 MB | 38.4 MB | 40.5 MB |

三次量測都是同一支腳本在同一台機器上的單次跑（非統計顯著的多次取樣），數字有 ±10 ms／±5 MB 量級的
自然抖動；**不宣稱**單調改善或單調變差，只誠實列出三個時間點的原始數字。

### 5.2 Character Session 新增指標（僅「修改後」，基線 fixture 沒有對應操作，見 `v0.6.0-baseline.md` §3.1）

來源：`scratchpad/baseline/perf-session-after.json`（after-hardening）。

| 指標 | 值 |
|---|---|
| join → negotiated | 3.0 ms |
| snapshot 大小 | 828 bytes（revision 2） |
| 第一次 touch：result／patch／command 延遲 | 各 2.8 ms；patch 712 bytes；status `applied` |
| touch result 延遲（n=20） | p50 4.9 ms／p95 7.7 ms／max 9.4 ms；20/20 `applied` |
| touch patch 延遲（n=20） | p50 4.9 ms／p95 7.7 ms |
| patch 大小中位數 | 492 bytes |
| burst100（連續灌 100 則 touch） | repliesSeen=30、acceptedOrApplied=30、rejectedOrError=0，8004.9 ms — **前 30 則被接受並套用後，既有 iPhone 線協定 v1 的 30 msg/s 速率限制把連線關閉**，其餘 70 則未送達（連線已斷），不是 Session 層邏輯錯誤，是既有速率限制與這支量測腳本互動的結果，列為觀察項 |
| resume | 5000.4 ms（`resumeKind: null`／`resumeBytes: null`——**這次量測沒有量到 resume 真正回應**，數字是等待逾時，不得引用為「resume 耗時 5 秒」；resume 的正確延遲數字見 §4.3 的 `reconnectToResumedMs` 5.7 ms） |
| 斷線偵測／重連／重連到 resume 完成 | 4.9 ms／27.2 ms／5.7 ms |
| 活動 RSS | 40.5 MB |
| diagnostics 計數器 | accepted=53、applied=52、intents.emitted=51、intents.dropped=35、intents.expired=16、patches=56、resumes=1、snapshots=3 |
| event log | cap=512、len=56（遠低於上限） |

`resumeMs` 一行是這份 fixture 量測腳本本身的缺口（可能量到的是逾時而非真實 resume 完成時間，
與 §4.3 Playwright／CLI E2E 觀察到的 resume 行為不一致），**不採用它作為 resume 的效能結論**，
改以 `reconnectToResumedMs`（5.7 ms，來自同一個 JSON 的 reconnect 流程）與 CLI E2E 的斷言（resume
確實回補、非逾時失敗）為準。

### 5.3 角色渲染效能（`pnpm perf`，headless Chromium，Apple Silicon；**非 Tauri WKWebView**）

| 指標 | 修改前（`pnpm-perf.log`） | 修改後（`pnpm-perf-after.log`） |
|---|---|---|
| drawRig 單角色一幀 | median 0.110／p95 0.150／max 2.160 ms（n=72） | median 0.110／p95 0.160／max 1.590 ms（n=72） |
| 全舞台一幀 | median 0.260／p95 0.600／max 0.840 ms（n=120） | median 0.260／p95 0.360／max 0.640 ms（n=120） |
| rAF 間隔 | median 8.300／p95 9.000／max 9.300 ms（n=600） | median 8.300／p95 9.300／max 10.300 ms（n=600） |
| stage loop | ticks=362 drawn=362，avgCost 0.270 ms | ticks=362 drawn=241，avgCost 0.313 ms |
| Reduced Motion 靜態短路 | 361 ticks 畫 7 幀（2.334 draws/s） | 361 ticks 畫 7 幀（2.332 draws/s） |
| 抓玩具延遲 | median 8.300／p95 8.800 ms（n=20，20/20 confirmed） | median 8.400／p95 8.700 ms（n=20，20/20 confirmed） |
| 看向游標延遲 | median 8.300／p95 9.300 ms | median 8.300／p95 9.600 ms |
| heap（600 幀，after-gc） | 2.37→2.71→2.36 MB | 4.03→2.97→2.36 MB（起點更高，gc 後收斂到同一水位 2.36 MB） |
| heap 浸泡 60 s | 2.41→2.86 MB（**Δ +462 KB**，18.7%），7 200 幀／29 次抓玩具，峰值 5.14 MB | 2.47→2.92 MB（**Δ +466 KB**，18.4%），7 157 幀／29 次抓玩具，峰值 5.93 MB |
| 有界性 | instances=1、eventRing=119/500、玩具 cap=4 of 23 spawns、3-day run finite=true withinClamp=true | instances=1、eventRing=118/500、玩具 cap=4 of 23 spawns、3-day run finite=true withinClamp=true |

浸泡涵蓋範圍與 v0.5.1 相同（`StageRenderer`＋`CharacterGateway` 真 shu adapter＋`InteractionDirector`＋
behavior／記憶＋500 筆事件環；不含 React 樹、Tauri IPC、真實 SSE）。**判讀（觀察項，非結論）**：
heap 浸泡 Δ 從 +462 KB 變成 +466 KB，差異在量測雜訊範圍內，看不出這輪的小樞脫核心／Session 掛載
造成額外保留；v0.5.1 記錄的「來源未定位」觀察項在 v0.6.0 仍然未定位，本輪也沒有做 heap snapshot
比對去定位它。渲染延遲（drawRig／stage frame／toy grab／gaze）修改前後在同一量級，**沒有觀察到
因為新增 AIP／Session 分層而拖慢渲染**——但這是 headless Chromium 的估計，不是 Tauri WKWebView
或真機的數字（見 §6）。

## 6. 未執行／未驗證清單

| 項目 | 狀態 | 說明 |
|---|---|---|
| iPhone 真機（配對、觸摸、resume、緊急停止投影等全部 AIP 路徑） | **implemented-unverified** | 程式碼與模擬器／fixture 測試存在，真機上**零執行**；`docs/releases/v0.5.1-iphone-device-evidence.md`／`v0.5.0-iphone-device-evidence.md` 是舊協定路徑的真機證據，**不涵蓋 AIP／Character Session**這一層新協定 |
| ESP32 真板 | **未執行** | 無實體裝置；ESP32／BLE 邊界句沿用 v0.5.1 固定用字，本輪未新增任何真板驗收 |
| 真 Tauri 視窗對「同步卡」／`CharacterSyncCard` 本身 | **未驗** | v0.5.1 §3 的真視窗驗收清單建立於 v0.5.1 的 UI；本輪新增的同步卡、十種狀態文案、五入口是否仍成立**只有 Playwright（Chromium）與 vitest（jsdom）覆蓋，沒有重跑一次真 `.app` 視窗走查** |
| 真 `codex`／`claude` 二進位對接 AIP／Character Session | **未跑** | 本輪的 agent 相關回歸（`agents_loop.rs`／`gateway_loop.rs` 等）延用既有 fixture agent（`fake_claude.sh`／`fake_codex.sh`），與 v0.6.0 新協定的整合仍是 unverified，非隱藏缺口——與 v0.5.1 已知限制一致 |
| Desktop→iPhone `celebrate` 場景的端到端 UI 截圖 | **未產出** | §4.2 只有單元／integration 證據，沒有 Playwright 或真視窗場景直接誘發並截圖 celebrate 呈現 |
| 多裝置同時連線同一 session | **未覆蓋** | `interaction-session` 的 members 設計上支援，但本輪所有測試（Rust／CLI E2E／Playwright）都只用單一 fixture 裝置 |
| 緊急停止場景在 390px 的同步卡截圖 | **未產出** | `desktop-character-sync-emergency.png` 只有桌面寬度版本 |
| 角色渲染效能在 Tauri WKWebView（而非 headless Chromium）下的數字 | **未計** | §5.3 全部數字的證據等級是 browser（headless Chromium），沿用 v0.5.1 就有的既知邊界，本輪未新增真 Tauri 視窗的效能量測 |
| `pnpm-perf-after.log` | 已產出（見 §5.3） | 與任務書預告的「可能尚在產生」不同，本次核對時已存在完整輸出，非「未跑」 |
| 對抗審查（`.claude/workflows/adversarial-review-v06.js`） | **workflow 已建立，執行狀態由並行 agent 群負責**，本文件不覆蓋其結果 | 見 `docs/acceptance-evidence.md` 對應章節（若已有結果會在那裡記錄） |
| `docs/releases/v0.6.0-recovery-matrix.md` §4「盤點未覆蓋的模組」與 §5「最小可遷移邊界」列出的既有缺口（`superseded` 分支無測試、CLI `mobile pair/revoke/status` 子指令無自動化測試、`release.sh`／CI／Release workflow 無自動化測試等） | **v0.6.0 本輪未新增覆蓋** | 這些是 Phase 0 盤點時就發現的既有缺口，非本輪新增，v0.6.0 尚未著手補上 |

## 7. 每列證據等級對照速查

| 等級 | 本輪出現在哪 |
|---|---|
| **真機** | 無（iPhone 真機零執行，ESP32 無實體板；見 §6） |
| **真 Tauri 視窗** | 無新增（見 §6，本輪未重跑真視窗走查） |
| **模擬器** | iOS build／XCTest 92/0（iPhone 17 模擬器，wave3）、`ios-sim-*.png` 截圖 |
| **fixture** | `crates/interaction-runtime/examples/fake_iphone.rs`（模擬 iPhone，真 wss＋真配對）、`fake_claude.sh`／`fake_codex.sh`、arduino-cli 編譯（沿用 v0.5.1，本輪未動） |
| **browser（Playwright）** | `pnpm test:e2e` 71/0（含 `character-session.spec.ts` 4、narrow +1、a11y +1）、`docs/assets/v06-evidence/*.png` |
| **integration（真 daemon／真 HTTP／真 wss，非 UI）** | CLI E2E 96/0（含 Character Session 段 14）、`character_session_loop.rs` 17、`mobile_loop.rs` 68、`api_e2e.rs` 30 |
| **單元** | `cargo test --workspace` 985/0（82 target，含 `interaction-aip` 14+10、`interaction-session` 77、`interaction-character-shu` 7、`migration_registry` 10、`character_host_registry` 4）、Tauri backend 54/0、vitest 1366/0（68 檔，含 8 個新檔 189 個 case）、iOS `AIPConformanceTests`／`SessionClientTests` 純函式 |
| **未執行／未驗** | 見 §6 全表 |
