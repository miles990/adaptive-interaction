# v0.6.0 Foundation：Phase 0 恢復矩陣（Repository 真實狀態優先於文件宣稱）

> 這是 v0.6.0 重構前的 Phase 0 恢復矩陣：對九個子系統逐一核對「文件宣稱」與「程式碼／測試的
> 實際狀態」，記錄下重構後**必須仍然成立**的行為（受保護行為），並誠實列出尚未盤點、尚未驗收
> 的缺口。原則與 `docs/releases/v0.6.0-baseline.md` 一致——Repository 的真實狀態優先於任何文件
> 的宣稱；能從實跑證據（log）算出的數字才寫數字，算不出的寫「未計」；證據等級（單元／fixture／
> 模擬器／browser／真 Tauri 視窗／真機）逐項標明，不得混為一談。
>
> - 起始 commit：`a94e3d5bb04e741d60994e90fc86abeb18e9faea`（`docs(release): mark readiness gate 3 as met`）
> - v0.5.1 tag：annotated tag `v0.5.1` → `ccf3bfe18612a083519b7cbfbd4bab3429b3d021`（`release: v0.5.1`）；
>   其後 3 個 commit（`5f3df32`／`6e4a8a2`／`a94e3d5`）在 `a94e3d5` 之內
> - 工作分支：`feature/v0.6.0-foundation`（從 `a94e3d5` 建立）
> - 文件產生日期：2026-09-04
> - 本文件的九列資料來源：8 列取自本輪 8 個子系統的「repoClaims／codeState／testState／
>   evidenceLevel／conclusion／discrepancies」唯讀盤點（scratchpad `maps/matrix-rows.json`），
>   缺的「General Mode」一列由本次任務依 `apps/interaction-desktop` 桌面 UI 資訊架構地圖
>   （`maps/af581b2d4995816ae.json`）補寫；所有測試通過數字改以 `scratchpad/baseline/` 下
>   2026-09-04 的實跑 log（`rust-test.log`／`pnpm-test.log`／`cli-e2e.log`／`pnpm-e2e.log`／
>   `ios/xctest.log`）逐一核對，取代矩陣原文與地圖中的靜態計數（grep `#[test]`／`it(` 之類）——
>   兩者常常不一致（例如 `it.each` 展開、或行號漂移），實跑數字才是本文件的準繩。
> - 本文件**只讀**：未執行任何 `cargo`／`pnpm`／daemon 指令，全部核實用 `rg`／`sed`／`cat`／
>   `git show a94e3d5:<path>` 完成（後者用來避開目前 worktree 中其他並行 agent 正在修改的檔案，
>   確保引用的行號對應的是 Phase 0 基線 commit，不是尚未提交的重構中間狀態）。

## 1. 總表

| 項目 | Repository 宣稱 | 程式碼狀態 | 測試狀態 | 證據層級 | 結論 |
|---|---|---|---|---|---|
| **Desktop character**（桌面角色小樞） | README:65-128／FEATURES:153-181／CHANGELOG `[0.5.1]`§Fixed:12-40 宣稱小樞 v3 分層 rig、CPP 1.0 是唯一契約（小樞只是 Reference Adapter）、hit-regions 改為多框 fail-closed（`companion-gameplay-032`）、Reduced Motion 逐 instance 真協商；`docs/releases/v0.5.1-test-matrix.md`§3 宣稱多項真 Tauri 視窗驗收，同表也列出 Reduced Motion／快捷選單／adapter crash fallback「未在真視窗驗收」。 | `crates/interaction-character`（純函式 Gateway，無 I/O，`gateway.rs:267`）＋`crates/interaction-runtime/src/character.rs`（2580 行）接線層＋`apps/interaction-desktop/src-tauri/src/lib.rs` 的 `sanitize_hit_regions:2221`／`store_hit_regions:2473`（45ms rate limit）＋TS 三個 reference adapter（`character/adapters/shu.ts`／`sprite.ts`／`text.ts`）皆存在，與宣稱一致。 | `crates/interaction-character/tests/gateway.rs` 37 個場景；`apps/interaction-desktop/src-tauri` 三個 target 共 **50 passed / 0 failed**（`tauri-test.log`；地圖寫「22 個內嵌 test」只是其中 hit-region/click-through 子集的計數，不是該 crate 的總數）；vitest 16 檔 84 處 `reducedMotion` 斷言；Playwright `character.spec.ts` 3 個（瀏覽器層，不含視窗本體）。 | unit（Rust＋vitest）＋real-Tauri-window（部分：視窗顯示/隱藏、click-through、estop overlay、角色匯入，2026-09-04 一次性人工+腳本驅動，非 CI 常態）＋browser（Playwright）＋unverified（Reduced Motion／快捷選單玩具使魔／adapter crash fallback 在真視窗下）。 | 程式碼與測試支持文件的核心宣稱；文件本身已誠實列出「未在真視窗驗收」的子項，本輪核實未發現隱藏的誇大宣稱。 |
| **iPhone companion** | README:120-123／FEATURES:169-172 宣稱 TLS wss、配對碼 HMAC、每機 token、4 受器／6 動器；v0.5.0（2026-09-03，iPhone 11／iOS 26.3.1）真機部分驗收；CHANGELOG `[0.5.1]` 宣稱本輪新增冷啟動自動重連與位址變更提示，模擬器 XCTest 46/46，真機本輪 **blocked**（鑰匙圈授權待人工）。 | `crates/interaction-runtime/src/mobile.rs`（3717 行）：`MOBILE_RECEPTORS`/`MOBILE_ACTUATORS`（:160-173）4/6 個；`RUNTIME_ONLY_STATES`（:195）拒絕 plan/agent 路徑；配對 HMAC 比對用裸 `!=`（:3313，非 constant-time）；`/v1/mobile/*` 對 legacy agent token 全排除（`crates/interaction-api/src/lib.rs:474`）。 | `crates/interaction-runtime/tests/mobile_loop.rs` 實跑 **64 passed / 0 failed**（`rust-test.log`；**須更正**：矩陣原文寫「93 個測試」、地圖寫「89」，兩者皆是靜態 `rg 'fn |#[test]|#[tokio::test]'` 計數，與實跑不同，以 64 為準）；`regressions-review3-mobile.test.tsx` 7；`iphone.spec.ts` 4（fixture）；iOS XCTest **46**（8+17+21，`ios/xctest.log` 實跑，模擬器）；`scripts/v03-cli-e2e.sh` 對 mobile 零命中。 | unit（`mobile_loop.rs`、vitest）＋fixture（`iphone.spec.ts`＋`fake_iphone.rs`）＋simulator（iOS 46）＋real-device（v0.5.0 部分驗收；v0.5.1 新增兩行為 blocked-by-human，0 次真機執行）。 | 真機證據停留在 v0.5.0 快照；v0.5.1 新增的冷啟動重連／位址變更提示只有模擬器證據；測試數字須以實跑 **64** 為準，不得沿用靜態 grep 的 93/89。 |
| **Pairing**（配對） | README:28-30／FEATURES:169 宣稱 TLS 埠只在配對過後才開、指紋釘選、每台手機獨立金鑰、可隨時撤銷；CHANGELOG `[0.5.1]` `mobile-server-060/061/062`／`safety-invariants-056` 宣稱已配對清單原子寫入、配對面板 2 秒查燒毀、停用高風險受器對仍在串流的手機送 stop。 | `mobile.rs`：`PairingSession{code,expires_at}`（:615-618，`Mutex<Option<_>>` 單一單次配對期）；`PAIRING_TTL_SECS=300`（:54）；`token_hash=sha256(token)`（:3357）只存雜湊、明文只回一次（:3392）；`mobile_revoke()`（:2341-2400+）寫檔失敗即整段回滾不留假撤銷；Bonjour 由 `INTERACT_AI_MOBILE_ADVERTISE`（:75）控制。 | `mobile_loop.rs`（見上，64 passed，含 `wrong_pairing_code_is_refused_and_session_burned`／`revoke_disconnects_live_connection_immediately` 等具名測試）；`api_e2e.rs::mobile_routes_are_human_only_for_agent_and_session_tokens`（:1058）；`iphone.spec.ts` 4（fixture 走真 TLS wss 握手）；iOS `ProtocolTests.swift` 17（純解析，非網路）；**CLI 的 `mobile pair/revoke/status` 子指令本身在 `scripts/v03-cli-e2e.sh` 與 `crates/interaction-cli/tests/cli_e2e.rs` 皆 0 命中**（rg 核實）。 | unit（`mobile_loop.rs` 直呼 Runtime，非經 HTTP）＋integration（`api_e2e.rs` 經真 HTTP 鑑權邊界）＋fixture（`iphone.spec.ts`＋`fake_iphone.rs`，真 TLS wss 握手，非真機）＋browser（Playwright 點 UI）＋unit（`ProtocolTests.swift`）。CLI 子指令面：unverified（無任何自動化測試）。 | 配對期／token/撤銷的核心不變量在 Rust unit＋HTTP integration＋fixture 三層都有覆蓋，信度較高；但 CLI `interact-ai mobile *` 子指令本身的參數解析與呼叫路徑完全沒有自動化測試，重構動到 `MobileAction`／`commands.rs` 不會被既有測試套件攔下。 |
| **Reconnect**（重連） | README:131-138／CHANGELOG `[0.5.1]`:68-79,194 宣稱本輪新增 iPhone 冷啟動自動重連（1s→15s 退避）與位址變更提示（連續 4 次或持續 60 秒），感測絕不隨重連恢復；真機本輪 blocked、只有模擬器 XCTest 21 條證據；文件用語與行為口徑一致，未發現誇大宣稱。 | iOS `ConnectionManager.swift`：`ColdStartConnectDecision`(:182-184)、`ReconnectDiagnosis`(:126起)、門檻 4 次／60 秒(:146-148)；桌面 `gatewayWiring.ts` `REHELLO_MIN_INTERVAL_MS=2000`(:806)；Runtime `character.rs::character_hello()`（同 instanceId 重送 generation+1，:866-940）、`character_resync_safety_state()`（estop 中重連補投 emergency，:1694-1706）；`character_ws.rs`：`HEARTBEAT_INTERVAL_MS=15_000`／`IDLE_TIMEOUT_MS=45_000`（`character.rs:58,60`）；**`character_ws_attach()` 同一 adapter 舊連線被新連線取代時送 `Goodbye{reason:"superseded"}`（`character.rs:1358`）**；mobile 側同 device_id 新連線取代時 `mobile.rs:3157,3635` 也有 `"superseded"` 分支。 | `character_loop.rs::hello_negotiates_the_desktop_instance_and_rehello_bumps_generation`(:204)、`external_adapter_transport_attaches_negotiates_and_times_out`(:1380，測的是**逾時後**重連而非**活連線被取代**)、`desktop_presence_expiry_disconnects_the_desktop_instance`(:1586)；`mobile_loop.rs::token_reconnect_works_until_revoked`(:735)、`phone_connecting_during_estop_receives_emergency`(:2468)；iOS `ReconnectHintTests.swift` **21 個**（`ios/xctest.log` 實跑確認）。**`rg -n "superseded" crates/interaction-runtime/tests/ crates/interaction-api/tests/ apps/interaction-desktop/e2e/` 於 `character.rs:1358` 與 `mobile.rs` 的 `"superseded"` 分支上為 0 命中**（本次獨立以 `git show a94e3d5` 重新核實，確認為真）。 | unit（Rust 內建測試，未經本次重新執行，僅靜態核對測試名稱存在；iOS `ReconnectHintTests` 21 條為純函式決策測試）＋fixture（`iphone.spec.ts` 用 `fake_iphone.rs` 模擬重連）＋unverified-real-device（冷啟動重連／位址變更提示，blocked、0 次執行）。**`superseded`（活連線被新連線取代）分支：僅程式碼存在，無任何測試斷言。** | 感測不隨重連恢復、estop 補投、re-hello 節流等核心行為都有具名測試鎖住；**唯一例外是 CPP WS／mobile 的「同一 adapter/裝置活連線被新連線取代」（`superseded` 分支）——rg 確認 0 測試命中，屬於「受保護但無測試」，v0.6.0 應優先補上。** |
| **Renderer**（角色呈現：CPP 協商、intent→adapter、system.text fallback、外部 WebSocket adapter） | README:124-128／FEATURES:175,293 宣稱 CPP 1.0 是角色無關的呈現層、不支援就誠實降級、小樞不是唯一角色；CHANGELOG `[0.5.1]` 宣稱本輪修 4 項對抗審查缺陷（`character-protocol-036/037/038/039/040`／`-042`／`-043`）；引用總體測試數字 827/1168/65/20。 | 純函式權威層 `crates/interaction-character/src/`（`gateway.rs:267` 唯一狀態機、`intent.rs:16,177,289`、`receipt.rs:108`）；Runtime 接線層 `crates/interaction-runtime/src/character.rs`（2580 行，`InstanceOrigin::External` 一律 `InputOutcome::NotObserved`）；外部 transport 唯一實作 `crates/interaction-api/src/character_ws.rs`（233 行）；stdio 仍只是規格文字，無 spawn 子程序實作。 | `crates/interaction-character` 的 `gateway.rs`(37)／`negotiation.rs`(16)／`manifest.rs`(18)／`conformance.rs`(3)／`intent_capabilities_golden.rs`(1)；`character_loop.rs` **21 passed**（`rust-test.log`，與矩陣原文一致）；`api_e2e.rs` 整體 **29 passed / 0 failed**（`rust-test.log`；**須更正**：矩陣原文「28 個 async fn 測試涉及 character 路由」只是該 target 內的子集計數，不是 `api_e2e.rs` 目標總數，目標總數是 29）；`tests/e2e/tests/golden.rs::golden_character_protocol_schema` 逐位元組比對。前端 vitest（`pnpm-test.log` 實跑，取代地圖的靜態「約 220 個 it」估計）：`character-gateway.test.ts` **45**（地圖靜態估計 62，`rg -c "^\s*it\("` 靜態算又只有 41——差異來自 `it.each` 展開，以 vitest 實跑 45 為準）、`character-protocol.test.ts` 35、`companion-imported-characters.test.ts` 24、`characterPage.test.tsx` 23、`characterName.test.tsx` 23、`character-shu-adapter.test.ts` **53**（地圖靜態估計僅 20，同樣是 `it.each` 展開造成的低估）、`character-adapters.test.ts` 18、`character-manifests.test.ts` 12（地圖寫 14）；以上 8 檔實跑合計 **233** 個測試，取代地圖「約 220」的估計。 | unit（`interaction-character` 純函式狀態機）＋integration（`character_loop.rs` 21、`api_e2e.rs` 29，真 Runtime／真 WebSocket fixture）＋unit（vitest，233 個 character-*.test 實跑）＋browser（`character.spec.ts` 3）＋real-Tauri-window（部分，見 Desktop character 列）＋golden-schema-diff（`golden.rs`）。本次為唯讀複核，未重跑套件本身；「827/1168/65/20」等總體數字引用自 CHANGELOG，本次改以 `scratchpad/baseline/` 的 2026-09-04 實跑（`rust-test.log`/`pnpm-test.log`/`pnpm-e2e.log`/`cli-e2e.log`）交叉核對一致（827 passed/0 failed・66 target；1168 passed/0 failed・60 檔；65 passed；82 passed）。 | 安全 intent 只能 fallback 到安全 intent、resolution 只能降級、外部輸入不合成觀察等核心不變量都有 unit/integration 測試鎖住；**測試數字須以實跑為準**：`api_e2e.rs` 是 29 非 28，`character-gateway.test.ts` 是 45 非 62，`character-shu-adapter.test.ts` 是 53 非 20——靜態 grep 計數（不論是「it(」還是「#[test]」）在 `it.each`／行號漂移下都不可靠。 |
| **Agent integration**（Claude/Codex connector、claimed≠verified、estop 傳播、companion.state.present） | README:84,93,118／FEATURES:136-139,162-163,170 宣稱本機直連 Codex app-server／Claude Code、claimed≠verified、estop 殺程序樹；CHANGELOG `[0.5.1]` 宣稱 `safety-invariants-078`（legacy agent token 不能 interrupt session）、agent resume 需 resolvedWorkdir 比對（已修 `c8e78c6`，Breaking）。 | `AgentSessionState::ClaimedCompleted`／`Unknown`（`crates/interaction-core/src/agent.rs:17,33,44`）；`verify_agent_session()` 為唯一能寫 `human_verified` 的函式（`agents.rs:1111,1134,1141`）；`emergency_stop()`→`estop_agent_sessions()`（`runtime.rs:954,1050`；`agents.rs:1384-1428` 全部 session 用 `join_all` 併行、`ESTOP_SESSION_TIMEOUT` 有界）；`gateway.rs:54-55` codex/claude-code 映射，`:297-299` codex maxCost>0 拒絕，`:304-311` codex intent-only 拒絕。 | `agents_loop.rs` **23 passed**（`rust-test.log`）含 `estop_cancels_all_open_sessions_and_blocks_new_ones`(:428)、`verify_agent_session` 多筆重複驗證拒絕案例；`gateway_loop.rs` **28 passed**含 estop 競態測試(:496)；`mobile_loop.rs::verify_agent_session`(:1581)；`presentation_loop.rs` **16 passed**含 `AcknowledgedOnly` 斷言多處；`api_e2e.rs::only_a_human_token_can_verify_a_claim_and_no_agent_can_self_upgrade`(:1238)；`scripts/v03-cli-e2e.sh`（`cli-e2e.log` 實跑：「estop cancels open agent session」「estop blocks new agent sessions」「estop propagated stop-all to the serial device」皆 PASS）。 | unit（`interaction-core`／`interaction-runtime` 型別與規則測試）＋fixture（`agents_loop.rs`/`gateway_loop.rs`/`mobile_loop.rs`/`presentation_loop.rs` 用 fake claude/codex 子程序，**非真 Claude Code／Codex 二進位**，已知限制文件明確承認）＋integration（`api_e2e.rs` human-only 驗證，真 HTTP）＋CLI-integration（`scripts/v03-cli-e2e.sh`，`cli-e2e.log` 82 passed，本次唯讀已用實跑 log 核對而非僅讀腳本）＋real-Tauri-window（`v0.5.1-test-matrix.md:78` 宣稱，本次未重跑）。real-device：iPhone 真機證據不涵蓋 agent session／estop 傳播；ESP32 與 agent 整合無關。 | claimed≠verified、estop 傳播、codex/claude-code 分權等核心不變量在 unit/fixture/integration/CLI-E2E 四層都有測試覆蓋，信度較高；**對真實 Claude Code／Codex 二進位的行為本身仍是 unverified（僅驗證過協定形狀）**，這點文件與程式碼一致承認，非隱藏缺口。 |
| **Memory**（記憶分層、owner、export、Context Bundle） | README:97,134／FEATURES:141 宣稱 10 層記憶、CAS 素材、Context Bundle；CHANGELOG `[0.5.1]`:58-64 宣稱 `count_memory(layer)` 精確化 limitReached、匯出範圍明列（`included`/`notIncluded`）；known-limitations #21/#22 明列「匯出範圍只有記憶項目」「bundle 卡在第一個裝不下的項目」為刻意保留。 | `MemoryLayer` 10 個 variant（`crates/interaction-core/src/memory.rs:17-38`）；`apply_actor_rules()`（:254-288）Agent 寫的 Fact 強制降 Inference、UserMemory 長期寫入降 Candidate＋30 天複查；`Runtime::memory_export()`（`crates/interaction-runtime/src/memory.rs:192-232`）用 `Store::count_memory(None)` 真實筆數判斷 limitReached；`Runtime::memory_context_bundle()`（:239-391）決定論選取＋六項 excluded 計數；`POST /v1/memory/context-bundle` 不在 `agent_request_allowed()`／`session_request_allowed()` 任一白名單內（僅程式碼推論，見下）。 | `crates/interaction-core/src/memory.rs` 內建 7 個單元測試；`crates/interaction-runtime/tests/memory_loop.rs` 實跑 **15 passed / 0 failed**（`rust-test.log`；**須更正**：矩陣原文寫「17 個 `#[tokio::test]`」，本次以 `rust-test.log` 逐行核對，實際只有 15 個，含 `crud_layers_and_status`／`memory_export_exact_limit_is_not_falsely_reported`／`context_bundle_reports_capacity_truncation` 等）；`crates/interaction-storage/src/lib.rs::count_memory_matches_actual_row_count`；`scripts/v03-cli-e2e.sh` 真 daemon＋真 CLI（`cli-e2e.log`：memory add/bundle/delete 相關 PASS）；vitest `backupSection.test.tsx` 等多檔。 | unit（`memory.rs` 7 個、`storage` count_memory 測試）＋fixture-integration（`memory_loop.rs`／`agents_loop.rs`，真 Runtime＋tempdir SQLite，非透過真 HTTP）＋CLI-E2E（真 daemon＋真 CLI）＋unit-jsdom（vitest，fetch mock）。**無 Playwright（browser）覆蓋、無真 Tauri 視窗覆蓋**（此子系統目前完全沒有瀏覽器層或真視窗層的自動化驗收）。`POST /v1/memory/context-bundle` 的 agent 存取邊界為程式碼閱讀推論，未見對應測試直接斷言「agent 呼叫應 403」。 | 10 層／三態保存期限／owner 降權／匯出範圍精確化等核心不變量有 unit＋fixture-integration＋CLI-E2E 三層覆蓋；**測試數字須更正為 15（非 17）**；Memory 子系統缺 browser／真 Tauri 視窗層級的自動化驗收，是本輪盤點新發現的證據等級缺口（非文件宣稱與程式碼不一致）。 |
| **General Mode**（一般模式：5 個一級入口、狀態投影、進階模式切換） | README.md:45-62「桌面控制中心預設是**一般模式**……之後只有 5 個一級入口：現在／〔目前角色〕／工作／連接與權限／更多」「打開『設定→顯示進階功能』即可切到進階模式，兩種模式共用同一套後端與安全規則」；FEATURES.md:102「一般／進階雙模式：預設一般模式全人話；進階模式保留全部技術頁面。同一套後端」；FEATURES.md:144「v0.5 起簡化為 5 個一級入口……舊 tab id 全部相容折疊」；FEATURES.md:183「一般模式狀態投影：所有工作／收件匣狀態走同一份 exhaustive 人話表；未知原始值顯示『結果不確定』而非原始字串」。 | `apps/interaction-desktop/src/App.tsx`：`SIMPLE_NAV`（:53，固定 5 項靜態表，第二項 label 為中立 fallback）、`simpleNavFor()`（:59-63，執行期把第二項換成 `useCharacterName()` 解出的真實角色）、`ADVANCED_NAV`（:79，9 個技術頁）、`LEGACY_ANCHORS`（:95，舊 tab id 折疊表）、`PageBody`（switch-case，含未知路由防呆頁）；`src/statusProjection.ts` 的 `WORK_STATE_PROJECTION`／`INBOX_ONLY_PROJECTION`／`CHARACTER_LIFECYCLE_BUCKET` 用 `satisfies Record<...>` 窮舉表投影，未知值一律「結果不確定」且標 `known:false`；`src/characterName.ts::useCharacterName()` 是角色名稱單一真相（prefs 名字＞manifest displayName＞中立「角色」）；`src/appstate.tsx` 明言「frontend is never a truth source」，`actionStatusLabel()`（:195）維持誠實階梯文案不升級。核心 9 個檔案（App/pages/components/appstate/statusProjection/characterName/desktop/transport/api）**沒有硬編「小樞」文字**；僅 3 處以 pack id 清單或 `entrypoint==='shu-rig'`（CPP 協定定義的 builtin entrypoint）做條件分支（`Onboarding.tsx:30`／`CompanionPage.tsx:142`／`pages/character/catalog.ts:207`），非任意裸字串硬編。 | vitest（`pnpm-test.log` 實跑）：`statusProjection.test.tsx` 26、`connectPage.test.tsx` 37、`homePage.test.tsx` 32、`onboarding.test.tsx` 27、`workPage.test.tsx` 22、`regressions-v05-round2.test.tsx` 17、`overlay.test.tsx` 15、`regressions-review2-ia.test.tsx` 15、`deviceCard.test.ts` 16、`pendingCountExact.test.tsx` 19、`firstSuccess.test.tsx` 10、`narrowNav.test.tsx` 9、`regressions-review3-ia.test.tsx` 8、`honesty.test.ts` 8、`phoneDeviceCard.test.tsx` 7、`regressions-review3-mobile.test.tsx` 7、`compat-routes.test.tsx` 5、`dialog.test.tsx` 5、`characterPage.test.tsx` 23、`characterName.test.tsx` 23——地圖列出的 20 個測試檔實跑合計 **331** 個測試（`pnpm-test.log` 逐檔核對，取代地圖「存在對應測試檔」層級的粗略描述）；Playwright `narrow.spec.ts` 4 個（`pnpm-e2e.log`，390px 底部導覽、鍵盤可達、無水平捲動，屬 65 個總數之一）。**本次未讀** `src-tauri` mobile pairing/BLE 實作細節、`character_ws.rs`、`character/gateway.ts` 全文（僅交叉引用），亦未執行任何測試——上述「測試涵蓋」是讀 `pnpm-test.log` 實跑輸出＋檔案本身內容比對得出，屬「檔案存在且實跑通過」層級，非本次重新驗證每一則斷言的邏輯正確性。 | unit（vitest jsdom，331 個測試實跑通過）＋browser（Playwright `narrow.spec.ts` 4 個，390px）。**無真 Tauri 視窗層級**針對「一般模式 5 入口」本身的自動化驗收（`v0.5.1-test-matrix.md`§3 的真視窗清單未見「一般模式導覽」本身列於其中，僅角色視窗顯示/隱藏等相鄰項目）；深連結（`companion_open_control_center`→`navigate` 事件）機制本次只讀程式碼路徑，未見對應的 Rust 內嵌測試或 Playwright 案例直接斷言此 IPC 呼叫本身（僅推論其存在，未見具名測試）。 | 5 個一級入口固定、進階模式共用後端、一般模式狀態投影窮舉不外洩原始字串等宣稱與程式碼及測試一致，未發現誇大；**深連結 IPC（`companion_open_control_center`）與「一般模式導覽本身」缺乏具名測試/真視窗證據**，屬本次盤點新發現的證據等級缺口。 |
| **Release pipeline**（release.sh、CI、Release workflow、golden schemas、版本檔） | CHANGELOG.md:6-8 宣稱四處版本號一次同步；README.md:107-141 宣稱 v0.5.0/v0.5.1 已發布；`v0.5.1-release-readiness.md` 關卡3 宣稱「Release workflow 可重現」met，18 個資產；關卡2 備註記載 release commit `ccf3bfe` 在 CI 上 Tauri backend job 因 2 個 CHANGELOG claim-check 測試紅燈、由後續 commit `5f3df32` 修正、tag 不移動——文件自己承認的已知限制。 | `scripts/release.sh:1-85`：worktree 檢查→改 4 個版本檔→CHANGELOG 插入（e70ef28 修法）→`GOLDEN_UPDATE=1 cargo test -p interaction-e2e` 重生 golden→commit+tag；四個版本檔目前皆 0.5.1；`.github/workflows/ci.yml`（137 行，4 個平行 job）／`release.yml`（175 行：create-release/cli/desktop/extras，加總 8+6+4=18 資產）；`lib.rs:3653 changelog_topmost_section()` 處理「Unreleased 空段、下一段非空」的形狀（v0.5.1/`ccf3bfe` 曾觸發）；`crates/interaction-cli/src/selfmgmt.rs:32-39 cli_asset_name()` 與 `release.yml:78-84` 的 bash 命名邏輯是**兩處獨立維護**的同一份規則，無自動交叉比對。 | `tests/e2e/tests/golden.rs::versions_are_in_sync`（隨 `cargo test --workspace` 一起跑，屬 **827 passed / 0 failed（66 target）** 的一部分，`rust-test.log` 已核對）；`apps/interaction-desktop/src-tauri/src/lib.rs` 兩個 CHANGELOG claim-check 測試（`changelog_unreleased_click_through_claims_match_code`／`changelog_topmost_section_follows_a_renamed_heading`）是 **50 passed（3 個 target）**（`tauri-test.log`）之中的 2 個；`selfmgmt.rs::asset_names_are_deterministic` 只驗證函式自身輸出等於寫死字串。**`scripts/release.sh`／`.github/workflows/{ci,release}.yml` 三個檔案本身沒有任何專屬自動化測試**（shell test／workflow lint／dry-run，`find`/`grep` 皆 0 命中）。 | unit（`golden.rs::versions_are_in_sync`、`lib.rs` 兩個 claim-check 測試、`selfmgmt.rs::asset_names_are_deterministic`）＋integration／real-CI（歷史一次性 GitHub Actions run，非本次重跑：release-readiness.md 記載的 5 個 CI run id 與 1 個 Release run id，18 資產＋4 個 sha256 逐一重算比對）＋unverified（`release.sh`／`ci.yml`／`release.yml` 三個檔本身，以及 `cli_asset_name()` 與 `release.yml` 命名規則的跨檔一致性——無自動化測試覆蓋，本行唯一完全沒有測試的一格）。 | 版本一致性、CHANGELOG claim-check、18 資產產出等有測試或歷史 CI 證據鎖住；**`release.sh`／CI／Release workflow 三個檔案的邏輯正確性本身無任何自動化回歸測試**，重構若動到這三個檔案不會被既有套件攔下，v0.6.0 應視為明確的已知限制而非隱含在「met」底下。 |

## 2. 逐列：受保護行為（重構後必須仍成立）

以下每一列先列出「受保護行為」（把矩陣/地圖的 conclusion 整理成可測的編號句子，每句標注測試證據），再列「文件 vs 程式碼不一致」。標籤含意：
【測試鎖住：檔名】＝本次核對確認有對應的自動化測試存在（檔案層級，未逐一重新核對每則斷言的內部邏輯）；
【無測試：需在 v0.6.0 補】＝本次核對（含獨立 `rg` 覆核）未找到任何自動化測試。

### 2.1 Desktop character

受保護行為：

1. `crates/interaction-character::gateway` 是唯一的 CPP 狀態機實作，全程無 I/O、無內嵌計時器，時間只能由呼叫端傳入。【測試鎖住：`crates/interaction-character/tests/gateway.rs`（37 個場景）】
2. adapter→runtime 方向的線上型別（`CommandReceipt`／`CharacterInputEvent`／`Negotiate`）不得出現 `truthState`／`verified` 欄位；`truthState` 只能由 Runtime 端投影函式決定。【測試鎖住：`crates/interaction-character/src/schema.rs` 內嵌斷言＋`tests/intent_capabilities_golden.rs`】
3. Reduced Motion 必須是每個 instance 各自透過協商決定的旗標，回執 resolution 只能降級不能升級。【測試鎖住：`gateway.rs`（37 場景之一）＋vitest 16 檔 84 處 `reducedMotion` 斷言（`rig.test.ts`／`character-gateway.test.ts`／`character-shu-adapter.test.ts` 等）】
4. Tauri host 的 `companion_hit_regions` IPC 必須維持有界＋fail-closed（≤16、逐一 clamp、單框不得兩軸≥80%、總面積≤80%、空清單/非有限值整批拒絕沿用舊值、45ms rate limit）。【測試鎖住：`apps/interaction-desktop/src-tauri/src/lib.rs` 內嵌測試（`hit_regions_are_bounded_clamped_and_fail_closed`／`hit_region_reports_are_rate_limited_on_the_host`／`hit_rect_rejects_nan_and_non_positive_sizes` 等，屬 `tauri-test.log` **50 passed** 的一部分）】
5. 游標只有落在核可的 hit region 內才被角色視窗攔截；`companion_hit_rect` 舊呼叫必須是新 IPC 的相容 shim，走同一份驗證路徑。【測試鎖住：`lib.rs::clickthrough_decision_ignores_padding_and_accepts_character`／`clickthrough_handles_many_regions_and_their_gaps`（`tauri-test.log`）】
6. `apps/interaction-desktop/src/character/adapters/` 下必須同時存在 shu（rig）、sprite（舊 pack 相容層）、text 三個 reference adapter，各自對接同一份 CPP TS 鏡射。【測試鎖住：`character-shu-adapter.test.ts`（53）／`character-adapters.test.ts`（18，含 sprite/text）】
7. Reduced Motion 在真 Tauri 視窗下的重新協商行為目前是**未驗收（需人類切換 OS 設定）**，重構後不得被悄悄升級成「已驗收」而無對應真視窗證據。【無測試：`defaults write` 在本環境被系統拒絕，需在 v0.6.0 另尋驗收路徑或持續誠實標示已知限制】
8. 多角色/玩具快捷選單與 adapter 崩潰後 fallback 只有 jsdom+vitest（`companion-gateway-wiring.test.ts`）覆蓋，尚未在真 Tauri 視窗誘發驗證。【測試鎖住（僅 jsdom 層級，非真視窗）：`companion-gateway-wiring.test.ts`（23）；真視窗層級【無測試：需在 v0.6.0 補】】

文件 vs 程式碼不一致：本次核實範圍內未發現文件宣稱與程式碼/測試不一致之處——`v0.5.1-test-matrix.md`／`known-limitations.md`／`v05-capability-gap-matrix.md`§13 三份文件互相一致，且已誠實列出「真視窗未驗」的子項。唯一可能造成誤讀的是 `docs/v05-capability-gap-matrix.md`§10（Phase 8 收尾舊快照）仍寫「characterLiveState 移入 statusProjection.ts——仍未做」，但已在同文件§13.2（v0.5.1 現況）與 `known-limitations.md`#14 更新為「已修」——非目前程式碼與文件的真實矛盾，只是舊章節保留歷史原貌，讀者若只讀舊章節可能誤判。另外，「src-tauri 22 個內嵌 test」的說法應更正為「Tauri 三個 target 共 50 passed（`tauri-test.log`）」，22 只是其中 hit-region/click-through 子集的計數。

### 2.2 iPhone companion

受保護行為：

1. `mobile.rs` 對外只宣告 4 個 receptor（`iphone.motion/battery/touch/mic-level`）與 6 個 actuator（`iphone.haptic/notify/tts/torch/flash/character`），重構後兩份清單成員與數量須維持一致。【測試鎖住：`crates/interaction-runtime/tests/mobile_loop.rs`（64 passed）】
2. `character.present` 的 `verified-success`／`emergency`（`RUNTIME_ONLY_STATES`）必須繼續被 `map_wire_params` 對 plan/agent 路徑一律拒絕。【測試鎖住：`mobile_loop.rs`】
3. 配對維持單次 5 分鐘配對期、任何錯誤 pair-response 整段作廢、token 只回傳明文一次且伺服器只存 SHA-256。【測試鎖住：`mobile_loop.rs::wrong_pairing_code_is_refused_and_session_burned`／`a_peer_that_burns_the_pairing_window_is_visible_to_the_user`】
4. `/v1/mobile/*` 對 legacy agent token 的 GET/POST/DELETE 必須全部拒絕。【測試鎖住：`crates/interaction-api/tests/api_e2e.rs::mobile_routes_are_human_only_for_agent_and_session_tokens`】
5. DoS／資源上限必須維持：`MOBILE_MAX_CONNS=8`、`MOBILE_AUTH_TIMEOUT_DEFAULT_MS=10s`、`MOBILE_WS_MAX_MESSAGE_BYTES=128KiB`、`MOBILE_MAX_INBOUND_PER_SEC=30`（`mobile.rs:92`，本次獨立以 `git show a94e3d5` 核實）、`ACT_TIMEOUT=4s`、`STOP_SENSORS_WAIT=2s`。【測試鎖住：`mobile_loop.rs`】
6. 斷線／撤銷／估停必須強制停用高風險受器（`iphone.mic-level`）且不自動恢復。【測試鎖住：`mobile_loop.rs::phone_connecting_during_estop_receives_emergency`】
7. `mobile_loop.rs`（**64**，非文件寫的 93、地圖寫的 89）、`regressions-review3-mobile.test.tsx`（7）、`iphone.spec.ts`（4）、iOS `InteractionCompanionTests`（**46**＝8+17+21）在重構後須全數存在並通過。【測試鎖住：以上四項，數字均來自 2026-09-04 `scratchpad/baseline/` 實跑 log，非靜態計數】
8. `scripts/v03-cli-e2e.sh` 目前完全不涵蓋 mobile provider（0 命中）；重構後若要靠此腳本做 mobile CLI 回歸，需額外新增案例。【無測試：需在 v0.6.0 補】
9. 任何「iPhone 真機驗收」文件用語必須保持誠實階梯：v0.5.0 部分證據仍有效，v0.5.1 新增的兩項行為 0 次真機執行，不得改寫成已驗收。【無測試（真機層級）：僅模擬器＋fixture】
10. `fake_iphone.rs`／`iphone.spec.ts` 產生的任何截圖或斷言必須繼續標示為【模擬 iPhone（fixture）】。【測試鎖住（文案自陳）：`fake_iphone.rs` 檔頭注解、`iphone.spec.ts` 檔頭注解】

文件 vs 程式碼不一致：**測試數字須更正**——`mobile_loop.rs` 實跑 **64 passed**（`rust-test.log`），非 README/CHANGELOG 引用脈絡或子系統地圖各自寫的「93 個測試」/「89」（皆為靜態 `rg` 計數，與實跑套件執行時展開的測試數不同）。配對 HMAC 用裸 `!=`（`mobile.rs:3313`，非 constant-time）與地圖描述一致，但地圖同時聲稱「Bearer token 驗證用了 constant_time_eq」——`crates/interaction-api/src/lib.rs:507-511` 確有此函式，惟本次未逐一核實所有 Bearer 路徑是否都呼叫它，屬部分核實。README/FEATURES 對「iPhone 真機部分驗收」引用的是 v0.5.0 證據，未在正文明顯區分 v0.5.1 本輪新增的兩個行為已 blocked，需讀 `known-limitations.md` 才看得到區分。

### 2.3 Pairing

受保護行為：

1. 配對期是單一、單次使用、記憶體內 `Mutex<Option<PairingSession>>`。【測試鎖住：`mobile_loop.rs`】
2. 配對期 TTL 固定 300 秒，到期後任何 `pair-response` 必須被拒絕且不得被 UI 誤報為有效。【測試鎖住：`mobile_loop.rs`＋`regressions-review3-mobile.test.tsx`（7，涵蓋燒毀/過期/仍有效三態）】
3. 配對 payload 是 `{v:1,host,port,fp,code}`，iOS 端須拒絕 `v!=1`/非 64-hex fp/越界 port/空 code。【測試鎖住：iOS `ProtocolTests.swift`（17，純解析）】
4. 任一方送出錯誤 `pair-response` 會把整段配對期燒毀，且該原因須在 status 與桌面 UI 誠實顯示。【測試鎖住：`mobile_loop.rs::a_peer_that_burns_the_pairing_window_is_visible_to_the_user`】
5. 配對成功後伺服器只存 `token_hash`，32-byte token 明文只在 `paired` 訊息回傳一次。【測試鎖住：`mobile_loop.rs`】
6. `mobile_identity_fingerprint()` 與驗證用 `token_hash` 數學上不同值、不可逆推，且對 legacy agent token 不可見。【測試鎖住：`mobile_loop.rs::the_public_device_fingerprint_is_not_the_token_verifier`】
7. 重連比對 `sha256(token)==token_hash`，失敗一律用同一句籠統文案拒絕。【測試鎖住：`mobile_loop.rs::token_reconnect_works_until_revoked`】
8. `mobile_revoke(device_id)` 落地寫檔失敗須整個回滾並回 `Err`。【測試鎖住：`mobile_loop.rs::a_revoke_that_cannot_be_persisted_fails_honestly`】
9. 撤銷成功後現存連線須在有界時間內（測試斷言 2 秒）收到 `auth-fail{reason:"revoked"}`，之後觀察不得被 ingest，provider 狀態停在 `Revoked`。【測試鎖住：`mobile_loop.rs::revoke_disconnects_live_connection_immediately`】
10. 撤銷後舊 token 之後任何重連嘗試都須被拒絕。【測試鎖住：`mobile_loop.rs`】
11. Bonjour 廣播由 `INTERACT_AI_MOBILE_ADVERTISE` 控制，關閉時須誠實回報 `advertised:false` 並只綁 `127.0.0.1`。【測試鎖住：`mobile_loop.rs::test_mode_never_advertises_bonjour_on_the_lan`／`serve_mode_can_disable_bonjour_via_env`】
12. `interact-ai mobile pair/revoke` 對應的 HTTP 端點鑑權邊界（僅 human token）在 HTTP 層有測試，但 **CLI 子指令本身**（`MobileAction` 參數解析、`commands.rs` 路徑組裝）無任何自動化測試。【無測試：需在 v0.6.0 補（`scripts/v03-cli-e2e.sh`／`crates/interaction-cli/tests/cli_e2e.rs` 對 mobile 皆 0 命中，本次已用 `rg -i mobile` 獨立核實）】

文件 vs 程式碼不一致：FEATURES.md:169「撤銷即斷線」沒有量化時間，實測是「2 秒內」而非瞬時，用詞「即」略樂觀，應理解為「有界快速斷線」。`docs/releases/v0.5.1-test-matrix.md` 的「真 Tauri 視窗驗收」清單逐項核對後，未見「iPhone 配對」本身被列為已在真 Tauri 視窗（非 Playwright headless Chromium）驗收——本次任務只讀了 CHANGELOG 轉述，未逐字重讀 test-matrix 全文比對，此點標記待查而非確認缺口。CLI 的 mobile 子指令在 `scripts/v03-cli-e2e.sh` 中完全沒有出現，與 CLAUDE.md 慣常的「CLI 驗收走 v03-cli-e2e.sh」預設覆蓋範圍不符，容易誤讀成「CLI 配對流程已測試」（該腳本測的是宣告式裝置 adapter 的 pairing，是另一套子系統）。

### 2.4 Reconnect

受保護行為：

1. iOS App 系統終止後冷啟動：Keychain 有配對紀錄且使用者上次連線意圖非明確 false 時，必須自動嘗試重連（1s→15s 指數退避）。【測試鎖住：`ReconnectHintTests.swift::testColdStartConnectsWhenPairedAndUserLastWantedConnection`（21 個之一，`ios/xctest.log` 實跑確認）】
2. 冷啟動自動重連完成後，所有感測旗標必須維持關閉，不得因重連自動恢復。【測試鎖住：`ReconnectHintTests.swift::testColdStartAutoConnectNeverResumesAnySensor`】
3. TLS 憑證指紋不符（pinningRejected）必須分類為 tlsMismatch，永不得被診斷成「位址變更」。【測試鎖住：`ReconnectHintTests.swift::testTlsMismatchIsNeverReportedAsAddressChange`】
4. ReconnectDiagnosis 須在連續 4 次 `.connectivity` 失敗或持續 ≥60 秒才建議重新配對。【測試鎖住：`ReconnectHintTests.swift::testFourConsecutiveConnectivityFailuresSuggestRepair`／`testSustainedConnectivityFailuresOverSixtySecondsSuggestRepair`】
5. 桌面 `PhoneDeviceCard`：未連線時顯示位址變更提示＋「重新配對」按鈕，按鈕只呼叫外部 `onRepair`。【測試鎖住：`phoneDeviceCard.test.tsx`】
6. 桌面角色視窗收到 `character.instance` 事件、`instanceId` 為自己且 `connected:false` 時，須在 2 秒節流窗內重新送 `character.hello`。【測試鎖住：`regressions-run2-companion.test.ts`】
7. 桌面角色視窗以 10 秒週期呼叫 presence 心跳；Runtime 端 presence 逾時須把 desktop instance 標記為斷線。**逾時門檻是 20 秒**（`crates/interaction-runtime/src/presentation.rs:39 PRESENCE_STALE_SECS=20`，本次獨立以 `git show a94e3d5` 核實）。【測試鎖住：`character_loop.rs::desktop_presence_expiry_disconnects_the_desktop_instance`(:1586)】
8. 桌面視窗以相同 instanceId 重送 hello 時 generation 須遞增 1，在途 AI 命令須標為 uncertain。【測試鎖住：`character_loop.rs::hello_negotiates_the_desktop_instance_and_rehello_bumps_generation`(:204)】
9. Runtime 若在緊急停止中偵測到某 instance 完成協商，須立即補投 emergency intent。【測試鎖住：`character_loop.rs`（緊急停止重連補投測試，:1970-2006）】
10. 外部 Character Adapter WebSocket：每 15 秒 Heartbeat、45 秒無 inbound 視為斷線、單次寫入逾時 5 秒。【測試鎖住：`character_loop.rs::external_adapter_transport_attaches_negotiates_and_times_out`(:1380)＋`character_ws.rs` 內嵌測試`a_peer_that_stops_reading_cannot_wedge_the_ws_loop_forever`】
11. **同一 adapter_id 重新 attach（含舊連線仍在時的新連線取代）時，新連線先收 Hello，舊連線收 Goodbye（取代情境 `reason="superseded"`）並被 cancel。** 【**無測試：需在 v0.6.0 補**——`character.rs:1358` 的 `Some("superseded".into())` 分支，本次以 `rg -n "superseded"` 對 `crates/interaction-runtime/tests/`／`crates/interaction-api/tests/`／`apps/interaction-desktop/e2e/` 三處搜尋為 0 命中（獨立核實，`external_adapter_transport_attaches_negotiates_and_times_out` 測的是**逾時後**重連，不是**活連線被取代**）；`mobile.rs:3157,3635` 的同名 `"superseded"` 分支同樣無測試命中】
12. iPhone 憑 token 重連，伺服器比對 `sha256(token)==token_hash`成功後回 `auth-ok`；同 device_id 舊連線立刻標記斷線。【測試鎖住：`mobile_loop.rs::token_reconnect_works_until_revoked`】
13. iPhone 撤銷後伺服器主動關閉現有連線並於 2 秒內送 `auth-fail{reason:"revoked"}`。【測試鎖住：`mobile_loop.rs::revoke_disconnects_live_connection_immediately`】
14. iPhone 在緊急停止生效期間重連（含首次連線），伺服器須主動送 `stop-all{sensors:true}` 並投影 emergency；裝置回覆前稽核須誠實標記為 `"unknown"`。【測試鎖住：`mobile_loop.rs::phone_connecting_during_estop_receives_emergency`(:2468)】
15. 上述所有『iPhone 冷啟動自動重連』與『位址變更提示』行為，只能維持既有的驗證等級聲明（模擬器 21 條＋fixture），真機驗證仍為 0 次執行。【無測試（真機層級）：僅模擬器＋fixture】

文件 vs 程式碼不一致：本行是本次盤點中唯一一個「文件與程式碼完全一致，且已誠實揭露 blocked 狀態」的列——CHANGELOG／final-report／known-limitations／release-readiness／iphone-device-evidence／migration／test-matrix 六份文件口徑一致，未發現矛盾。**新發現的缺口**（非文件宣稱不一致，而是本次盤點揭露的測試覆蓋缺口）：CPP WS 與 mobile provider 的「同一連線被新連線取代（superseded）」分支完全沒有測試，且 `presentation.rs` 的實際 presence 逾時常數是 20 秒，而 `apps/interaction-desktop/src-tauri/src/lib.rs:1908` 的文件註解寫的是「45 s presence timeout」——**這句 45 秒的註解本次未能在程式碼任何常數中找到對應依據，疑似是舊版本遺留或指涉另一個尚未確認的逾時值，標記為未解決的不一致，建議 v0.6.0 動工前先確認這句註解要更正還是背後另有一個目前找不到的 45 秒常數**。

### 2.5 Renderer

受保護行為：

1. `crates/interaction-character::gateway` 全程無 I/O、無內嵌計時器，37 個場景須保持確定性可重跑。【測試鎖住：`gateway.rs`（37）】
2. adapter→runtime 方向線上型別不得出現 `truthState`/`verified` 欄位。【測試鎖住：`schema.rs` 內嵌斷言＋`intent_capabilities_golden.rs`】
3. `capability::negotiate()` 對協定 major 版本不符須直接拒絕，不得猜測相容性。【測試鎖住：`negotiation.rs`（16）】
4. 安全 intent 的 fallback 只能解析到另一個安全 intent（`character-protocol-036`）。【測試鎖住：`gateway.rs`（37 場景含此案例）＋`manifest.rs`（18）＋前端三個 adapter 測試（`character-shu-adapter.test.ts` 53／`character-gateway.test.ts` 45／`character-protocol.test.ts` 35，皆以 `envelope.intent` 為準）】
5. 任何非 Completed 的安全 intent 終態回執都須觸發 `system.text` fallback 補送。【測試鎖住：`gateway.rs`】
6. 零連線時安全 intent 仍須走 `publish_character_system_text` 保底。【測試鎖住：`gateway.rs`】
7. adapter 回報的 resolution 在 `on_receipt()` 合併時只能降級（取 max）。【測試鎖住：`gateway.rs`】
8. `on_receipt()`/`on_event()` 須先比對 generation，世代不符一律 audit 丟棄。【測試鎖住：`gateway.rs`＋`character_loop.rs`（21）】
9. AI 發起的命令終態回執只能由 `remember_ai_command` 登記的 `settle_instance` 結算。【測試鎖住：`character_loop.rs`（21）】
10. `Origin=External` 的 instance 輸入須一律回 `InputOutcome::NotObserved`。【測試鎖住：`character.rs` 內嵌 `#[cfg(test)]`（5）＋`character_loop.rs`】
11. manifest 未宣告的輸入能力須在觀察邊界與純 Gateway 進佇列前雙重擋下。【測試鎖住：`manifest.rs`（18）＋`character_loop.rs`】
12. 外部 adapter token 只存 sha256，revoked 記錄持久化後重啟仍須維持撤銷狀態。【測試鎖住（部分）：`scripts/v03-cli-e2e.sh`（`cli-e2e.log`：「revoke marks the adapter revoked and disconnected」「revoked adapter token is unauthorized (401)」PASS）——**重啟後仍維持撤銷**這一子句本次未見對應的重啟後再驗證的具名測試，屬部分核實】
13. `character_ws.rs` 是目前唯一真正實作的外部 transport；stdio 仍只是規格文字。【測試鎖住：`character_ws.rs` 內嵌測試（1）確認 WS 存在；stdio 無實作故無測試，符合預期】
14. `dedupe_ring`（clamp 1..=256）是有界去重環，超出環大小的重放不會被偵測。【無測試：本次 testState 未見具名測試斷言此邊界，需在 v0.6.0 核實或補】
15. iPhone 的 `character.present` 目前獨立於 CPP 之外（7 態 actuator）。【部分核實：`ActuatorCenter.swift` 程式碼確認獨立實作；iOS `ProtocolTests.swift` 未見專門斷言「不走 CPP」，屬程式碼推論非測試鎖住】
16. `golden.rs::golden_character_protocol_schema` 須持續逐位元組 diff `schemas/character-protocol.schema.json`。【測試鎖住：`tests/e2e/tests/golden.rs`】

文件 vs 程式碼不一致：`known-limitations.md` 記載 `character-protocol-039` 的契約文字（README §8「丟最舊的非安全 intent」）與實作不符——實際行為是丟「剛進來的」非安全訊息，文件用字與程式碼行為有落差。`v0.5.1-test-matrix.md` 對「角色視窗顯示/隱藏」宣稱『已用真 Tauri 視窗驗收』，但同文件同時列出 Reduced Motion／快捷選單／使魔／玩具／adapter 崩潰 fallback『未在真視窗驗收』，Renderer 這一列的證據等級不能一概而論寫成『真視窗已驗』，須按子行為分別標示。**測試數字更正**：`api_e2e.rs` 目標總數是 **29 passed**（非「28」，28 是子集計數）；vitest `character-gateway.test.ts` 實跑 **45**（非地圖估計的 62）、`character-shu-adapter.test.ts` 實跑 **53**（非地圖估計的 20）、`character-manifests.test.ts` 實跑 **12**（非地圖估計的 14）——這些落差主要來自 `it.each` 展開後的實際測試數與靜態 `it(` 呼叫計數不一致；8 個 character-*.test 檔實跑合計 **233**，非原地圖「約 220」的粗估。

### 2.6 Agent integration

受保護行為：

1. `ClaimedCompleted` 與帶 `human_verified` 的已驗證狀態須維持不同值，`is_open()` 對 `ClaimedCompleted` 回 true。【測試鎖住：`agents_loop.rs`（23）】
2. 只有 `verify_agent_session()`（或 human-only API/CLI 入口）能把 `human_verified` 從 None 寫成 Some。【測試鎖住：`agents_loop.rs`＋`api_e2e.rs::only_a_human_token_can_verify_a_claim_and_no_agent_can_self_upgrade`(:1238)】
3. 對已帶 `human_verified` 的 session 再次呼叫 `verify_agent_session()` 須被拒絕。【測試鎖住：`agents_loop.rs`（多筆重複驗證拒絕案例，:621,629,644,657,698,724,961,1015,1027,1038,1181）】
4. 新的 claim 須清除該 session 先前的 `human_verified`。【測試鎖住：`agents_loop.rs`】
5. `emergency_stop()` 須呼叫 `estop_agent_sessions()`，全部 open session 併行關閉、各自有界（`ESTOP_SESSION_TIMEOUT`）。【測試鎖住：`agents_loop.rs::estop_cancels_all_open_sessions_and_blocks_new_ones`(:428)／`estop_leaves_a_runtime_note_instead_of_a_cancel_task`(:1068)＋`gateway_loop.rs`（28，含 estop 競態測試 :496）】
6. estop 觸發的 session 關閉須終止對應子程序樹（SIGTERM→SIGKILL 有界流程）。【測試鎖住：`agents_loop.rs`＋`scripts/v03-cli-e2e.sh`（`cli-e2e.log`「estop propagated stop-all to the serial device」PASS，屬 estop 傳播的間接佐證）】
7. Codex connector 建立 session 時 `maxCost>0` 須被拒絕建立。【測試鎖住：`gateway_loop.rs`】
8. Codex connector 建立 session 時 intent-only toolScope 須被拒絕；Claude Code connector 不受此限。【測試鎖住：`gateway_loop.rs`（含檔頭註解「不可用 agent 誠實拒絕」）】
9. `companion.state.present` 須要求 `behaviorIntent` 參數，缺少時 `ActuatorError::Rejected`。【測試鎖住：`presentation_loop.rs`（16）】
10. `companion.state.present` 的 `ActionReceipt` 完成時 `VerificationVerdict` 須是 `AcknowledgedOnly`，不得是 `Observed`。【測試鎖住：`presentation_loop.rs`（多處 `presentation_ack` 呼叫斷言，:171,526 等）】
11. 主動式對話 `generativeAgent` 只能接受 claude-code（或 null），不得接受 codex。【測試鎖住：`scripts/v03-cli-e2e.sh`（`cli-e2e.log`「proactive generative agent 限定 claude-code」相關斷言）】
12. legacy 共享 agent token 不得用來 interrupt 任何 agent session（一律 403）。【測試鎖住：CHANGELOG 記載 `safety-invariants-078` 已修（`af8d1f1`）——具體測試檔名本次 testState 未逐一指名，標記部分核實】
13. agent session resume 時，若 workdir 與 `resolvedWorkdir` 不符或記錄缺此欄位，須一律拒絕（`PolicyBlocked`）。【測試鎖住：CHANGELOG 記載已修（`c8e78c6`，Breaking）——具體測試檔名本次未逐一核對，標記部分核實】

文件 vs 程式碼不一致：本次核實範圍聚焦 agent/estop-agent-session 路徑，未重新核對「estop 同時停手機感測」「感測不靜默」的程式碼行號（沿用子系統地圖既有結論，非本輪獨立驗證）。`presentation_ack()` 作為「唯一能把 receipt 推向 Completed 的入口」一說，本輪僅核對呼叫點與測試斷言，未重新讀取函式本體逐行核實內部鎖與防偽比對邏輯，行號未在本輪核實。`v0.5.1-test-matrix.md` 的「claimed≠verified…真 Tauri 視窗」證據是 v0.5.1 發布時的既有文件證據，本次未重新執行 Tauri 視窗操作，證據時效性以 v0.5.1 發布當下為準，未涵蓋 `a94e3d5` 是否有回歸。Rust 側測試普遍使用 fake claude/codex 子程序而非真實二進位——這是文件與程式碼一致承認的已知限制，非隱藏缺口。

### 2.7 Memory

受保護行為：

1. `MemoryLayer` 型別須恰為 10 個 variant，新增/刪除任一層須同步改前端 `LAYER_LABEL`。【測試鎖住：`crates/interaction-core/src/memory.rs` 內建 7 個單元測試之一（`defaults_follow_the_spec_table`）】
2. 三態保存期限語意（Active/Stale/Expired）不變。【測試鎖住：`memory.rs::retention_three_states_derive_status`】
3. `memory_get()`/`memory_update()` 對 Expired 記憶須回 NotFound。【測試鎖住：`memory_loop.rs::expired_memories_are_refused_before_sweep`（**15 passed** 之一，`rust-test.log` 實跑核實，非矩陣原文寫的 17）】
4. `apply_actor_rules()` 須在 create/update 兩處都被呼叫，只能降權不能升權。【測試鎖住：`memory.rs::agent_rules_demote_claims_and_long_term_user_memory`／`far_future_horizon_counts_as_long_term`＋`memory_loop.rs::agent_writes_are_demoted_and_secrets_rejected`】
5. `validate_memory_item()` 須拒絕 secret 樣態寫入。【測試鎖住：`memory.rs::secrets_never_enter_memory`／`secrets_in_tags_and_provenance_rejected`＋`memory_loop.rs::secrets_in_tags_and_provenance_are_rejected`】
6. `visible_to_agent()` denylist 須優先於 visibility。【測試鎖住：`memory.rs::visibility_denylist_wins_and_sensitive_layers_default_hidden`】
7. `GET /v1/memory`／`/export` 須用 `Store::count_memory()` 真實筆數計算 `limitReached`（剛好上限不得誤報）。【測試鎖住：`memory_loop.rs::memory_export_exact_limit_is_not_falsely_reported`／`memory_export_one_over_limit_reports_truncation`＋`interaction-storage::count_memory_matches_actual_row_count`】
8. `GET /v1/memory/export` 須固定包含 `scope`/`included`/`notIncluded` 四項精確清單。【測試鎖住：`memory_loop.rs::memory_export_declares_scope_and_reports_its_limit`】
9. `memory_context_bundle()` 對排除項須逐一計數而非靜默丟棄，撞上限後仍持續累計 `excluded.overCapacity`。【測試鎖住：`memory_loop.rs::context_bundle_is_deterministic_and_honest`／`context_bundle_reports_capacity_truncation`】
10. `mailbox_send()` 對 kind="task" 訊息須重算 Context Bundle 並覆寫呼叫端提供的值。【測試鎖住：`agents_loop.rs::every_task_receives_and_persists_the_exact_session_scoped_context_bundle`（**23 passed** 之一）】
11. `POST /v1/memory/context-bundle`、`POST /v1/memory`（create）等端點在白名單維持排除狀態的話，legacy agent token/session-scoped token 呼叫須被拒絕。【無測試：本次核實為程式碼推論（`agent_request_allowed()`/`session_request_allowed()` 白名單不含此路徑），未找到直接斷言「agent 呼叫應 403」的測試，需在 v0.6.0 補】

文件 vs 程式碼不一致：文件面（README/FEATURES/CHANGELOG/known-limitations）對記憶匯出範圍、Context Bundle 決定論選取等限制均誠實列為刻意保留，與程式碼行為一致，本次未發現誇大宣稱。**測試數字更正**：`memory_loop.rs` 實跑 **15 passed / 0 failed**（`rust-test.log` 逐行核對，含 `crud_layers_and_status`／`memory_export_exact_limit_is_not_falsely_reported`／`context_bundle_reports_capacity_truncation` 等 15 個具名測試），非矩陣原文寫的「17 個 `#[tokio::test]`」——本次獨立用實跑 log 核實發現此落差，屬本輪新更正項。唯一的證據等級落差：Memory 子系統完全沒有 Playwright（browser）或真 Tauri 視窗層級的自動化測試，所有覆蓋止於 unit／fixture-integration／CLI E2E／jsdom-mock 四級,本輪盤點未在任何 release 文件中看到對此缺口的明確自陳。

### 2.8 General Mode

受保護行為：

1. 一般模式固定 5 個一級入口（現在／〔目前角色〕／工作／連接與權限／更多），第二項在執行期由 `useCharacterName()` 解出真實角色。【測試鎖住：`characterName.test.tsx`（23）＋`compat-routes.test.tsx`（5）】
2. 舊 tab id 全部經 `LEGACY_ANCHORS`／`PageBody` 折疊到新 5 入口，未知 route 落防呆頁而非空白。【測試鎖住：`compat-routes.test.tsx`（5）＋`regressions-review3-ia.test.tsx`（8，「未知路由不得靜默空白」）】
3. 進階模式開關唯一位置在「更多→進階模式」，開啟後才顯示 `ADVANCED_NAV` 9 個技術頁與原始技術細節；兩模式共用同一套後端。【測試鎖住：`connectPage.test.tsx`（37）】
4. 所有會上一般模式畫面的原始 taxonomy 字串經 `statusProjection.ts` 窮舉表投影，未知值一律「結果不確定」且 `known:false`，不外洩原始字串。【測試鎖住：`statusProjection.test.tsx`（26）】
5. `GlobalSearch`／`MemoryKnowledgePage` 在一般模式下，`memoryLayerLabel()` 不得回傳 `agent-handoff`／`skill` 等原始技術字串。【測試鎖住：`memory` 相關 vitest（見 Memory 列）＋CHANGELOG `memory-ui-006` 已修記錄】
6. 390px 響應式只有一個斷點，側欄→底部導覽＋「更多」抽屜，逐頁核對單欄可達、頁面主體不產生水平捲動。【測試鎖住：`narrowNav.test.tsx`（9）＋Playwright `narrow.spec.ts`（4，`pnpm-e2e.log`：底部導覽可抵達所有頁面／鍵盤可操作／不產生水平捲動／「現在」第一屏可見可按）】
7. 誠實階梯文案（`actionStatusLabel`）在一般模式下維持不升級（queued/acknowledged/completed/verified/uncertain 各自獨立）。【測試鎖住：`honesty.test.ts`（8）＋`pendingCountExact.test.tsx`（19）】
8. `useNavigation` 的 `mountKey` 機制須讓緊急停止中重複點擊「前往解除」不死點擊。【測試鎖住：`regressions-review2-ia.test.tsx`（15）】
9. iPhone 卡片文案（`phoneCardModel`）在一般模式下優先顯示人話（如「你的 iPhone」），不外洩原始 id/device 字串。【測試鎖住：`phoneDeviceCard.test.tsx`（7）＋`deviceCard.test.ts`（16）】
10. 深連結（`companion_open_control_center` Tauri 事件 → `navigate` → `goTo(tab)`）未知 tab 字串須落防呆頁而非崩潰或空白。【無測試：本次未見對此 IPC 呼叫路徑本身的具名 Rust 內嵌測試或 Playwright 案例，僅程式碼路徑推論，需在 v0.6.0 補】

文件 vs 程式碼不一致：README.md:45-62／FEATURES.md:102,144,183 的宣稱與程式碼、vitest 實跑一致，本次未發現誇大宣稱；核心 9 個檔案沒有硬編「小樞」文字，僅 3 處合法的 CPP entrypoint 分支（`Onboarding.tsx`／`CompanionPage.tsx`／`catalog.ts`），與文件「小樞只是 Reference Adapter」的宣稱一致。**本次盤點新發現的缺口**（非文件不一致，是架構層面的觀察，供 v0.6.0 參考）：① 深連結目前只是站內 Tauri 事件，沒有 OS 層級 URI scheme，若 v0.6.0 目標要外部可定址深連結需要新機制；② `statusProjection.ts` 是純前端手動同步的窮舉表，Rust 端狀態字串變動需人工同步（雖有 `satisfies Record` typecheck 守門）；③ `CompanionPage.tsx` 用 `entrypoint==='shu-rig'` 直接二選一渲染兩套設定 UI，沒有「能力宣告驅動 UI」的中介層，接第 4 個 entrypoint 需再加 if 分支。以上三點取自 `af581b2d4995816ae.json` 地圖的 `gaps` 欄位，本次未進一步獨立驗證程式碼細節之外的架構判斷，標記為沿用地圖結論。

### 2.9 Release pipeline

受保護行為：

1. `scripts/release.sh <version>` 在 worktree 非空或 tag 已存在時須以非 0 exit code 失敗，不產生新 commit/tag。【無測試：本次未見對 `release.sh` 本身的自動化測試（shell test/dry-run），僅讀腳本原始碼確認邏輯存在】
2. `scripts/release.sh` 成功後四個版本檔須同步為同一字串。【測試鎖住：`tests/e2e/tests/golden.rs::versions_are_in_sync`】
3. CHANGELOG 既無 `## [<version>]` 也無 `## [Unreleased]` 時須失敗，不得靜默跳過。【無測試：僅程式碼與歷史 CI 事實佐證（e70ef28 修復），無專屬自動化回歸測試】
4. `GOLDEN_UPDATE=1 cargo test -p interaction-e2e` 重生 golden 後再驗證一次；省略此步驟會被 golden 內容測試偵測到漂移。【測試鎖住：`golden.rs`（整體 golden 測試，屬 `interaction_e2e` target，`rust-test.log` 中該 target 6 passed）】
5. `versions_are_in_sync` 須持續斷言四個版本檔字串完全相同。【測試鎖住：`golden.rs::versions_are_in_sync`（屬 **827 passed / 0 failed（66 target）** 的一部分）】
6. `changelog_topmost_section()` 遇到「頂部 Unreleased 為空段、下一段非空」的形狀時須跳過空段改用下一個非空段。【測試鎖住：`lib.rs::changelog_topmost_section_follows_a_renamed_heading`（屬 **50 passed（3 target）** 之一，`tauri-test.log`）】
7. `changelog_unreleased_click_through_claims_match_code` 須持續斷言最上層段落不含固定樣板字樣且點擊穿透輪詢 X 值等於 `CLICKTHROUGH_POLL_MS`（目前 80）。【測試鎖住：`lib.rs::changelog_unreleased_click_through_claims_match_code`（`tauri-test.log`）】
8. `release.yml` 在 `v*` tag push 後須產出固定 18 項資產（4 CLI+4 sha256、6 桌面 bundle、skill zip、install.sh、QUICKSTART、CHANGELOG）。【無測試：本次未見任何自動化測試驗證 workflow 本身的產出數量，唯一佐證是歷史一次性 GitHub Actions run 記錄（`release-readiness.md` 記載 run id）】
9. `ci.yml`／`release.yml` 都須從 `rust-toolchain.toml` 讀 Rust 版本，不得硬編不同版本號。【無測試：僅程式碼閱讀確認兩處皆讀同一檔案，無自動化交叉比對測試】
10. annotated tag `v0.5.1`（`ccf3bfe`）在歷史 CI run 上 Tauri backend job 對 2 個 CHANGELOG claim-check 測試曾是紅燈（`5f3df32` 修復，tag 不移動）——此為既定歷史事實，須原樣保留，不得誤判為「待修」。【測試鎖住（現況）：`lib.rs` 兩個 claim-check 測試目前在 `a94e3d5` 上皆為綠燈（`tauri-test.log` 確認），歷史 run 的紅燈記錄本身不需要、也不可能「再修一次」】
11. `cli_asset_name()` 產生的檔名格式須與 `release.yml` 的 `cli` job 實際上傳檔名格式一致。【無測試：`selfmgmt.rs::asset_names_are_deterministic` 只驗證函式自身輸出等於寫死字串，未讀取比對 `release.yml` 實際 bash 命名邏輯，需在 v0.6.0 補一個跨檔一致性測試】

文件 vs 程式碼不一致：`cli_asset_name()` 與 `release.yml` 的命名規則是兩處獨立維護、無自動交叉比對，子系統地圖（docs-and-release／tests-and-fixtures 兩份）都沒有提到這個耦合點與其無測試覆蓋的事實，屬本次核實後新發現的缺口，不是文件宣稱與實作不符，而是既有文件盤點本身的遺漏。`release-readiness.md` 關卡3「Release workflow 可重現」標為 met，佐證是「v0.5.1 那一輪實際跑過一次並記錄 run id」的歷史事實，而非「有一組會在每次改動 release.sh／workflow 檔時自動重跑並攔住迴歸的測試」——文件用字本身沒有宣稱後者，但若讀者把「關卡3 met」誤讀為「這條路徑有持續回歸保護」，會與程式碼現況（無此類測試）產生落差，v0.6.0 恢復矩陣把這點明確寫成已知限制。

## 3. 證據層級對照

| 證據層級 | 出現在 | 2026-09-04 實跑數字（來自 `scratchpad/baseline/`） |
|---|---|---|
| 真機（real-device） | iPhone companion／Pairing／Reconnect（三列共用同一份真機證據） | v0.5.0（2026-09-03，iPhone 11／iOS 26.3.1，真 daemon 非 loopback）：配對、5 動器（acknowledged）、character 6 正常態、plan 偽造 emergency/verified-success 被擋、背景斷線強制停用高風險受器、IP 變更需重新配對、撤銷（在線/離線）、觀察 battery/touch、BLE scan（connect/GATT 未驗）、停止感測 313ms、estop 投影+停感測 178ms、解除不自動恢復。v0.5.1（2026-09-04）：冷啟動自動重連／位址變更提示兩項新行為 **0 次真機執行，blocked**（鑰匙圈授權待人工）。**未計**：observe-motion（未搖手機）、BLE connect/GATT read/write/subscribe、Wi-Fi 切換自動恢復、系統終止後 App 冷啟動恢復（真機）。ESP32 真板：**未執行**（無實體裝置，`v0.6.0-baseline.md`§2）。 |
| 真 Tauri 視窗（real-Tauri-window） | Desktop character | 2026-09-04 一次性人工＋腳本（macOS System Events AX＋CGEvent 真滑鼠事件）：主視窗啟動、Tray、角色視窗顯示/隱藏、click-through 空白穿透與角色本體攔截、緊急停止可信 overlay、角色匯入資料夾。**未驗**：Reduced Motion（`defaults write` 被系統拒絕）、多角色/玩具快捷選單（僅 jsdom）、adapter crash fallback（僅 vitest `companion-gateway-wiring`）。General Mode（5 入口本身）在 `v0.5.1-test-matrix.md`§3 清單中**未見獨立列出**，屬本輪新發現的證據等級缺口。 |
| 模擬器（simulator） | iPhone companion／Pairing／Reconnect | iOS `InteractionCompanionTests`：**46 tests, 0 failures**（`ios/xctest.log` 實跑：MotionClassifier 8＋Protocol 17＋ReconnectHint 21，iPhone 17 模擬器）；ESP32：`compile.sh` 預設組態與 `--ble` 組態各 exit 0（`v0.6.0-baseline.md`§2，arduino-cli 編譯檢查，非真板）。 |
| fixture | iPhone companion／Pairing／Reconnect／Agent integration／Renderer（外部 adapter） | `fake_iphone.rs`（程序外可執行檔，真 TLS wss 握手，模擬 iPhone）；`fake_claude.sh`/`fake_codex.sh`/`fake_codex_exec.sh`（假 agent 子程序）；`examples/character-adapters/text-adapter.mjs`（外部 WebSocket fixture）。CLI E2E `scripts/v03-cli-e2e.sh`：**82 passed, 0 failed**（`cli-e2e.log`，真 daemon＋serial pty 模擬器＋WebSocket 模擬 adapter fixture）。 |
| browser（Playwright，headless Chromium） | Desktop character／Pairing／iPhone companion／Reconnect／Agent integration／General Mode | `INTERACT_AI_MOBILE_ADVERTISE=0 pnpm test:e2e`：**65 passed（1.9 分）**（`pnpm-e2e.log`，真 daemon＋真 Chromium；`iphone.spec.ts` 4 個對接模擬 iPhone fixture；`narrow.spec.ts` 4 個涵蓋 General Mode 390px；`character.spec.ts` 3 個瀏覽器層角色頁）。 |
| 單元（unit，含 fixture-integration） | 全部九列 | `cargo test --workspace --no-fail-fast`：**827 passed / 0 failed（66 個 test target）**（`rust-test.log`）；`cargo test --manifest-path apps/interaction-desktop/src-tauri/Cargo.toml`：**50 passed / 0 failed（3 個 target）**（`tauri-test.log`）；`pnpm test`（vitest）：**1168 passed / 0 failed（60 檔）**（`pnpm-test.log`）。逐 target 明細（`rust-test.log` 逐行核對）：`mobile_loop.rs` 64、`character_loop.rs` 21、`api_e2e.rs` 29、`memory_loop.rs` **15**（更正）、`agents_loop.rs` 23、`gateway_loop.rs` 28、`presentation_loop.rs` 16、`interaction_character` 單元 45、`gateway.rs` 37、`negotiation.rs` 16、`manifest.rs` 18、`interaction_core` 單元 41、`interaction_runtime` 單元 50。 |

## 4. 盤點未覆蓋的模組（誠實列出，不宣稱涵蓋）

以下模組本次任務**未讀取原始碼**，本文件對它們沒有結論，重構前若牽動這些範圍須另行盤點：

- `crates/interaction-policy`（政策/consent 引擎） — **未盤點，結論不涵蓋**
- `crates/interaction-registry`（provider/adapter registry） — **未盤點，結論不涵蓋**
- `crates/interaction-runtime` 的 `sensors.rs`／`providers.rs`／`activity.rs`／`hardware.rs`（本次僅各自 grep 一兩個字面值，未逐行讀取） — **未盤點，結論不涵蓋**
- `crates/interaction-adapter-declarative`（YAML→HTTP/SSE/Serial/MQTT/BLE 宣告式 adapter） — **未盤點，結論不涵蓋**
- `crates/interaction-adapter-sdk` — **未盤點，結論不涵蓋**
- `crates/interaction-agent-gateway`（若與 `interaction-runtime/src/gateway.rs` 為不同 crate，需確認邊界） — **未盤點，結論不涵蓋**
- `crates/interaction-recipe`（YAML 配方引擎） — **未盤點，結論不涵蓋**
- `crates/interaction-tool-schema`（Canonical Tool Manifest 產生器） — **未盤點，結論不涵蓋**
- `adapters/builtin`／`adapters/media`（內建受器動器＋麥克風感測） — **未盤點，結論不涵蓋**
- `crates/interaction-api/src/routes.rs` 全文（本次只交叉引用少數幾行 character/mobile 相關路由，未逐行通讀） — **未盤點，結論不涵蓋**
- `apps/interaction-desktop/src/character/` 與 `src/companion/` 全文（本次只讀了 9 個核心 IA 檔案＋部分 character adapter 檔頭，`character/gateway.ts`／`companion/rig/` 完整實作未逐行讀） — **未盤點，結論不涵蓋**
- Playwright e2e spec 內容本身（本次只讀 `pnpm-e2e.log` 的執行結果與 test 標題，未逐一開啟 `*.spec.ts` 核對斷言邏輯） — **未盤點，結論不涵蓋**
- `firmware/esp32-companion/*.ino`（本次只確認 `compile.sh` exit 0，未讀韌體原始碼本身） — **未盤點，結論不涵蓋**

## 5. 最小可遷移邊界（三個候選，供 v0.6.0 排序參考）

> 以下三個候選取自 completeness critic 的建議，附驗證路徑；本文件不對「該不該做」下結論，只給出可執行的驗證方式。**注意**：撰寫本文件時，worktree 中已觀察到與候選①②高度相關的檔案處於未提交的修改/新增狀態（`crates/interaction-aip/`、`crates/interaction-character-shu/`、`crates/interaction-session/` 等，`git status --porcelain` 可見），這是其他並行 agent 的進行中工作，不屬於 `a94e3d5` 基線範圍，本文件不對其現況背書，僅供讀者知悉——驗證時應以屆時的實際 commit 為準重新檢查，不要假設下面的驗證路徑「已經通過」。

1. **`interaction-aip` 純 crate＋golden**：把 Adaptive Interaction Protocol 的版本化訊息 envelope／outcome／error／capability negotiation／limits 抽成一個無 I/O 的純函式 crate（仿照 `interaction-character` 目前的做法：`gateway.rs:267` 無 tokio、無 I/O，時間由呼叫端注入）。
   驗證路徑：`cargo test -p interaction-aip` 全綠且該 crate `Cargo.toml` 的 `[dependencies]` 不含 `tokio`/`axum`/任何 I/O crate（`cargo tree -p interaction-aip -i tokio` 應為空）；新增一份 golden schema（仿 `schemas/character-protocol.schema.json` 的模式）並在 `tests/e2e/tests/golden.rs` 加一個逐位元組 diff 測試，隨 `cargo test --workspace` 一起跑，不得引入可被跳過的獨立 diff 腳本。

2. **小樞資料搬到 `interaction-character-shu`**：把小樞專屬的 rig/sprite 資料、表情表、動畫參數從 `apps/interaction-desktop/src/companion/rig/` 與相關 Rust 端資料抽成獨立 crate/package，讓 `interaction-runtime`／`interaction-character` 核心不再耦合小樞專屬資料，呼應 CLAUDE.md「Runtime／頁面不得再引用小樞的部位、表情名或 pack id」的不變量。
   驗證路徑：`rg -n "耳朵|尾巴|maid|小樞" crates/interaction-runtime/src/ crates/interaction-character/src/` 應為 0 命中（目前 `character.rs` 本次核實無此類字面值，Desktop character 列已確認）；`cargo test -p interaction-character-shu`（或對應套件名）獨立可跑；`apps/interaction-desktop` 的 `character/adapters/shu.ts` 改為從該套件匯入資料而非本地硬編，`pnpm typecheck` 與既有 `character-shu-adapter.test.ts`（53 個）維持全綠。

3. **runtime 呈現面／裝置身分去 iPhone 字面值**：把 `crates/interaction-runtime/src/character.rs::is_presentation_surface_actuator`（:342，目前硬編判斷 `"iphone.character"` 等字面值）、`activity.rs`（:79，`sensor.starts_with("iphone.")` 才顯示「iPhone」）、`sensors.rs`（:207，`"sensor": "iphone.mic-level"` 字面值）、`providers.rs` 改成透過 provider/裝置能力宣告判斷，而非硬編 `iphone.*` 字串前綴，讓未來新增其他行動裝置 provider 不需要改這幾個檔案的 if 分支。
   驗證路徑：`rg -n '"iphone' crates/interaction-runtime/src/character.rs crates/interaction-runtime/src/activity.rs crates/interaction-runtime/src/sensors.rs crates/interaction-runtime/src/providers.rs` 逐一核對每個命中改為透過 provider 能力宣告（例如 provider metadata 的 `kind`/`category` 欄位）取得，而非字串前綴比對；`is_presentation_surface_actuator` 的既有測試（`character.rs:2575-2578`，`assert!(is_presentation_surface_actuator("iphone.character"))` 等）須全數維持綠燈（行為不變，只換實作機制），並新增一個非 iPhone 字面值的假 provider 案例證明去耦合成功。

---

**回報摘要**：本文件的 9 列總表＋逐列受保護行為，全部以 2026-09-04 `scratchpad/baseline/` 下的實跑 log（`rust-test.log`／`tauri-test.log`／`pnpm-test.log`／`cli-e2e.log`／`pnpm-e2e.log`／`ios/xctest.log`）與 `git show a94e3d5:<path>` 核對的原始碼為準；本次核實過程中另外發現並更正了矩陣/地圖原文之外的一項數字落差（`memory_loop.rs` 實際 15 個測試而非 17 個）。
