# v0.7.0已知限制

> 本文件彙整**第二輪**（分支 `feature/v0.6.x-maintainability`，起點 `f5b8e2f`；審查對象 `713f8fe`
> 為第 28 個 commit，加上對抗審查五路修復與本次收斂後目前 **58** 個；最終數字由整合者回填）
> 結束時仍然成立的限制。每一條都指得出來源：程式碼檔案、測試名、文件章節，或
> `CHANGELOG.md` `[Unreleased]` 的「本輪（v0.7.0）」段。
>
> **條目數（以本檔實際列數為準）**：共 **53** 條——§1 真機／真板／受測者 8、§2 協定與同步 13、
> §3 裝置生命週期與感測停止 9、§4 桌面控制中心 11、§5 iOS companion 4、§6 文件與工具 8。
> 編號連續不重複（1.1–1.8、2.1–2.13、3.1–3.9、4.1–4.11、5.1–5.4、6.1–6.8）。§0 是對抗審查
> `713f8fe` 的處置表、§7 另記本輪修掉不再是限制的條目，兩者都**不**計入這 53 條。引用本檔數量時
> 請以這一行為準，勿轉述他處的摘要數字。
>
> **分類用字**：`needs-environment` ＝ 需要本機沒有的硬體／人；`deferred` ＝ 有設計方向但本輪
> 刻意不做；`partial` ＝ 主要缺口已關、剩下的範圍寫在該條；`by-design` ＝ 刻意保留的取捨。
> 真機／真板／受測者的執行次數一律照實寫，模擬器（iOS Simulator、pty `esp32-serial-sim.py`、
> in-process MQTT broker）與 fixture 逐條標明，**不得**被引用成真機／真板證據。
>
> 對抗審查 `713f8fe-20260905T181800Z` **已執行並處置完畢**：54 送審／42 confirmed／11 refuted／
> 1 fixed-meanwhile，逐條處置表見本檔 **§0**（格式比照 `docs/releases/v0.6.0-known-limitations.md` §2）。
> 下面各節已依處置結果就地更新——被修掉的條目改寫或移進 §7，因審查而新增的窄限制是 §2.13 與 §3.9。
>
> 上一版仍然成立的限制見 `docs/releases/v0.6.0-known-limitations.md` §3／§4 與
> `docs/releases/v0.6.x-final-report.md` §11（其中三條已由本輪修掉，該檔已就地標註）。

## 0. 對抗審查 `713f8fe-20260905T181800Z` 處置表

> 報告全文：`docs/reviews/adversarial/713f8fe-20260905T181800Z.md`（＋同名 `.json`，記於
> `436ccd7`）。審查對象是本分支相對 `f5b8e2f` 的 diff（head `713f8fe`），12 維度 find →
> **獨立懷疑者逐條 verify**。修復分五路並行（docs／adapter-declarative／ios／desktop-ts／runtime），
> 每一路先寫「舊行為下會紅」的回歸測試再修；SHA 一律是本分支 **rebase 之後**的值。
>
> **處置用字**：`fixed` ＝ 根因消除且有具名回歸測試；`partial` ＝ 主要缺口關掉、剩下的範圍寫在
> 備註並在下面各節留一條限制；`accepted-limitation` ＝ 確認但刻意不修，轉成本文件的具名限制；
> `deferred` ＝ 不在本輪範圍，轉下一版；`fixed-meanwhile` ＝ 審查跑完到修復開始之間就已經被
> 其他 commit 修掉，本輪只補文件。

### 0.1 統計

| 指標 | 數量 |
|---|---|
| reviewed（送審） | 54 |
| confirmed（已確認） | 42 |
| refuted（獨立懷疑者駁回） | 11 |
| fixed-meanwhile（審查後、修復前已被修掉） | 1 |
| unverified | 0 |
| confirmed → `fixed` | 40 |
| confirmed → `partial` | 2 |
| confirmed → `accepted-limitation`／`deferred` | 0 |

confirmed 依 severity：**high 11（全部 fixed）／medium 18（全部 fixed）／low 13（fixed 11、partial 2）**。
兩則 `partial` 是 `declarative-aip-binding-020`（`pending-full-state` 的殘留缺口，見 §2.13）與
`declarative-aip-binding-024`（稽核佇列有界，溢位計數但仍會丟最舊，見備註）。

> 誠實邊界：`cargo test` 在本 worktree **未跑**（磁碟共用，硬規則禁止），所以 Rust 那三路的
> 回歸測試名稱與計數是靜態核對（`grep -cE '#\[tokio::test|#\[test\]'`）＋各修復者在自己
> worktree 的實跑紀錄，本文件不把它寫成「本機重跑通過」。桌面 TS 那一路本輪實跑：
> `pnpm test` **1816 passed / 0 failed（84 檔）**、`pnpm typecheck` 乾淨。

### 0.2 confirmed（42 則，依 severity 分節）

> id 為完整 finding id 去掉共同前綴 `F-713f8fe-20260905T181800Z-`
>（例：`sensor-source-stop-030` 對應 `F-713f8fe-20260905T181800Z-sensor-source-stop-030`）。

#### high（11 則）

| id | 標題（節錄） | 處置 | commit | 回歸測試 |
|---|---|---|---|---|
| `sensor-source-stop-030` | 「未解決停止」對 AI 的封鎖被 `/v1/status` 與 `interaction.status` 整份繞過 | fixed | `33ad1ca` | `api_e2e.rs::the_status_an_agent_reads_carries_no_human_layer_records` |
| `sensor-source-stop-031` | rebind 握手逾時後新建的裝置線永遠被生命週期閘門拒收，provider 卻收斂成 Available | fixed | `a1015a4` | `declarative_session_loop.rs::rebind_timeout_is_bounded_and_honest`／`rebind_generation_rejects_late_callbacks` |
| `sensor-source-stop-032` | 本機旗標捏出的 already-stopped 被當成裝置確認，刪掉舊世代的 `UnresolvedStop` | fixed | `6fdd267`（part 1）＋`a1015a4`（part 2） | `sensors_loop.rs::an_already_stopped_without_device_contact_does_not_clear_an_old_generation`；`declarative_session_loop.rs::an_idle_declarative_source_still_asks_the_device_and_says_where_the_answer_came_from` |
| `capability-registry-owner-012` | rebind 失敗後裝置晚一步握上手：說成 Available，但所有 AIP frame 被永久拒收 | fixed | `a1015a4` | `declarative_session_loop.rs::rebind_generation_rejects_late_callbacks`（`provider.rebind-recovered` 稽核） |
| `capability-registry-owner-013` | rebind 在握手之前就清掉「人類手動關掉的受器」名單 | fixed | `a1015a4` | `declarative_session_loop.rs::a_failed_rebind_keeps_the_human_disabled_list_for_the_next_attempt` |
| `declarative-aip-binding-019` | 併發 `send_aip` 的分片交錯上線，兩則都被對端丟棄，host 兩次都回 `Ok` | fixed | `e924f4f` | `aip_outbound_atomicity.rs::two_concurrent_fragmented_sends_do_not_interleave_on_the_wire` |
| `declarative-aip-binding-020` | `syncProfile=full-state` 建立在裝置自報的 `hello.caps` 上 | **partial** | `a1015a4`（Runtime）＋本輪 desktop 文案 | `declarative_session_loop.rs::a_frag_claiming_device_is_only_full_state_after_a_snapshot_really_went_out`；`statusProjection-session.test.ts`「pending-full-state 說「尚未確認」而不是「拿不到」」。**剩餘範圍見 §2.13** |
| `general-mode-ux-026` | 連接頁裝置條目用 provider id 去查 `syncProfile`（鍵是 AIP device id），production 永遠查不到 | fixed | `89904c6`（桌面改讀後端 `providerId`）＋`a1015a4`（Runtime 真的送出 `providerId`） | `connectPage.test.tsx`「裝置條目：非 full-state 的來源自己說得出「不算已同步」（provider id ≠ 裝置識別碼）」／「後端沒有回報 providerId 時不猜」／「providerId 對不上這一台時不掛別人的字」 |
| `docs-evidence-release-005` | CHANGELOG `[Unreleased]` 的 Known limitations 有兩條已被修掉卻沒刪；28 個 commit 一條條目都沒加 | fixed | `0b42ac2` | `docs-claims.sh`：CHANGELOG `[Unreleased]` Known limitations 對帳（每條限制綁一個「它已被修掉」的可執行證據；加入前實跑 4 條紅） |
| `async-bounds-unwrap-052` | 分片出站沒有每條線的原子性：呼叫端拿到 `Ok`，對端整批丟棄 | fixed | `e924f4f` | `aip_outbound_atomicity.rs::a_mid_transfer_write_failure_is_reported_as_uncertain_not_ok`／`a_failure_on_the_first_fragment_still_says_nothing_was_written` |
| `async-bounds-unwrap-053` | rebind 握手逾時（20 s）短於 binding 自己的重試退避（最長 15 s） | fixed | `a1015a4` | `declarative_lifecycle.rs` 的 const 斷言（`REBIND_HANDSHAKE_BUDGET >= HANDSHAKE_BACKOFF_MAX + HANDSHAKE_TIMEOUT`）＋`declarative_session_loop.rs::rebind_timeout_is_bounded_and_honest`。預算改為 **握手 25 s／總 50 s**（見 §3.3） |

#### medium（18 則）

| id | 標題（節錄） | 處置 | commit | 回歸測試 |
|---|---|---|---|---|
| `session-client-rollback-036` | `CharacterSyncCard` 每則事件重餵整個保留視窗，三則舊 patch 燒光 realign 預算 | fixed | `a99a70a` | `character-sync-card.test.tsx`「掛載時保留視窗裡的舊事件不重播」／「舊 snapshot 也不當成現在的狀態」／「初次讀取還在飛時陸續到的補丁」 |
| `hash-numeric-contract-017` | TS canonical JSON 的鍵排序不是 code point 序（補充平面鍵排在 BMP 鍵之前） | fixed | `363c3d7`（修）＋`5817777`（跨語言向量釘死） | `canonical-hash.test.ts` 3 支＋`canonical_vectors.rs::the_ordering_vector_actually_separates_code_point_from_utf16_order` |
| `sensor-source-stop-033` | CHANGELOG／v0.6.x 最終報告仍宣稱「停用→啟用要重啟 daemon」 | fixed | `0b42ac2` | `docs-claims.sh` 過期宣稱把關（放寬為 ±1 行段落窗、不再只認 `v` 前綴） |
| `capability-registry-owner-014` | `/v1/status` 內嵌 `unresolvedStops`，繞過同分支剛加的 agent token 排除 | fixed | `33ad1ca` | 同 `-030` |
| `capability-registry-owner-015` | `converge_provider_after_rebind`／`note_provider_rebind_failed` 沒拿 provider 序列化鎖 | fixed | `a1015a4` | `declarative_session_loop.rs::revoke_during_rebind_does_not_resurrect` |
| `declarative-aip-binding-021` | `retire()` 先 abort 掉唯一的稽核出口，取消的入站分片不留 `aip.fragment-dropped` | fixed | `a1015a4` | `declarative_session_loop.rs::a_transfer_cancelled_by_disabling_the_provider_is_audited_before_the_loop_is_gone` |
| `declarative-aip-binding-022` | 收斂順序讓 provider 先 Available、lifecycle 後 Bound，中間的 `aip` 全被拒收 | fixed | `a1015a4` | `declarative_session_loop.rs::reenable_rebinds_without_restart`（`note_declarative_link_ready`：先建 binding 再翻狀態） |
| `character-settings-binding-001` | 解除未解決停止失敗時把後端原文（含 `sourceId`／`generation`）印進一般模式 | fixed | `89904c6` | `unresolvedStops.test.tsx`「解除失敗的一般模式文案不得帶後端原文」／「進階模式才看得到後端原文」 |
| `general-mode-ux-027` | CHANGELOG／final-report 仍把「Disabled→Available 需重啟」列為已知限制 | fixed | `0b42ac2` | 同 `-033` |
| `ios-lifecycle-heartbeat-042` | `iphone-companion.md` 的接收端規則表四列與出貨的 Swift 直接矛盾 | fixed | `0b42ac2` | 改成不重抄決策表、指名 `character-session.md` §7.2 為唯一契約；`docs-claims.sh` 測試數對帳（`SessionClientTests` 34、`ConnectionManagerGateTests` 7） |
| `ios-lifecycle-heartbeat-043` | 背景時 presence 仍會對桌面說謊：只有 legacy status 被閘住，AIP 出站沒有閘門 | fixed | `3d3f136` | `ConnectionManagerGateTests.testBackgroundDoesNotLetAnAipFrameProveTheDeviceIsAlive`／`testTheSameStateFrameInTheForegroundStillAsksForRealignment`／`testAnAuthOkThatLandedInTheBackgroundIsRenegotiatedOnForeground` |
| `docs-evidence-release-007` | drills 宣稱「每個演練各自 commit」，但演練 1／3 的 commit 不是 HEAD 的祖先 | fixed | `0b42ac2` | 改寫為「做在本機 `wt/drills`，不合併也沒推 origin」；`docs-claims.sh` drills 對帳 |
| `docs-evidence-release-008` | 同一批測試檔在同一輪文件裡有互相矛盾的計數 | fixed | `0b42ac2`＋`ac30285` | `docs-claims.sh`：「文件引用的 `<test>.rs` N 測」必須等於檔案實數（粗體寫法也算） |
| `docs-evidence-release-009` | `AGENTS.md` §7 的接續進度指向不存在的章節與檔案 | fixed | `0b42ac2` | `docs-claims.sh`：AGENTS.md §7 指到的進度檔必須存在、Blockers 節號要對得上 |
| `docs-evidence-release-010` | `docs-claims.sh` 對 CHANGELOG `[Unreleased]` 的 Known limitations 零覆蓋 | fixed | `0b42ac2` | `docs-claims.sh` 本身：111 → 154 checks（本輪再驗 **154 passed / 0 failed**） |
| `honesty-ladder-audit-048` | drills §7 F3 的「重現」命令與結論在 HEAD 上是假的（宣稱 0 命中、實際 45） | fixed | `0b42ac2` | 改成 HEAD 實跑的 45 命中／9 檔，`intent-only` 的桌面顯示更正為 `partial-sync` |
| `honesty-ladder-audit-049` | CHANGELOG `[Unreleased]` 的已知限制被修掉至少 5 項卻一字未改 | fixed | `0b42ac2` | 同 `-010`（就地劃掉並註明修它的 commit 與釘住它的測試） |
| `honesty-ladder-audit-050` | 同一支測試檔在四份文件裡有三個不同的計數 | fixed | `0b42ac2`＋`ac30285` | 同 `-008`；本輪 `declarative_session_loop.rs` 再由 26 更正為 **30** |

#### low（13 則）

| id | 標題（節錄） | 處置 | commit | 回歸測試 |
|---|---|---|---|---|
| `hash-numeric-contract-018` | `stateHashDoublePaths` 的推導只走 properties／items／oneOf，map／tuple 形狀靜默失效 | fixed | `80a2548` | `state_hash_fixtures.rs::double_paths_are_derived_through_maps_and_tuples`／`map_shaped_fields_are_covered_by_the_keys_that_actually_appear`／`a_schema_too_deep_to_walk_panics_instead_of_skipping_silently` |
| `capability-registry-owner-016` | 綁定表滿（>64 台）時停用→啟用回報 Available 卻沒有警告 | fixed | `a1015a4` | 收斂成 `Disconnected` ＋ detail 說明＋稽核；`declarative_session_loop.rs` 生命週期上限路徑 |
| `declarative-aip-binding-023` | `accept_fragment` 對「完全沒宣告 caps」的舊韌體照收分片 | fixed | `e924f4f` | `aip_link.rs::inbound_fragments_from_a_device_that_declared_no_caps_are_dropped` |
| `declarative-aip-binding-024` | `pending_fragment_drop` 是單一槽：第二次取消覆蓋掉還沒取走的稽核 | **partial** | `e924f4f` | `aip_link.rs::two_supersedes_before_a_drain_keep_both_audits_in_order`／`the_pending_audit_queue_is_bounded_and_counts_what_it_had_to_drop`。剩餘範圍：改為有界 FIFO（8 筆），**溢位仍會丟最舊**，但被計數並記錄（`DeviceLink::fragment_audit_overflow`），不再是靜默覆蓋 |
| `character-settings-binding-004` | `applyCompanionPreset` 在檔位交易化之後成為零呼叫端的第二份同名邏輯 | fixed | `e506ed1` | `companion-presets.test.ts`「presets.ts 只留一份套用邏輯（不得再 export 第二個入口）」 |
| `general-mode-ux-028` | `character-session.md` §11 沒有收錄 `partial-sync` 與其 action id | fixed | `0b42ac2` | 補上該列與 `open-devices`；狀態總數以 `CharacterSyncState` union 為準 |
| `general-mode-ux-029` | e2e 任務 11 的 else 分支沒取消任何東西，卻被記成 completed | fixed | `073eac0` | `general-mode-metrics.test.tsx`「找不到中斷鈕的那一條路記成 not-run」／「宣告 completed 的那一條路要附上『真的取消到了』的證據」 |
| `ios-lifecycle-heartbeat-044` | 新加的 `wallClockNow` 只接了兩個呼叫點，註解宣稱的範圍遠大於實際 | fixed | `3d3f136` | `ConnectionManagerGateTests.testTheSessionReadsTheSameInjectedClockAsTheLifecycleGates` |
| `ios-lifecycle-heartbeat-045` | `decideResumeBatch`／`budget.observing` 在 App 內是死碼，出貨路徑另有手寫實作 | fixed | `3d3f136` | `ReceiveDecisionConformanceTests.testTheClientAndTheDecisionTableAgreeOnOneResumeBatch`／`testARealignRequestThatNeverLeftTheDeviceIsNotCountedAsAnAttempt` |
| `ios-lifecycle-heartbeat-046` | 「五個接線點」的第 4 點（`scheduleRetry` 的背景閘門）不可達 | fixed | `3d3f136` | `ConnectionManagerGateTests.testTheReconnectGateIsAskedExactlyOncePerDisconnect` |
| `docs-evidence-release-011` | `evidence-index.json` 的 `candidates` 區塊沒有被任何 lint 覆蓋 | fixed | `0b42ac2` | `docs-claims.sh`：`candidates[]` 最小 schema（必填欄位／branch／baseCommit 是真 ref／progress 指到存在的檔／進度檔在 `baseCommit..HEAD` 之間被改過／versionPolicy 與版本戳記不得互相矛盾），五條各以反例驗證 |
| `honesty-ladder-audit-051` | 演練文件的「桌面全套」數字在 HEAD 上重現不出來 | fixed | `0b42ac2` | drills §1／§6 改成「量於演練樹」＋標明量測 commit 的 HEAD 實跑值（本輪 HEAD 已再變動，見 §6.8） |
| `async-bounds-unwrap-054` | rebind 關鍵區段在 `timeout_at` 之下不是 cancel-safe：人類關過的受器不會被關回去 | fixed | `a1015a4` | drop guard 重新關閉那些受器並稽核；`declarative_session_loop.rs::rebind_keeps_human_disabled_receptors_off`／`a_failed_rebind_keeps_the_human_disabled_list_for_the_next_attempt` |

### 0.3 fixed-meanwhile（1 則）

| id | 標題 | 說明 |
|---|---|---|
| `docs-evidence-release-006` | drills §7 F3 把已在同一分支關掉的缺口寫成未解決，並附一個輸出是錯的「重現」命令 | 缺口本身在審查跑完之前就由 `bc6d834` 修掉（桌面已接線 `syncProfile` 並進 `partial-sync`）。本輪只補文件：`0b42ac2` 把 F3 的「0 命中」改成 HEAD 實跑的 45 命中／9 檔，並把 `intent-only` 的桌面顯示由「已同步」更正為 `partial-sync`。內容與 `honesty-ladder-audit-048` 重疊，兩則同一次修好 |

### 0.4 refuted（11 則，獨立懷疑者駁回）

| id | severity（原報） | 標題（節錄） | 駁回理由（一句） |
|---|---|---|---|
| `session-client-rollback-035` | high | unrecoverable 觸發的 `reset-local` 清掉 `pendingRequestId`，把仍然當令的權威回覆丟掉 | 技術描述屬實，但把「還在飛的請求」算成缺陷之前得先看它的定位；**行為本身仍在本輪一併修掉**（`a99a70a`：`reset-local` 只丟本地副本，保留 `pendingRequestId` 與去重環） |
| `snapshot-format-persist-037` | medium | store parked 的「原因」到不了任何桌面介面 | 引用的程式碼在 base `f5b8e2f` 就逐字存在，不是本分支造成的——超出本輪 diff 範圍 |
| `snapshot-format-persist-038` | low | 一般模式對所有 parked 一律說「重新啟動之後會再試一次」 | 同上：單一 `store-issue` 文案在 base 就是這樣，本分支沒有讓它變錯 |
| `snapshot-format-persist-039` | low | `quarantine()` 在 rename 失敗時直接刪檔，`storeNote` 仍宣稱「已隔離」 | 程式碼屬實但同屬 base 既有行為，且 `remove_file` 是明確的最後手段，不在本輪 diff 範圍 |
| `snapshot-format-persist-040` | low | 有界讀取排在格式辨識之前：超過 1 MiB 的「未來版本」快照會被隔離 | 誤讀契約：`character-session.md` 的表把「超過上限」明確歸在「真正損毀 → 隔離」那一列，行為與文件一致 |
| `snapshot-format-persist-041` | low | 隔離檔名固定 `<file>.corrupt`，第二次損毀覆寫第一次 | 機制屬實但是 base 既有行為，本分支未動該路徑 |
| `sensor-source-stop-034` | low | `request_stop` 翻旗標與記 stop-pending 之間有可觀察的靜默窗 | 整段程式碼（含指名的那一行與註解）在 base `f5b8e2f` 逐字存在，不是本分支引入 |
| `character-settings-binding-002` | medium | §3.5 拆出來的 `pages/companion/**` 不在 entrypoint 字面棘輪範圍內 | 每個事實都對，但棘輪的鍵是 entrypoint 而不是頁面檔清單，範圍本來就不涵蓋這些檔 |
| `character-settings-binding-003` | medium | `companionScene`／使魔配色的全域白名單只從 TS 拿掉，Tauri host 仍是閉合清單 | 靜態事實成立，但違反審查自己的範圍規則（舊碼未動、且本分支沒有讓它變錯） |
| `general-mode-ux-025` | high（原報） | `partial-sync` 排在 `connectedButNotSynced` 之前，吞掉「需要重新確認裝置」 | 排序確實如此，但兩態的嚴重度與落點需要一起裁決而非逕判缺陷；**排序仍在本輪一併改掉**（`89904c6`：要人動手的那一態排前面） |
| `honesty-ladder-audit-047` | high（原報） | 讀不到 diagnostics 時把「不知道 `syncProfile`」四捨五入成綠勾 | 行為屬實，但那是本分支自己明寫並用具名測試釘住的刻意設計（「沒有回報 ≠ 非 full-state，不憑空降級也不憑空升級」），不是意外違規 |

> 兩則 refuted（`-035`／`-025`）的行為仍在本輪被修掉：駁回的是「這算不算本輪的缺陷」，不是
> 「這個行為好」。本文件照 verdict 欄位歸類，不因為順手修了就改記成 confirmed。

## 1. 真機／真板／受測者（needs-environment、not-run）

| # | 限制 | 來源 | 狀態 |
|---|---|---|---|
| 1.1 | **ESP32 真板驗收為零**：所有裝置線證據來自 `scripts/esp32-serial-sim.py` 的 pty 模擬器；韌體只有 `arduino-cli` 編譯檢查（兩組態 EXIT=0） | `firmware/esp32-companion/compile.sh`；`crates/interaction-runtime/tests/declarative_session_loop.rs` | needs-environment |
| 1.2 | **裝置線 v1.2 分片在真板上一次都沒有執行過**：參考韌體刻意不宣告 `aip.frag/1`，所以它的行為與 v1.1 完全相同（snapshot／含 members 的 patch 仍送不到，成員模式 `intent-only`）——這是誠實降級，不是已修好 | `firmware/esp32-companion/*.ino`（明確忽略 `aip-frag`）；`docs/aip/device-profile.md` §6.3 | by-design＋needs-environment |
| 1.3 | **iPhone 真機零執行**：本輪 iOS 證據全部來自 iPhone 17 **模擬器**（iOS 26.2 runtime，simctl 注入）與 fixture；`docs/releases/v0.5.0-iphone-device-evidence.md` 的真機證據是舊協定路徑，**不涵蓋**本輪的接收端決策表 | `apps/interaction-ios/README.md`（XCTest 期望值 146）；CHANGELOG 本輪段 Known limitations | needs-environment |
| 1.4 | **`URLSessionSocket` 本身沒有被測到**：背景閘門的七支行為測試用替身 socket 與替身排程；真 TLS 指紋固定與真 URLSession 回呼執行緒只有 code review | `apps/interaction-ios/InteractionCompanion/Services/SocketTransport.swift`；`ConnectionManagerGateTests` | partial |
| 1.5 | **五份非開發者受測者腳本仍未執行**：沒有受測者、沒有觀察紀錄 | `docs/releases/v0.6.x-general-mode-tasks.md` §4 | not-run |
| 1.6 | **真 Tauri AX 走查不在 CI**：需要 macOS 桌面工作階段與輔助使用權限；腳本在無權限時誠實記 `needs-environment`。走查刻意不涵蓋手機相關任務與「設定安靜時段」「取消工作」（那些的後端路徑與桌面 host 無關，由瀏覽器模式覆蓋，文件記為 `not-run`） | `scripts/tauri-ax-walkthrough.sh`；`docs/releases/v0.6.x-general-mode-tasks.md` §3.5 | needs-environment |
| 1.7 | 走查用的 `.app` 是 debug build，不是 release；結果 JSON 與截圖在 repo 之外的 scratchpad，不入版控 | 同上 | by-design |
| 1.8 | 真 `codex`／`claude` 二進位未跑（沿用 fixture agent），與 v0.6.0 一致 | `docs/releases/v0.6.0-known-limitations.md` §3.4 | 沿承 |

## 2. 協定與同步（AIP 1.0 接收端決策表、裝置線 v1.2）

| # | 限制 | 來源 | 狀態 |
|---|---|---|---|
| 2.1 | **SSE 事件流本身不帶連線世代**：決策表規則 0 只能標上「處理當下的世代」，真正擋得住的是飛行中的 GET／resume 回覆（帶著發出當下的世代回來）；「換連線那一瞬間才送達的舊 SSE 事件」擋不住 | `apps/interaction-desktop/src/components/CharacterSyncCard.tsx`（註解）、`src/aip/sessionClient.ts` | by-design（已記錄） |
| 2.2 | **typed boundary 三端都不要求 `payload.hash`**：缺 hash 的 snapshot 由決策表規則 2 擋下（永不套用），缺 hash 的 **patch** 依規則 14「兩邊都知道才算不符」照樣套用。裁決為維持三端同一位置、同一結論，不單獨收緊 Swift。**對抗審查 `713f8fe` 沒有推翻這個裁決**（該輪 12 個維度都沒有把它列成 confirmed finding），仍是 by-design | `crates/interaction-aip/src/envelope.rs`、`apps/interaction-desktop/src/aip/envelope.ts`、`apps/interaction-ios/InteractionCompanion/Models/AIPEnvelope.swift`；`docs/aip/transport-bindings.md` §7 | by-design |
| 2.3 | **決策表之外有一條桌面自己的規則**：patch merge 之後產不出物件時回 `reject-invalid`（Rust 的表不做 merge，那是呼叫端責任），沒有 fixture 涵蓋 | `apps/interaction-desktop/src/aip/sessionClient.ts`（原始碼註明「表之外的呼叫端責任」） | by-design |
| 2.4 | **TS 不驗證 Rust 那串 SHA-256 的字面值**：fixture 的 hash 是不透明字串，TS 端自己算，搬過去的是「哪兩個 hash 相同／不同」的關係；字面值由 `stateHashes` 段的 `canonical-hash.test.ts` 負責 | `apps/interaction-desktop/src/test/receive-decision-fixtures.test.ts` 檔頭 | by-design |
| 2.5 | **fixture 期望值由 Rust 產生器算出**（不是手寫）：規則與期望一起改掉時產生器不會紅；守門是只讀 JSON 的第二消費者＋突變驗證＋三端交答案 | `crates/interaction-session/tests/receive_decision_fixtures.rs`／`receive_decisions_from_json.rs` | by-design |
| 2.6 | **resume 超過上限的客戶端分支無法從 wire 驗**：513 則補丁的 response payload 超過 32 KiB，連 typed boundary 都過不了，權威 host 超限時也會改回 snapshot；該分支只有純函式測試涵蓋 | `decide_resume_batch`／`resumeExceedsBound` 的測試 | partial |
| 2.7 | **局部投影（scope／`scopeRevision`／scope-only hash）未實作**：含 members 的 patch 仍整段重送；設計方向（獨立 `scopeRevision` ＋ scope-only hash，不沿用 session hash）只記在文件 | `docs/aip/transport-bindings.md` §8.1 | deferred |
| 2.8 | **出站分片中途失敗不重送**：第 k 片寫不出去只回報「這一則沒有完整送出」，對端因缺片／逾時整筆丟棄並留稽核（重送可能重複外部副作用） | `crates/interaction-adapter-declarative/src/fragment.rs`；CHANGELOG 本輪段 | by-design |
| 2.9 | **入站分片對沒有宣告 `aip.frag/1` 的裝置一律拒絕**（`reason: not-advertised`）：刻意的嚴格，代價是舊韌體若自行送分片會被丟掉並留稽核 | 同上；`docs/aip/deprecation-ledger.md` §3.2 | by-design |
| 2.10 | **`syncProfile` 由協商好的 `role` 推導，不看逐項 intents**：「宣告 remote-renderer 但 intents 為空」的成員仍被算成 `intent-only`（為守住 `interaction-session` 零變更，`MemberView` 沒有加新欄位）。對抗審查 `713f8fe`（`declarative-aip-binding-020`）之後值域變成**四個**（多了 `pending-full-state`），但推導的輸入沒變 | `crates/interaction-runtime/src/character_session.rs::derive_sync_profile`；`docs/aip/device-profile.md` §3.1 | by-design |
| 2.11 | **MQTT／BLE 沒有 AIP session 端到端測試**：三者共用同一段 `AipChannel<L>`／`fragment.rs`，MQTT 有一支 rebind 閉環測試（in-process broker＋fake device **模擬器**，`b6d849c`），BLE 兩者皆無；只有 serial 有 pty 模擬器可跑 | `crates/interaction-runtime/tests/mqtt_rebind_loop.rs::mqtt_reenable_rebinds_without_restart`；CHANGELOG 本輪段 | partial |
| 2.12 | `aip.outbound-undeliverable` 的 `reason` 欄位受 `AUDIT_FIELD_MAX_CHARS=64` 截斷，整行長度改由新增的 `envelopeBytes` 表達（上限未動） | `crates/interaction-runtime/src/character_session.rs` | by-design |
| 2.13 | **`pending-full-state` 的殘留缺口**（對抗審查 `declarative-aip-binding-020` 的 partial 部分）：升級成 `full-state` 的條件已從「裝置在 `hello.caps` 說它會重組」改成「Runtime 真的把一份完整快照寫上那條線、每一片都寫出成功」，但 `aip/1.0` 對 `state` **沒有 wire 層回執**——「宣稱會重組、也收下了、卻沒組回來」這一種偵測不到，那台裝置會停在 `full-state` 而不會退回。誠實界線寫在契約裡，不假裝沒有 | `crates/interaction-runtime/src/character_session.rs::note_full_state_delivered`；`docs/aip/device-profile.md` §3.1「已知限制（誠實界線）」 | partial（by-design 的剩餘） |

## 3. 裝置生命週期與感測停止

| # | 限制 | 來源 | 狀態 |
|---|---|---|---|
| 3.1 | **rebind 失敗後不會把第 7 步已建好的連線拆掉**：serial／mqtt／ble supervisor 繼續以退避重連（與「開機時裝置沒插上」同一種狀態），`ProviderState` 留在 `disconnected`；裝置之後自己接上時 `note_ready` 會收斂成 `available` | `crates/interaction-runtime/src/providers.rs`；`declarative_session_loop.rs::rebind_timeout_is_bounded_and_honest` | by-design |
| 3.2 | **rebind 等同重新註冊 spec**：能力旗標回到「剛啟動時」的預設。人類手動關掉的**非 consent 受器**已由 `keptDisabledReceptors` 保留（`declarative_session_loop.rs::rebind_keeps_human_disabled_receptors_off`），但**動器**的 `enabled` 旗標仍回到預設（外部副作用動器預設本來就是關的，所以沒有同一個缺口，但沒有測試釘住） | `docs/aip/device-profile.md` §6.1 步驟 7 | partial |
| 3.3 | **rebind 的時間預算固定不可設定**（握手 **25 s**、總預算 **50 s**）：對抗審查 `async-bounds-unwrap-053` 之前是 20 s／40 s，握手預算比 binding 自己最壞的一輪重試（退避上限＋握手逾時）還短，於是「逾時」量的是我們自己的退避而不是裝置。現在由 const 斷言綁死 `REBIND_HANDSHAKE_BUDGET >= HANDSHAKE_BACKOFF_MAX + HANDSHAKE_TIMEOUT`，`rebind_timeout_is_bounded_and_honest` 這支測試本身也跟著要跑約 26 秒 | `crates/interaction-runtime/src/declarative_lifecycle.rs`（`REBIND_HANDSHAKE_BUDGET`／`REBIND_TOTAL_BUDGET`） | by-design |
| 3.4 | **`unresolvedStops` 只在記憶體，不跨重啟**：重啟後那些「不知道結果」的記錄會消失（歷史仍在稽核裡） | `crates/interaction-runtime/src/sensor_source.rs`；`docs/aip/privacy.md` §5.1 | 未做 |
| 3.5 | **`UnboundReason::Removed` 沒有任何 production 路徑會設定它**：spec 檔被刪只會在下一次啟動時「沒有被載入」，runtime 偵測不到檔案刪除；`Runtime::note_declarative_removed()` 是明確表態入口，doc comment 誠實寫明沒有 production 呼叫者 | `declarative_lifecycle.rs`；`declarative_session_loop.rs::a_removed_declarative_binding_is_never_rebound` | by-design |
| 3.6 | **`transition_provider(id, Available)` 對宣告式裝置回傳 `Disconnected`**：誠實（還沒連上），但是對外可見的行為變更，依賴「回傳狀態＝請求狀態」的呼叫端要一起看 | CHANGELOG 本輪段 Changed；`docs/releases/v0.7.0-migration.md` §5 | 行為變更 |
| 3.7 | **純 HTTP 宣告式來源的高風險受器停止後持續 `stop-unknown`**（沒有裝置線可送 stop-all）——`docs/releases/v0.6.x-final-report.md` §11 那一行的後半仍然成立 | `crates/interaction-runtime/src/sensor_source.rs` | 沿承 |
| 3.8 | `sensor_clock` 的 `advance()` 是 `#[doc(hidden)]` 的公開 API（測試用，production 不呼叫），仍然是公開介面的一部分 | `crates/interaction-runtime/src/sensor_source.rs` | by-design |
| 3.9 | **AI 讀 `/v1/status` 只拿得到 `unresolvedStopCount`，拿不到是哪一個來源**（對抗審查 `sensor-source-stop-030`／`capability-registry-owner-014` 之後，status 依 principal 投影：agent／session／adapter token 沒有 `unresolvedStops` 也沒有 `characterSessionSync`）。刻意的取捨：AI 仍然可以據此拒絕宣稱「所有感測都停了」，但無法自行針對某一個來源做事——那是人類層的收件匣 | `crates/interaction-api/src/lib.rs`（投影函式，兩個入口共用）；`api_e2e.rs::the_status_an_agent_reads_carries_no_human_layer_records` | by-design（行為變更，見 migration §5.14） |

## 4. 桌面控制中心

| # | 限制 | 來源 | 狀態 |
|---|---|---|---|
| 4.1 | **陪伴預設的恢復流程只有 vitest（mock Tauri host）＋src-tauri 單元證據**：marker 真的落到 `~/.adaptive-interaction/state/desktop.json` 並在重開後被補送這條路徑沒有真桌面實跑 | `apps/interaction-desktop/src/companion/applyPresetPlan.ts`；`companion-preset-recovery.test.tsx` | partial |
| 4.2 | **清 marker 是第三次寫入**：它自己失敗時 marker 會留到下一次 mount 才被補送／清除。此時狀態顯示 `applied`（有效值確實等於目標），不謊稱失敗，但偏好檔會多留一筆 marker 一段時間 | 同上 | by-design |
| 4.3 | **`projectPresetStatus` 在交易進行中不下「半套用」判決**：第一段寫完、第二段還在飛的那段時間顯示 `custom-effective` 而不是 `partially-applied` | 同上 | by-design |
| 4.4 | **persist 失敗的回滾是用可注入的 persist closure 重現**，不是真的把磁碟寫滿；`commit_prefs_patch` 之外的呼叫端（`close_decision` 等）仍是「持鎖寫記憶體再存檔」的舊順序 | `apps/interaction-desktop/src-tauri/src/lib.rs` | partial |
| 4.5 | **`opId` 由 presetId＋發起時間（ms）決定**：同一毫秒內的兩次交易會同名（只用來分辨「是不是同一次」，不影響正確性） | `applyPresetPlan.ts` | by-design |
| 4.6 | **進階診斷的 alignment 計數名稱換了**（`stale`→`staleConnection`、`invalid`→`rejectedInvalid`、`ignoredRollback`→`ignoredStale`，移除 `hostRegressed`，新增 `recovered`／`rejectedIdentity`）：既有截圖／證據若印過這些標籤會過時 | `apps/interaction-desktop/src/aip/sessionClient.ts`；CHANGELOG 本輪段 Changed | 行為變更 |
| 4.7 | **指南的把關只比對「狀態」層的一句話，不比對「每台裝置」那一句**：`partial-sync` 那一列與 `PENDING_GUIDE_ROWS`（現在是空的）已經對上，但裝置條目的三種補充句（只接收指令／只回報事件／尚未確認能收到完整狀態）沒有任何測試要求它們出現在指南裡——本輪是人工補寫的 | `apps/interaction-desktop/src/test/regressions-v06-round2-general-mode.test.tsx`（`evidence-honesty-015`）；`docs/DESKTOP-GUIDE.md`「角色同步」 | partial |
| 4.8 | **tray 的「感測停止待確認 N」只在 `refresh_tray` 週期更新**，不是即時推送；狀態列那一行讀 `/v1/status`、連接頁讀專屬端點，兩者可能相差一次刷新（兩邊都不會把「讀不到」說成「沒有」） | `apps/interaction-desktop/src/statusProjection/unresolvedStops.ts`；`src-tauri/src/host_safety.rs` | by-design |
| 4.9 | **「重新連線中」的判定只看 provider `detail`**，不看 `provider.rebinding` 稽核（detail 同步且權威；讀稽核 payload 會多開一個技術值外洩面） | `apps/interaction-desktop/src/statusProjection/provider.ts` | by-design |
| 4.10 | 桌面 e2e 未涵蓋新的未解決停止區塊（Playwright 未跑該路由）；本輪桌面驗證都在 vitest（jsdom）＋src-tauri 單元 | `docs/releases/v0.7.0-progress.md` §4 | partial |
| 4.11 | `CharacterPreview.tsx`／`CharacterLibrary.tsx` 的頁面層棘輪仍未收斂（沿承自上一輪） | `docs/releases/v0.6.x-final-report.md` §11 | 沿承 |

## 5. iOS companion

| # | 限制 | 來源 | 狀態 |
|---|---|---|---|
| 5.1 | **TDD 紀律只做到一半**：接收端決策表那一輪的新測試與實作同一輪寫成，第一次執行是紅的（136/4）並修到綠，但沒有單獨對舊實作跑過一次「新行為斷言全紅」；`ConnectionManager` 那一輪則是真的先紅（144/1）再綠 | 修復者自陳，記於此 | 流程限制 |
| 5.2 | **hash-mismatch 的復原策略是客戶端選擇**：決策仍是 realign（三端一致），但 iPhone 送的是 snapshot query 而不是 resume（補丁鏈已無意義） | `apps/interaction-ios/InteractionCompanion/Services/SessionReceive.swift`；`docs/aip/transport-bindings.md` §7 | by-design |
| 5.3 | **`onMain` 改成「已在主執行緒即同步執行」**會讓正式路徑上 Timer 回呼少一次 async hop；URLSession 回呼仍在背景佇列。這一點沒有在真裝置上量過 | `apps/interaction-ios/InteractionCompanion/Services/ConnectionManager.swift` | partial |
| 5.4 | iOS 仍未送 AIP `heartbeat`，靠舊 `status` 心跳撐存活證明（沿承 v0.6.0 §3.5） | `docs/aip/transport-bindings.md` §1.4 | 沿承 |

## 6. 文件與工具

| # | 限制 | 來源 | 狀態 |
|---|---|---|---|
| 6.1 | **`SemanticState` 不在任何跨語言 schema 裡**：golden schema 只由 `crates/interaction-aip` 的型別產生，權威語意狀態住在 `crates/interaction-session`，所以加一個 state 欄位對 TS／Swift **零機械傳播**；三端對 state 的唯一錨是 9 份 `state-hash-*.json` fixture | `docs/releases/v0.7.0-drills.md` §7 F4 | deferred（建議：新增 state 欄位必須同時新增一份帶該欄位的 state-hash fixture） |
| 6.2 | **沒有任何既有測試擋得住「選填欄位寫成 `null` 進 canonical」**：拿掉 `skip_serializing_if` 之後 9 份 fixture 全紅，但 `AIP_UPDATE_FIXTURES=1` 會愉快地全部重生，重生後三端全綠——這條不變量目前只靠人記得 | `docs/releases/v0.7.0-drills.md` §7 F5 | deferred |
| 6.3 | **`docs/aip/adapter-development.md` §1 把「加一個角色」與「加一個新 adapter」混成一步**：實測加一個角色完全不需要動兩份 host 白名單 | `docs/releases/v0.7.0-drills.md` §7 F1 | 文件債 |
| 6.4 | **兩支演練腳本沒有任何自動化把關**：`scripts/drills/*.sh` 只在演練當下跑過，不在 CI 也不在 `release-scripts.sh` 的 `bash -n` 清單以外做任何事 | `docs/releases/v0.7.0-drills.md` §7 F8 | by-design |
| 6.5 | **`architecture-checks.sh` 的 swift 列永遠是 SKIP**（XCTest 需要 iOS 模擬器）：本輪已改成驗證檔案與測試函式存在（缺席則 FAIL），但它不在 docs／ts／rust 的 SKIPPED 統計裡 | `scripts/tests/architecture-checks.sh` | by-design |
| 6.6 | **「移除已匯入角色」沒有 CLI 入口**（只有桌面 IPC 與 `character_store::remove`） | `docs/releases/v0.7.0-drills.md` §7 F10 | 觀察項 |
| 6.7 | 整合全套已於 `a1015a4`（五路審查修復合併後）同機依序實跑（`v0.7.0-final-report.md` §7）；之後的兩個 commit（`1a585cc`、`e5e14d4`）只改桌面文案與文件，對它們重跑 typecheck／vitest（1816/0）／docs-claims（154/0）／release-scripts（58/0）。**真機／真板／受測者仍為零**（§1） | `docs/releases/v0.7.0-final-report.md` §7 | 已完成（整合）；真機 needs-environment |
| 6.8 | 演練文件 §7 F3（`syncProfile` 在桌面／iOS 零消費端）**已由 `bc6d834` 修掉**：桌面已接線並進 `partial-sync` 態；該節記的是演練當下的事實，不是 HEAD 的事實。iOS 端仍然沒有 `syncProfile` 消費端。另外，drills 裡所有「桌面全套」的絕對數字都是**量測當下**的值（演練樹 1759/84、`0b42ac2` 當時的 HEAD 1769/83、本輪 HEAD **1816/84**），沒有 lint 擋得住它再度過期——引用前先自己跑一次 | `docs/releases/v0.7.0-drills.md` §7 F3／§1／§6；`apps/interaction-desktop/src/statusProjection/characterSync.ts` | partial |

## 7. 本輪修掉、不再是限制的三條

| 原限制（`docs/releases/v0.6.x-final-report.md` §11） | 現況 | commit |
|---|---|---|
| 宣告式裝置 Disabled→Available 需重啟才重新綁定 | 顯式 `DeclarativeLifecycle` ＋ 八步有界背景 rebind；期間狀態誠實是 `disconnected`。**同一行後半的 stop-unknown 仍成立**（見 §3.7） | `b32ba10` |
| 三端對「snapshot epoch 不同但非 `session-reset`」不一致 | 統一為 realign，45 個共用 fixtures 三端逐筆對答案 | `d941a9e`／`e2179d5`／`050bac7`／`dbeed51` |
| Serial 成員完全拿不到 snapshot 與含 members 的 patch | 改寫為：**只對不宣告 `aip.frag/1` 的裝置（含目前的參考韌體）仍然送不到**；宣告的裝置由裝置線 v1.2 分片送達（pty 模擬器實測） | `9799b1e` |

另外兩條也已收斂，原文保留在該檔並就地標註：陪伴預設兩段寫入「仍非原子」（`49eee6c` 改為可恢復的交易），
以及 Tauri-only 正向路徑「未在真 Tauri 視窗走查」（`e7348a8`／`2010852` 的 AX 走查 9/9，兩次）。

## 8. 相關文件

- `CHANGELOG.md` `[Unreleased]` →「本輪（v0.7.0，2026-09-05／06）」段（逐條附 commit）
- `docs/releases/v0.7.0-progress.md`（差異矩陣、每步數字、下一動作）
- `docs/releases/v0.7.0-migration.md`（新訊息／新欄位／行為變更／回退）
- `docs/releases/v0.7.0-drills.md`（五個演練與 §7 的十條發現）
- `docs/releases/v0.6.0-known-limitations.md`（上一版的完整清單）
- `docs/reviews/adversarial/713f8fe-20260905T181800Z.md`／`.json`（對抗審查全文；處置見本檔 §0）
- `docs/acceptance-evidence.md`（本輪證據等級表）
